const K={cases:'imode_test_v532_cases',tech:'imode_v5_tech',customers:'imode_test_v532_customers',machines:'imode_test_v532_machines',notifications:'imode_test_v532_notifications',quotes:'imode_test_v532_quotes',settings:'imode_v5_settings',cloud:'imode_v5_cloud',user:'imode_v5_current_user',warranties:'imode_test_v532_warranties',documents:'imode_test_v532_machine_documents',lineRequests:'imode_test_v532_line_requests',serviceReports:'imode_test_v532_service_reports',qcRecords:'imode_v66_qc_records',fontPrefs:'imode_v5_font_preferences'};

const PERMISSION_CATALOG=[
 {group:'ภาพรวมระบบ',groupEn:'Overview',items:[
  ['dashboard.view','ดูแดชบอร์ด','View dashboard'],['team.view','ดูทีมช่าง','View technician team']
 ]},
 {group:'เคสงานบริการ',groupEn:'Service Cases',items:[
  ['case.view','ดูเคส','View cases'],['case.create','สร้างเคส','Create cases'],['case.edit','แก้ไขเคส','Edit cases'],['case.assign','มอบหมายงาน','Assign cases'],['case.close','ปิดเคส','Close cases']
 ]},
 {group:'ลูกค้าและเครื่องจักร',groupEn:'Customer & Machine',items:[
  ['customer.view','ดูลูกค้า','View customers'],['customer.edit','เพิ่ม/แก้ไขลูกค้า','Manage customers'],['machine.view','ดูเครื่องจักร','View machines'],['machine.edit','เพิ่ม/แก้ไขเครื่องจักร','Manage machines']
 ]},
 {group:'QC เครื่องจักร',groupEn:'Machine QC',items:[
  ['qc.view','ดู QC เครื่อง','View machine QC'],['qc.edit','สร้าง/แก้ไข QC','Create/edit machine QC'],['qc.approve','อนุมัติผล QC','Approve QC result']
 ]},
 {group:'ปฏิทินและงานภาคสนาม',groupEn:'Calendar & Field Service',items:[
  ['calendar.view','ดูปฏิทิน','View calendar'],['calendar.edit','จัดตารางนัดหมาย','Manage appointments'],['field.view','เข้า Field Service','Access field service'],['field.status','อัปเดตสถานะงาน','Update job status'],['field.checkin','Check-in หน้างาน','Field check-in'],['field.report','สร้าง/แก้ไขใบตรวจ','Manage inspection reports'],['field.media','เพิ่มรูป/วิดีโอ','Add photos/videos'],['field.signature','ลงลายเซ็นจบงาน','Capture signatures']
 ]},
 {group:'ใบเสนอราคาและการอนุมัติ',groupEn:'Quotation & Approval',items:[
  ['quotation.view','ดูใบเสนอราคา','View quotations'],['quotation.create','สร้าง/แก้ไขใบเสนอราคา','Create/edit quotations'],['quotation.approve','อนุมัติราคา','Approve quotations']
 ]},
 {group:'Warranty และเอกสาร',groupEn:'Warranty & Documents',items:[
  ['warranty.view','ดูข้อมูลประกัน','View warranty'],['warranty.edit','จัดการประกัน','Manage warranty'],['documents.view','ดูเอกสารเครื่อง','View machine documents'],['documents.edit','เพิ่ม/แก้ไขเอกสาร','Manage machine documents']
 ]},
 {group:'รายงานและการสื่อสาร',groupEn:'Reports & Communication',items:[
  ['reports.view','ดูรายงาน','View reports'],['reports.export','Export / Print / PDF','Export / Print / PDF'],['notifications.view','ดูการแจ้งเตือน','View notifications'],['notifications.manage','จัดการการแจ้งเตือน','Manage notifications'],['line.view','ดูคำขอจาก LINE OA','View LINE OA requests'],['line.manage','จัดการ LINE OA / ส่ง Report','Manage LINE OA / send reports']
 ]},
 {group:'ผู้ดูแลระบบ',groupEn:'Administration',items:[
  ['users.manage','จัดการผู้ใช้งานและสิทธิ์','Manage users & roles'],['settings.manage','แก้ไขตั้งค่าระบบ','Manage system settings'],['integrations.manage','จัดการ Integration / Cloud','Manage integrations / cloud'],['audit.view','ดู Audit / ประวัติการเปลี่ยนแปลง','View audit trail']
 ]}
];
function allPermissionKeys(){return PERMISSION_CATALOG.flatMap(g=>g.items.map(x=>x[0]))}
function permissionPresetForRoleName(name=''){
 const n=String(name).toLowerCase();
 const all=allPermissionKeys();
 if(n.includes('service manager')||n.includes('manager')||n.includes('supervisor'))return [...all];
 if(n.includes('admin')||n.includes('coordinator'))return ['dashboard.view','team.view','case.view','case.create','case.edit','case.assign','customer.view','customer.edit','machine.view','machine.edit','qc.view','qc.edit','qc.approve','calendar.view','calendar.edit','field.view','warranty.view','warranty.edit','documents.view','documents.edit','reports.view','reports.export','notifications.view','notifications.manage','line.view','line.manage'];
 if(n.includes('technician')||n==='r&d'||n.includes('engineer'))return ['dashboard.view','team.view','case.view','customer.view','machine.view','qc.view','qc.edit','calendar.view','field.view','field.status','field.checkin','field.report','field.media','field.signature','warranty.view','documents.view','reports.view','reports.export','notifications.view'];
 if(n.includes('sales')||n.includes('marketing'))return ['dashboard.view','case.view','case.create','customer.view','customer.edit','machine.view','qc.view','quotation.view','quotation.create','warranty.view','reports.view','reports.export','notifications.view'];
 return ['dashboard.view'];
}
function migrateLegacyPermissions(text='',roleName=''){
 if(Array.isArray(text))return [...new Set(text.filter(x=>allPermissionKeys().includes(x)))];
 const s=String(text||'').toLowerCase();
 if(!s)return permissionPresetForRoleName(roleName);
 if(s.includes('ทุกโมดูล')||s.includes('approval'))return permissionPresetForRoleName('Service Manager');
 const keys=new Set();
 if(s.includes('เคส'))['case.view','case.create','case.edit'].forEach(x=>keys.add(x));
 if(s.includes('ลูกค้า'))['customer.view','customer.edit'].forEach(x=>keys.add(x));
 if(s.includes('นัดหมาย'))['calendar.view','calendar.edit'].forEach(x=>keys.add(x));
 if(s.includes('เอกสาร'))['documents.view','documents.edit'].forEach(x=>keys.add(x));
 if(s.includes('รายงาน'))['reports.view','reports.export'].forEach(x=>keys.add(x));
 if(s.includes('หลักฐาน'))['field.view','field.media','field.report'].forEach(x=>keys.add(x));
 if(s.includes('ใบเสนอราคา'))['quotation.view','quotation.create'].forEach(x=>keys.add(x));
 if(s.includes('อนุมัติ'))keys.add('quotation.approve');
 return keys.size?[...keys]:permissionPresetForRoleName(roleName)
}
function normalizeRoleSetting(r={}){return{name:r.name||'Role',users:Number(r.users)||0,teamScope:r.teamScope||'',positionScope:r.positionScope||'',permissions:migrateLegacyPermissions(r.permissions,r.name||'')}}
const ROLE_TEAM_OPTIONS=['','Technical','R&D','Admin','Management','Sales'];
const ROLE_POSITION_OPTIONS=['','Admin / Coordinator','Service Manager','Executive Secretary','Technical Supervisor','Technician','Senior Technician','R&D Supervisor','R&D','Sales','Engineer'];
function roleScopeLabel(value,type='team'){const th=settings?.language!=='en';if(!value)return th?'ทุก'+(type==='team'?'ทีม':'ตำแหน่ง'):'All '+(type==='team'?'Teams':'Positions');return value}
function roleSelectOptions(list,selected='',type='team'){return list.map(v=>`<option value="${esc(v)}" ${String(selected||'')===String(v)?'selected':''}>${esc(roleScopeLabel(v,type))}</option>`).join('')}
function getRoleScopedUsers(role){const roleObj=typeof role==='string'?(settings.roles.find(r=>r.name===role)||{name:role}):(role||{});const name=String(roleObj.name||'').trim();const team=String(roleObj.teamScope||'').trim();const pos=String(roleObj.positionScope||'').trim();const seen=new Map();[...demoUsers,currentUser].filter(Boolean).forEach(u=>{if(!u?.id)return;if(!seen.has(u.id))seen.set(u.id,u)});return [...seen.values()].filter(u=>{const raw=String(u?.role||'').trim();const mapped=resolvePermissionRoleName(u);const okName=!name||mapped===name||raw===name;const okTeam=!team||String(u?.team||'').trim()===team;const okPos=!pos||raw===pos||raw.toLowerCase()===pos.toLowerCase();return okName&&okTeam&&okPos})}

const defaults={
 serviceTypes:['เข้าบริการหน้างาน','ลูกค้าส่งเครื่องเข้าบริษัท','Remote Support','PM / Preventive Maintenance','ติดตั้งเครื่อง'],
 statuses:['เคสใหม่','มอบหมายแล้ว','นัดหมายแล้ว','กำลังดำเนินการ','รออะไหล่','รอส่งงาน','เสร็จสิ้น','ปิดเคส'],
 priorities:['ด่วนมาก','สูง','ปกติ','ต่ำ'],
 notificationLeadHours:24,
 companyName:'I-MODE Plus Co., Ltd.',
 companyNameTh:'บริษัท ไอโมด พลัส จำกัด',
 systemName:'I-MODE Plus Service & Maintenance',
 companyEmail:'service@imode.co.th',
 companyPhone:'02-727-0130',
 companyAddress:'241/46 Kanchanaphisek Rd., Dokmai, Prawes, Bangkok 10250',
 companyTaxId:'',
 locale:'th-TH',
 language:'th',
 typographyConfig:{defaultScale:100,minScale:90,maxScale:130},
 currency:'THB',
 roles:[
  {name:'Admin / Coordinator',users:0,permissions:permissionPresetForRoleName('Admin / Coordinator')},
  {name:'Technician',users:0,permissions:permissionPresetForRoleName('Technician')},
  {name:'Sales',users:0,permissions:permissionPresetForRoleName('Sales')},
  {name:'Service Manager',users:0,permissions:permissionPresetForRoleName('Service Manager')}
 ],
 sla:{urgentResponseMin:30,resolutionHours:4,autoEscalateMin:30,workStart:'08:30',workEnd:'17:30',repairWarrantyDays:30},
 notificationConfig:{leadHours:24,lineOA:true,email:true,push:true,appointment:true,slaRisk:true,partsWaiting:true,submitWaiting:true},
 integrations:{lineOA:'connected',googleMaps:'ready',googleDrive:'plan',erp:'plan'},
 systemBehavior:{auditTrail:true,offlineDraft:true,autosave:true,unsavedGuard:true,errorTracking:true,exportEnabled:true,enforceRolePermissions:false},
 travelZones:[
  {name:'Z0',min:0,max:15,fee:250,perKm:0},
  {name:'Z1',min:16,max:30,fee:400,perKm:0},
  {name:'Z2',min:31,max:50,fee:600,perKm:0},
  {name:'Z3',min:51,max:80,fee:900,perKm:0},
  {name:'Z4',min:81,max:120,fee:1300,perKm:0},
  {name:'Z5',min:121,max:180,fee:1900,perKm:0},
  {name:'Z6',min:181,max:null,fee:0,perKm:10}
 ],
 quoteConfig:{
   onsiteS:1500, onsiteL:3000, workshopS:800, workshopL:1500, diagnosisS:500, diagnosisL:1000,
   nextS:800, nextL:1500, laborS:400, laborL:500, urgentPct:30, emergencyPct:50, vatPct:7,
   markupStock:22.5, markupSpecial:27.5, markupImported:32.5, markupHigh:17.5, validityDays:30,
   criticalMode:'manual'
 },
 warrantyConfig:{defaultMonths:12,reminderDays:30,packagePrices:{6:0,12:0,24:0}},
 lineConfig:{officialAccountId:'',liffId:'',publicAppUrl:'',backendEndpoint:'',webhookUrl:'',addFriendUrl:'',richMenuEnabled:true},
 fieldServiceConfig:{appointmentReminderHours:24,maxPhotosEach:4,requireCustomerAcceptance:true,requireTechSignature:true,checkInRequired:true},
 mapConfig:{enabled:true,useCoordinatesFirst:true,checkInGps:true,openDirections:true,autoQuoteDistance:true,originAddress:'241/46 Kanchanaphisek Rd., Dokmai, Prawes, Bangkok 10250',originLat:'',originLng:'',googleMapsApiKey:''},
 mediaConfig:{maxPhotosEach:4,maxVideos:2,maxVideoMB:2.5,compressQuality:78,requireBeforeAfter:false},
 reportConfig:{defaultFormat:'PDF',includePhotos:true,includeSignatures:true,includeTimeline:true,lineSendEnabled:true},
 documentConfig:{allowUpload:true,allowDriveLink:true,maxLocalFileMB:3},
 documentCategories:['Operating Manual','Technical Datasheet','Electrical Diagram','Mechanical Drawing','PM Checklist','SOP / Work Instruction','Software / Firmware','Calibration / Certificate','Spare Parts List','Training Material'],
 quotationStatuses:['ร่าง','ส่งแล้ว','รออนุมัติ','อนุมัติ','ไม่อนุมัติ','หมดอายุ']
};
function mergeSettings(raw={}){
 const lead=raw.notificationConfig?.leadHours ?? raw.notificationLeadHours ?? defaults.notificationLeadHours;
 return {
  ...defaults,...raw,
  quoteConfig:{...defaults.quoteConfig,...(raw.quoteConfig||{})},
  sla:{...defaults.sla,...(raw.sla||{})},
  notificationLeadHours:lead,
  notificationConfig:{...defaults.notificationConfig,...(raw.notificationConfig||{}),leadHours:lead},
  integrations:{...defaults.integrations,...(raw.integrations||{})},
  systemBehavior:{...defaults.systemBehavior,...(raw.systemBehavior||{})},
  warrantyConfig:{...defaults.warrantyConfig,...(raw.warrantyConfig||{}),packagePrices:{...defaults.warrantyConfig.packagePrices,...(raw.warrantyConfig?.packagePrices||{})}},
  lineConfig:{...defaults.lineConfig,...(raw.lineConfig||{})},
  fieldServiceConfig:{...defaults.fieldServiceConfig,...(raw.fieldServiceConfig||{})},
  mapConfig:{...defaults.mapConfig,...(raw.mapConfig||{})},
  mediaConfig:{...defaults.mediaConfig,...(raw.mediaConfig||{})},
  reportConfig:{...defaults.reportConfig,...(raw.reportConfig||{})},
  typographyConfig:{...defaults.typographyConfig,...(raw.typographyConfig||{})},
  documentConfig:{...defaults.documentConfig,...(raw.documentConfig||{})},
  language:raw.language==='en'?'en':'th',
  documentCategories:Array.isArray(raw.documentCategories)&&raw.documentCategories.length?raw.documentCategories:[...defaults.documentCategories],
  roles:Array.isArray(raw.roles)&&raw.roles.length?raw.roles.map(normalizeRoleSetting):defaults.roles.map(normalizeRoleSetting),
  travelZones:Array.isArray(raw.travelZones)&&raw.travelZones.length?raw.travelZones.map(x=>({...x})):defaults.travelZones.map(x=>({...x}))
 }
}
let settings=mergeSettings(JSON.parse(localStorage.getItem(K.settings)||'{}'));
let cases=JSON.parse(localStorage.getItem(K.cases)||'[]');
let technicians=JSON.parse(localStorage.getItem(K.tech)||'null')||[
 {id:'T001',name:'สมชาย ใจดี',role:'Service Technician',phone:'081-000-0001',email:'',color:'blue'},
 {id:'T002',name:'ณัฐพล ช่างดี',role:'Senior Technician',phone:'081-000-0002',email:'',color:'orange'},
 {id:'T003',name:'พีรพัฒน์ เทคนิค',role:'Service Technician',phone:'081-000-0003',email:'',color:'green'}
];
let customers=(JSON.parse(localStorage.getItem(K.customers)||'[]')).map(c=>({mapUrl:'',latitude:'',longitude:'',address:'',serviceStatus:'ไม่ระบุ',source:'',...c}));
let machines=(JSON.parse(localStorage.getItem(K.machines)||'[]')).map(m=>({photo:'',serviceStatus:'ไม่ระบุ',issueYear:'',source:'',sizeStatus:'',sizeBasis:'',serviceSize:'',...m,nameTh:m.nameTh||m.name||'',nameEn:m.nameEn||m.name||''}));
let notifications=JSON.parse(localStorage.getItem(K.notifications)||'[]');
let quotations=JSON.parse(localStorage.getItem(K.quotes)||'[]');
let warranties=JSON.parse(localStorage.getItem(K.warranties)||'[]');
let machineDocuments=JSON.parse(localStorage.getItem(K.documents)||'[]');
let lineRequests=JSON.parse(localStorage.getItem(K.lineRequests)||'[]');
let serviceReports=JSON.parse(localStorage.getItem(K.serviceReports)||'[]');
let qcRecords=JSON.parse(localStorage.getItem(K.qcRecords)||'[]');
let crmLeads=[];
let crmOpportunities=[];
let crmActivities=[];
let salesQuotes=[];
let salesQuoteEditingId='',salesQuoteDraftItems=[];
let crmActiveTab='overview';
const CRM_STAGES=['Prospect','Contacted','Needs Analysis','Demo','Quotation','Negotiation','Waiting PO','Won','Lost'];
const CRM_STAGE_LABELS={Prospect:['ลูกค้าใหม่','New Customer'],Contacted:['ติดต่อแล้ว','Contacted'],'Needs Analysis':['เก็บความต้องการ','Needs Analysis'],Demo:['Demo / สำรวจ','Demo / Survey'],Quotation:['ออกใบเสนอราคา','Quotation'],Negotiation:['ติดตาม / เจรจา','Follow-up / Negotiation'],'Waiting PO':['รอ PO','Waiting PO'],Won:['ปิดการขาย','Won'],Lost:['ไม่สำเร็จ','Lost']};
const CRM_STAGE_PROB={Prospect:10,Contacted:20,'Needs Analysis':35,Demo:45,Quotation:60,Negotiation:75,'Waiting PO':90,Won:100,Lost:0};

const DEMO_DB_MARKER='imode_demo_customer_database_v532_test10_20260828';
(function importCustomerDatabaseDemo(){
 if(localStorage.getItem(DEMO_DB_MARKER)==='1')return;
 const dc=window.IMODE_DEMO_CUSTOMERS||[],dm=window.IMODE_DEMO_MACHINES||[],dw=window.IMODE_DEMO_WARRANTIES||[];
 const cids=new Set(customers.map(x=>x.id));dc.forEach(x=>{if(!cids.has(x.id))customers.push({...x})});
 const byMachineId=new Map(machines.map(x=>[x.id,x]));
 dm.forEach(x=>{
   const old=byMachineId.get(x.id);
   if(!old){const added={...x,nameTh:x.nameTh||x.name||'',nameEn:x.nameEn||x.name||''};machines.push(added);byMachineId.set(x.id,added);return}
   // V5.29 migration: fill S/L and bilingual metadata on imported/legacy demo rows without overwriting a user's manual S/L choice.
   if(old.size!=='S'&&old.size!=='L')old.size=x.size||'';
   if(!old.sizeStatus)old.sizeStatus=x.sizeStatus||'';
   if(!old.sizeBasis)old.sizeBasis=x.sizeBasis||'';
   if(!old.serviceSize)old.serviceSize=x.serviceSize||'';
   if(!old.nameTh)old.nameTh=x.nameTh||x.name||'';
   if(!old.nameEn)old.nameEn=x.nameEn||x.name||'';
 });
 const wids=new Set(warranties.map(x=>x.id));dw.forEach(x=>{if(!wids.has(x.id))warranties.push({...x})});
 localStorage.setItem(K.customers,JSON.stringify(customers));localStorage.setItem(K.machines,JSON.stringify(machines));localStorage.setItem(K.warranties,JSON.stringify(warranties));localStorage.setItem(DEMO_DB_MARKER,'1');
})();



let cloudSettings=JSON.parse(localStorage.getItem(K.cloud)||'{}');
let currentUser=JSON.parse(localStorage.getItem(K.user)||'null');
let fontPreferences=JSON.parse(localStorage.getItem(K.fontPrefs)||'{}');
function fontUserKey(){return currentUser?.id||'guest'}
function clampFontScale(v){const min=Number(settings?.typographyConfig?.minScale||90),max=Number(settings?.typographyConfig?.maxScale||130);return Math.min(max,Math.max(min,Math.round(Number(v)||100)))}
function currentFontScale(){const saved=fontPreferences[fontUserKey()];return clampFontScale(saved??settings?.typographyConfig?.defaultScale??100)}
function saveFontPreference(v){const pct=clampFontScale(v);fontPreferences[fontUserKey()]=pct;localStorage.setItem(K.fontPrefs,JSON.stringify(fontPreferences));return pct}
function fontScaleLabel(v=currentFontScale()){const pct=clampFontScale(v);if(settings?.language==='en'){if(pct<=90)return'Small';if(pct<=100)return'Standard';if(pct<=110)return'Large';if(pct<=120)return'Extra Large';return'Maximum'}if(pct<=90)return'เล็ก';if(pct<=100)return'มาตรฐาน';if(pct<=110)return'ใหญ่';if(pct<=120)return'ใหญ่มาก';return'ใหญ่ที่สุด'}
function applyFontScale(){const pct=currentFontScale();document.documentElement.style.setProperty('--ui-font-scale',(pct/100).toFixed(2));document.documentElement.dataset.fontScale=String(pct);if(typeof fontQuickBtn!=='undefined'){fontQuickBtn.innerHTML=`<span class="font-aa">Aa</span><b>${pct}%</b>`;fontQuickBtn.title=settings?.language==='en'?`Font size ${pct}%`:`ขนาดตัวอักษร ${pct}%`}}
function setFontScaleLive(v){const pct=saveFontPreference(v);applyFontScale();if(typeof fontScaleRange!=='undefined')fontScaleRange.value=pct;if(typeof fontScaleValue!=='undefined')fontScaleValue.textContent=pct+'%';if(typeof fontScaleName!=='undefined')fontScaleName.textContent=fontScaleLabel(pct);if(typeof fontPreviewBox!=='undefined')fontPreviewBox.setAttribute('data-preview-scale',pct);if(typeof setTypographySummary!=='undefined')setTypographySummary.textContent=(settings.language==='en'?`${pct}% · ${fontScaleLabel(pct)}`:`${pct}% · ${fontScaleLabel(pct)}`)}window.setFontScaleLive=setFontScaleLive;
function adjustFontScale(delta){setFontScaleLive(currentFontScale()+Number(delta||0))}window.adjustFontScale=adjustFontScale;
function setFontPreset(v){setFontScaleLive(v)}window.setFontPreset=setFontPreset;
function resetFontScale(){setFontScaleLive(settings.typographyConfig?.defaultScale||100)}window.resetFontScale=resetFontScale;
function saveTypographyPreference(){saveFontPreference(currentFontScale());saveLocal();cloudSaveSettings();closeModal();renderSettings();toastMsg(settings.language==='en'?'Font size preference saved':'บันทึกขนาดตัวอักษรแล้ว')}window.saveTypographyPreference=saveTypographyPreference;
function openTypographySettingModal(){const th=settings.language!=='en',pct=currentFontScale();openModal(th?'ขนาดตัวอักษรและการเข้าถึง':'Font Size & Accessibility',th?'ปรับขนาดตัวอักษรให้เหมาะกับสายตาของผู้ใช้งานแต่ละคน':'Adjust text size for each user’s visual comfort',`<div class="font-setting-wrap"><div class="font-user-note"><span>👤</span><div><b>${th?'การตั้งค่าส่วนบุคคล':'Personal preference'}</b><small>${th?'มีผลเฉพาะผู้ใช้งานนี้บนอุปกรณ์นี้ ไม่กระทบผู้ใช้งานคนอื่น':'Applies only to this user on this device and does not affect other users'}</small></div></div><div class="font-current"><div><small>${th?'ขนาดปัจจุบัน':'Current size'}</small><b id="fontScaleValue">${pct}%</b><span id="fontScaleName">${fontScaleLabel(pct)}</span></div><div class="font-stepper"><button type="button" data-no-i18n="true" onclick="adjustFontScale(-5)">A-</button><button type="button" data-no-i18n="true" onclick="adjustFontScale(5)">A+</button></div></div><input id="fontScaleRange" class="font-range" type="range" min="${settings.typographyConfig.minScale}" max="${settings.typographyConfig.maxScale}" step="5" value="${pct}" oninput="setFontScaleLive(this.value)"><div class="font-range-labels"><span>${settings.typographyConfig.minScale}%</span><span>${settings.typographyConfig.defaultScale}%</span><span>${settings.typographyConfig.maxScale}%</span></div><div class="font-preset-grid"><button type="button" onclick="setFontPreset(90)"><b data-no-i18n="true">A</b><small>${th?'เล็ก':'Small'}<br>90%</small></button><button type="button" onclick="setFontPreset(100)"><b data-no-i18n="true">A</b><small>${th?'มาตรฐาน':'Standard'}<br>100%</small></button><button type="button" onclick="setFontPreset(110)"><b data-no-i18n="true">A</b><small>${th?'ใหญ่':'Large'}<br>110%</small></button><button type="button" onclick="setFontPreset(120)"><b data-no-i18n="true">A</b><small>${th?'ใหญ่มาก':'Extra Large'}<br>120%</small></button><button type="button" onclick="setFontPreset(130)"><b data-no-i18n="true">A</b><small>${th?'ใหญ่ที่สุด':'Maximum'}<br>130%</small></button></div><div id="fontPreviewBox" class="font-preview-box"><small>${th?'ตัวอย่างการแสดงผล':'Preview'}</small><h3>${th?'ข้อความตัวอย่างสำหรับการใช้งานระบบ':'Sample interface text'}</h3><p>${th?'ลูกค้า • เครื่องจักร • เคสงานบริการ • ปฏิทิน • รายงาน':'Customer • Machine • Service Case • Calendar • Report'}</p><button type="button" class="primary-btn">${th?'ปุ่มตัวอย่าง':'Sample button'}</button></div><div class="info-box">${th?'แนะนำ: 100% สำหรับจอทั่วไป • 110–120% สำหรับผู้ที่ต้องการอ่านง่ายขึ้น • 130% สำหรับการมองเห็นขนาดใหญ่':'Recommended: 100% for standard displays • 110–120% for easier reading • 130% for maximum visibility'}</div><div class="button-row"><button type="button" class="soft-btn" onclick="resetFontScale()">${th?'รีเซ็ตค่าเริ่มต้น':'Reset default'}</button><button type="button" class="primary-btn" onclick="saveTypographyPreference()">${th?'บันทึกและปิด':'Save & close'}</button></div></div>`,true);setTimeout(()=>applyFontScale(),20)}window.openTypographySettingModal=openTypographySettingModal;

const demoUsers=[
 {id:'USR-001',name:'อภิชาติ ปิมแปง',role:'Service Manager',team:'Management'},
 {id:'USR-002',name:'Patama Somkeaw',role:'Executive Secretary',team:'Admin'},
 {id:'USR-003',name:'Artivara Polsri',role:'Technical Supervisor',team:'Technical'},
 {id:'USR-004',name:'Chaichana Photaya',role:'R&D Supervisor',team:'R&D'},
 {id:'USR-005',name:'Thirapong Phengkliang',role:'Technician',team:'Technical'},
 {id:'USR-006',name:'Samak Thammachad',role:'Technician',team:'Technical'},
 {id:'USR-007',name:'Narongsak Poomipalai',role:'Technician',team:'Technical'},
 {id:'USR-008',name:'Thanawat Suvunchato',role:'R&D',team:'R&D'}
];

function resolvePermissionRoleName(user=currentUser){
 const raw=String(user?.permissionRole||user?.role||'');
 if(settings.roles.some(r=>r.name===raw))return raw;
 const n=raw.toLowerCase();
 if(n.includes('service manager'))return settings.roles.find(r=>r.name==='Service Manager')?.name||raw;
 if(n.includes('supervisor'))return settings.roles.find(r=>r.name==='Service Manager')?.name||raw;
 if(n.includes('executive secretary')||n.includes('admin')||n.includes('coordinator'))return settings.roles.find(r=>r.name==='Admin / Coordinator')?.name||raw;
 if(n.includes('technician')||n==='r&d'||n.includes('engineer'))return settings.roles.find(r=>r.name==='Technician')?.name||raw;
 if(n.includes('sales')||n.includes('marketing'))return settings.roles.find(r=>r.name==='Sales')?.name||raw;
 return raw
}
function roleUserCount(role){return getRoleScopedUsers(role).length}
function currentRoleConfig(){const n=resolvePermissionRoleName(currentUser);return settings.roles.find(r=>r.name===n)}
function canPermission(key){
 if(!settings.systemBehavior?.enforceRolePermissions)return true;
 if(!currentUser)return true; // prototype safety: do not lock the UI before Auth is connected
 const r=currentRoleConfig();if(!r)return false;return (r.permissions||[]).includes(key)
}
function requirePermission(key){if(canPermission(key))return true;toastMsg(settings.language==='en'?'You do not have permission for this action':'คุณไม่มีสิทธิ์ใช้งานฟังก์ชันนี้');return false}
const PAGE_PERMISSION={dashboard:'dashboard.view',cases:'case.view',quotation:'quotation.view',customers:'customer.view',machines:'machine.view',qc:'qc.view',technicians:'team.view',calendar:'calendar.view',warranty:'warranty.view',documents:'documents.view',notifications:'notifications.view',reports:'reports.view',settings:'settings.manage','field-service':'field.view'};
function applyRoleVisibility(){
 document.querySelectorAll('[data-page]').forEach(el=>{const k=PAGE_PERMISSION[el.dataset.page];if(k)el.style.display=canPermission(k)?'':'none'});
}

let supa=null,calendarView='week',calendarOffset=0,techTeamFilter='all',calendarTeamFilter='all';
technicians=technicians.map(t=>({photo:'',status:'พร้อมรับงาน',skills:'',team:'Technical',...t}));

function normalizeDateTimeLocal(v=''){if(!v)return'';try{const d=new Date(v);if(Number.isNaN(d.getTime()))return String(v).slice(0,16);const local=new Date(d.getTime()-d.getTimezoneOffset()*60000);return local.toISOString().slice(0,16)}catch(e){return String(v).slice(0,16)}}
function formatTime24Input(el){let v=String(el.value||'').replace(/\D/g,'').slice(0,4);if(v.length>2)v=v.slice(0,2)+':'+v.slice(2);el.value=v}window.formatTime24Input=formatTime24Input;
function validTime24(v=''){return /^(?:[01]\d|2[0-3]):[0-5]\d$/.test(String(v))}
function datetimeSplitHTML(id,value='',required=false){return `<input id="${id}" class="appointment-datetime" type="datetime-local" step="60" ${required?'required':''} value="${esc(normalizeDateTimeLocal(value))}" aria-label="${settings.language==='en'?'Date and time':'วันที่และเวลา'}">`}
function syncSplitDateTime(id){const d=document.getElementById(id+'Date')?.value||'',raw=document.getElementById(id+'Time')?.value||'';const h=document.getElementById(id);const ok=!raw||validTime24(raw);if(h)h.value=d&&ok?(d+'T'+(raw||'00:00')):'';const ti=document.getElementById(id+'Time');if(ti)ti.classList.toggle('invalid-time',!!raw&&!validTime24(raw))}window.syncSplitDateTime=syncSplitDateTime;
const IMODE_LOGO=new URL('./assets/imode-document-logo.webp',location.href).href;
const IMODE_UI_LOGO=new URL('./assets/imode-ui-logo-v532.png',location.href).href;
const PAGE_INFO={th:{dashboard:['I-MODE Plus Service & Maintenance','ภาพรวมงาน Service & Maintenance วันนี้'],cases:['เคสงานบริการ','รับเรื่อง มอบหมาย นัดหมาย ส่งงาน และปิดเคส'],quotation:['ทำใบเสนอราคา','คำนวณค่าบริการ ค่าเดินทาง งานด่วน อะไหล่ และเชื่อม Service Case'],customers:['ฐานข้อมูลลูกค้า','ข้อมูลลูกค้า เครื่องจักร Location และประวัติ Service'],machines:['ฐานข้อมูลเครื่องจักร','ข้อมูลหลักเครื่องจักรและประวัติการซ่อม'],qc:['QC เครื่องจักร','ตรวจรับเครื่อง / ก่อนส่งมอบ / หลังซ่อม / ก่อนติดตั้ง พร้อม Checklist และผลการอนุมัติ'],technicians:['ทีมช่าง','ทีม Service / Technical / R&D สำหรับ PM, Maintenance, Onsite และ Workshop'],calendar:['ปฏิทิน Service Maintenance','ตารางงานและนัดหมายของทีม Service Maintenance'],warranty:['ระบบประกันเครื่อง','ทะเบียนประกันและเอกสารการรับประกันเครื่องจักร'],documents:['คลังเอกสารเครื่องจักร','คู่มือ Drawing SOP PM และข้อมูลใช้งานเครื่อง'],notifications:['การแจ้งเตือน','นัดหมาย เคสด่วน งานรออะไหล่ และการส่งงาน'],reports:['รายงาน Service','สรุปข้อมูล KPI และ Service Report'],settings:['ตั้งค่าระบบ','กำหนดระบบ ภาษา โมดูล การแจ้งเตือน และ Cloud Database'],'field-service':['Field Service สำหรับทีมช่าง','คิวงาน Check-in ใบตรวจ รูปภาพ ลายเซ็น และ Service Report'],'customer-portal':['บริการลูกค้าผ่าน LINE OA','แจ้งปัญหา ตรวจประกัน ประวัติ Service และขอใบเสนอราคา']},en:{dashboard:['I-MODE Plus Service & Maintenance','Today’s Service & Maintenance overview'],cases:['Service Cases','Intake, assign, schedule, complete and close service cases'],quotation:['Quotation Builder','Calculate service, travel, emergency and parts linked to service cases'],customers:['Customer Database','Customers, machines, locations and service history'],machines:['Machine Database','Machine master data and repair history'],qc:['Machine QC','Incoming / pre-delivery / post-service / pre-installation with checklist and approval'],technicians:['Service Maintenance','Service / Technical / R&D teams for PM, maintenance, onsite and workshop'],calendar:['Service Maintenance Calendar','Service staff schedules and appointments'],warranty:['Machine Warranty','Warranty registry and warranty certificates'],documents:['Machine Document Center','Manuals, drawings, SOP, PM and machine documents'],notifications:['Notifications','Appointments, urgent cases, parts waiting and submissions'],reports:['Service Reports','KPI, summaries and service reports'],settings:['System Settings','System, language, modules, notifications and cloud database'],'field-service':['Technician Field Service','Queue, check-in, inspection, media, signatures and reports'],'customer-portal':['LINE OA Customer Service','Issue reporting, warranty, service history and quotation requests']}};let pageInfo=PAGE_INFO[settings.language||'th'];
const esc=(v='')=>String(v??'').replace(/[&<>"']/g,s=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[s]));
const uid=()=>crypto.randomUUID?crypto.randomUUID():'ID'+Date.now()+Math.random();
const uiLocale=()=>settings.language==='en'?'en-GB':'th-TH';
const fmt=v=>v?new Date(v).toLocaleString(settings.language==='en'?'en-GB':'th-TH',{dateStyle:'medium',timeStyle:'short'}):'-';
const fmtDay=v=>v?new Date(v).toLocaleDateString(settings.language==='en'?'en-GB':'th-TH',{dateStyle:'medium'}):'-';
function toastMsg(msg){toast.textContent=translatePartialUI(translateTextValue(msg,settings.language),settings.language);toast.classList.add('show');setTimeout(()=>toast.classList.remove('show'),2200)}
function saveLocal(){localStorage.setItem(K.cases,JSON.stringify(cases));localStorage.setItem(K.tech,JSON.stringify(technicians));localStorage.setItem(K.customers,JSON.stringify(customers));localStorage.setItem(K.machines,JSON.stringify(machines));localStorage.setItem(K.notifications,JSON.stringify(notifications));localStorage.setItem(K.quotes,JSON.stringify(quotations));localStorage.setItem(K.settings,JSON.stringify(settings));localStorage.setItem(K.user,JSON.stringify(currentUser));localStorage.setItem(K.warranties,JSON.stringify(warranties));localStorage.setItem(K.documents,JSON.stringify(machineDocuments));localStorage.setItem(K.lineRequests,JSON.stringify(lineRequests));localStorage.setItem(K.serviceReports,JSON.stringify(serviceReports));localStorage.setItem(K.qcRecords,JSON.stringify(qcRecords))}
function genTicket(){const d=new Date(),p=`SRV-${d.getFullYear()}${String(d.getMonth()+1).padStart(2,'0')}${String(d.getDate()).padStart(2,'0')}-`;let n=1;cases.forEach(c=>{if(c.ticket?.startsWith(p))n=Math.max(n,(parseInt(c.ticket.split('-').pop())||0)+1)});return p+String(n).padStart(3,'0')}
const techById=id=>technicians.find(t=>t.id===id),customerById=id=>customers.find(c=>c.id===id),machineById=id=>machines.find(m=>m.id===id);
function machineNameTh(m){return m?.nameTh||m?.name||m?.model||'เครื่องจักร'}
function machineNameEn(m){return m?.nameEn||m?.name||m?.model||'Machine'}
function machinePrimaryName(m){return settings.language==='en'?machineNameEn(m):machineNameTh(m)}
function machineSecondaryName(m){return settings.language==='en'?machineNameTh(m):machineNameEn(m)}
function machinePairHTML(m){return `<b>${esc(machinePrimaryName(m))}</b><small class="machine-name-alt">${esc(machineSecondaryName(m))}</small>`}
function machineOptionLabel(m){return `${machinePrimaryName(m)} / ${machineSecondaryName(m)} · ${m?.model||'-'}`}

const badge=(type,v)=>`<span class="badge ${type==='prio'?'prio-'+v:'status-'+v}">${esc(v)}</span>`;

const I18N_PAIRS=[['แดชบอร์ด','Dashboard'],['เคสงานบริการ','Service Cases'],['ทำใบเสนอราคา','Quotation Builder'],['ลูกค้า','Customers'],['เครื่องจักร','Machines'],['ทีมช่าง','Technician Team'],['ปฏิทินงาน','Calendar'],['ระบบประกันเครื่อง','Machine Warranty'],['เอกสารเครื่องจักร','Machine Documents'],['การแจ้งเตือน','Notifications'],['รายงาน','Reports'],['ตั้งค่าระบบ','System Settings'],['หน้าหลัก','Home'],['เคส','Cases'],['รับเคส','New Case'],['ปฏิทิน','Calendar'],['เพิ่มเติม','More'],['รับเคสใหม่','New Service Case'],['รับเคสบริการใหม่','New Service Case'],['ค้นหา...','Search...'],['ยกเลิก','Cancel'],['บันทึก','Save'],['แก้ไข','Edit'],['รายละเอียด','Details'],['ลบ','Delete'],['เพิ่มเครื่อง','Add Machine'],['เพิ่มช่าง','Add Technician'],['เพิ่มนัดหมาย','Add Appointment'],['วันและเวลานัดหมาย','Appointment Date & Time'],['วันที่','Date'],['เวลา','Time'],['สถานะ','Status'],['ทีมรับผิดชอบ','Responsible Team'],['ช่างผู้รับผิดชอบ','Assigned Technician'],['ยังไม่มอบหมาย','Unassigned'],['ผู้ใช้งานและสิทธิ์','Users & Roles'],['SLA และขั้นตอนงาน','SLA & Workflow'],['ข้อมูลหลักของระบบ','Master Data'],['ภาษาและการแสดงผล','Language & Display'],['ข้อมูลบริษัทและระบบ','Company & System'],['ค่าบริการและใบเสนอราคา','Service Commercial & Quotation'],['อัตราค่าบริการ','Service Rates'],['โซนและค่าเดินทาง','Travel Zones'],['งานด่วนและการรับประกันงานซ่อม','Emergency & Repair Warranty'],['ใบเสนอราคาและอะไหล่','Quotation & Parts'],['โมดูล Service ที่เพิ่มใหม่','New Service Modules'],['งานภาคสนามและใบตรวจช่าง','Field Service & Inspection'],['ตำแหน่งลูกค้าและ Google Maps','Customer Location & Google Maps'],['คลังเอกสารเครื่องจักร','Machine Document Center'],['รูปภาพ วิดีโอ และลายเซ็น','Photos, Videos & Signatures'],['รายงานและการส่งออกไฟล์','Reports & File Export'],['การสื่อสาร การเชื่อมต่อ และพฤติกรรมระบบ','Communication, Integration & System Behavior'],['การเชื่อมต่อระบบ','Integrations'],['LINE OA สำหรับลูกค้า','LINE OA Customer Portal'],['พฤติกรรมและความปลอดภัยระบบ','System Behavior & Security'],['ซิงก์และรีเฟรชข้อมูล','Sync & Refresh'],['ฐานข้อมูล Cloud','Cloud Database'],['สรุปค่ากำหนดระบบ','Configuration Summary'],['สถานะระบบ','System Status'],['ส่งออก JSON','Export JSON'],['ที่อยู่หน้างานแบบเต็ม','Full Site Address'],['ใช้ตำแหน่งอุปกรณ์นี้','Use Device Location'],['เปิด Google Maps','Open Google Maps'],['สถานะงาน','Work Status'],['ใบตรวจ / Report','Inspection / Report'],['ประเภทใบตรวจ','Inspection Type'],['Checklist จุดตรวจเครื่อง','Machine Inspection Checklist'],['อะไหล่ที่เปลี่ยน','Replaced Parts'],['รูปก่อนทำงาน','Before Photos'],['รูปหลังทำงาน','After Photos'],['วิดีโอประกอบงาน','Work Videos'],['สถานะการทำงานของช่าง','Technician Work Status'],['ลายเซ็นลูกค้า / ผู้รับมอบงาน','Customer / Receiver Signature'],['ลายเซ็นทีมช่าง','Technician Signature'],['บันทึก / จบงาน','Save / Complete Job'],['ดูตัวอย่าง Report','Preview Report'],['ส่ง Report เข้า LINE','Send Report via LINE'],['อยู่ในประกัน','Under Warranty'],['หมดประกัน','Warranty Expired'],['ใกล้หมดอายุ','Expiring Soon'],['ทุกสถานะ','All Statuses'],['ทุกทีม','All Teams'],['ทุก Priority','All Priorities'],['ด่วนมาก','Critical'],['สูง','High'],['ปกติ','Normal'],['ต่ำ','Low'],['เคสใหม่','New Case'],['มอบหมายแล้ว','Assigned'],['นัดหมายแล้ว','Scheduled'],['กำลังดำเนินการ','In Progress'],['รออะไหล่','Waiting for Parts'],['รอส่งงาน','Waiting for Submission'],['เสร็จสิ้น','Completed'],['ปิดเคส','Closed']];
const I18N_EXTRA_PAIRS=[["Excel (.xls)","Excel (.xls)"],["ทีมที่ใช้","Team scope"],["ทุกตำแหน่ง","All Positions"],["ตำแหน่ง","Position scope"],["หน้าหลัก","Home"],["แดชบอร์ด","Dashboard"],["เคส","Cases"],["รับเคส","New Case"],["รับเคสใหม่","New Case"],["รับเคสบริการ","Service Intake"],["รับเคสบริการใหม่","New Service Case"],["เพิ่มเติม","More"],["ค้นหา...","Search..."],["พิมพ์คำค้น...","Type to search..."],["ดู","View"],["เปิด","Open"],["แก้ไข","Edit"],["ลบ","Delete"],["บันทึก","Save"],["ยกเลิก","Cancel"],["รายละเอียด","Details"],["ดูรายละเอียด","View Details"],["ดูทั้งหมด","View All"],["ดูทั้งหมด →","View All →"],["ถัดไป →","Next →"],["← ก่อนหน้า","← Previous"],["วันนี้","Today"],["ทั้งหมด","All"],["ไม่มี","None"],["ไม่ระบุ","Not specified"],["ไม่ทราบ","Unknown"],["พร้อม","Ready"],["แนบ","Attached"],["จัดการ","Manage"],["ตั้งค่า","Settings"],["ทางลัด","Quick Access"],["กิจกรรม","Activity"],["รายการ","Items"],["ยอดรวม","Grand Total"],["ส่วนลด","Discount"],["ต้นทุน","Cost"],["ภาษา","Language"],["ภาษาไทย","Thai"],["ไทย","Thai"],["ไทย (th-TH)","Thai (th-TH)"],["ไทย / English","Thai / English"],["ภาษาไทย / English","Thai / English"],["เปลี่ยนภาษา / Change language","Change Language"],["ค้นหาเลขเคส ลูกค้า เครื่อง Serial...","Search case no., customer, machine, serial..."],["ค้นหาลูกค้า เครื่อง เบอร์โทร หรือรายละเอียด...","Search customer, machine, phone or details..."],["ค้นหาชื่อเอกสาร, Model, Version...","Search document, model or version..."],["ค้นหา Warranty No., ลูกค้า, เครื่อง, Serial...","Search warranty no., customer, machine or serial..."],["เริ่มพิมพ์เพื่อค้นหา","Start typing to search"],["กดเพื่อดูรายละเอียด","Click to view details"],["ไม่พบข้อมูล","No data found"],["ไม่มีรายการ","No items"],["ล้างข้อมูล","Clear Data"],["ส่งออก JSON","Export JSON"],["เคสงานบริการ","Service Cases"],["ทำใบเสนอราคา","Quotation Builder"],["ฐานข้อมูลลูกค้า","Customer Database"],["ฐานข้อมูลเครื่องจักร","Machine Database"],["ทีมช่าง","Technician Team"],["ปฏิทินงาน","Calendar"],["ปฏิทินงานช่าง","Technician Calendar"],["ระบบประกันเครื่อง","Machine Warranty"],["คลังเอกสารเครื่องจักร","Machine Document Center"],["เอกสารเครื่องจักร","Machine Documents"],["การแจ้งเตือน","Notifications"],["รายงาน","Reports"],["รายงาน Service","Service Reports"],["ตั้งค่าระบบ","System Settings"],["บริการลูกค้าผ่าน LINE OA","LINE OA Customer Service"],["Field Service สำหรับทีมช่าง","Technician Field Service"],["หน้างานช่าง","Field Service"],["งานหน้างาน","Field Work"],["ศูนย์การแจ้งเตือน","Notification Center"],["ศูนย์ควบคุมและกำกับระบบ","System Control & Governance"],["ระบบและ Cloud","System & Cloud"],["ฐานข้อมูล Cloud","Cloud Database"],["สถานะระบบ","System Status"],["โหมดข้อมูล","Data Mode"],["ภาพรวมงาน Service & Maintenance วันนี้","Today’s Service & Maintenance overview"],["รับเรื่อง มอบหมาย นัดหมาย ส่งงาน และปิดเคส","Intake, assign, schedule, complete and close service cases"],["คำนวณค่าบริการ ค่าเดินทาง งานด่วน อะไหล่ และเชื่อม Service Case","Calculate service, travel, emergency and parts linked to service cases"],["ข้อมูลลูกค้า เครื่องจักร Location และประวัติ Service","Customer, machine, location and service history"],["ข้อมูลหลักเครื่องจักรและประวัติการซ่อม","Machine master data and repair history"],["ภาระงาน นัดหมาย และเคสที่รับผิดชอบ","Workload, appointments and assigned cases"],["ตารางงานของช่างแต่ละคน","Individual technician schedules"],["ทะเบียนประกันและเอกสารการรับประกันเครื่องจักร","Warranty registry and warranty certificates"],["คู่มือ Drawing SOP PM และข้อมูลใช้งานเครื่อง","Manuals, drawings, SOP, PM and machine documents"],["นัดหมาย เคสด่วน งานรออะไหล่ และการส่งงาน","Appointments, urgent cases, parts waiting and submissions"],["สรุปข้อมูล KPI และ Service Report","KPI summaries and service reports"],["กำหนดระบบ ภาษา โมดูล การแจ้งเตือน และ Cloud Database","Configure system, language, modules, notifications and cloud database"],["คิวงาน Check-in ใบตรวจ รูปภาพ ลายเซ็น และ Service Report","Queue, check-in, inspection, media, signatures and service reports"],["แจ้งปัญหา ตรวจประกัน ประวัติ Service และขอใบเสนอราคา","Issue reporting, warranty checks, service history and quotation requests"],["ดูนัดหมายตามวัน สัปดาห์ เดือน ปี และกรองตามทีม","View appointments by day, week, month or year and filter by team"],["Machine Master และประวัติการซ่อม","Machine master and repair history"],["กำหนดผู้ใช้งาน ขั้นตอนงาน SLA ค่าบริการ การแจ้งเตือน ภาษา การเชื่อมต่อ และโมดูลทั้งหมดจากหน้าเดียว","Manage users, workflow, SLA, service rates, notifications, language, integrations and all modules from one page"],["ลูกค้า","Customers"],["ลูกค้า *","Customer *"],["บริษัท","Company"],["บริษัทลูกค้า *","Customer Company *"],["ผู้ติดต่อ","Contact Person"],["โทรศัพท์","Phone"],["อีเมล","Email"],["สาขา","Branch"],["จังหวัด / พื้นที่","Province / Area"],["สถานที่","Location"],["สถานที่ให้บริการ","Service Location"],["ที่อยู่","Address"],["ที่อยู่หน้างานแบบเต็ม","Full Site Address"],["สถานที่ / ที่อยู่หน้างาน","Site / Service Address"],["ข้อมูลลูกค้า","Customer Information"],["จำนวนลูกค้า","Customer Count"],["ลูกค้าที่มีเคส","Customers with Cases"],["ลูกค้าเชื่อม LINE","LINE-linked Customers"],["เครื่อง","Machine"],["เครื่องจักร","Machines"],["เครื่องจักร *","Machine *"],["ชื่อเครื่อง *","Machine Name *"],["ขนาดเครื่อง","Machine Size"],["S - เครื่องเล็ก","S - Small Machine"],["L - เครื่องใหญ่","L - Large Machine"],["จำนวนเครื่อง","Machine Count"],["เครื่องพร้อม QR","Machines with QR"],["เครื่อง / Serial","Machine / Serial"],["ทุกเครื่อง","All Machines"],["เลือกเครื่อง","Select Machine"],["ยังไม่มีเครื่อง","No Machines"],["ยังไม่มีข้อมูลเครื่องจักร","No Machine Data"],["เครื่องที่มีประวัติ","Machines with Service History"],["รูปภาพเครื่องจักร","Machine Photo"],["เพิ่มเครื่อง","Add Machine"],["＋ เพิ่มเครื่อง","＋ Add Machine"],["＋ เปิดเคสเครื่องนี้","＋ Open Case for This Machine"],["เปิดเคส","Open Case"],["＋ เปิดเคสใหม่","＋ Open New Case"],["📋 เปิดเคส","📋 Open Case"],["อ้างอิง Service Case","Reference Service Case"],["ไม่อ้างอิงเคส","No Case Reference"],["เลขเคส","Case No."],["รายการเคส","Case List"],["เคสล่าสุด","Recent Cases"],["เคสทั้งหมด","All Cases"],["เคสตามสถานะ","Cases by Status"],["เคสตามลูกค้า","Cases by Customer"],["เคสตามเครื่อง","Cases by Machine"],["เคสในประกัน","Warranty Cases"],["เคสที่เปิดอยู่","Open Cases"],["เคสด่วน","Urgent Cases"],["เคสด่วนมาก","Critical Cases"],["ไม่มีเคส","No Cases"],["ยังไม่มีเคส","No Cases Yet"],["เปิดเคสที่เกี่ยวข้อง","Open Related Case"],["1. อ้างอิงเคสและลูกค้า","1. Case & Customer Reference"],["1. ลูกค้าและเครื่องจักร","1. Customer & Machine"],["2. รายละเอียดเคส","2. Case Details"],["3. เครื่องจักร","3. Machines"],["3. มอบหมาย / นัดหมาย","3. Assignment / Appointment"],["อาการ / ปัญหา *","Issue / Problem *"],["อาการ / รายละเอียด *","Issue / Details *"],["อาการ / รายละเอียด","Issue / Details"],["หมายเหตุ","Notes"],["หมายเหตุภายใน","Internal Note"],["หมายเหตุ / คำอธิบาย","Notes / Description"],["หมายเหตุ / S/N","Notes / S/N"],["ช่องทางรับแจ้ง","Intake Channel"],["ประเภทบริการ","Service Type"],["ประเภทงาน","Work Type"],["ประเภทงาน Service","Service Work Type"],["ประเภทงานใน Case","Case Work Type"],["ระดับความเร่งด่วน","Priority"],["ทุก Priority","All Priorities"],["ความเร่งด่วน","Urgency"],["ด่วนมาก","Critical"],["สูง","High"],["ปกติ","Normal"],["ต่ำ","Low"],["สถานะ","Status"],["ทุกสถานะ","All Statuses"],["ใหม่","New"],["เคสใหม่","New Case"],["มอบหมายแล้ว","Assigned"],["นัดหมายแล้ว","Scheduled"],["กำลังดำเนินการ","In Progress"],["รออะไหล่","Waiting for Parts"],["รอส่งงาน","Waiting for Submission"],["เสร็จสิ้น","Completed"],["ปิดเคส","Closed"],["พร้อมรับงาน","Available"],["กำลังปฏิบัติงาน","Working"],["ลงพื้นที่","On Site"],["ไม่พร้อม","Unavailable"],["สถานะถัดไป","Next Status"],["เปลี่ยนสถานะ","Change Status"],["บันทึกสถานะ","Save Status"],["Workflow / สถานะเคส","Workflow / Case Status"],["งานที่ปิดแล้ว","Completed Jobs"],["งานที่กำลังเปิด","Open Work"],["งานเปิด","Open Jobs"],["งานของฉัน","My Jobs"],["งานวันนี้","Today’s Jobs"],["7 วันข้างหน้า","Next 7 Days"],["งานใกล้ถึง","Upcoming Jobs"],["รอส่ง Report","Waiting for Report"],["ปิดงานแล้ว","Completed Jobs"],["ไม่มีงานที่รับผิดชอบ","No Assigned Work"],["ไม่มีงานนัดหมายในวันนี้","No appointments today"],["ยังไม่มีคิวนัดหมาย","No Scheduled Queue"],["ยังไม่มีนัดหมายใกล้ถึง","No Upcoming Appointments"],["ไม่มีงานตามเงื่อนไขที่เลือก","No jobs match the selected filters"],["ปฏิทิน","Calendar"],["วัน","Day"],["สัปดาห์","Week"],["เดือน","Month"],["ปี","Year"],["วันที่","Date"],["เวลา","Time"],["วันและเวลานัดหมาย","Appointment Date & Time"],["วันและเวลา *","Date & Time *"],["นัดหมาย","Appointments"],["จัดตารางนัดหมาย","Manage Appointments"],["เพิ่มนัดหมาย","Add Appointment"],["＋ เพิ่มนัดหมาย","＋ Add Appointment"],["📅 เพิ่มนัดหมาย","📅 Add Appointment"],["📅 นัดหมาย","📅 Appointment"],["บันทึกนัดหมาย","Save Appointment"],["ทีม","Team"],["ทุกทีม","All Teams"],["ทีม Technical","Technical Team"],["ทีม R&D","R&D Team"],["ทีมรับผิดชอบ","Responsible Team"],["ช่าง","Technician"],["ช่าง *","Technician *"],["ช่างผู้รับผิดชอบ","Assigned Technician"],["ช่างผู้ใช้งาน","Active Technician"],["เลือกช่าง","Select Technician"],["ยังไม่มอบหมาย","Unassigned"],["มอบหมายงาน","Assign Cases"],["งานที่มอบหมาย","Assigned Jobs"],["บุคลากรทั้งหมด","Total Personnel"],["ภาระงาน Service Maintenance","Technician Workload"],["ดูงานของช่าง","View Technician Jobs"],["ดูทีมช่าง","View Technician Team"],["ปฏิทินเต็ม","Full Calendar"],["ดูปฏิทินทั้งหมด →","View Full Calendar →"],["สรุปนัดหมาย","Appointment Summary"],["ภาพรวมงานนัดหมายทั้งเดือน","Monthly Appointment Overview"],["ชื่อ-นามสกุล *","Full Name *"],["ตำแหน่ง","Position"],["ทักษะ / Skills","Skills"],["สีในปฏิทิน","Calendar Color"],["น้ำเงิน","Blue"],["ส้ม","Orange"],["เขียว","Green"],["ม่วง","Purple"],["รูปประจำตัวช่าง","Technician Photo"],["📷 เลือกรูป","📷 Choose Photo"],["ลบรูป","Remove Photo"],["เพิ่มช่าง","Add Technician"],["＋ เพิ่มช่าง","＋ Add Technician"],["บันทึกช่าง","Save Technician"],["จัดการทีม","Manage Team"],["อัปเดตสถานะงาน","Update Job Status"],["สถานะงาน","Work Status"],["สถานะหน้างาน","Field Status"],["สถานะการทำงานของช่าง","Technician Work Status"],["กำลังเดินทาง","Traveling"],["ถึงหน้างาน","Arrived On Site"],["เริ่มตรวจเช็ก","Inspection Started"],["กำลัง PM / Maintenance","PM / Maintenance in Progress"],["กำลังซ่อม Service","Service Repair in Progress"],["ทดสอบเครื่อง","Machine Testing"],["รอลูกค้าตรวจรับ","Waiting for Customer Acceptance"],["จบงาน","Job Completed"],["ยังไม่อัปเดต","Not Updated"],["📡 สถานะงาน","📡 Work Status"],["Check-in หน้างาน","Field Check-in"],["📍 Check-in","📍 Check-in"],["🗺 นำทาง","🗺 Navigate"],["📞 โทร","📞 Call"],["🔔 เตือนลูกค้า","🔔 Remind Customer"],["ใบตรวจ / Report","Inspection / Report"],["ประเภทใบตรวจ","Inspection Type"],["Checklist จุดตรวจเครื่อง","Machine Inspection Checklist"],["งานที่ดำเนินการ","Work Performed"],["Diagnosis / Findings","Diagnosis / Findings"],["Recommendation / งานที่ควรทำต่อ","Recommendation / Follow-up Work"],["อะไหล่ที่เปลี่ยน","Replaced Parts"],["ชื่ออะไหล่","Part Name"],["＋ เพิ่มอะไหล่","＋ Add Part"],["รูปก่อนทำงาน","Before Photos"],["รูปหลังทำงาน","After Photos"],["วิดีโอประกอบงาน","Work Videos"],["📷 เพิ่มรูปก่อนทำงาน","📷 Add Before Photo"],["📷 เพิ่มรูปหลังทำงาน","📷 Add After Photo"],["🎥 เพิ่มวิดีโอ","🎥 Add Video"],["📎 เพิ่มรูปหรือวิดีโอ","📎 Add Photo or Video"],["หลักฐานสถานะงาน (รูป / วิดีโอ)","Work Status Evidence (Photo / Video)"],["รายละเอียด / สิ่งที่พบ","Details / Findings"],["หมายเหตุ / ค่าที่ตรวจวัด","Notes / Measured Values"],["ผู้รับมอบงาน / ลูกค้า","Customer / Receiver"],["PM ครั้งถัดไป","Next PM"],["ลายเซ็นลูกค้า / ผู้รับมอบงาน","Customer / Receiver Signature"],["ลายเซ็นทีมช่าง","Technician Signature"],["ล้างลายเซ็น","Clear Signature"],["ใช้นิ้วหรือเมาส์เซ็นในกรอบ","Sign in the box using a finger or mouse"],["บันทึก / จบงาน","Save / Complete Job"],["ดูตัวอย่าง Report","Preview Report"],["ส่ง Report เข้า LINE","Send Report via LINE"],["💬 ส่ง Report เข้า LINE","💬 Send Report via LINE"],["รายงานและการส่งออกไฟล์","Reports & File Export"],["＋ ทำรายงานหน้างาน","＋ Create Field Report"],["ยังไม่มี Service Report","No Service Reports Yet"],["ไม่มีการเปลี่ยนอะไหล่","No Parts Replaced"],["ยังไม่มีอะไหล่ที่เปลี่ยน","No Replaced Parts"],["ยังไม่มี Timeline","No Timeline Yet"],["Location ลูกค้า","Customer Location"],["คิวเข้าหาลูกค้า","Customer Visit Queue"],["เรียงตามวันและเวลานัดหมาย","Sorted by Appointment Date & Time"],["ที่อยู่ GPS และ Check-in","GPS Location & Check-in"],["รูปสูงสุดต่อ Before / After","Max Photos per Before / After Set"],["วิดีโอสูงสุดต่อ Report","Max Videos per Report"],["ขนาดวิดีโอสูงสุด (MB)","Max Video Size (MB)"],["จำนวนรูปสูงสุดต่อชุด","Max Photos per Set"],["คุณภาพบีบอัดรูป (%)","Image Compression Quality (%)"],["ประกัน","Warranty"],["การรับประกัน","Warranty"],["Warranty งานซ่อม","Repair Warranty"],["ทะเบียนการรับประกัน","Warranty Registry"],["Warranty ทั้งหมด","All Warranties"],["อยู่ในประกัน","Under Warranty"],["หมดประกัน","Warranty Expired"],["ใกล้หมดอายุ","Expiring Soon"],["ยังไม่เริ่ม","Not Started"],["ยังไม่มี Warranty","No Warranty"],["ยังไม่มี Warranty ตามเงื่อนไข","No Warranties Match the Filters"],["เช็คประกัน","Check Warranty"],["เช็ค Warranty","Check Warranty"],["ดูข้อมูลประกัน","View Warranty"],["จัดการประกัน","Manage Warranty"],["แก้ไข Warranty","Edit Warranty"],["＋ เพิ่ม Warranty","＋ Add Warranty"],["＋ ลงทะเบียน Warranty","＋ Register Warranty"],["ใบรับประกัน","Warranty Certificate"],["ใบรับประกันเครื่องจักร","Machine Warranty Certificate"],["🖨 ใบรับประกัน","🖨 Warranty Certificate"],["เริ่มประกัน","Warranty Start"],["เริ่มประกัน *","Warranty Start *"],["สิ้นสุดประกัน","Warranty End"],["สิ้นสุดประกัน *","Warranty End *"],["วันที่ติดตั้ง","Installation Date"],["วันที่ซื้อ / ส่งมอบ","Purchase / Delivery Date"],["ระยะเวลา","Duration"],["ระยะเวลา (เดือน)","Duration (Months)"],["ระยะประกันมาตรฐาน (เดือน)","Default Warranty Period (months)"],["แจ้งเตือนก่อนหมดประกัน (วัน)","Warranty Expiry Reminder (days)"],["ขอบเขตการรับประกัน","Warranty Coverage"],["ข้อยกเว้น","Exclusions"],["ข้อยกเว้น / ไม่ครอบคลุม","Exclusions / Not Covered"],["ข้อยกเว้น / EXCLUSIONS","EXCLUSIONS"],["ขอบเขตการรับประกัน / WARRANTY COVERAGE","WARRANTY COVERAGE"],["ประกันและเอกสาร","Warranty & Documents"],["Warranty และเอกสาร","Warranty & Documents"],["ขอซื้อ Warranty","Purchase Warranty"],["ซื้อ / ต่อ Warranty","Purchase / Extend Warranty"],["ขอราคา Warranty","Request Warranty Quote"],["ขอใบเสนอราคา Warranty","Request Warranty Quotation"],["แพ็กเกจ Warranty","Warranty Package"],["แพ็กเกจที่สนใจ","Interested Package"],["แพ็กเกจ 6 เดือน","6-Month Package"],["แพ็กเกจ 12 เดือน","12-Month Package"],["แพ็กเกจ 24 เดือน","24-Month Package"],["6 เดือน","6 Months"],["12 เดือน","12 Months"],["24 เดือน","24 Months"],["ราคา Extended Warranty สำหรับใบเสนอราคา","Extended Warranty Pricing for Quotations"],["ระยะประกัน เตือนหมดอายุ และแพ็กเกจ","Warranty Period, Expiry Alerts & Packages"],["Warranty อาการเดิม / จุดเดิม","Warranty for Same Issue / Same Component"],["ไม่ใช่ Warranty","Not Warranty"],["เอกสารแนบ Warranty / Purchase","Warranty / Purchase Attachment"],["วันเริ่ม/หมดอายุ Warranty","Warranty Start / Expiry Date"],["เอกสาร","Documents"],["เอกสารเครื่อง","Machine Documents"],["📁 เอกสารเครื่อง","📁 Machine Documents"],["ชื่อเอกสาร *","Document Name *"],["หมวดหมู่","Category"],["หมวดหมู่ *","Category *"],["ทุกหมวดหมู่","All Categories"],["วันที่เอกสาร","Document Date"],["ผู้จัดทำเอกสาร","Document Owner"],["ผู้จัดทำ","Prepared By"],["ภาษา / Locale","Language / Locale"],["ไฟล์ / Link","File / Link"],["📎 เลือกไฟล์","📎 Choose File"],["ลบไฟล์","Remove File"],["ลบไฟล์แนบ","Remove Attachment"],["เปิดเอกสาร","Open Document"],["เพิ่มเอกสาร","Add Document"],["＋ เพิ่มเอกสาร","＋ Add Document"],["บันทึกเอกสาร","Save Document"],["แก้ไขหมวดเอกสาร","Edit Document Categories"],["เอกสารและข้อมูลใช้งานเครื่อง","Machine Documents & Operating Information"],["หมวดเอกสาร Upload / Drive Link","Document Categories, Upload / Drive Link"],["เอกสารทั่วไป / ทุกเครื่อง","General Documents / All Machines"],["ยังไม่มีเอกสาร","No Documents Yet"],["ยังไม่มีเอกสารตามเงื่อนไข","No Documents Match the Filters"],["ยังไม่มีเอกสารสำหรับเครื่องนี้","No Documents for This Machine"],["คู่มือเครื่อง","Machine Manual"],["📘 คู่มือและเอกสาร","📘 Manuals & Documents"],["ใบเสนอราคา","Quotation"],["รายการใบเสนอราคา","Quotation List"],["ใบเสนอราคาใหม่","New Quotation"],["สรุปใบเสนอราคา","Quotation Summary"],["ดูใบเสนอราคา","View Quotation"],["ทำใบเสนอราคา","Create Quotation"],["฿ ทำใบเสนอราคา","฿ Create Quotation"],["ขอใบเสนอราคา","Request Quotation"],["ส่งคำขอใบเสนอราคาแล้ว","Quotation Request Submitted"],["💾 บันทึกใบเสนอราคา","💾 Save Quotation"],["🖨 ดู / พิมพ์ใบเสนอราคา","🖨 View / Print Quotation"],["🖨 พิมพ์ / Save PDF","🖨 Print / Save PDF"],["ร่าง","Draft"],["ส่งแล้ว","Sent"],["รออนุมัติ","Pending Approval"],["อนุมัติ","Approved"],["ไม่อนุมัติ","Rejected"],["หมดอายุ","Expired"],["ไม่คิด VAT","No VAT"],["ส่วนลด %","Discount %"],["หัก Diagnosis","Less Diagnosis"],["ค่าใช้จ่ายอื่น","Other Expenses"],["ค่าทางด่วน","Toll Fee"],["ชั่วโมงเพิ่มเติม","Additional Hours"],["จำนวนช่างเพิ่ม","Additional Technicians"],["เครื่องถัดไป S","Additional Machine S"],["เครื่องถัดไป L","Additional Machine L"],["ระยะทางเที่ยวเดียว (km)","One-way Distance (km)"],["ระยะทางและค่าเดินทาง","Distance & Travel Charge"],["ประเภทบริการ ระยะทาง ความเร่งด่วน และ Warranty","Service type, distance, urgency and warranty"],["2. เงื่อนไขบริการ","2. Service Conditions"],["4. ค่าแรงเพิ่มเติม / Diagnosis","4. Additional Labor / Diagnosis"],["6. ค่าใช้จ่ายอื่น / ส่วนลด","6. Other Expenses / Discount"],["Critical • เสนอราคาเป็นกรณี","Critical • Quote Case by Case"],["เครื่องแรกคิดเต็มราคา เครื่องถัดไปใช้อัตรา Additional Machine","First machine charged at full rate; additional machines use the Additional Machine rate"],["เครื่องต้องระบุ Size S/L ใน Machine Master เพื่อคำนวณราคาได้ถูกต้อง","Machine Size S/L must be set in Machine Master for accurate pricing"],["เชื่อม Service Case + Customer + Machine S/L + Travel + Emergency + Spare Parts","Link Service Case + Customer + Machine S/L + Travel + Emergency + Spare Parts"],["Zone 0 • 250 บาท","Zone 0 • THB 250"],["บาท","THB"],["บาท/km","THB/km"],["ค่าบริการ","Service Fee"],["ค่าเดินทาง","Travel Charge"],["อะไหล่","Parts"],["ไม่มีรายการอะไหล่","No Parts"],["ยังไม่มีรายการอะไหล่","No Parts Yet"],["ยังไม่มีเครื่องในใบเสนอราคา","No Machines in Quotation"],["ยังไม่มีใบเสนอราคา","No Quotations Yet"],["หมายเหตุ / REMARK","REMARK"],["Delivery : ตามตกลง","Delivery: As Agreed"],["Payment : ตามเงื่อนไข","Payment: Per Agreed Terms"],["ผู้ใช้งานและสิทธิ์","Users & Roles"],["SLA และขั้นตอนงาน","SLA & Workflow"],["ข้อมูลหลักของระบบ","Master Data"],["ภาษาและการแสดงผล","Language & Display"],["ข้อมูลบริษัทและระบบ","Company & System"],["ค่าบริการและใบเสนอราคา","Service Commercial & Quotation"],["อัตราค่าบริการ","Service Rates"],["โซนและค่าเดินทาง","Travel Zones"],["งานด่วนและการรับประกันงานซ่อม","Emergency & Repair Warranty"],["ใบเสนอราคาและอะไหล่","Quotation & Parts"],["โมดูล Service ที่เพิ่มใหม่","New Service Modules"],["งานภาคสนามและใบตรวจช่าง","Field Service & Inspection"],["ตำแหน่งลูกค้าและ Google Maps","Customer Location & Google Maps"],["รูปภาพ วิดีโอ และลายเซ็น","Photos, Videos & Signatures"],["การสื่อสาร การเชื่อมต่อ และพฤติกรรมระบบ","Communication, Integration & System Behavior"],["การเชื่อมต่อระบบ","Integrations"],["LINE OA สำหรับลูกค้า","LINE OA Customer Portal"],["พฤติกรรมและความปลอดภัยระบบ","System Behavior & Security"],["ซิงก์และรีเฟรชข้อมูล","Sync & Refresh"],["สรุปค่ากำหนดระบบ","Configuration Summary"],["แก้ไขได้ทุกส่วน","All Settings Editable"],["✓ แก้ไขได้ทุกส่วน","✓ All Settings Editable"],["การจัดการระบบและขั้นตอนงาน","System & Workflow Management"],["การจัดการระบบและ Workflow","System & Workflow Management"],["ชื่อระบบ","System Name"],["ชื่อบริษัท (TH)","Company Name (TH)"],["ชื่อบริษัท (EN)","Company Name (EN)"],["อีเมล Service","Service Email"],["เริ่มเวลาทำการ","Business Hours Start"],["สิ้นสุดเวลาทำการ","Business Hours End"],["Urgent Response (นาที)","Urgent Response (min)"],["Resolution Target (ชม.)","Resolution Target (hr)"],["Auto Escalate (นาที)","Auto Escalate (min)"],["Repair Warranty (วัน)","Repair Warranty (days)"],["Warranty งานซ่อม (วัน)","Repair Warranty (days)"],["Urgent เพิ่ม (%)","Urgent Surcharge (%)"],["Emergency เพิ่ม (%)","Emergency Surcharge (%)"],["อายุใบเสนอราคา (วัน)","Quotation Validity (days)"],["Extra Labor S / ชม.","Extra Labor S / hr"],["Extra Labor L / ชม.","Extra Labor L / hr"],["แจ้งเตือนก่อนนัดหมาย (ชั่วโมง)","Appointment Reminder (hours)"],["ขนาดไฟล์ Local สูงสุด (MB)","Max Local File Size (MB)"],["รูปแบบหลัก","Default Format"],["Event ที่ต้องแจ้ง","Notification Events"],["ช่องทางแจ้งเตือน","Notification Channels"],["ข้อมูลเชื่อมโยง","Integration Details"],["ตั้งค่า LINE OA","Configure LINE OA"],["⚙ ตั้งค่า LINE OA","⚙ Configure LINE OA"],["URL โครงการ Supabase","Supabase Project URL"],["เชื่อม Cloud","Connect Cloud"],["ใช้ข้อมูลในเครื่อง","Use Local Data"],["ซิงก์ข้อมูลกับ Cloud และรีเฟรชระบบ","Sync cloud data and refresh the system"],["ค่าหลักที่ระบบกำลังใช้งาน","Current Active Configuration"],["จำนวนข้อมูลและการเชื่อมต่อ","Data & Connection Summary"],["ภาพรวมระบบ","System Overview"],["ผู้ดูแลระบบ","Administration"],["ดูแดชบอร์ด","View Dashboard"],["ดูเคส","View Cases"],["สร้างเคส","Create Cases"],["แก้ไขเคส","Edit Cases"],["เพิ่ม/แก้ไขลูกค้า","Manage Customers"],["ดูเครื่องจักร","View Machines"],["เพิ่ม/แก้ไขเครื่องจักร","Manage Machines"],["ดูปฏิทิน","View Calendar"],["เข้า Field Service","Access Field Service"],["สร้าง/แก้ไขใบตรวจ","Manage Inspection Reports"],["เพิ่มรูป/วิดีโอ","Add Photos / Videos"],["ลงลายเซ็นจบงาน","Capture Completion Signatures"],["สร้าง/แก้ไขใบเสนอราคา","Create / Edit Quotations"],["อนุมัติราคา","Approve Quotations"],["เพิ่ม/แก้ไขเอกสาร","Manage Documents"],["ดูเอกสารเครื่อง","View Machine Documents"],["ดูรายงาน","View Reports"],["ดูการแจ้งเตือน","View Notifications"],["จัดการการแจ้งเตือน","Manage Notifications"],["ดูคำขอจาก LINE OA","View LINE OA Requests"],["จัดการ LINE OA / ส่ง Report","Manage LINE OA / Send Reports"],["จัดการผู้ใช้งานและสิทธิ์","Manage Users & Roles"],["แก้ไขตั้งค่าระบบ","Manage System Settings"],["จัดการ Integration / Cloud","Manage Integrations / Cloud"],["ดู Audit / ประวัติการเปลี่ยนแปลง","View Audit Trail"],["ทุกโมดูล","All Modules"],["สิทธิ์ระบบ / Role","System Role / Permission"],["ชื่อผู้ใช้งาน","User Name"],["บันทึกและเข้าสู่ระบบ","Save & Sign In"],["เลือกผู้ใช้งาน","Select User"],["คุณไม่มีสิทธิ์ใช้งานฟังก์ชันนี้","You do not have permission to use this function"],["แจ้งเตือน","Notifications"],["ไม่มีการแจ้งเตือน","No Notifications"],["ดูการแจ้งเตือนทั้งหมด →","View All Notifications →"],["คำขอจาก LINE OA / QR เครื่อง","LINE OA / Machine QR Requests"],["คำขอใหม่จาก LINE","New LINE Requests"],["ยังไม่มีคำขอจาก LINE OA","No LINE OA Requests Yet"],["สถานะคำขอจาก LINE","LINE Request Status"],["เปิด Service Case ผ่าน LINE","Open Service Case via LINE"],["ลูกค้า + LINE Official Account","Customer + LINE Official Account"],["ติดต่อ Service","Contact Service"],["โทร / LINE Official Account","Call / LINE Official Account"],["แจ้งปัญหา","Report Issue"],["แจ้งปัญหาเครื่อง","Report Machine Issue"],["🔧 แจ้งปัญหาเครื่อง","🔧 Report Machine Issue"],["ส่งแจ้งปัญหา","Submit Issue"],["รับแจ้งปัญหาแล้ว","Issue Received"],["🕘 ประวัติ Service","🕘 Service History"],["ประวัติ Service","Service History"],["ประวัติ Service ของเครื่อง","Machine Service History"],["ข้อมูลทั้งหมดจะอ้างอิงจาก QR ประจำเครื่องนี้","All information is linked to this machine’s QR code"],["เลือกบริการด้านบนได้เลย","Select a service above to continue"],["กรุณาติดต่อ I-MODE Plus Service","Please contact I-MODE Plus Service"],["กรุณาติดต่อ Service ตามข้อมูลบริษัท","Please contact Service using the company contact details"],["ทีม Service จะติดต่อกลับเพื่อยืนยันนัดหมาย","The Service team will contact you to confirm the appointment"],["ทีม I-MODE Plus จะจัดทำราคา Warranty และติดต่อกลับ","I-MODE Plus will prepare the warranty quotation and contact you"],["ระบุความต้องการเพิ่มเติม","Enter additional requirements"],["Customer / ผู้รับเอกสาร","Customer / Document Recipient"],["ยังไม่มีข้อมูลลูกค้า","No Customer Data"],["ยังไม่มีประวัติ","No History Yet"],["ยังไม่มีประวัติ Service","No Service History Yet"],["ยังไม่มีกิจกรรม","No Activity Yet"],["ยังไม่มีไฟล์แนบ","No Attachment"],["ยังไม่มีไฟล์ Upload","No Uploaded File"],["ไม่มีรายชื่อในทีมนี้","No Team Members"],["ไม่มีงานของช่างคนนี้","No Jobs for This Technician"],["ยังไม่มีงานของช่างคนนี้","No Jobs for This Technician"],["ข้อมูลคำนวณจากเคสในระบบ","Calculated from service cases in the system"],["เข้าถึงข้อมูลหลักได้ทันที","Quick access to key data"],["ดูงานที่เพิ่งรับเข้าระบบ","View recently received cases"],["เปิดข้อมูลลูกค้าและประวัติ Service","Open customer information and service history"],["สร้างเคสใหม่และส่งต่อให้ทีมช่าง","Create a new case and assign it to the technician team"],["ลูกค้า → เครื่อง → อาการ → Priority","Customer → Machine → Issue → Priority"],["รับแจ้งปัญหา ขอ Warranty และติดตามคำขอจากลูกค้า","Receive issues, warranty requests and track customer requests"],["เลือกเคสเพื่อดึงข้อมูลลูกค้าและเครื่องอัตโนมัติ","Select a case to load customer and machine data automatically"],["ค้นหาเอกสารตามเครื่อง Model หมวดหมู่ Version และภาษา","Search documents by machine, model, category, version and language"],["เชื่อมลูกค้า เครื่องจักร LINE OA และประวัติ Service","Connect customers, machines, LINE OA and service history"],["รายงานส่งลูกค้า พร้อม PM Checklist, อะไหล่ และ Before / After","Customer-ready reports with PM checklist, parts and before/after photos"],["LINE OA, Google Maps, Cloud, การแจ้งเตือน และการทำงานของ Web App","LINE OA, Google Maps, Cloud, notifications and Web App behavior"],["ตั้งค่า Field Service, Location, Warranty, Documents, Media, Report และ Export","Configure Field Service, Location, Warranty, Documents, Media, Reports and Export"],["แยกทีม Technical / R&D พร้อม Field Service Workspace สำหรับงาน PM, Maintenance, Onsite และ Workshop","Separate Technical / R&D teams with Field Service Workspace for PM, Maintenance, Onsite and Workshop"],["คู่มือใช้งาน • Datasheet • Drawing • Electrical Diagram • PM Checklist • SOP • Software/Firmware","Operating Manual • Datasheet • Drawing • Electrical Diagram • PM Checklist • SOP • Software/Firmware"],["ทะเบียน Warranty • ติดตามวันหมดอายุ • ประวัติเคสในประกัน • ออกเอกสารรับประกัน","Warranty Registry • Expiry Tracking • Warranty Case History • Warranty Certificates"],["รับแจ้งปัญหา • QR ประจำเครื่อง • เช็ค Warranty • ประวัติ Service/อะไหล่ • ขอซื้อ/ต่อ Warranty และใบเสนอราคา","Issue Intake • Machine QR • Warranty Check • Service/Parts History • Purchase/Extend Warranty & Quotations"],["คิวงาน • นัดหมาย • Check-in • PM Checklist • Before/After • อะไหล่ • Service Report","Queue • Appointments • Check-in • PM Checklist • Before/After • Parts • Service Report"],["Local Prototype จำกัดไฟล์ประมาณ 1.2 MB ต่อไฟล์ • Production แนะนำเก็บใน Google Drive / Supabase Storage","Local prototype limits files to about 1.2 MB each • For production, use Google Drive / Supabase Storage"],["Prototype: รูปจะถูกย่ออัตโนมัติ • วิดีโอแนะนำไม่เกิน 2.5 MB/ไฟล์ และสูงสุด 2 ไฟล์","Prototype: photos are compressed automatically • videos should be no larger than 2.5 MB each, maximum 2 files"],["รองรับ JPG / PNG / WEBP ระบบจะย่อรูปอัตโนมัติเพื่อให้ Web ทำงานได้เร็วขึ้น","Supports JPG / PNG / WEBP; images are automatically resized for faster web performance"],["เพิ่มรูปเครื่องเพื่อให้หน้า Machine Master อ่านง่ายขึ้น และใช้ประกอบรายละเอียดเครื่อง","Add a machine photo to improve Machine Master readability and machine details"],["ทีมช่างจะดึง Location จาก ฐานข้อมูลลูกค้า อัตโนมัติเมื่อเปิดคิวงานและกดนำทาง","Technicians automatically use the customer location from ฐานข้อมูลลูกค้า for the job queue and navigation"],["Checklist จะเปลี่ยนตามประเภทงาน","Checklist changes according to work type"],["ระบบทดลองจำกัดวิดีโอ 2 ไฟล์ ไฟล์ละไม่เกิน 2.5 MB","Prototype limits videos to 2 files, maximum 2.5 MB each"],["เช่น ตรวจพบเสียงผิดปกติบริเวณ Motor...","e.g. Abnormal noise detected near the motor..."],["ที่อยู่สำหรับทีมช่าง / ใช้ค้นหาใน Google Maps","Site address for technicians / Google Maps search"],["https://maps.google.com/... หรือ https://goo.gl/maps/...","https://maps.google.com/... or https://goo.gl/maps/..."],["ยังไม่พบข้อมูลการรับประกันของเครื่องนี้","No warranty information found for this machine"],["ไม่พบข้อมูลเครื่องจาก QR Code","No machine data found from the QR code"],["QR Library ยังโหลดไม่สำเร็จ","QR library failed to load"],["เชื่อม Customer + Machine + Serial No. และออก Warranty Certificate ได้","Link Customer + Machine + Serial No. and issue a Warranty Certificate"],["LIFF / QR เครื่อง / Webhook Backend","LIFF / Machine QR / Webhook Backend"],["แนะนำใช้ LIFF URL + Machine QR Token แบบสุ่ม และให้ Backend ตรวจสิทธิ์ก่อนคืนข้อมูลลูกค้า/เครื่อง","Use a LIFF URL with a random machine QR token and validate access on the backend before returning customer/machine data"],["ส่วนนี้สำหรับ IT เชื่อมฐานข้อมูลกลางภายหลัง โดยระบบยังใช้งานแบบ Local ได้ตามปกติ","This section is for IT to connect the central database later; the system can continue operating in Local mode"],["ถ้าใส่ “บาท/km” มากกว่า 0 ระบบจะใช้ระยะทาง × บาท/km แทนค่าเหมาจ่ายของ Zone นั้น","If THB/km is greater than 0, the system uses distance × THB/km instead of the zone flat rate"],["หน้างาน / Workshop / ตรวจวิเคราะห์ / ค่าแรง","Onsite / Workshop / Diagnosis / Labor"],["VAT / Markup / อายุใบเสนอราคา","VAT / Markup / Quotation Validity"],["งานด่วน / ฉุกเฉิน / Warranty","Urgent / Emergency / Warranty"],["เวลาตอบรับ แก้ไข และยกระดับงาน","Response, resolution and escalation targets"],["ประเภทงาน สถานะ และความสำคัญ","Work types, statuses and priorities"],["ชื่อระบบ บริษัท และข้อมูลเอกสาร","System name, company and document information"],["LINE OA / Email / Push / เหตุการณ์","LINE OA / Email / Push / Events"],["ค่าที่ใช้คำนวณบริการ ค่าเดินทาง ประกัน อะไหล่ และใบเสนอราคา","Rates used for service, travel, warranty, parts and quotation calculations"],["ผู้ใช้งาน สิทธิ์ ขั้นตอนงาน ข้อมูลหลัก ภาษา และข้อมูลบริษัท","Users, permissions, workflow, master data, language and company information"],["แจ้งปัญหา • เช็ค Warranty • ประวัติ Service • คู่มือเครื่อง","Report Issue • Check Warranty • Service History • Machine Manual"],["บริษัท ไอโมด พลัส จำกัด","I-MODE Plus Co., Ltd."]];
let i18nBusy=false;
const I18N_FINAL_PAIRS=[["รูป","Photo"],["โซน","Zone"],["ขนาด","Size"],["ระดับ","Level"],["ดู KPI","View KPI"],["ประเภท","Type"],["เลขที่","No."],["คงเหลือ","Remaining"],["ปิดแล้ว","Closed"],["ฝ่ายขาย","Sales"],["สิ้นสุด","End"],["หลักฐาน","Evidence"],["PM ถัดไป","Next PM"],["ทุกประเภท","All Types"],["ฉุกเฉิน +50%","Emergency +50%"],["ผู้ดำเนินการ","Performed By"],["＋ เพิ่ม Zone","＋ Add Zone"],["เร่งด่วน +30%","Urgent +30%"],["กำหนด PM ถัดไป","Set Next PM"],["งานที่ต้องติดตาม","Follow-up Items"],["เริ่ม  → สิ้นสุด","Start → End"],["🖨 พิมพ์สติกเกอร์","🖨 Print Label"],["เข้าบริการหน้างาน","Onsite Service"],["ใช้ราคา Emergency","Use Emergency Pricing"],["Diagnosis ที่ชำระแล้ว","Paid Diagnosis"],["📱 ดูหน้า Customer Portal","📱 View Customer Portal"],["สรุปประสิทธิภาพงาน Service","Service Performance Summary"],["Webhook URL (สำหรับ IT อ้างอิง)","Webhook URL (IT Reference)"]];

I18N_FINAL_PAIRS.push(...[["เคสงานบริการ แยกตามสถานะ","Service Cases by Status"],["ภาพรวมงานที่อยู่ในระบบ","Overview of all jobs in the system"],["แจ้งเตือนสำคัญ","Important Alerts"],["สถานะทีมช่าง","Technician Team Status"],["ทีมที่พร้อมรับงานและภาระงานปัจจุบัน","Available technicians and current workload"],["ผลการดำเนินงาน","Performance Overview"],["สรุปประสิทธิภาพการให้บริการจากข้อมูลในระบบ","Service performance summary from system data"],["นัดหมายวันนี้","Today\'s Appointments"],["ตารางเข้าบริการที่ใกล้ที่สุด","Nearest service appointments"],["รายการที่เพิ่งเข้าสู่ระบบ","Recently created items"],["ดูช่างทั้งหมด","View all technicians"],["ดูรายงาน","View reports"],["ดูทั้งหมด","View all"],["เคสทั้งหมด","Total cases"],["เคสล่าสุด","Latest Cases"],["อัปเดตตามข้อมูลในระบบล่าสุด","Updated from the latest data in the system"]]);
const ALL_I18N_PAIRS=[...I18N_PAIRS,...I18N_EXTRA_PAIRS,...I18N_FINAL_PAIRS].filter((x,i,a)=>a.findIndex(y=>y[0]===x[0])===i);
function translatePatternValue(text,lang){
 let s=String(text||'');
 const en=lang==='en';
 const rules=en?[
  [/^(\d+)\s*งาน$/, '$1 jobs'],[/^(\d+)\s*รายการ$/, '$1 items'],[/^(\d+)\s*คน$/, '$1 users'],[/^(\d+)\s*วัน$/, '$1 days'],[/^(\d+)\s*เดือน$/, '$1 months'],[/^(\d+)\s*โซน$/, '$1 zones'],[/^(\d+)\s*หมวด$/, '$1 categories'],
  [/^เหลือ\s*(\d+)\s*วัน$/, '$1 days remaining'],[/^เกิน\s*(\d+)\s*วัน$/, '$1 days overdue'],[/^\+\s*อีก\s*(\d+)\s*รายการ$/, '+ $1 more'],[/^อัปเดตล่าสุด\s*(.*)$/, 'Last updated $1'],[/^ทีม\s+(.+)$/, 'Team $1'],[/^ลูกค้า:\s*(.*)$/, 'Customer: $1'],[/^เครื่อง:\s*(.*)$/, 'Machine: $1'],[/^(\d+)\s*ชม\.$/, '$1 hr'],[/^(\d+)\s*นาที$/, '$1 min']
 ]:[
  [/^(\d+)\s*jobs?$/, '$1 งาน'],[/^(\d+)\s*items?$/, '$1 รายการ'],[/^(\d+)\s*users?$/, '$1 คน'],[/^(\d+)\s*days?$/, '$1 วัน'],[/^(\d+)\s*months?$/, '$1 เดือน'],[/^(\d+)\s*zones?$/, '$1 โซน'],[/^(\d+)\s*categories$/, '$1 หมวด'],
  [/^(\d+)\s*days remaining$/, 'เหลือ $1 วัน'],[/^(\d+)\s*days overdue$/, 'เกิน $1 วัน'],[/^\+\s*(\d+)\s*more$/, '+ อีก $1 รายการ'],[/^Last updated\s*(.*)$/, 'อัปเดตล่าสุด $1'],[/^Team\s+(.+)$/, 'ทีม $1'],[/^Customer:\s*(.*)$/, 'ลูกค้า: $1'],[/^Machine:\s*(.*)$/, 'เครื่อง: $1']
 ];
 for(const [rx,rp] of rules){if(rx.test(s))return s.replace(rx,rp)}
 return s
}
function translateTextValue(text,lang){
 const original=String(text??''),clean=original.trim();if(!clean)return original;
 for(const [th,en] of ALL_I18N_PAIRS){if(clean===th||clean===en)return original.replace(clean,lang==='en'?en:th)}
 return original.replace(clean,translatePatternValue(clean,lang))
}
function translatePartialUI(text,lang){
 let out=String(text??'');
 const list=[...ALL_I18N_PAIRS].sort((a,b)=>Math.max(b[0].length,b[1].length)-Math.max(a[0].length,a[1].length));
 for(const [th,en] of list){const from=lang==='en'?th:en,to=lang==='en'?en:th;if(from&&out.includes(from))out=out.split(from).join(to)}
 return translatePatternValue(out,lang)
}
function shouldPartialTranslate(el){return !!el?.matches?.('button,label,h1,h2,h3,h4,h5,h6,th,.section-title,.setting-help,.module-eyebrow,.nav-item,.bottom-nav b,.tab-btn,.seg-btn,.field-status-chip,.line-type,.setting-action-card b,.setting-action-card small,.permission-item span,.permission-group-title,.empty')}
function translateAttr(el,attr,lang){const v=el.getAttribute(attr);if(!v)return;const x=translateTextValue(v,lang);el.setAttribute(attr,x===v?translatePartialUI(v,lang):x)}
function applyLanguageTo(root=document.body){
 if(i18nBusy||!root)return;i18nBusy=true;
 try{
  const lang=settings.language||'th';document.documentElement.lang=lang==='en'?'en':'th';pageInfo=PAGE_INFO[lang];
  if(typeof languageQuickBtn!=='undefined'){languageQuickBtn.textContent=lang==='en'?'EN':'TH';languageQuickBtn.title=lang==='en'?'Switch to Thai':'เปลี่ยนเป็นภาษาอังกฤษ'}
  const walker=document.createTreeWalker(root,NodeFilter.SHOW_TEXT);const nodes=[];while(walker.nextNode())nodes.push(walker.currentNode);
  nodes.forEach(n=>{const p=n.parentElement;if(!p||['SCRIPT','STYLE','TEXTAREA','OPTION'].includes(p.tagName)||p.closest?.('[data-no-i18n="true"]'))return;let x=translateTextValue(n.nodeValue,lang);if(x===n.nodeValue&&shouldPartialTranslate(p))x=translatePartialUI(n.nodeValue,lang);if(x!==n.nodeValue)n.nodeValue=x});
  root.querySelectorAll?.('option').forEach(o=>{if(!o.dataset.i18nValue){o.dataset.i18nValue=o.value;if(!o.hasAttribute('value'))o.value=o.dataset.i18nValue}const raw=o.textContent;const x=translateTextValue(raw,lang);o.textContent=x===raw?translatePartialUI(raw,lang):x;o.value=o.dataset.i18nValue});
  root.querySelectorAll?.('[placeholder]').forEach(el=>translateAttr(el,'placeholder',lang));
  root.querySelectorAll?.('[title]').forEach(el=>translateAttr(el,'title',lang));
  root.querySelectorAll?.('[aria-label]').forEach(el=>translateAttr(el,'aria-label',lang));
  root.querySelectorAll?.('input[type="button"],input[type="submit"]').forEach(el=>{const raw=el.value||'';const x=translateTextValue(raw,lang);el.value=x===raw?translatePartialUI(raw,lang):x});
 }finally{i18nBusy=false}
}
function setLanguage(lang){settings.language=lang==='en'?'en':'th';settings.locale=settings.language==='en'?'en-GB':'th-TH';saveLocal();pageInfo=PAGE_INFO[settings.language];renderAll();const active=document.querySelector('.page.active')?.id?.replace('page-','')||'dashboard';if(pageInfo[active]){pageTitle.textContent=pageInfo[active][0];pageDesc.textContent=pageInfo[active][1]}applyLanguageTo(document.body);applyFontScale();toastMsg(settings.language==='en'?'Language changed to English':'เปลี่ยนภาษาเป็นภาษาไทยแล้ว')}window.setLanguage=setLanguage;function toggleLanguage(){setLanguage(settings.language==='en'?'th':'en')}window.toggleLanguage=toggleLanguage;
function userInitials(name='User'){return String(name).split(' ').map(x=>x[0]).join('').slice(0,2).toUpperCase()}
function userTeamLabel(team=''){return team?`ทีม ${team}`:''}
function renderUserCard(){
 if(typeof topbarUserCard==='undefined')return;
 const logged=!!currentUser;
 const avatar=currentUser?.photo?`<img class="top-user-photo" src="${currentUser.photo}" alt="${esc(currentUser.name)}">`:`<div class="top-user-avatar">${userInitials(currentUser?.name||'Guest')}</div>`;
 topbarUserCard.className='topbar-user-card'+(logged?'':' guest');
 topbarUserCard.innerHTML=`${avatar}<div class="top-user-meta"><b>${esc(currentUser?.name||'เข้าสู่ระบบ')}</b><small>${esc(logged?`${currentUser.role||'User'}${currentUser.team?' · '+currentUser.team:''}`:'เลือกผู้ใช้งาน')}</small></div><span class="top-user-arrow">⌄</span>`
}
function loginUserPhoto(u){const n=String(u?.name||'').trim().toLowerCase();const t=technicians.find(x=>String(x.name||'').trim().toLowerCase()===n);return u?.photo||t?.photo||''}
let pendingLoginPhoto='';
function loginAvatarHTML(u){const photo=loginUserPhoto(u);return photo?`<img class="mini-avatar-photo" src="${photo}" alt="${esc(u.name||'User')}">`:`<div class="mini-avatar">${userInitials(u.name)}</div>`}
function chooseUser(id){const selected=demoUsers.find(u=>u.id===id);if(!selected)return;currentUser={...selected,photo:loginUserPhoto(selected)};saveLocal();closeModal();renderAll();toastMsg('เข้าสู่ระบบเป็น '+selected.name)}window.chooseUser=chooseUser;
async function previewLoginPhoto(input){const f=input.files?.[0];if(!f)return;if(!f.type.startsWith('image/')){toastMsg('กรุณาเลือกไฟล์รูปภาพ');return}pendingLoginPhoto=await compressPhoto(f);if(typeof loginPhotoPreview!=='undefined')loginPhotoPreview.innerHTML=`<img class="login-photo-preview" src="${pendingLoginPhoto}" alt="User photo">`}window.previewLoginPhoto=previewLoginPhoto;
function clearLoginPhoto(){pendingLoginPhoto='';if(typeof loginPhotoPreview!=='undefined')loginPhotoPreview.innerHTML='<div class="login-photo-placeholder">👤</div>'}window.clearLoginPhoto=clearLoginPhoto;
function saveManualUser(e){e.preventDefault();const name=loginName.value.trim();if(!name){toastMsg('กรุณากรอกชื่อผู้ใช้งาน');return}currentUser={id:'USR-'+Date.now(),name,role:loginRole.value.trim()||'User',team:loginTeam.value||'',photo:pendingLoginPhoto||''};saveLocal();closeModal();renderAll();toastMsg('บันทึกและเข้าสู่ระบบแล้ว')}window.saveManualUser=saveManualUser;
function openUserLoginModal(){pendingLoginPhoto='';openModal('เข้าสู่ระบบแบบรายบุคคล','เลือกผู้ใช้งานเพื่อเข้าใช้งานระบบ หรือกรอกชื่อเองสำหรับการใช้งานชั่วคราว',`<div class="login-user-list">${demoUsers.map(u=>`<div class="login-user-item" onclick="chooseUser('${u.id}')"><div class="left">${loginAvatarHTML(u)}<div><b>${esc(u.name)}</b><small>${esc(u.role)}</small></div></div><span class="login-chip">${esc(u.team)}</span></div>`).join('')}</div><div class="login-manual-box"><form id="manualLoginForm"><div class="login-photo-editor"><div id="loginPhotoPreview"><div class="login-photo-placeholder">👤</div></div><div><b>รูปผู้ใช้งาน</b><small>เพิ่มรูปสำหรับบัญชีที่กรอกเอง</small><div class="button-row compact"><label class="soft-btn" style="cursor:pointer">📷 เลือกรูป<input type="file" accept="image/*" style="display:none" onchange="previewLoginPhoto(this)"></label><button type="button" class="soft-btn" onclick="clearLoginPhoto()">ลบรูป</button></div></div></div><div class="form-grid cols2"><div class="field"><label>ชื่อผู้ใช้งาน</label><input id="loginName" placeholder="เช่น อภิชาติ ปิมแปง"></div><div class="field"><label>สิทธิ์ระบบ / Role</label><select id="loginRole">${settings.roles.map(r=>`<option value="${esc(r.name)}">${esc(r.name)}</option>`).join('')}</select></div><div class="field"><label>ทีม</label><select id="loginTeam"><option value="">ไม่ระบุ</option><option value="Management">Management</option><option value="Admin">Admin</option><option value="Technical">Technical</option><option value="R&D">R&D</option><option value="Sales">Sales</option></select></div></div><div class="button-row"><button type="button" class="soft-btn" onclick="closeModal()">ยกเลิก</button><button class="primary-btn">บันทึกและเข้าสู่ระบบ</button></div></form></div>`,true);setTimeout(()=>manualLoginForm.onsubmit=saveManualUser,20)}window.openUserLoginModal=openUserLoginModal;

function goPage(name){
 const pagePerm=PAGE_PERMISSION[name];if(pagePerm&&!requirePermission(pagePerm))return;
 document.body.classList.toggle('customer-portal-mode',name==='customer-portal');
 document.querySelectorAll('.page').forEach(p=>p.classList.remove('active'));document.getElementById('page-'+name).classList.add('active');
 document.querySelectorAll('.nav-item').forEach(b=>b.classList.toggle('active',b.dataset.page===name));
 document.querySelectorAll('.bottom-nav [data-page]').forEach(b=>b.classList.toggle('active',b.dataset.page===name));
 pageTitle.textContent=pageInfo[name]?.[0]||name;pageDesc.textContent=pageInfo[name]?.[1]||'';sidebar.classList.remove('open');renderAll();applyLanguageTo(document.body);window.scrollTo({top:0,behavior:'smooth'})
}
window.goPage=goPage;
document.querySelectorAll('.nav-item').forEach(b=>b.onclick=()=>goPage(b.dataset.page));
document.querySelectorAll('.bottom-nav [data-page]').forEach(b=>b.onclick=()=>goPage(b.dataset.page));
menuBtn.onclick=()=>sidebar.classList.toggle('open');

function ensureMasters(){
 cases.forEach(c=>{
  if(c.customer&&!c.customerId){let cu=customers.find(x=>x.name===c.customer);if(!cu){cu={id:uid(),name:c.customer,branch:c.branch||'',contact:c.contact||'',phone:c.phone||'',email:c.email||'',location:c.location||'',note:''};customers.push(cu)}c.customerId=cu.id}
  if(c.machine&&!c.machineId){let m=machines.find(x=>(x.serial&&c.serial&&x.serial===c.serial)||(!x.serial&&x.name===c.machine&&x.customerId===c.customerId));if(!m){m={id:uid(),customerId:c.customerId,name:c.machine,model:c.model||'',serial:c.serial||'',size:c.machineSize||'',warranty:c.warranty||'ไม่ทราบ',pmDue:'',note:''};machines.push(m)}c.machineId=m.id}
 });machines.forEach(m=>{if(!m.qrToken)m.qrToken='QR-'+uid().replace(/[^a-zA-Z0-9]/g,'').slice(0,20)});saveLocal()
}
function renderFilters(){const sv=caseStatusFilter.value,pv=casePriorityFilter.value,statuses=caseFilterStatuses();const byStatus=caseMatchesSearch().filter(c=>!sv||c.status===sv);caseStatusFilter.innerHTML='<option value="">ทุกสถานะ</option>'+statuses.map(x=>`<option value="${esc(x)}">${esc(x)}</option>`).join('');casePriorityFilter.innerHTML=`<option value="">ทุกความด่วน (${byStatus.length})</option>`+settings.priorities.map(x=>`<option value="${esc(x)}">${esc(x)} (${byStatus.filter(c=>c.priority===x).length})</option>`).join('');if(statuses.includes(sv))caseStatusFilter.value=sv;if(settings.priorities.includes(pv))casePriorityFilter.value=pv}
function openCases(){return cases.filter(c=>!['เสร็จสิ้น','ปิดเคส'].includes(c.status))}
function dashText(th,en){return settings.language==='en'?en:th}
function translateUi(v){return settings.language==='en'?translateTextValue(v,'en'):v}
function progressTone(val){return val>=85?'good':val>=60?'mid':'warn'}
function dashboardStatusData(){
 const palette=['#0f67d8','#ff7a22','#13a86f','#ef4444','#8b5cf6','#14b8a6','#f59e0b','#64748b'];
 return settings.statuses.map((status,i)=>({status,count:cases.filter(c=>c.status===status).length,color:palette[i%palette.length]}));
}
function dashboardDonutHTML(items,total){
 const size=184,stroke=16,r=64,circ=2*Math.PI*r;
 if(!total)return `<div class="dashboard-donut"><svg class="status-donut-svg" viewBox="0 0 ${size} ${size}" aria-hidden="true"><circle cx="92" cy="92" r="${r}" fill="none" stroke="#edf2fb" stroke-width="${stroke}"></circle><circle cx="92" cy="92" r="${r}" fill="none" stroke="#dbe7fb" stroke-width="${stroke}" stroke-dasharray="${circ} ${circ}" transform="rotate(-90 92 92)"></circle></svg><div class="donut-center"><b>0</b><small>${dashText('เคสทั้งหมด','Total cases')}</small></div></div>`;
 let acc=0;
 const seg=items.filter(x=>x.count>0).map(x=>{
   const segLen=(x.count/total)*circ; const el=`<circle cx="92" cy="92" r="${r}" fill="none" stroke="${x.color}" stroke-width="${stroke}" stroke-linecap="round" stroke-dasharray="${segLen} ${circ-segLen}" stroke-dashoffset="-${acc}" transform="rotate(-90 92 92)"></circle>`; acc+=segLen; return el;
 }).join('');
 return `<div class="dashboard-donut"><svg class="status-donut-svg" viewBox="0 0 ${size} ${size}" aria-hidden="true"><circle cx="92" cy="92" r="${r}" fill="none" stroke="#edf2fb" stroke-width="${stroke}"></circle>${seg}</svg><div class="donut-center"><b>${total}</b><small>${dashText('เคสทั้งหมด','Total cases')}</small></div></div>`;
}
function renderDashboard(){
 const L=dashText;
 const open=openCases(), doneCases=cases.filter(c=>['เสร็จสิ้น','ปิดเคส'].includes(c.status));
 mOpen.textContent=open.length;
 mUrgent.textContent=open.filter(c=>c.priority==='ด่วนมาก').length;
 mAssigned.textContent=open.filter(c=>c.assignee).length;
 mDone.textContent=doneCases.length;

 const now=new Date();
 const todayKey=now.toISOString().slice(0,10);
 const upcoming=[...open].filter(c=>c.appointment&&new Date(c.appointment)>=new Date(Date.now()-3600000)).sort((a,b)=>new Date(a.appointment)-new Date(b.appointment));
 const todayList=upcoming.filter(c=>String(c.appointment||'').slice(0,10)===todayKey).slice(0,5);
 const scheduleList=(todayList.length?todayList:upcoming.slice(0,5));
 dashboardSchedule.innerHTML=scheduleList.length?scheduleList.map(c=>`<div class="stack-item" onclick="openCaseDetail('${c.id}')" style="cursor:pointer"><span class="stack-icon">📅</span><div><b>${esc(c.customer)} · ${esc(c.machine)}</b><small>${fmt(c.appointment)} · ${esc(techById(c.assignee)?.name||L('ยังไม่มอบหมาย','Unassigned'))}</small></div></div>`).join(''):`<div class="empty">${L('ยังไม่มีนัดหมายใกล้ถึง','No upcoming appointments')}</div>`;
 dashScheduleDate.textContent=todayList.length?L('งานนัดหมายวันนี้',"Today's appointments"):L('ตารางเข้าบริการที่ใกล้ที่สุด','Nearest service appointments');

 const alerts=buildNotifications().slice(0,5);
 dashboardAlerts.innerHTML=alerts.length?alerts.map(n=>`<div class="stack-item" onclick="openNotificationDetail('${n.key}')" style="cursor:pointer"><span class="stack-icon">${n.icon}</span><div><b>${esc(n.title)}</b><small>${esc(n.message)}</small></div></div>`).join(''):`<div class="empty">${L('ไม่มีการแจ้งเตือน','No notifications')}</div>`;
 dashNotifyChip.textContent=alerts.length;

 const statusData=dashboardStatusData();
 const totalCases=cases.length;
 dashboardStatusDonut.innerHTML=dashboardDonutHTML(statusData,totalCases);
 dashboardStatusLegend.innerHTML=statusData.filter(x=>x.count>0||!totalCases).map(x=>`<div class="status-legend-item"><span class="dot" style="background:${x.color}"></span><div><b>${esc(translateUi(x.status))}</b><small>${x.count} ${L('เคส','cases')}</small></div></div>`).join('') || `<div class="empty">${L('ยังไม่มีข้อมูลเคส','No case data yet')}</div>`;
 dashboardChartUpdated.textContent=L('อัปเดตตามข้อมูลในระบบล่าสุด','Updated from the latest data in the system');

 const latest=[...cases].sort((a,b)=>new Date(b.createdAt||0)-new Date(a.createdAt||0)).slice(0,6);
 dashboardLatestCases.innerHTML=latest.length?latest.map(c=>`<button class="dashboard-list-item" onclick="openCaseDetail('${c.id}')"><div class="dashboard-list-main"><b>${esc(c.ticket||'-')}</b><small>${esc(c.customer||'-')} · ${esc(c.machine||'-')}</small></div><div class="dashboard-list-side">${badge('status',c.status)}<span>${fmt(c.createdAt)}</span></div></button>`).join(''):`<div class="empty">${L('ยังไม่มีข้อมูลเคส','No case data yet')}</div>`;

 const techList=[...technicians].sort((a,b)=>open.filter(c=>c.assignee===b.id).length-open.filter(c=>c.assignee===a.id).length);
 dashboardTeamStatus.innerHTML=techList.length?techList.slice(0,5).map(t=>{const jobs=open.filter(c=>c.assignee===t.id).length;return `<div class="team-status-row"><div class="team-status-main"><div class="team-status-avatar">${techAvatarHTML(t)}</div><div><b>${esc(t.name)}</b><small>${esc(t.role||'Technician')} · ${esc(t.team||'Technical')}</small></div></div><div class="team-status-side"><span class="tech-status-dot ${techStatusClass(t.status)}">${esc(translateUi(t.status||L('พร้อมรับงาน','Available')))}</span><strong>${jobs} ${L('งาน','jobs')}</strong></div></div>`}).join(''):`<div class="empty">${L('ยังไม่มีข้อมูลทีมช่าง','No technician data yet')}</div>`;

 const assignedRate=open.length?Math.round((open.filter(c=>c.assignee).length/open.length)*100):0;
 const closedRate=totalCases?Math.round((doneCases.length/totalCases)*100):0;
 const serviceCoverage=customers.length?Math.round((new Set(cases.map(c=>c.customerId).filter(Boolean)).size/Math.max(1,customers.length))*100):0;
 const urgentControl=open.length?Math.max(0,100-Math.round((open.filter(c=>c.priority==='ด่วนมาก').length/open.length)*100)):100;
 const perfItems=[
   {label:L('อัตรามอบหมายงาน','Assignment rate'), value:assignedRate, target:L('เป้าหมาย ≥ 90%','Target ≥ 90%')},
   {label:L('อัตราปิดงาน','Closure rate'), value:closedRate, target:L('เป้าหมาย ≥ 80%','Target ≥ 80%')},
   {label:L('ความครอบคลุมลูกค้า','Customer coverage'), value:serviceCoverage, target:L('ลูกค้าที่มีประวัติเคส','Customers with service history')},
   {label:L('ควบคุมงานด่วน','Urgent case control'), value:urgentControl, target:L('ค่ายิ่งสูงยิ่งดี','Higher is better')}
 ];
 const perfCards=perfItems.map(x=>`<div class="performance-card ${progressTone(x.value)}"><div class="performance-head"><b>${x.label}</b><strong>${x.value}%</strong></div><div class="performance-meter"><i style="width:${Math.min(100,Math.max(0,x.value))}%"></i></div><small>${x.target}</small></div>`).join('');
 const reportPending=serviceReports.filter(r=>!(r.sentToLine||r.sentToCustomer)).length;
 dashboardPerformance.innerHTML=`<div class="dashboard-performance-grid">${perfCards}</div><div class="dashboard-mini-summary"><div><small>${L('ลูกค้าทั้งหมด','Customers')}</small><b>${customers.length}</b></div><div><small>${L('เครื่องทั้งหมด','Machines')}</small><b>${machines.length}</b></div><div><small>${L('รายงาน Service','Service reports')}</small><b>${serviceReports.length}</b></div><div><small>${L('รอส่ง Report','Pending reports')}</small><b>${reportPending}</b></div></div>`;
}
function caseFilterStatuses(){return [...new Set([...settings.statuses,...cases.map(c=>c.status).filter(Boolean)])]}
function caseSearchText(c){return [c.ticket,c.customer,c.machine,c.model,c.serial,c.issue,techById(c.assignee)?.name].join(' ').toLowerCase()}
function caseMatchesSearch(){const q=(caseSearch.value||'').toLowerCase();return q?cases.filter(c=>caseSearchText(c).includes(q)):[...cases]}
/* ---- Shared list pagination (display limit + total chip + pager) ---- */
const LIST_PAGERS={};
const LIST_PAGER_CONFIG={
 cases:{pageId:'page-cases',render:'renderCases',unit:'เคส'},
 customers:{pageId:'page-customers',render:'renderCustomers',unit:'ราย'},
 qc:{pageId:'page-qc',render:'renderQc',unit:'รายการ'},
 quotation:{pageId:'page-quotation',render:'renderQuotationList',unit:'ใบ'},
 warranty:{pageId:'page-warranty',render:'renderWarranties',unit:'รายการ'},
 documents:{pageId:'page-documents',render:'renderMachineDocuments',unit:'ไฟล์'},
 spareParts:{pageId:'page-spare-parts',render:'renderSpareParts',unit:'รายการ'},
 pettyCash:{pageId:'page-petty-cash',render:'renderPettyCash',unit:'รายการ'}
};
function listPagerState(key){return LIST_PAGERS[key]||(LIST_PAGERS[key]={limit:50,page:1,sig:''})}
function listPagerRerender(key){const fn=window[LIST_PAGER_CONFIG[key]?.render||''];if(typeof fn==='function')fn()}
function setListLimit(key,value){const s=listPagerState(key);s.limit=value==='all'?'all':Math.max(1,Number(value)||50);s.page=1;listPagerRerender(key)}
window.setListLimit=setListLimit;
function setListPage(key,page){const s=listPagerState(key);s.page=Math.max(1,Number(page)||1);listPagerRerender(key);const anchor=document.querySelector(`#${LIST_PAGER_CONFIG[key]?.pageId} .toolbar`);if(anchor)setTimeout(()=>anchor.scrollIntoView({behavior:'smooth',block:'start'}),30)}
window.setListPage=setListPage;
function listPageItems(current,total){
 if(total<=7)return Array.from({length:total},(_,i)=>i+1);
 const items=[1];let start=Math.max(2,current-1),end=Math.min(total-1,current+1);
 if(current<=4){start=2;end=Math.min(5,total-1)}
 if(current>=total-3){start=Math.max(2,total-4);end=total-1}
 if(start>2)items.push('…');
 for(let p=start;p<=end;p++)items.push(p);
 if(end<total-1)items.push('…');
 items.push(total);return items;
}
function paginateList(key,filtered,signature=''){
 const s=listPagerState(key);
 if(s.sig&&s.sig!==signature)s.page=1;
 s.sig=signature;
 const allMode=s.limit==='all';
 const limit=allMode?Math.max(1,filtered.length):Math.max(1,Number(s.limit||50));
 const pageCount=allMode?1:Math.max(1,Math.ceil(filtered.length/limit));
 s.page=Math.min(Math.max(1,s.page),pageCount);
 const startIndex=filtered.length?(s.page-1)*limit:0;
 const list=allMode?filtered:filtered.slice(startIndex,startIndex+limit);
 return {list,page:s.page,pageCount,total:filtered.length,firstShown:filtered.length?startIndex+1:0,lastShown:filtered.length?startIndex+list.length:0};
}
function renderListPager(key,info,totalAll){
 const s=listPagerState(key),unit=LIST_PAGER_CONFIG[key]?.unit||'รายการ';
 const limitEl=document.getElementById(key+'DisplayLimit');
 if(limitEl&&limitEl.value!==String(s.limit))limitEl.value=String(s.limit);
 const totalEl=document.getElementById(key+'TotalCount');
 if(totalEl)totalEl.textContent=Number(totalAll==null?info.total:totalAll).toLocaleString('th-TH');
 const pager=document.getElementById(key+'Pagination');
 if(!pager)return;
 const summary=`<div class="machine-page-summary">แสดง <b>${info.firstShown.toLocaleString('th-TH')}–${info.lastShown.toLocaleString('th-TH')}</b> จากทั้งหมด <b>${info.total.toLocaleString('th-TH')}</b> ${unit}</div>`;
 if(info.total===0){pager.innerHTML=summary;return}
 if(info.pageCount<=1){pager.innerHTML=summary+`<div class="machine-page-controls"><span class="machine-page-one">หน้า 1 / 1</span></div>`;return}
 const pages=listPageItems(info.page,info.pageCount).map(it=>it==='…'?`<span class="machine-page-ellipsis">…</span>`:`<button type="button" class="machine-page-btn ${it===info.page?'active':''}" onclick="setListPage('${key}',${it})" aria-label="หน้า ${it}">${it}</button>`).join('');
 pager.innerHTML=summary+`<div class="machine-page-controls"><button type="button" class="machine-page-btn" onclick="setListPage('${key}',${info.page-1})" ${info.page<=1?'disabled':''}>← ก่อนหน้า</button>${pages}<button type="button" class="machine-page-btn" onclick="setListPage('${key}',${info.page+1})" ${info.page>=info.pageCount?'disabled':''}>ถัดไป →</button></div>`;
}
function listLimitControlHTML(key){const unit=LIST_PAGER_CONFIG[key]?.unit||'รายการ';return `<div class="machine-list-meta"><label class="machine-display-control">แสดง <select id="${key}DisplayLimit" onchange="setListLimit('${key}',this.value)"><option value="10">10</option><option value="25">25</option><option value="50" selected>50</option><option value="100">100</option><option value="all">ทั้งหมด</option></select> ${unit}</label><span class="machine-total-chip">ทั้งหมด <b id="${key}TotalCount">0</b> ${unit}</span></div>`}
function selectCaseStatus(status){caseStatusFilter.value=status;caseCurrentPage=1;renderCases()}
window.selectCaseStatus=selectCaseStatus;
function renderCaseStatusKpis(){
 const host=document.getElementById('caseStatusKpis');if(!host)return;
 const pf=casePriorityFilter.value,sf=caseStatusFilter.value,en=settings.language==='en';
 const matching=caseMatchesSearch().filter(c=>!pf||c.priority===pf);
 const card=(status,label,count)=>`<button type="button" class="module-kpi-card" data-status="${esc(status)}" aria-pressed="${sf===status}" onclick="selectCaseStatus(this.dataset.status)"><small>${esc(label)}</small><b>${count}</b><span>${en?'cases':'รายการ'}</span></button>`;
 host.innerHTML=card('',en?'All cases':'เคสทั้งหมด',matching.length)+caseFilterStatuses().map(s=>card(s,translateUi(s),matching.filter(c=>c.status===s).length)).join('');
}
function openCaseFromRow(event){
 if(event.target.closest('button,a,input,select,textarea'))return;
 if(event.type==='keydown'){if(event.target!==event.currentTarget||!['Enter',' '].includes(event.key))return;event.preventDefault()}
 openCaseDetail(event.currentTarget.dataset.caseId)
}
function caseQuotationButton(c,buttonClass){return canPermission('quotation.view')&&canPermission('quotation.create')?`<button type="button" class="${buttonClass} case-quote-btn" data-case-id="${esc(c.id)}" onclick="prepareQuotation(this.dataset.caseId)">${settings.language==='en'?'Create quotation':'สร้างใบเสนอราคา'}</button>`:''}
function renderCases(){
 renderFilters();renderCaseStatusKpis();const sf=caseStatusFilter.value,pf=casePriorityFilter.value;
 const filtered=caseMatchesSearch().filter(c=>(!sf||c.status===sf)&&(!pf||c.priority===pf)).sort((a,b)=>new Date(b.createdAt)-new Date(a.createdAt));
 const info=paginateList('cases',filtered,[(caseSearch.value||'').toLowerCase(),sf,pf].join('|'));
 const list=info.list;
 caseTable.innerHTML=list.length?list.map(c=>`<tr class="case-clickable" data-case-id="${esc(c.id)}" tabindex="0" aria-label="${esc(c.ticket)}" onclick="openCaseFromRow(event)" onkeydown="openCaseFromRow(event)"><td><span class="ticket">${esc(c.ticket)}</span><span class="subtext">${fmt(c.createdAt)}</span></td><td><b>${esc(c.customer)}</b></td><td>${esc(c.machine)}<span class="subtext">${esc(c.model||'')} ${c.serial?'· '+esc(c.serial):''}</span></td><td>${esc(c.serviceType)}</td><td>${badge('prio',c.priority)}</td><td>${badge('status',c.status)}</td><td>${esc(techById(c.assignee)?.name||'-')}</td><td>${fmt(c.appointment)}</td><td class="case-manage-cell"><div class="row-actions case-row-actions"><button class="mini-btn" onclick="openCaseDetail('${c.id}')">ดู</button><button class="mini-btn" onclick="openCaseModal('${c.id}')">แก้ไข</button>${caseQuotationButton(c,'mini-btn')}</div></td></tr>`).join(''):`<tr><td colspan="9"><div class="empty">ไม่พบข้อมูล</div></td></tr>`;
 caseCards.innerHTML=list.length?list.map(c=>`<div class="mobile-data-card case-clickable" data-case-id="${esc(c.id)}" tabindex="0" aria-label="${esc(c.ticket)}" onclick="openCaseFromRow(event)" onkeydown="openCaseFromRow(event)"><div class="card-top"><div><h4>${esc(c.ticket)}</h4><p>${esc(c.customer)} · ${esc(c.machine)}</p></div>${badge('prio',c.priority)}</div><p>${esc(c.issue).slice(0,90)}</p><div class="card-meta">${badge('status',c.status)}<span class="badge">${esc(techById(c.assignee)?.name||'ยังไม่มอบหมาย')}</span></div><div class="card-actions"><button class="soft-btn" onclick="openCaseDetail('${c.id}')">ดูรายละเอียด</button><button class="primary-btn" onclick="openCaseModal('${c.id}')">แก้ไข</button>${caseQuotationButton(c,'soft-btn')}</div></div>`).join(''):'<div class="empty">ไม่พบข้อมูล</div>';
 renderListPager('cases',info,cases.length);
}
function customerLineLinked(cu){return !!(cu.lineUserId||lineRequests.some(r=>r.customerId===cu.id&&r.lineUserId))}
function renderCustomers(){
 const q=(typeof customerMasterSearch!=='undefined'?customerMasterSearch.value:'').trim().toLowerCase(),sf=(typeof customerServiceFilter!=='undefined'?customerServiceFilter.value:'all');
 const filtered=customers.filter(cu=>(!q||[cu.name,cu.contact,cu.phone,cu.address,cu.location].join(' ').toLowerCase().includes(q))&&(sf==='all'||cu.serviceStatus===sf));
 const info=paginateList('customers',filtered,[q,sf].join('|')),list=info.list;
 customerTable.innerHTML=list.length?list.map(cu=>{const mc=machines.filter(m=>m.customerId===cu.id).length,cc=cases.filter(c=>c.customerId===cu.id).length,linked=customerLineLinked(cu);return `<tr><td><b>${esc(cu.name)}</b><span class="subtext">Service: ${esc(cu.serviceStatus||'ไม่ระบุ')}</span></td><td>${esc(cu.contact||'-')}</td><td>${esc(cu.phone||'-')}</td><td><span class="badge ${linked?'status-เสร็จสิ้น':''}">${linked?'LINE Linked':'ยังไม่เชื่อม'}</span></td><td>${esc(cu.address||cu.location||cu.branch||'-')}<span class="subtext"><button class="mini-btn" onclick="openCustomerMap('${cu.id}')">🗺 Maps</button></span></td><td><b>${mc}</b></td><td>${cc}</td><td><button class="mini-btn" onclick="openCustomerDetail('${cu.id}')">ดู</button> <button class="mini-btn" onclick="openCustomerModal('${cu.id}')">แก้ไข</button></td></tr>`}).join(''):`<tr><td colspan="8"><div class="empty">ไม่พบข้อมูลลูกค้าตามเงื่อนไข</div></td></tr>`;
 customerCards.innerHTML=list.length?list.map(cu=>`<div class="mobile-data-card"><div class="card-top"><div><h4>${esc(cu.name)}</h4><p>${esc(cu.contact||'-')} · ${esc(cu.phone||'-')}</p></div><span class="badge">${esc(cu.serviceStatus||'-')}</span></div><p>${esc(cu.address||cu.location||cu.branch||'-')}</p><p class="subtext">${machines.filter(m=>m.customerId===cu.id).length} เครื่อง</p><div class="card-actions"><button class="soft-btn" onclick="openCustomerMap('${cu.id}')">🗺 Maps</button><button class="soft-btn" onclick="openCustomerDetail('${cu.id}')">รายละเอียด</button><button class="primary-btn" onclick="openCustomerModal('${cu.id}')">แก้ไข</button></div></div>`).join(''):'<div class="empty">ไม่พบข้อมูลลูกค้า</div>';
 renderListPager('customers',info,customers.length);
 renderLineRequests()
}
function renderMachines(){
 const q=(typeof machineMasterSearch!=='undefined'?machineMasterSearch.value:'').trim().toLowerCase(),sf=(typeof machineServiceFilter!=='undefined'?machineServiceFilter.value:'all'),wf=(typeof machineWarrantyFilter!=='undefined'?machineWarrantyFilter.value:'all'),zf=(typeof machineSizeFilter!=='undefined'?machineSizeFilter.value:'all');
 const list=machines.filter(m=>{const cu=customerById(m.customerId),sz=normalizeMachineSize(m.size);return (!q||[machineNameTh(m),machineNameEn(m),m.model,m.serial,cu?.name].join(' ').toLowerCase().includes(q))&&(sf==='all'||m.serviceStatus===sf)&&(wf==='all'||m.warranty===wf)&&(zf==='all'||(zf==='confirm'?!sz:sz===zf))});
 machineTable.innerHTML=list.length?list.map(m=>{const cu=customerById(m.customerId),cc=cases.filter(c=>c.machineId===m.id).length,sz=normalizeMachineSize(m.size);return `<tr><td>${machineMediaHTML(m)}</td><td><div class="machine-cell-name">${machinePairHTML(m)}<small>${esc(m.note||'')}</small></div></td><td>${esc(m.model||'-')}</td><td>${esc(m.serial||'-')}</td><td>${esc(cu?.name||'-')}</td><td>${esc(m.issueYear||'-')}</td><td><span class="machine-size-badge ${sz||'confirm'}" title="${esc(m.sizeStatus||'')}">${esc(sz||'Confirm')}</span></td><td><span class="service-contract-badge ${m.serviceStatus==='ต่อ Service'?'on':'off'}">${esc(m.serviceStatus||'ไม่ระบุ')}</span></td><td>${esc(m.warranty||'-')}</td><td>${cc}</td><td><button class="mini-btn" onclick="openMachineQR('${m.id}')">QR</button> <button class="mini-btn" onclick="openMachineDetail('${m.id}')">ดู</button> <button class="mini-btn" onclick="openQcModal('', '${m.id}')">QC</button> <button class="mini-btn" onclick="openMachineModal('${m.id}')">แก้ไข</button></td></tr>`}).join(''):`<tr><td colspan="11"><div class="empty">ไม่พบข้อมูลเครื่องจักรตามเงื่อนไข</div></td></tr>`;
 machineCards.innerHTML=list.length?list.map(m=>{const sz=normalizeMachineSize(m.size);return `<div class="mobile-data-card"><div class="card-top"><div><h4>${esc(machinePrimaryName(m))}</h4><p class="machine-alt-mobile">${esc(machineSecondaryName(m))}</p><p>${esc(m.model||'-')} · S/N ${esc(m.serial||'-')}</p></div><div class="machine-mobile-meta"><span class="machine-size-badge ${sz||'confirm'}">${esc(sz||'Confirm')}</span><span class="badge">${esc(m.issueYear||'-')}</span></div></div>${machineMediaHTML(m)}<p style="margin-top:10px">${esc(customerById(m.customerId)?.name||'-')}</p><div class="machine-status-row"><span class="service-contract-badge ${m.serviceStatus==='ต่อ Service'?'on':'off'}">${esc(m.serviceStatus||'ไม่ระบุ')}</span><span>${esc(m.warranty||'-')}</span></div><div class="card-actions"><button class="soft-btn" onclick="openMachineQR('${m.id}')">QR</button><button class="soft-btn" onclick="openMachineDetail('${m.id}')">รายละเอียด</button><button class="soft-btn" onclick="openQcModal('', '${m.id}')">QC</button><button class="primary-btn" onclick="openMachineModal('${m.id}')">แก้ไข</button></div></div>`}).join(''):'<div class="empty">ไม่พบข้อมูลเครื่องจักร</div>'
}
function techAvatarHTML(t,detail=false){
 const initials=esc((t.name||'T').split(' ').map(x=>x[0]).join('').slice(0,2));
 if(t.photo)return `<img class="person-photo" src="${t.photo}" alt="${esc(t.name)}">`;
 return `<div class="avatar">${initials}</div>`
}
function techStatusClass(s){return s==='ไม่พร้อม'?'off':s==='กำลังปฏิบัติงาน'||s==='ลงพื้นที่'?'busy':''}
function techSkillTags(t){return (t.skills||'').split(/[,/]/).map(x=>x.trim()).filter(Boolean).slice(0,5).map(x=>`<span class="skill-tag">${esc(x)}</span>`).join('')}
function teamClass(team){return team==='R&D'?'rd':'technical'}
function teamBadge(team){const t=team||'Technical';return `<span class="team-badge ${teamClass(t)}">${t==='R&D'?'🧪 ทีม R&D':'🛠 ทีม Technical'}</span>`}
const MACHINE_FAMILY_PHOTOS=Object.freeze({
 hydrogenHo500:'./assets/machines/hydrogen-ho500wt.webp',
 hydrogenHo200:'./assets/machines/hydrogen-ho200wt.webp',
 nitrogen:'./assets/machines/nitrogen-generator.webp',
 laserMarking:'./assets/machines/laser-marking-bgf20.jpg',
 laserWelding:'./assets/machines/laser-welding.webp',
 blueThunder:'./assets/machines/welding-blue-thunder.jpg',
 centrifugalVa20:'./assets/machines/centrifugal-va20.jpg',
 centrifugalVa38:'./assets/machines/centrifugal-va38.jpg',
 polishingFamily:'./assets/machines/polishing-family.webp'
});
function machineFamilyPhoto(m){
 const text=[m?.model,m?.name,m?.nameTh,m?.nameEn].filter(Boolean).join(' ').toLowerCase();
 const compact=text.replace(/[\s_-]+/g,'');
 if(compact.includes('ho500wt'))return MACHINE_FAMILY_PHOTOS.hydrogenHo500;
 if(compact.includes('ho200wt'))return MACHINE_FAMILY_PHOTOS.hydrogenHo200;
 if(/\bn\s*-?\s*(20|40)\b/i.test(text)||text.includes('nitrogen gas generator')||text.includes('เครื่องผลิตแก๊สไนโตรเจน'))return MACHINE_FAMILY_PHOTOS.nitrogen;
 if(/blue\s*-?\s*thunder/i.test(text))return MACHINE_FAMILY_PHOTOS.blueThunder;
 if(/laser\s*weld|bull\s*thunder|เครื่องเชื่อมเลเซอร์/i.test(text))return MACHINE_FAMILY_PHOTOS.laserWelding;
 if(/bgf\s*-?\s*20|laser\s*mark|เครื่องมาร์กเลเซอร์/i.test(text))return MACHINE_FAMILY_PHOTOS.laserMarking;
 if(/\bva\s*38\b/i.test(text))return MACHINE_FAMILY_PHOTOS.centrifugalVa38;
 if(/\bva\s*20\b/i.test(text))return MACHINE_FAMILY_PHOTOS.centrifugalVa20;
 if(/\bva\s*18\b|20\s*l\s*x\s*3|\bmsh\b|gyratory\s*polish|เครื่องขัดระบบไจเรต/i.test(text))return MACHINE_FAMILY_PHOTOS.polishingFamily;
 if(/centrifugal\s*disc\s*finishing|เครื่องขัดแบบจานหมุนเหวี่ยง/i.test(text))return MACHINE_FAMILY_PHOTOS.polishingFamily;
 return '';
}
window.machineFamilyPhoto=machineFamilyPhoto;
function machineMediaHTML(m,large=false){
 const actualPhoto=String(m?.photo||'').trim();
 const familyPhoto=actualPhoto?'':machineFamilyPhoto(m);
 const src=actualPhoto||familyPhoto;
 const frameClass=large?'machine-thumb-lg':'machine-thumb';
 if(!src)return `<div class="${frameClass} machine-thumb-placeholder">No Image</div>`;
 const label=familyPhoto?`<span class="machine-reference-badge">${typeof settings!=='undefined'&&settings.language==='en'?'Reference Image':'ภาพอ้างอิง'}</span>`:'';
 const onerror="const p=this.parentElement;this.onerror=null;if(p){p.textContent='No Image';p.classList.add('machine-thumb-placeholder');p.classList.remove('has-family-photo')}";
 return `<div class="${frameClass}${familyPhoto?' has-family-photo':''}"><img src="${esc(src)}" alt="${esc(machinePrimaryName(m)||'Machine')}" loading="lazy" onerror="${onerror}">${label}</div>`;
}
window.machineMediaHTML=machineMediaHTML;
function filteredTechnicians(team=techTeamFilter){return technicians.filter(t=>team==='all'||(t.team||'Technical')===team)}
function setTechTeamFilter(team='all'){techTeamFilter=team;renderTechnicians();renderCalendar()}window.setTechTeamFilter=setTechTeamFilter;
function renderTechnicians(){
 const list=filteredTechnicians();
 const technical=technicians.filter(t=>(t.team||'Technical')==='Technical').length;
 const rd=technicians.filter(t=>(t.team||'Technical')==='R&D').length;
 if(typeof technicianTeamSummary!=='undefined')technicianTeamSummary.innerHTML=`<div class="team-stat"><small>บุคลากรทั้งหมด</small><b>${technicians.length}</b></div><div class="team-stat"><small>ทีม Technical</small><b>${technical}</b></div><div class="team-stat"><small>ทีม R&D</small><b>${rd}</b></div>`;
 document.querySelectorAll('#techTeamSegment .seg-btn').forEach(btn=>btn.classList.toggle('active',btn.dataset.team===techTeamFilter));
 technicianGrid.innerHTML=list.map(t=>{const jobs=openCases().filter(c=>c.assignee===t.id).length;return `<div class="person-card"><div class="person-card-top"><div class="person-card-main"><h4>${esc(t.name)}</h4><p class="person-role">${esc(t.role||'Service Technician')}</p><div class="person-badges">${teamBadge(t.team)}<span class="tech-status-dot ${techStatusClass(t.status)}">${esc(t.status||'พร้อมรับงาน')}</span></div>${t.skills?`<div class="skill-tags">${techSkillTags(t)}</div>`:''}<p class="person-phone">${esc(t.phone||'-')}</p></div><div class="person-hero">${techAvatarHTML(t)}</div></div><div class="person-bottom"><div class="person-stat"><span>งานเปิด</span><b>${jobs}</b></div><div class="person-actions"><button class="soft-btn" onclick="openFieldService('${t.id}')">งานหน้างาน</button><button class="soft-btn" onclick="openTechnicianDetail('${t.id}')">รายละเอียด</button><button class="primary-btn" onclick="openTechnicianModal('${t.id}')">แก้ไข</button></div></div></div>`}).join('')||'<div class="empty">ไม่มีรายชื่อในทีมนี้</div>'
}
function monday(offset=0){const d=new Date(),day=d.getDay()||7;d.setHours(0,0,0,0);d.setDate(d.getDate()-day+1+offset*7);return d}
function getBaseCalendarDate(){const d=new Date();d.setHours(0,0,0,0);if(calendarView==='day'){d.setDate(d.getDate()+calendarOffset);return d}if(calendarView==='week'){const day=d.getDay()||7;d.setDate(d.getDate()-day+1+calendarOffset*7);return d}if(calendarView==='month'){d.setDate(1);d.setMonth(d.getMonth()+calendarOffset);return d}d.setMonth(0,1);d.setFullYear(d.getFullYear()+calendarOffset);return d}
function setCalendarView(view){calendarView=view;calendarOffset=0;renderCalendar()}window.setCalendarView=setCalendarView;
function setCalendarTeamFilter(v){calendarTeamFilter=v;renderCalendar()}window.setCalendarTeamFilter=setCalendarTeamFilter;
function changeCalendar(v,reset=false){calendarOffset=reset?0:calendarOffset+v;renderCalendar()}window.changeCalendar=changeCalendar;
function caseTeam(c){return c.serviceTeam||techById(c.assignee)?.team||'Technical'}
function teamMatchedCase(c){return calendarTeamFilter==='all'||caseTeam(c)===calendarTeamFilter}
function renderCalendar(){
 const base=getBaseCalendarDate(),techs=filteredTechOptions(calendarTeamFilter);
 document.querySelectorAll('[data-cal-view]').forEach(btn=>btn.classList.toggle('active',btn.dataset.calView===calendarView));
 if(typeof calendarTeamFilter!=='undefined' && document.getElementById('calendarTeamFilter'))document.getElementById('calendarTeamFilter').value=calendarTeamFilter;
 let html='';
 if(calendarView==='day'){
   calendarRange.textContent=base.toLocaleDateString(uiLocale(),{weekday:'long',day:'numeric',month:'long',year:'numeric'});
   html=`<div class="day-list">${techs.map(t=>{const jobs=cases.filter(c=>c.assignee===t.id&&c.appointment&&teamMatchedCase(c)&&new Date(c.appointment).toDateString()===base.toDateString()).sort((a,b)=>new Date(a.appointment)-new Date(b.appointment));return `<div class="day-tech-card"><div class="day-tech-head"><div><h4>${esc(t.name)}</h4><p>${esc(t.role||'')}</p>${teamBadge(t.team)}</div><span class="tech-status-dot ${techStatusClass(t.status)}">${esc(t.status||'พร้อมรับงาน')}</span></div>${jobs.length?jobs.map(c=>`<div class="day-job" onclick="openCaseDetail('${c.id}')"><b>${new Date(c.appointment).toLocaleTimeString(uiLocale(),{hour:'2-digit',minute:'2-digit'})} · ${esc(c.customer)}</b><small>${esc(c.machine)} · ${esc(c.status)}</small></div>`).join(''):'<div class="day-empty">ไม่มีงานนัดหมายในวันนี้</div>'}</div>`}).join('')}</div>`;
 }
 else if(calendarView==='week'){
   const days=[...Array(7)].map((_,i)=>new Date(base.getTime()+i*86400000));
   calendarRange.textContent=`${days[0].toLocaleDateString(uiLocale(),{day:'numeric',month:'short'})} - ${days[6].toLocaleDateString(uiLocale(),{day:'numeric',month:'short',year:'numeric'})}`;
   html=`<div class="cal-header"><div>ช่าง</div>${days.map(d=>`<div>${d.toLocaleDateString(uiLocale(),{weekday:'short',day:'numeric',month:'short'})}</div>`).join('')}</div>`;
   techs.forEach(t=>{html+=`<div class="cal-row"><div class="tech-cell"><div class="tech-name">${esc(t.name)}</div><div class="tech-role">${esc(t.role||'')}</div>${teamBadge(t.team)}</div>${days.map(d=>{const jobs=cases.filter(c=>c.assignee===t.id&&c.appointment&&teamMatchedCase(c)&&new Date(c.appointment).toDateString()===d.toDateString());return `<div>${jobs.map(c=>`<button class="job-chip ${t.color||'blue'}" onclick="openCaseDetail('${c.id}')"><b>${new Date(c.appointment).toLocaleTimeString(uiLocale(),{hour:'2-digit',minute:'2-digit'})} · ${esc(c.customer)}</b>${esc(c.machine)}</button>`).join('')}</div>`}).join('')}</div>`});
 }
 else if(calendarView==='month'){
   const first=new Date(base.getFullYear(),base.getMonth(),1),start=new Date(first);const day=(start.getDay()||7)-1;start.setDate(start.getDate()-day);const monthLabel=first.toLocaleDateString(uiLocale(),{month:'long',year:'numeric'});calendarRange.textContent=monthLabel;html=`<div class="month-grid">`;
   for(let i=0;i<42;i++){
     const d=new Date(start.getTime()+i*86400000),muted=d.getMonth()!==first.getMonth();
     const jobs=cases.filter(c=>c.appointment&&teamMatchedCase(c)&&new Date(c.appointment).toDateString()===d.toDateString()).sort((a,b)=>new Date(a.appointment)-new Date(b.appointment));
     html+=`<div class="month-cell ${muted?'muted':''}"><div class="month-date"><span>${d.toLocaleDateString(uiLocale(),{day:'numeric',month:'short'})}</span>${jobs.length?`<span class="month-count">${jobs.length}</span>`:''}</div><div class="month-appts">${jobs.slice(0,3).map(c=>`<div class="month-appt ${caseTeam(c)==='R&D'?'rd':''}" onclick="openCaseDetail('${c.id}')">${new Date(c.appointment).toLocaleTimeString(uiLocale(),{hour:'2-digit',minute:'2-digit'})} · ${esc(c.customer)}</div>`).join('')}${jobs.length>3?`<div class="month-more">+ อีก ${jobs.length-3} รายการ</div>`:''}</div></div>`
   }
   html+='</div>';
 }
 else {
   calendarRange.textContent=base.toLocaleDateString(uiLocale(),{year:'numeric'});
   html='<div class="year-grid">';
   for(let m=0;m<12;m++){
     const start=new Date(base.getFullYear(),m,1),end=new Date(base.getFullYear(),m+1,1);
     const jobs=cases.filter(c=>c.appointment&&teamMatchedCase(c)&&new Date(c.appointment)>=start&&new Date(c.appointment)<end);
     const tech=jobs.filter(c=>caseTeam(c)==='Technical').length,rd=jobs.filter(c=>caseTeam(c)==='R&D').length;
     html+=`<div class="year-card"><h4>${start.toLocaleDateString(uiLocale(),{month:'long'})}</h4><p>ภาพรวมงานนัดหมายทั้งเดือน</p><div class="year-count">${jobs.length}</div><small>นัดหมาย</small><div class="year-mini"><span>Technical ${tech}</span><span>R&D ${rd}</span></div></div>`
   }
   html+='</div>';
 }
 technicianCalendar.innerHTML=html;
 const agendaCases=(calendarView==='day'?
   cases.filter(c=>c.appointment&&teamMatchedCase(c)&&new Date(c.appointment).toDateString()===base.toDateString()):
   calendarView==='week'?
   cases.filter(c=>c.appointment&&teamMatchedCase(c)&&(()=>{const d=new Date(c.appointment);const end=new Date(base.getTime()+6*86400000);return d>=base&&d<=end})()):
   calendarView==='month'?
   cases.filter(c=>c.appointment&&teamMatchedCase(c)&&new Date(c.appointment).getMonth()===base.getMonth()&&new Date(c.appointment).getFullYear()===base.getFullYear()):
   cases.filter(c=>c.appointment&&teamMatchedCase(c)&&new Date(c.appointment).getFullYear()===base.getFullYear())
 ).sort((a,b)=>new Date(a.appointment)-new Date(b.appointment));
 mobileAgenda.innerHTML=agendaCases.length?`<div class="agenda-day"><h4>${calendarView==='day'?'นัดหมายวันนี้':calendarView==='week'?'นัดหมายสัปดาห์นี้':calendarView==='month'?'นัดหมายเดือนนี้':'นัดหมายปีนี้'}</h4>${agendaCases.slice(0,20).map(c=>`<div class="agenda-job" onclick="openCaseDetail('${c.id}')"><b>${fmt(c.appointment)} · ${esc(c.customer)}</b><small>${esc(c.machine)} · ${esc(techById(c.assignee)?.name||'ยังไม่มอบหมาย')} · ${esc(caseTeam(c))}</small></div>`).join('')}</div>`:'<div class="agenda-day"><h4>สรุปนัดหมาย</h4><small>ไม่มีงานตามเงื่อนไขที่เลือก</small></div>'
}


// ==================== V6.0 CRM & SALES MANAGEMENT ====================
function crmL(th,en){return settings.language==='en'?en:th}
function crmStageLabel(stage){const x=CRM_STAGE_LABELS[stage]||[stage,stage];return settings.language==='en'?x[1]:x[0]}
function crmMoney(v){return Number(v||0).toLocaleString(settings.language==='en'?'en-US':'th-TH',{maximumFractionDigits:0})}
function crmCustomer(id){return customers.find(x=>x.id===id)}
function crmOpportunity(id){return crmOpportunities.find(x=>x.id===id)}
function crmLead(id){return crmLeads.find(x=>x.id===id)}
function crmActivity(id){return crmActivities.find(x=>x.id===id)}
function crmDate(v){return v?new Date(v).toLocaleString(uiLocale(),{day:'2-digit',month:'short',year:'numeric',hour:'2-digit',minute:'2-digit',hour12:false}):'-'}
function crmDateOnly(v){return v?new Date(v+'T00:00:00').toLocaleDateString(uiLocale(),{day:'2-digit',month:'short',year:'numeric'}):'-'}
function crmTodayKey(){return new Date().toISOString().slice(0,10)}
function crmActivityStatus(a){if(a.status==='Done')return'Done';if(new Date(a.dueAt)<new Date())return'Overdue';return a.status||'Planned'}
function crmStageClass(s){return 'stage-'+String(s||'').toLowerCase().replace(/[^a-z0-9]+/g,'-')}
function setCrmTab(tab='overview'){crmActiveTab=tab;document.querySelectorAll('.crm-tab').forEach(b=>b.classList.toggle('active',b.dataset.crmTab===tab));document.querySelectorAll('.crm-tab-panel').forEach(p=>p.classList.remove('active'));const id={overview:'crmOverview',leads:'crmLeads',pipeline:'crmPipeline',quotations:'crmQuotations',activities:'crmActivities',customers360:'crmCustomers360',renewal:'crmRenewal'}[tab];document.getElementById(id)?.classList.add('active');renderCrm()}window.setCrmTab=setCrmTab;
function renderCrm(){
 if(typeof crmKpiLeads==='undefined')return;
 renderCrmOverview();renderCrmLeads();renderCrmPipeline();renderSalesQuotes();renderCrmActivities();renderCrmCustomer360List();renderCrmRenewals();
}
function renderCrmOverview(){
 const openLeads=crmLeads.filter(x=>!['Converted','Lost'].includes(x.status));
 const openOpp=crmOpportunities.filter(x=>!['Won','Lost'].includes(x.stage));
 const pendingQuotes=salesQuotes.filter(q=>!['อนุมัติ','ไม่สำเร็จ'].includes(q.status));
 const followToday=crmActivities.filter(a=>crmActivityStatus(a)!=='Done'&&String(a.dueAt||'').slice(0,10)<=crmTodayKey()).length;
 if(typeof crmKpiLeads!=='undefined')crmKpiLeads.textContent=openLeads.length;
 if(typeof crmKpiPipeline!=='undefined')crmKpiPipeline.textContent=openOpp.length;
 if(typeof crmKpiExpected!=='undefined')crmKpiExpected.textContent=pendingQuotes.length;
 if(typeof crmKpiFollowup!=='undefined')crmKpiFollowup.textContent=followToday;
 if(typeof salesOpenValue!=='undefined')salesOpenValue.textContent=crmMoney(openOpp.reduce((s,x)=>s+Number(x.value||0),0))+' บาท';
 if(typeof salesQuoteOpenValue!=='undefined')salesQuoteOpenValue.textContent=crmMoney(pendingQuotes.reduce((s,x)=>s+Number(x.grand||0),0))+' บาท';
 if(typeof salesWaitingPO!=='undefined')salesWaitingPO.textContent=crmOpportunities.filter(o=>o.stage==='Waiting PO').length+' งาน';
 const acts=[...crmActivities].filter(a=>crmActivityStatus(a)!=='Done').sort((a,b)=>new Date(a.dueAt)-new Date(b.dueAt)).slice(0,6);
 if(typeof crmFollowupList!=='undefined')crmFollowupList.innerHTML=acts.length?acts.map(a=>{const cu=crmCustomer(a.customerId),op=crmOpportunity(a.opportunityId),lead=crmLead(a.leadId);return `<button class="crm-activity-mini ${crmActivityStatus(a)==='Overdue'?'overdue':''}" onclick="openCrmActivityModal('${a.id}')"><span>${crmActivityIcon(a.type)}</span><div><b>${esc(a.subject)}</b><small>${esc(cu?.name||lead?.company||op?.title||'-')} · ${crmDate(a.dueAt)}</small></div><em>${crmActivityStatus(a)==='Overdue'?'เกินกำหนด':crmActivityStatus(a)==='Done'?'เสร็จแล้ว':'รอติดตาม'}</em></button>`}).join(''):`<div class="empty">ไม่มีงานติดตามค้าง</div>`;
 const hot=[...openOpp].filter(o=>['Quotation','Negotiation','Waiting PO'].includes(o.stage)).sort((a,b)=>Number(b.probability)-Number(a.probability)).slice(0,5);
 if(typeof crmHotOpportunities!=='undefined')crmHotOpportunities.innerHTML=hot.length?hot.map(o=>`<button class="crm-hot-item" onclick="openCrmOpportunityDetail('${o.id}')"><div><b>${esc(o.title)}</b><small>${esc(crmCustomer(o.customerId)?.name||crmLead(o.leadId)?.company||'-')} · ${crmStageLabel(o.stage)}</small></div><div><strong>${crmMoney(o.value)}</strong><span>${o.probability}%</span></div></button>`).join(''):'<div class="empty">ยังไม่มีงานช่วงเสนอราคา / เจรจา / รอ PO</div>';
 const renewal=crmRenewalCandidates().slice(0,5);
 if(typeof crmRenewalPreview!=='undefined')crmRenewalPreview.innerHTML=renewal.length?renewal.map(r=>crmRenewalCard(r,true)).join(''):`<div class="empty">ยังไม่มีรายการต่อประกัน / ขายเพิ่ม</div>`;
 if(typeof crmRecentQuotes!=='undefined')crmRecentQuotes.innerHTML=[...salesQuotes].sort((a,b)=>new Date(b.updatedAt||b.createdAt)-new Date(a.updatedAt||a.createdAt)).slice(0,5).map(q=>salesQuoteMiniCard(q)).join('')||'<div class="empty">ยังไม่มีใบเสนอราคาฝ่ายขาย</div>';
}
function crmActivityIcon(t){return({Call:'📞',LINE:'💬',Email:'✉️',Visit:'📍',Demo:'🧪','Follow-up':'↻',Meeting:'👥'}[t]||'●')}
function renderCrmLeads(){
 if(typeof crmLeadTable==='undefined')return;const q=(crmLeadSearch?.value||'').toLowerCase(),src=crmLeadSourceFilter?.value||'',st=crmLeadStatusFilter?.value||'';
 const list=[...crmLeads].filter(l=>(!q||[l.company,l.contact,l.phone,l.interest,l.owner].join(' ').toLowerCase().includes(q))&&(!src||l.source===src)&&(!st||l.status===st)).sort((a,b)=>new Date(b.createdAt)-new Date(a.createdAt));
 crmLeadTable.innerHTML=list.length?list.map(l=>`<tr><td><b>${esc(l.company)}</b><span class="subtext">Score ${Number(l.score||0)}/100</span></td><td>${esc(l.contact||'-')}<span class="subtext">${esc(l.phone||'-')}</span></td><td><span class="crm-source">${esc(l.source||'-')}</span></td><td>${esc(l.interest||'-')}</td><td>${esc(l.owner||'-')}</td><td>${crmDate(l.nextFollowUp)}</td><td><span class="crm-status ${String(l.status).toLowerCase()}">${esc(l.status)}</span></td><td><button class="mini-btn" onclick="openCrmLeadDetail('${l.id}')">ดู</button> <button class="mini-btn" onclick="openCrmLeadModal('${l.id}')">แก้ไข</button></td></tr>`).join(''):`<tr><td colspan="8"><div class="empty">${crmL('ยังไม่มี Lead','No leads')}</div></td></tr>`;
 crmLeadCards.innerHTML=list.map(l=>`<div class="mobile-data-card"><div class="card-top"><div><h4>${esc(l.company)}</h4><p>${esc(l.interest||'-')}</p></div><span class="crm-status ${String(l.status).toLowerCase()}">${esc(l.status)}</span></div><p>${esc(l.contact||'-')} · ${esc(l.phone||'-')}</p><div class="card-actions"><button class="soft-btn" onclick="openCrmLeadDetail('${l.id}')">รายละเอียด</button><button class="primary-btn" onclick="openCrmLeadModal('${l.id}')">แก้ไข</button></div></div>`).join('');
}
function openCrmLeadModal(id=''){
 if(!requirePermission('crm.lead'))return;const old=crmLead(id)||{},th=settings.language!=='en';
 openModal(old.id?(th?'แก้ไขลูกค้าเป้าหมาย':'Edit Lead'):(th?'เพิ่มลูกค้าเป้าหมาย':'Add Lead'),th?'เก็บข้อมูลเบื้องต้นก่อนยืนยันเป็นลูกค้าจริงและสร้างงานขาย':'Capture basic prospect information before creating a customer and sales job',`<form id="crmLeadForm"><input id="clId" type="hidden" value="${esc(old.id||'')}"><div class="form-grid cols2"><div class="field"><label>${th?'บริษัท / ชื่อลูกค้า *':'Company / Prospect *'}</label><input id="clCompany" required value="${esc(old.company||'')}"></div><div class="field"><label>${th?'ผู้ติดต่อ':'Contact'}</label><input id="clContact" value="${esc(old.contact||'')}"></div><div class="field"><label>${th?'โทรศัพท์':'Phone'}</label><input id="clPhone" value="${esc(old.phone||'')}"></div><div class="field"><label>Email</label><input id="clEmail" value="${esc(old.email||'')}"></div><div class="field"><label>Source</label><select id="clSource">${['LINE OA','Website / QR','Exhibition','Referral','Sales','Service'].map(x=>`<option ${old.source===x?'selected':''}>${x}</option>`).join('')}</select></div><div class="field"><label>${th?'สถานะ':'Status'}</label><select id="clStatus"><option value="New" ${old.status==='New'?'selected':''}>ลูกค้าใหม่</option><option value="Contacted" ${old.status==='Contacted'?'selected':''}>ติดต่อแล้ว</option><option value="Qualified" ${old.status==='Qualified'?'selected':''}>สนใจจริง / พร้อมทำงานขาย</option><option value="Converted" ${old.status==='Converted'?'selected':''}>ยืนยันเป็นลูกค้าแล้ว</option><option value="Lost" ${old.status==='Lost'?'selected':''}>ไม่สนใจ / ยกเลิก</option></select></div><div class="field full"><label>${th?'สินค้า / บริการที่สนใจ':'Product / Service Interest'}</label><input id="clInterest" value="${esc(old.interest||'')}"></div><div class="field"><label>${th?'งบประมาณ':'Budget'}</label><input id="clBudget" type="number" min="0" value="${Number(old.budget||0)}"></div><div class="field"><label>ระดับความสนใจ %</label><input id="clScore" type="number" min="0" max="100" value="${Number(old.score||50)}"></div><div class="field"><label>ผู้รับผิดชอบ</label><input id="clOwner" value="${esc(old.owner||currentUser?.name||'Sales Team')}"></div><div class="field"><label>วัน/เวลาติดตามครั้งถัดไป</label><input id="clNext" type="datetime-local" value="${old.nextFollowUp?old.nextFollowUp.slice(0,16):''}"></div><div class="field full"><label>${th?'หมายเหตุ':'Note'}</label><textarea id="clNote">${esc(old.note||'')}</textarea></div></div><div class="button-row"><button type="button" class="soft-btn" onclick="closeModal()">${th?'ยกเลิก':'Cancel'}</button><button class="primary-btn">${th?'บันทึกลูกค้าเป้าหมาย':'Save Prospect'}</button></div></form>`,true);
 setTimeout(()=>crmLeadForm.onsubmit=e=>{e.preventDefault();saveCrmLead()},20)
}window.openCrmLeadModal=openCrmLeadModal;
function saveCrmLead(){const old=crmLead(clId.value),obj={id:old?.id||'LEAD-'+String(crmLeads.length+1).padStart(4,'0'),company:clCompany.value.trim(),contact:clContact.value.trim(),phone:clPhone.value.trim(),email:clEmail.value.trim(),source:clSource.value,status:clStatus.value,interest:clInterest.value.trim(),budget:Number(clBudget.value)||0,score:Number(clScore.value)||0,owner:clOwner.value.trim(),nextFollowUp:clNext.value?new Date(clNext.value).toISOString():'',note:clNote.value.trim(),createdAt:old?.createdAt||new Date().toISOString(),updatedAt:new Date().toISOString()};if(old)crmLeads=crmLeads.map(x=>x.id===obj.id?obj:x);else crmLeads.unshift(obj);saveLocal();closeModal();renderCrm();toastMsg(crmL('บันทึกลูกค้าเป้าหมายแล้ว','Lead saved'))}window.saveCrmLead=saveCrmLead;
function openCrmLeadDetail(id){const l=crmLead(id);if(!l)return;openModal('ลูกค้าเป้าหมาย: '+l.company,`${l.contact||'-'} · ${l.phone||'-'}`,`<div class="detail-grid"><div class="detail-box"><small>ที่มา</small><b>${esc(l.source||'-')}</b></div><div class="detail-box"><small>สถานะ</small><b>${esc(l.status||'-')}</b></div><div class="detail-box"><small>ระดับความสนใจ</small><b>${Number(l.score||0)}/100</b></div><div class="detail-box"><small>งบประมาณโดยประมาณ</small><b>${crmMoney(l.budget)} ${crmL('บาท','THB')}</b></div><div class="detail-box full"><small>${crmL('สิ่งที่สนใจ','Interest')}</small><b>${esc(l.interest||'-')}</b></div><div class="detail-box full"><small>${crmL('หมายเหตุ','Note')}</small><b>${esc(l.note||'-')}</b></div></div><div class="button-row" style="margin-top:14px"><button class="soft-btn" onclick="openCrmActivityModal('','','${l.id}')">＋ งานติดตาม</button>${l.status!=='Converted'?`<button class="primary-btn" onclick="convertCrmLead('${l.id}')">${crmL('ยืนยันเป็นลูกค้า + สร้างงานขาย','Convert to Customer + Opportunity')}</button>`:''}</div>`) }window.openCrmLeadDetail=openCrmLeadDetail;
function convertCrmLead(id){const l=crmLead(id);if(!l)return;let cu=customers.find(c=>c.name===l.company);if(!cu){cu={id:'CUST-CRM-'+uid().replace(/[^a-zA-Z0-9]/g,'').slice(-8),name:l.company,branch:'',contact:l.contact||'',phone:l.phone||'',email:l.email||'',location:'',address:'',mapUrl:'',latitude:'',longitude:'',note:'Converted from '+l.id,serviceStatus:'CRM Lead',source:'CRM',lineUserId:'',lineDisplayName:'',lineLinkedAt:''};customers.push(cu)}l.status='Converted';l.customerId=cu.id;l.updatedAt=new Date().toISOString();saveLocal();closeModal();renderAll();openCrmOpportunityModal('',cu.id,l.id);toastMsg(crmL('สร้างลูกค้าแล้ว กรุณาสร้างงานขายต่อ','Customer created. Create the sales job next.'))}window.convertCrmLead=convertCrmLead;
function renderCrmPipeline(){
 if(typeof crmPipelineBoard==='undefined')return;
 const groups=[
  {label:'1 · ลูกค้าใหม่ / ติดต่อแล้ว',stages:['Prospect','Contacted'],tone:'new'},
  {label:'2 · เก็บความต้องการ',stages:['Needs Analysis'],tone:'need'},
  {label:'3 · Demo / สำรวจ',stages:['Demo'],tone:'demo'},
  {label:'4 · ใบเสนอราคา',stages:['Quotation'],tone:'quote'},
  {label:'5 · ติดตาม / รอ PO',stages:['Negotiation','Waiting PO'],tone:'follow'},
  {label:'6 · ปิดการขาย / ไม่สำเร็จ',stages:['Won','Lost'],tone:'closed'}
 ];
 crmPipelineBoard.innerHTML=groups.map(g=>{const list=crmOpportunities.filter(o=>g.stages.includes(o.stage));const total=list.reduce((s,o)=>s+Number(o.value||0),0);return `<div class="crm-stage-column simple-stage ${g.tone}"><div class="crm-stage-column-head"><div><b>${g.label}</b><small>${list.length} งาน</small></div><strong>${crmMoney(total)}</strong></div><div class="crm-stage-cards">${list.length?list.map(o=>crmOpportunityCard(o)).join(''):`<div class="crm-stage-empty">ยังไม่มีงานในขั้นตอนนี้</div>`}</div></div>`}).join('');
}
function crmOpportunityCard(o){const cu=crmCustomer(o.customerId),lead=crmLead(o.leadId),idx=CRM_STAGES.indexOf(o.stage),canNext=idx>=0&&idx<CRM_STAGES.indexOf('Won');return `<div class="crm-opp-card sales-job-card"><button class="crm-opp-main" onclick="openCrmOpportunityDetail('${o.id}')"><span class="crm-prob">${Number(o.probability||0)}%</span><b>${esc(o.title)}</b><small>${esc(cu?.name||lead?.company||'-')}</small><strong>${crmMoney(o.value)} บาท</strong><em>${crmStageLabel(o.stage)}${o.nextAction?` · ต่อไป: ${esc(o.nextAction)}`:''}</em></button><div class="sales-job-actions"><button onclick="openCrmOpportunityDetail('${o.id}')">รายละเอียด</button>${canNext?`<button class="next" onclick="moveCrmOpportunity('${o.id}',1)">ขั้นตอนถัดไป ›</button>`:''}</div></div>`}
function moveCrmOpportunity(id,delta){if(!requirePermission('crm.opportunity'))return;const o=crmOpportunity(id);if(!o)return;let i=CRM_STAGES.indexOf(o.stage);i=Math.max(0,Math.min(CRM_STAGES.length-1,i+delta));o.stage=CRM_STAGES[i];o.probability=CRM_STAGE_PROB[o.stage];o.updatedAt=new Date().toISOString();saveLocal();renderCrm();toastMsg(crmL('อัปเดตขั้นตอนแล้ว','Stage updated'))}window.moveCrmOpportunity=moveCrmOpportunity;
function openCrmOpportunityModal(id='',presetCustomer='',presetLead=''){
 if(!requirePermission('crm.opportunity'))return;const old=crmOpportunity(id)||{},th=settings.language!=='en',customerId=old.customerId||presetCustomer||'',leadId=old.leadId||presetLead||'';
 openModal(old.id?(th?'แก้ไขงานขาย':'Edit Opportunity'):(th?'เพิ่มงานขาย':'Add Opportunity'),th?'เชื่อมลูกค้า เครื่องจักร ใบเสนอราคา และงานติดตามไว้ในงานขายเดียว':'Connect customer, machine, quotation and follow-up in one sales job',`<form id="crmOppForm"><input id="coId" type="hidden" value="${esc(old.id||'')}"><div class="form-grid cols2"><div class="field full"><label>${th?'ชื่องานขาย *':'Sales Job Name *'}</label><input id="coTitle" required value="${esc(old.title||'')}"></div><div class="field"><label>${th?'ลูกค้า':'Customer'}</label><select id="coCustomer" onchange="crmOppCustomerChanged()"><option value="">ยังไม่เป็นลูกค้า</option>${customers.map(c=>`<option value="${c.id}" ${customerId===c.id?'selected':''}>${esc(c.name)}</option>`).join('')}</select></div><div class="field"><label>ลูกค้าเป้าหมายเดิม (ถ้ามี)</label><select id="coLead"><option value="">ไม่ระบุ</option>${crmLeads.map(l=>`<option value="${l.id}" ${leadId===l.id?'selected':''}>${esc(l.company)}</option>`).join('')}</select></div><div class="field"><label>${th?'เครื่องจักร':'Machine'}</label><select id="coMachine"></select></div><div class="field"><label>${th?'สินค้า / บริการที่สนใจ':'Product / Service'}</label><input id="coInterest" value="${esc(old.interest||'')}"></div><div class="field"><label>${th?'มูลค่างานขาย':'Sales Value'}</label><input id="coValue" type="number" min="0" value="${Number(old.value||0)}"></div><div class="field"><label>Stage</label><select id="coStage" onchange="coProbability.value=CRM_STAGE_PROB[this.value]">${CRM_STAGES.map(s=>`<option value="${s}" ${old.stage===s?'selected':''}>${esc(crmStageLabel(s))}</option>`).join('')}</select></div><div class="field"><label>โอกาสปิดการขาย %</label><input id="coProbability" type="number" min="0" max="100" value="${Number(old.probability??CRM_STAGE_PROB[old.stage||'Prospect'])}"></div><div class="field"><label>คาดว่าจะปิดการขาย</label><input id="coClose" type="date" value="${esc(old.expectedClose||'')}"></div><div class="field"><label>ผู้รับผิดชอบ</label><input id="coOwner" value="${esc(old.owner||currentUser?.name||'Sales Team')}"></div><div class="field full"><label>${th?'สิ่งที่ต้องทำต่อ':'Next Action'}</label><input id="coNextAction" value="${esc(old.nextAction||'')}"></div><div class="field"><label>วัน/เวลาติดตามครั้งถัดไป</label><input id="coNextFollow" type="datetime-local" value="${old.nextFollowUp?old.nextFollowUp.slice(0,16):''}"></div><div class="field"><label>Source</label><select id="coSource">${['CRM','Existing Customer','LINE OA','Service','Warranty','Exhibition','Referral'].map(x=>`<option ${old.source===x?'selected':''}>${x}</option>`).join('')}</select></div><div class="field full"><label>${th?'หมายเหตุ':'Note'}</label><textarea id="coNote">${esc(old.note||'')}</textarea></div></div><div class="button-row"><button type="button" class="soft-btn" onclick="closeModal()">${th?'ยกเลิก':'Cancel'}</button><button class="primary-btn">${th?'บันทึกงานขาย':'Save Sales Job'}</button></div></form>`,true);
 setTimeout(()=>{crmOppCustomerChanged(old.machineId||'');crmOppForm.onsubmit=e=>{e.preventDefault();saveCrmOpportunity()}},20)
}window.openCrmOpportunityModal=openCrmOpportunityModal;
function crmOppCustomerChanged(selected=''){const cid=coCustomer.value;const list=machines.filter(m=>!cid||m.customerId===cid);coMachine.innerHTML='<option value="">ไม่ระบุเครื่อง</option>'+list.map(m=>`<option value="${m.id}">${esc(machinePrimaryName(m))} · ${esc(m.model||'-')} · ${esc(m.serial||'-')}</option>`).join('');if(selected&&list.some(m=>m.id===selected))coMachine.value=selected}window.crmOppCustomerChanged=crmOppCustomerChanged;
function saveCrmOpportunity(){const old=crmOpportunity(coId.value),obj={id:old?.id||'OPP-'+String(crmOpportunities.length+1).padStart(4,'0'),customerId:coCustomer.value,leadId:coLead.value,title:coTitle.value.trim(),interest:coInterest.value.trim(),machineId:coMachine.value,value:Number(coValue.value)||0,stage:coStage.value,probability:Number(coProbability.value)||0,expectedClose:coClose.value,owner:coOwner.value.trim(),source:coSource.value,nextAction:coNextAction.value.trim(),nextFollowUp:coNextFollow.value?new Date(coNextFollow.value).toISOString():'',note:coNote.value.trim(),quotationId:old?.quotationId||'',createdAt:old?.createdAt||new Date().toISOString(),updatedAt:new Date().toISOString()};if(old)crmOpportunities=crmOpportunities.map(x=>x.id===obj.id?obj:x);else crmOpportunities.unshift(obj);saveLocal();closeModal();renderCrm();toastMsg(crmL('บันทึกงานขายแล้ว','Sales job saved'))}window.saveCrmOpportunity=saveCrmOpportunity;
function openCrmOpportunityDetail(id){const o=crmOpportunity(id);if(!o)return;const cu=crmCustomer(o.customerId),m=machineById(o.machineId),acts=crmActivities.filter(a=>a.opportunityId===o.id).sort((a,b)=>new Date(b.dueAt)-new Date(a.dueAt));openModal('งานขาย: '+o.id,o.title,`<div class="crm-opportunity-hero"><div><small>${esc(crmStageLabel(o.stage))}</small><h3>${esc(o.title)}</h3><p>${esc(cu?.name||crmLead(o.leadId)?.company||'-')}</p></div><div><b>${crmMoney(o.value)}</b><span>${crmL('บาท','THB')} · ${o.probability}%</span></div></div><div class="detail-grid"><div class="detail-box"><small>คาดว่าจะปิดการขาย</small><b>${crmDateOnly(o.expectedClose)}</b></div><div class="detail-box"><small>ผู้รับผิดชอบ</small><b>${esc(o.owner||'-')}</b></div><div class="detail-box"><small>${crmL('เครื่องจักร','Machine')}</small><b>${esc(m?machinePrimaryName(m):'-')}</b></div><div class="detail-box"><small>ที่มา</small><b>${esc(o.source||'-')}</b></div><div class="detail-box full"><small>สิ่งที่ต้องทำต่อ</small><b>${esc(o.nextAction||'-')}</b></div></div><div class="crm-link-actions"><button onclick="openCrmActivityModal('','${o.id}')">↻ เพิ่มงานติดตาม</button>${cu?`<button onclick="closeModal();openCrmCustomer360('${cu.id}')">👥 ข้อมูลลูกค้ารวม</button><button onclick="closeModal();goPage('customers');setTimeout(()=>openCustomerDetail('${cu.id}'),60)">ฐานข้อมูลลูกค้า</button>`:''}${m?`<button onclick="closeModal();goPage('machines');setTimeout(()=>openMachineDetail('${m.id}'),60)">⚙ เครื่องจักร</button>`:''}<button class="orange" onclick="openSalesQuoteModal('','${o.customerId}','${o.id}')">฿ สร้างใบเสนอราคา</button><button onclick="createQuotationFromCrmOpportunity('${o.id}')">🔧 ใบเสนอราคา Service</button></div><div class="section-title" style="margin-top:16px">${crmL('กิจกรรมล่าสุด','Recent Activities')}</div><div class="crm-activity-list">${acts.length?acts.slice(0,6).map(a=>`<button class="crm-activity-mini" onclick="openCrmActivityModal('${a.id}')"><span>${crmActivityIcon(a.type)}</span><div><b>${esc(a.subject)}</b><small>${crmDate(a.dueAt)}</small></div><em>${esc(crmActivityStatus(a))}</em></button>`).join(''):`<div class="empty">-</div>`}</div><div class="button-row" style="margin-top:14px"><button class="soft-btn" onclick="openCrmOpportunityModal('${o.id}')">${crmL('แก้ไข','Edit')}</button></div>`,true)}window.openCrmOpportunityDetail=openCrmOpportunityDetail;
function createQuotationFromCrmOpportunity(id){const o=crmOpportunity(id),cu=crmCustomer(o?.customerId);if(!o||!cu){toastMsg(crmL('งานขายต้องเชื่อมลูกค้าก่อน','Sales job must be linked to a customer'));return}closeModal();goPage('quotation');setTimeout(()=>{qCustomer.value=cu.id;quoteCustomerChanged();if(o.machineId){quoteMachineLines=[o.machineId];renderQuoteMachineLines()}qContact.value=cu.contact||'';qLocation.value=cu.address||cu.location||'';calcQuote();toastMsg(crmL('ดึงข้อมูลจาก CRM เข้าใบเสนอราคาแล้ว','CRM data loaded into quotation'))},100)}window.createQuotationFromCrmOpportunity=createQuotationFromCrmOpportunity;
function openCrmActivityModal(id='',presetOpp='',presetLead=''){
 if(!requirePermission('crm.activity'))return;const old=crmActivity(id)||{},opp=crmOpportunity(old.opportunityId||presetOpp),customerId=old.customerId||opp?.customerId||'',leadId=old.leadId||presetLead||'',th=settings.language!=='en';
 openModal(old.id?(th?'แก้ไขกิจกรรม':'Edit Activity'):(th?'เพิ่มงานติดตาม':'Add Activity / Follow-up'),th?'งานติดตามสามารถเชื่อมกับลูกค้า ลูกค้าเป้าหมาย หรืองานขาย เพื่อให้ทีมเห็นสิ่งที่ต้องทำต่อ':'Follow-up tasks can link to a customer, prospect, or sales job',`<form id="crmActivityForm"><input id="caId" type="hidden" value="${esc(old.id||'')}"><div class="form-grid cols2"><div class="field"><label>${th?'ประเภทกิจกรรม':'Activity Type'}</label><select id="caType">${['Call','LINE','Email','Visit','Demo','Follow-up','Meeting'].map(x=>`<option ${old.type===x?'selected':''}>${x}</option>`).join('')}</select></div><div class="field"><label>${th?'สถานะ':'Status'}</label><select id="caStatus"><option value="Planned" ${old.status==='Planned'?'selected':''}>รอดำเนินการ</option><option value="Done" ${old.status==='Done'?'selected':''}>เสร็จแล้ว</option></select></div><div class="field full"><label>${th?'หัวข้อ *':'Subject *'}</label><input id="caSubject" required value="${esc(old.subject||'')}"></div><div class="field"><label>${th?'ลูกค้า':'Customer'}</label><select id="caCustomer"><option value="">ไม่ระบุ</option>${customers.map(c=>`<option value="${c.id}" ${customerId===c.id?'selected':''}>${esc(c.name)}</option>`).join('')}</select></div><div class="field"><label>งานขาย</label><select id="caOpp"><option value="">ไม่ระบุ</option>${crmOpportunities.map(o=>`<option value="${o.id}" ${(old.opportunityId||presetOpp)===o.id?'selected':''}>${esc(o.id+' · '+o.title)}</option>`).join('')}</select></div><div class="field"><label>ลูกค้าเป้าหมาย</label><select id="caLead"><option value="">ไม่ระบุ</option>${crmLeads.map(l=>`<option value="${l.id}" ${leadId===l.id?'selected':''}>${esc(l.company)}</option>`).join('')}</select></div><div class="field"><label>${th?'กำหนดเวลา':'Due Date / Time'}</label><input id="caDue" type="datetime-local" value="${old.dueAt?old.dueAt.slice(0,16):''}"></div><div class="field"><label>ผู้รับผิดชอบ</label><input id="caOwner" value="${esc(old.owner||currentUser?.name||'Sales Team')}"></div><div class="field full"><label>${th?'หมายเหตุ':'Note'}</label><textarea id="caNote">${esc(old.note||'')}</textarea></div></div><div class="button-row"><button type="button" class="soft-btn" onclick="closeModal()">${th?'ยกเลิก':'Cancel'}</button><button class="primary-btn">${th?'บันทึกงานติดตาม':'Save Follow-up'}</button></div></form>`,true);setTimeout(()=>crmActivityForm.onsubmit=e=>{e.preventDefault();saveCrmActivity()},20)
}window.openCrmActivityModal=openCrmActivityModal;
function saveCrmActivity(){const old=crmActivity(caId.value),obj={id:old?.id||'ACT-'+String(crmActivities.length+1).padStart(4,'0'),customerId:caCustomer.value,opportunityId:caOpp.value,leadId:caLead.value,type:caType.value,subject:caSubject.value.trim(),dueAt:caDue.value?new Date(caDue.value).toISOString():new Date().toISOString(),status:caStatus.value,owner:caOwner.value.trim(),note:caNote.value.trim(),createdAt:old?.createdAt||new Date().toISOString(),updatedAt:new Date().toISOString()};if(old)crmActivities=crmActivities.map(x=>x.id===obj.id?obj:x);else crmActivities.unshift(obj);saveLocal();closeModal();renderCrm();toastMsg(crmL('บันทึกงานติดตามแล้ว','Activity saved'))}window.saveCrmActivity=saveCrmActivity;
function renderCrmActivities(){
 if(typeof crmActivityTimeline==='undefined')return;const q=(crmActivitySearch?.value||'').toLowerCase(),type=crmActivityTypeFilter?.value||'',status=crmActivityStatusFilter?.value||'';
 const list=[...crmActivities].filter(a=>{const cu=crmCustomer(a.customerId),op=crmOpportunity(a.opportunityId),lead=crmLead(a.leadId),st=crmActivityStatus(a);return(!q||[a.subject,a.note,cu?.name,op?.title,lead?.company].join(' ').toLowerCase().includes(q))&&(!type||a.type===type)&&(!status||st===status)}).sort((a,b)=>new Date(a.dueAt)-new Date(b.dueAt));
 crmActivityTimeline.innerHTML=list.length?list.map(a=>{const cu=crmCustomer(a.customerId),op=crmOpportunity(a.opportunityId),lead=crmLead(a.leadId),st=crmActivityStatus(a);return `<div class="crm-timeline-row ${st==='Overdue'?'overdue':''}"><div class="crm-timeline-icon">${crmActivityIcon(a.type)}</div><div class="crm-timeline-main"><div><b>${esc(a.subject)}</b><span class="crm-status ${st.toLowerCase()}">${esc(st)}</span></div><small>${esc(cu?.name||lead?.company||op?.title||'-')} · ${crmDate(a.dueAt)} · ${esc(a.owner||'-')}</small><p>${esc(a.note||'')}</p></div><div class="crm-timeline-actions"><button class="mini-btn" onclick="openCrmActivityModal('${a.id}')">${crmL('แก้ไข','Edit')}</button>${st!=='Done'?`<button class="mini-btn" onclick="completeCrmActivity('${a.id}')">✓ Done</button>`:''}</div></div>`}).join(''):`<div class="empty">${crmL('ยังไม่มีกิจกรรม','No activities')}</div>`;
}
function completeCrmActivity(id){const a=crmActivity(id);if(!a)return;a.status='Done';a.updatedAt=new Date().toISOString();saveLocal();renderCrm();toastMsg('Done')}window.completeCrmActivity=completeCrmActivity;
function renderCrmCustomer360List(){
 if(typeof crmCustomer360List==='undefined')return;const q=(crmCustomerSearch?.value||'').toLowerCase();const list=customers.filter(c=>!q||[c.name,c.contact,c.phone,c.email].join(' ').toLowerCase().includes(q));
 crmCustomer360List.innerHTML=list.map(c=>{const ms=machines.filter(m=>m.customerId===c.id),ops=crmOpportunities.filter(o=>o.customerId===c.id),openOps=ops.filter(o=>!['Won','Lost'].includes(o.stage)),qs=salesQuotes.filter(x=>x.customerId===c.id),cs=cases.filter(x=>x.customerId===c.id),ws=warranties.filter(x=>x.customerId===c.id);return `<button class="crm-customer-card" onclick="openCrmCustomer360('${c.id}')"><div class="crm-customer-card-head"><div><span>${esc((c.name||'?').slice(0,1))}</span><div><b>${esc(c.name)}</b><small>${esc(c.contact||'-')} · ${esc(c.phone||'-')}</small></div></div><strong>${openOps.length} งานขาย</strong></div><div class="crm-customer-stats"><span><b>${ms.length}</b><small>${crmL('เครื่อง','Machines')}</small></span><span><b>${qs.length}</b><small>ใบเสนอราคา</small></span><span><b>${cs.length}</b><small>Service</small></span><span><b>${ws.length}</b><small>Warranty</small></span></div></button>`}).join('');
}
function openCrmCustomer360(cid){const c=crmCustomer(cid);if(!c)return;const ms=machines.filter(m=>m.customerId===c.id),ops=crmOpportunities.filter(o=>o.customerId===c.id),qs=salesQuotes.filter(x=>x.customerId===c.id),cs=cases.filter(x=>x.customerId===c.id),ws=warranties.filter(x=>x.customerId===c.id),docs=machineDocuments.filter(d=>ms.some(m=>m.id===d.machineId)),acts=crmActivities.filter(a=>a.customerId===c.id||ops.some(o=>o.id===a.opportunityId)).sort((a,b)=>new Date(b.dueAt)-new Date(a.dueAt));openModal('ข้อมูลลูกค้ารวม',c.name,`<div class="crm-360-header"><div><h3>${esc(c.name)}</h3><p>${esc(c.contact||'-')} · ${esc(c.phone||'-')} · ${esc(c.email||'-')}</p><small>📍 ${esc(c.address||c.location||'-')}</small></div><div class="crm-360-score"><b>${ops.length}</b><span>งานขาย</span></div></div><div class="crm-360-kpis"><button onclick="closeModal();goPage('machines')"><b>${ms.length}</b><small>${crmL('เครื่องจักร','Machines')}</small></button><button onclick="closeModal();setCrmTab('pipeline')"><b>${ops.length}</b><small>งานขาย</small></button><button onclick="closeModal();setCrmTab('quotations')"><b>${qs.length}</b><small>ใบเสนอราคา</small></button><button onclick="closeModal();goPage('cases')"><b>${cs.length}</b><small>เคส Service</small></button><button onclick="closeModal();goPage('qc')">✅<b>QC เครื่อง</b><small>ตรวจรับ / ก่อนส่งมอบ / หลังซ่อม</small></button><button onclick="closeModal();goPage('warranty')"><b>${ws.length}</b><small>Warranty</small></button><button onclick="closeModal();goPage('documents')"><b>${docs.length}</b><small>เอกสาร</small></button></div><div class="crm-link-actions"><button onclick="openCrmOpportunityModal('','${c.id}')">＋ งานขาย</button><button onclick="openCrmActivityModal('','','');setTimeout(()=>{if(typeof caCustomer!=='undefined')caCustomer.value='${c.id}'},30)">↻ เพิ่มงานติดตาม</button><button onclick="closeModal();openCustomerMap('${c.id}')">🗺 Maps</button><button class="orange" onclick="openSalesQuoteModal('','${c.id}','')">฿ สร้างใบเสนอราคา</button><button onclick="closeModal();goPage('cases');setTimeout(()=>openCaseForCustomer('${c.id}'),70)">📋 Service Case</button></div><div class="crm-360-columns"><div><h4>งานขาย</h4>${ops.length?ops.map(o=>`<button class="crm-360-row" onclick="openCrmOpportunityDetail('${o.id}')"><b>${esc(o.title)}</b><small>${crmStageLabel(o.stage)} · ${crmMoney(o.value)} ${crmL('บาท','THB')}</small></button>`).join(''):'<div class="empty">-</div>'}</div><div><h4>${crmL('กิจกรรม / Timeline','Activity Timeline')}</h4>${acts.length?acts.slice(0,8).map(a=>`<button class="crm-360-row" onclick="openCrmActivityModal('${a.id}')"><b>${crmActivityIcon(a.type)} ${esc(a.subject)}</b><small>${crmDate(a.dueAt)} · ${crmActivityStatus(a)}</small></button>`).join(''):'<div class="empty">-</div>'}</div></div>`,true)}window.openCrmCustomer360=openCrmCustomer360;
function crmRenewalCandidates(){const now=new Date(),future=new Date();future.setDate(future.getDate()+120);const out=[];warranties.forEach(w=>{const cu=crmCustomer(w.customerId),m=machineById(w.machineId);if(!cu||!m||!w.endDate)return;const e=new Date(w.endDate+'T00:00:00');const days=Math.ceil((e-now)/86400000);if(days<=120&&days>=-365)out.push({type:'Warranty',customerId:cu.id,machineId:m.id,title:`${crmL('ต่อประกัน','Warranty Renewal')} · ${machinePrimaryName(m)}`,subtitle:`${m.model||'-'} · S/N ${m.serial||'-'}`,days,endDate:w.endDate,priority:days<=30?'high':days<=90?'medium':'low',suggestedValue:Number(settings.warrantyConfig?.packagePrices?.[12]||0)})});machines.forEach(m=>{const cu=crmCustomer(m.customerId);if(!cu)return;const hasOpp=crmOpportunities.some(o=>o.customerId===cu.id&&o.machineId===m.id&&!['Won','Lost'].includes(o.stage));if(!hasOpp&&m.serviceStatus&&String(m.serviceStatus).includes('ไม่'))out.push({type:'Service',customerId:cu.id,machineId:m.id,title:`${crmL('เสนอ Service Contract','Service Contract Opportunity')} · ${machinePrimaryName(m)}`,subtitle:`${m.model||'-'} · ${crmL('สถานะ','Status')} ${m.serviceStatus}`,days:null,priority:'medium',suggestedValue:m.size==='L'?36000:18000})});return out.sort((a,b)=>(a.days??999)-(b.days??999)).slice(0,40)}
function crmRenewalCard(r,compact=false){const cu=crmCustomer(r.customerId),m=machineById(r.machineId);return `<div class="crm-renewal-card ${r.priority}"><div class="crm-renewal-icon">${r.type==='Warranty'?'🛡':'🔧'}</div><div class="crm-renewal-main"><b>${esc(r.title)}</b><small>${esc(cu?.name||'-')} · ${esc(r.subtitle||'')}</small>${r.days!==null?`<em>${r.days<0?crmL('หมดอายุแล้ว','Expired')+' '+Math.abs(r.days):crmL('เหลือ','Remaining')+' '+r.days} ${crmL('วัน','days')}</em>`:''}</div>${compact?'':`<div class="crm-renewal-actions"><button class="soft-btn" onclick="openCrmCustomer360('${r.customerId}')">360°</button><button class="primary-btn" onclick="createOpportunityFromRenewal('${encodeURIComponent(JSON.stringify(r))}')">＋ งานขาย</button></div>`}</div>`}
function renderCrmRenewals(){if(typeof crmRenewalList==='undefined')return;const list=crmRenewalCandidates();crmRenewalList.innerHTML=list.length?list.map(r=>crmRenewalCard(r)).join(''):`<div class="empty">${crmL('ยังไม่มีรายการ Renewal / Cross-sell','No renewal / cross-sell candidates')}</div>`}
function createOpportunityFromRenewal(encoded){const r=JSON.parse(decodeURIComponent(encoded)),cu=crmCustomer(r.customerId),m=machineById(r.machineId);openCrmOpportunityModal('',r.customerId);setTimeout(()=>{coTitle.value=r.title;coInterest.value=r.type==='Warranty'?'Extended Warranty 12M':'Service Contract';coMachine.value=r.machineId;coValue.value=Number(r.suggestedValue||0);coStage.value='Prospect';coProbability.value=10;coSource.value=r.type==='Warranty'?'Warranty':'Service';coNextAction.value=crmL('ติดต่อลูกค้าและจัดทำข้อเสนอ','Contact customer and prepare proposal')},50)}window.createOpportunityFromRenewal=createOpportunityFromRenewal;

// ==================== V6.1 SALES EASY QUOTATION ====================
function salesQuoteNo(){
 const y=new Date().getFullYear();let max=0;
 salesQuotes.forEach(q=>{const m=String(q.id||'').match(/SQ-\d{4}-(\d+)/);if(m)max=Math.max(max,Number(m[1])||0)});
 return `SQ-${y}-${String(max+1).padStart(4,'0')}`;
}
function salesQuoteCustomer(id){return customers.find(c=>c.id===id)}
function salesQuoteStatusPill(s){const cls=String(s||'ร่าง').replace(/\s+/g,'-');return `<span class="sales-quote-status ${cls}">${esc(s||'ร่าง')}</span>`}
function salesQuoteTotals(q){
 const subtotal=(q.items||[]).reduce((s,it)=>s+(Number(it.qty)||0)*(Number(it.unitPrice)||0),0);
 const discount=subtotal*(Number(q.discountPct)||0)/100;
 const net=Math.max(0,subtotal-discount);
 const vat=q.vatOn?net*((Number(q.vatPct)||7)/100):0;
 return {subtotal,discount,net,vat,grand:net+vat};
}
function salesQuoteMiniCard(q){const cu=salesQuoteCustomer(q.customerId);return `<button class="sales-quote-mini" onclick="previewSalesQuote('${q.id}')"><div><b>${esc(q.id)}</b><small>${esc(cu?.name||q.customerName||'-')} · ${crmDateOnly(q.quoteDate)}</small></div><div><strong>${crmMoney(q.grand)} บาท</strong>${salesQuoteStatusPill(q.status)}</div></button>`}
function renderSalesQuotes(){
 if(typeof crmQuoteTable==='undefined')return;
 const q=(crmQuoteSearch?.value||'').toLowerCase(),st=crmQuoteStatusFilter?.value||'';
 const list=[...salesQuotes].filter(x=>{const cu=salesQuoteCustomer(x.customerId),txt=[x.id,cu?.name,x.customerName,x.contact,(x.items||[]).map(i=>i.description).join(' ')].join(' ').toLowerCase();return(!q||txt.includes(q))&&(!st||x.status===st)}).sort((a,b)=>new Date(b.updatedAt||b.createdAt)-new Date(a.updatedAt||a.createdAt));
 crmQuoteTable.innerHTML=list.length?list.map(x=>{const cu=salesQuoteCustomer(x.customerId),op=crmOpportunity(x.opportunityId);return `<tr><td><b class="ticket">${esc(x.id)}</b>${x.isDemo?'<span class="subtext">TEST DATA</span>':''}</td><td>${crmDateOnly(x.quoteDate)}</td><td>${esc(cu?.name||x.customerName||'-')}</td><td>${esc(op?.title||'-')}</td><td><b>${crmMoney(x.grand)} บาท</b></td><td>${salesQuoteStatusPill(x.status)}</td><td>${esc(x.preparedBy||'-')}</td><td><div class="row-actions"><button class="mini-btn" onclick="previewSalesQuote('${x.id}')">ดู / พิมพ์</button><button class="mini-btn" onclick="openSalesQuoteModal('${x.id}')">แก้ไข</button><button class="mini-btn" onclick="openSalesQuoteStatus('${x.id}')">สถานะ</button></div></td></tr>`}).join(''):`<tr><td colspan="8"><div class="empty">ยังไม่มีใบเสนอราคาฝ่ายขาย — กด “สร้างใบเสนอราคาขาย” เพื่อเริ่ม</div></td></tr>`;
 crmQuoteCards.innerHTML=list.map(x=>`<div class="mobile-data-card"><div class="card-top"><div><h4>${esc(x.id)}</h4><p>${esc(salesQuoteCustomer(x.customerId)?.name||'-')}</p></div>${salesQuoteStatusPill(x.status)}</div><p><b>${crmMoney(x.grand)} บาท</b></p><div class="card-actions"><button class="soft-btn" onclick="previewSalesQuote('${x.id}')">ดู / พิมพ์</button><button class="primary-btn" onclick="openSalesQuoteModal('${x.id}')">แก้ไข</button></div></div>`).join('');
}
function openSalesQuoteModal(id='',presetCustomer='',presetOpp=''){
 if(!requirePermission('quotation.create'))return;
 const old=salesQuotes.find(q=>q.id===id)||{};
 salesQuoteEditingId=old.id||'';
 salesQuoteDraftItems=JSON.parse(JSON.stringify(old.items?.length?old.items:[{description:'',qty:1,unitPrice:0}]));
 const cid=old.customerId||presetCustomer||'',oppid=old.opportunityId||presetOpp||'';
 openModal(old.id?'แก้ไขใบเสนอราคาฝ่ายขาย':'สร้างใบเสนอราคาฝ่ายขาย','ฟอร์มง่ายสำหรับทีมขาย — เลือกลูกค้า ใส่รายการ จำนวน ราคา แล้วบันทึก / พิมพ์',`<form id="salesQuoteForm">
 <div class="sales-quote-form-head">
   <div class="field"><label>ลูกค้า *</label><select id="sqCustomer" required onchange="salesQuoteCustomerChanged()"><option value="">เลือกลูกค้า</option>${customers.map(c=>`<option value="${c.id}" ${cid===c.id?'selected':''}>${esc(c.name)}</option>`).join('')}</select></div>
   <div class="field"><label>งานขายที่เกี่ยวข้อง</label><select id="sqOpportunity"><option value="">ไม่ระบุ</option>${crmOpportunities.map(o=>`<option value="${o.id}" ${oppid===o.id?'selected':''}>${esc(o.id+' · '+o.title)}</option>`).join('')}</select></div>
   <div class="field"><label>ผู้ติดต่อ</label><input id="sqContact" value="${esc(old.contact||'')}"></div>
   <div class="field"><label>วันที่</label><input id="sqDate" type="date" value="${esc(old.quoteDate||new Date().toISOString().slice(0,10))}"></div>
   <div class="field"><label>สถานะ</label><select id="sqStatus">${['ร่าง','ส่งแล้ว','รอลูกค้าตอบ','รอ PO','อนุมัติ','ไม่สำเร็จ'].map(s=>`<option ${old.status===s?'selected':''}>${s}</option>`).join('')}</select></div>
   <div class="field"><label>ผู้จัดทำ</label><input id="sqPreparedBy" value="${esc(old.preparedBy||currentUser?.name||'')}"></div>
 </div>
 <div class="section-title sales-quote-items-title"><span>รายการเสนอขาย</span><button type="button" class="soft-btn" onclick="addSalesQuoteItem()">＋ เพิ่มรายการ</button></div>
 <div id="salesQuoteItems" class="sales-quote-items"></div>
 <div class="sales-quote-bottom">
   <div class="sales-quote-options">
    <div class="field"><label>ส่วนลด %</label><input id="sqDiscount" type="number" min="0" max="100" value="${Number(old.discountPct||0)}" oninput="calcSalesQuoteDraft()"></div>
    <div class="field"><label>VAT</label><select id="sqVat" onchange="calcSalesQuoteDraft()"><option value="yes" ${old.vatOn!==false?'selected':''}>VAT ${(old.vatPct||settings.quoteConfig?.vatPct||7)}%</option><option value="no" ${old.vatOn===false?'selected':''}>ไม่คิด VAT</option></select></div>
    <div class="field full"><label>เงื่อนไข / หมายเหตุ</label><textarea id="sqNote">${esc(old.note||'')}</textarea></div>
   </div>
   <div class="sales-quote-draft-summary">
    <div><span>รวมรายการ</span><b id="sqSubtotal">0</b></div><div><span>ส่วนลด</span><b id="sqDiscountAmt">0</b></div><div><span>VAT</span><b id="sqVatAmt">0</b></div><div class="grand"><span>ยอดรวมสุทธิ</span><b id="sqGrand">0</b></div>
   </div>
 </div>
 <div class="button-row"><button type="button" class="soft-btn" onclick="closeModal()">ยกเลิก</button><button class="primary-btn action-3d-orange">💾 บันทึกใบเสนอราคา</button></div>
 </form>`,true);
 setTimeout(()=>{salesQuoteCustomerChanged();if(oppid&&[...sqOpportunity.options].some(o=>o.value===oppid))sqOpportunity.value=oppid;renderSalesQuoteItems();calcSalesQuoteDraft();salesQuoteForm.onsubmit=e=>{e.preventDefault();saveSalesQuote()}},20)
}
window.openSalesQuoteModal=openSalesQuoteModal;
function salesQuoteCustomerChanged(){
 const c=salesQuoteCustomer(sqCustomer?.value);if(c&&!sqContact.value)sqContact.value=c.contact||'';
 const selected=sqOpportunity?.value||'';
 if(typeof sqOpportunity!=='undefined'){
  const list=crmOpportunities.filter(o=>!sqCustomer.value||o.customerId===sqCustomer.value);
  sqOpportunity.innerHTML='<option value="">ไม่ระบุ</option>'+list.map(o=>`<option value="${o.id}">${esc(o.id+' · '+o.title)}</option>`).join('');
  if(list.some(o=>o.id===selected))sqOpportunity.value=selected;
 }
}
window.salesQuoteCustomerChanged=salesQuoteCustomerChanged;
function renderSalesQuoteItems(){
 if(typeof salesQuoteItems==='undefined')return;
 salesQuoteItems.innerHTML=salesQuoteDraftItems.map((it,i)=>`<div class="sales-quote-item-row"><div class="field item-desc"><label>${i===0?'รายการ / รุ่น / รายละเอียด':''}</label><input value="${esc(it.description||'')}" placeholder="เช่น เครื่อง Laser Welding รุ่น ..." oninput="updateSalesQuoteItem(${i},'description',this.value)"></div><div class="field"><label>${i===0?'จำนวน':''}</label><input type="number" min="0" step="1" value="${Number(it.qty||1)}" oninput="updateSalesQuoteItem(${i},'qty',this.value)"></div><div class="field"><label>${i===0?'ราคาต่อหน่วย':''}</label><input type="number" min="0" step="0.01" value="${Number(it.unitPrice||0)}" oninput="updateSalesQuoteItem(${i},'unitPrice',this.value)"></div><div class="sales-line-amount"><small>${i===0?'รวม':''}</small><b>${crmMoney((Number(it.qty)||0)*(Number(it.unitPrice)||0))}</b></div><button type="button" class="sales-line-remove" onclick="removeSalesQuoteItem(${i})" ${salesQuoteDraftItems.length<=1?'disabled':''}>×</button></div>`).join('')
}
function addSalesQuoteItem(){salesQuoteDraftItems.push({description:'',qty:1,unitPrice:0});renderSalesQuoteItems();calcSalesQuoteDraft()}window.addSalesQuoteItem=addSalesQuoteItem;
function removeSalesQuoteItem(i){if(salesQuoteDraftItems.length<=1)return;salesQuoteDraftItems.splice(i,1);renderSalesQuoteItems();calcSalesQuoteDraft()}window.removeSalesQuoteItem=removeSalesQuoteItem;
function updateSalesQuoteItem(i,k,v){if(!salesQuoteDraftItems[i])return;salesQuoteDraftItems[i][k]=['qty','unitPrice'].includes(k)?Number(v)||0:v;const row=salesQuoteItems.children[i];if(row){const b=row.querySelector('.sales-line-amount b');if(b)b.textContent=crmMoney((Number(salesQuoteDraftItems[i].qty)||0)*(Number(salesQuoteDraftItems[i].unitPrice)||0))}calcSalesQuoteDraft()}window.updateSalesQuoteItem=updateSalesQuoteItem;
function calcSalesQuoteDraft(){
 const tmp={items:salesQuoteDraftItems,discountPct:Number(sqDiscount?.value||0),vatOn:sqVat?.value!=='no',vatPct:Number(settings.quoteConfig?.vatPct||7)};
 const t=salesQuoteTotals(tmp);
 if(typeof sqSubtotal!=='undefined')sqSubtotal.textContent=crmMoney(t.subtotal)+' บาท';if(typeof sqDiscountAmt!=='undefined')sqDiscountAmt.textContent=crmMoney(t.discount)+' บาท';if(typeof sqVatAmt!=='undefined')sqVatAmt.textContent=crmMoney(t.vat)+' บาท';if(typeof sqGrand!=='undefined')sqGrand.textContent=crmMoney(t.grand)+' บาท';return t
}
window.calcSalesQuoteDraft=calcSalesQuoteDraft;
function saveSalesQuote(){
 const c=salesQuoteCustomer(sqCustomer.value);if(!c){toastMsg('กรุณาเลือกลูกค้า');return}
 const clean=salesQuoteDraftItems.filter(i=>String(i.description||'').trim()&&(Number(i.qty)||0)>0);if(!clean.length){toastMsg('กรุณาใส่รายการเสนอขายอย่างน้อย 1 รายการ');return}
 const old=salesQuotes.find(q=>q.id===salesQuoteEditingId),vatPct=Number(settings.quoteConfig?.vatPct||7);
 const rec={id:old?.id||salesQuoteNo(),customerId:c.id,customerName:c.name,opportunityId:sqOpportunity.value,contact:sqContact.value.trim(),quoteDate:sqDate.value,status:sqStatus.value,preparedBy:sqPreparedBy.value.trim(),items:clean,discountPct:Number(sqDiscount.value)||0,vatOn:sqVat.value!=='no',vatPct,note:sqNote.value.trim(),isDemo:false,createdAt:old?.createdAt||new Date().toISOString(),updatedAt:new Date().toISOString()};
 Object.assign(rec,salesQuoteTotals(rec));if(old)salesQuotes=salesQuotes.map(q=>q.id===rec.id?rec:q);else salesQuotes.unshift(rec);
 if(rec.opportunityId){const o=crmOpportunity(rec.opportunityId);if(o){o.quotationId=rec.id;if(['Prospect','Contacted','Needs Analysis','Demo'].includes(o.stage)){o.stage='Quotation';o.probability=CRM_STAGE_PROB.Quotation}o.updatedAt=new Date().toISOString()}}
 saveLocal();closeModal();renderCrm();toastMsg('บันทึก '+rec.id+' แล้ว');setTimeout(()=>previewSalesQuote(rec.id),80)
}
window.saveSalesQuote=saveSalesQuote;
function openSalesQuoteStatus(id){const q=salesQuotes.find(x=>x.id===id);if(!q)return;openModal('สถานะใบเสนอราคา '+q.id,q.customerName||salesQuoteCustomer(q.customerId)?.name||'',`<div class="field"><label>สถานะ</label><select id="salesQuoteStatusSelect">${['ร่าง','ส่งแล้ว','รอลูกค้าตอบ','รอ PO','อนุมัติ','ไม่สำเร็จ'].map(s=>`<option ${s===q.status?'selected':''}>${s}</option>`).join('')}</select></div><div class="button-row"><button class="soft-btn" onclick="closeModal()">ยกเลิก</button><button class="primary-btn" onclick="saveSalesQuoteStatus('${q.id}')">บันทึกสถานะ</button></div>`,true)}window.openSalesQuoteStatus=openSalesQuoteStatus;
function saveSalesQuoteStatus(id){const q=salesQuotes.find(x=>x.id===id);if(!q)return;q.status=salesQuoteStatusSelect.value;q.updatedAt=new Date().toISOString();const o=crmOpportunity(q.opportunityId);if(o){if(q.status==='รอ PO'){o.stage='Waiting PO';o.probability=90}else if(q.status==='อนุมัติ'){o.stage='Won';o.probability=100}else if(q.status==='ไม่สำเร็จ'){o.stage='Lost';o.probability=0}else if(['ส่งแล้ว','รอลูกค้าตอบ'].includes(q.status)&&CRM_STAGES.indexOf(o.stage)<CRM_STAGES.indexOf('Quotation')){o.stage='Quotation';o.probability=60}}saveLocal();closeModal();renderCrm();toastMsg('อัปเดตสถานะแล้ว')}window.saveSalesQuoteStatus=saveSalesQuoteStatus;
function salesQuoteDocHTML(q){
 const c=salesQuoteCustomer(q.customerId),t=salesQuoteTotals(q),logo=document.querySelector('.brand img,.sidebar-logo img,.login-logo img')?.src||'';
 const rows=(q.items||[]).map((it,i)=>`<tr><td>${i+1}</td><td>${esc(it.description)}</td><td class="center">${Number(it.qty||0)}</td><td class="right">${crmMoney(it.unitPrice)}</td><td class="right">${crmMoney((Number(it.qty)||0)*(Number(it.unitPrice)||0))}</td></tr>`).join('');
 return `<div id="salesQuotePrintDoc" class="sales-quote-doc"><div class="sales-quote-doc-head"><div>${logo?`<img src="${logo}">`:''}<b>${esc(settings.companyNameTh||'บริษัท ไอโมด พลัส จำกัด')}</b><small>${esc(settings.companyAddress||'')}</small><small>Tel. ${esc(settings.companyPhone||'')} · ${esc(settings.companyEmail||'')}</small></div><div><h2>QUOTATION</h2><b>${esc(q.id)}</b><small>Date: ${crmDateOnly(q.quoteDate)}</small></div></div><div class="sales-quote-customer"><div><small>ลูกค้า / Company</small><b>${esc(c?.name||q.customerName||'-')}</b></div><div><small>ผู้ติดต่อ / Contact</small><b>${esc(q.contact||c?.contact||'-')}</b></div></div>${q.isDemo?'<div class="sales-demo-warning">TEST DATA — ใบเสนอราคาตัวอย่างสำหรับทดสอบระบบ ไม่ใช่ราคาขายจริง</div>':''}<table class="sales-quote-doc-table"><thead><tr><th>No.</th><th>Description</th><th>Qty</th><th>Unit Price</th><th>Amount</th></tr></thead><tbody>${rows}</tbody></table><div class="sales-quote-doc-bottom"><div class="sales-quote-doc-note"><b>หมายเหตุ / Terms</b><p>${esc(q.note||'-')}</p><small>Prepared by: ${esc(q.preparedBy||'-')}</small></div><div class="sales-quote-doc-total"><div><span>Subtotal</span><b>${crmMoney(t.subtotal)}</b></div>${q.discountPct?`<div><span>Discount ${q.discountPct}%</span><b>- ${crmMoney(t.discount)}</b></div>`:''}<div><span>VAT ${q.vatOn?q.vatPct:0}%</span><b>${crmMoney(t.vat)}</b></div><div class="grand"><span>Grand Total</span><b>${crmMoney(t.grand)} บาท</b></div></div></div></div>`
}
function previewSalesQuote(id){const q=salesQuotes.find(x=>x.id===id);if(!q)return;openModal('ใบเสนอราคา '+q.id,(salesQuoteCustomer(q.customerId)?.name||q.customerName||'')+' · '+q.status,salesQuoteDocHTML(q)+`<div class="button-row" style="margin-top:14px"><button class="soft-btn" onclick="printSalesQuote('${q.id}')">🖨 พิมพ์ / Save PDF</button><button class="soft-btn" onclick="openSalesQuoteModal('${q.id}')">แก้ไข</button><button class="primary-btn" onclick="openSalesQuoteStatus('${q.id}')">เปลี่ยนสถานะ</button></div>`,true)}window.previewSalesQuote=previewSalesQuote;
function printSalesQuote(id){const q=salesQuotes.find(x=>x.id===id);if(!q)return;const doc=salesQuoteDocHTML(q),w=window.open('','_blank','width=1100,height=860');w.document.write(`<!doctype html><html lang="th"><head><meta charset="utf-8"><title>${q.id}</title><style>body{font-family:Arial,Tahoma,sans-serif;padding:20px;color:#1d2738}.sales-quote-doc{max-width:960px;margin:auto;border:1px solid #dbe3ef;background:#fff;padding:22px}.sales-quote-doc-head{display:flex;justify-content:space-between;gap:20px;border-bottom:3px solid #1748a5;padding-bottom:12px}.sales-quote-doc-head>div:first-child{display:grid;gap:3px}.sales-quote-doc-head img{max-width:180px;max-height:70px;object-fit:contain;object-position:left center}.sales-quote-doc-head h2{color:#1748a5;margin:0}.sales-quote-doc-head small{display:block;font-size:11px;color:#5e6b7e}.sales-quote-customer{display:grid;grid-template-columns:1fr 1fr;gap:10px;margin:14px 0}.sales-quote-customer>div{border:1px solid #dfe6ef;padding:10px}.sales-quote-customer small{display:block;color:#68788e}.sales-demo-warning{background:#fff1df;border:1px solid #ffb46a;color:#a5570d;padding:8px;margin:8px 0;font-weight:bold}.sales-quote-doc-table{width:100%;border-collapse:collapse}.sales-quote-doc-table th,.sales-quote-doc-table td{border:1px solid #39465c;padding:8px;font-size:12px}.sales-quote-doc-table th{background:#eef4ff;color:#1748a5}.center{text-align:center}.right{text-align:right}.sales-quote-doc-bottom{display:grid;grid-template-columns:1fr 330px;gap:18px;margin-top:12px}.sales-quote-doc-note{font-size:11px}.sales-quote-doc-total>div{display:flex;justify-content:space-between;border-bottom:1px solid #dfe4ec;padding:6px}.sales-quote-doc-total .grand{font-size:15px;color:#1748a5;font-weight:bold}@media print{body{padding:0}.sales-quote-doc{border:0}}</style></head><body>${doc}<script>window.onload=()=>window.print()<\/script></body></html>`);w.document.close()}window.printSalesQuote=printSalesQuote;



(function serviceFocusCleanupV64(){
 ['imode_test_v60_crm_leads','imode_test_v60_crm_opportunities','imode_test_v60_crm_activities','imode_test_v61_sales_quotes']
 .forEach(k=>{try{localStorage.removeItem(k)}catch(e){}});
})();

// ==================== V5.8 WARRANTY & DOCUMENT CENTER ====================
let pendingWarrantyAttachment=null,pendingMachineDocumentFile=null,warrantyPresetMachine='',documentPresetMachine='';
function dateISO(v){return v?new Date(v+'T00:00:00'):null}
function addMonthsISO(dateStr,months){if(!dateStr)return'';const d=dateISO(dateStr);const original=d.getDate();d.setMonth(d.getMonth()+Number(months||0));if(d.getDate()<original)d.setDate(0);return d.toISOString().slice(0,10)}
function warrantyNo(){const d=new Date(),p=`WTY-${d.getFullYear()}${String(d.getMonth()+1).padStart(2,'0')}-`;let n=1;warranties.forEach(w=>{if(w.warrantyNo?.startsWith(p))n=Math.max(n,(parseInt(w.warrantyNo.split('-').pop())||0)+1)});return p+String(n).padStart(3,'0')}
function warrantyState(w){const now=new Date();now.setHours(0,0,0,0);const s=dateISO(w.startDate),e=dateISO(w.endDate);if(!e)return'none';if(s&&now<s)return'upcoming';if(now>e)return'expired';const days=Math.ceil((e-now)/86400000);return days<=Number(settings.warrantyConfig?.reminderDays||30)?'expiring':'active'}
function warrantyStateLabel(s){return ({active:'อยู่ในประกัน',expiring:'ใกล้หมดอายุ',expired:'หมดประกัน',upcoming:'ยังไม่เริ่ม',none:'ไม่ระบุ'}[s]||s)}
function warrantyDaysLeft(w){const e=dateISO(w.endDate);if(!e)return null;const now=new Date();now.setHours(0,0,0,0);return Math.ceil((e-now)/86400000)}
function latestWarrantyForMachine(mid){return [...warranties].filter(w=>w.machineId===mid).sort((a,b)=>new Date(b.endDate||0)-new Date(a.endDate||0))[0]}
function setWarrantyStatusFilter(v){warrantyStatusFilter.value=v;renderWarranties()}window.setWarrantyStatusFilter=setWarrantyStatusFilter;
function renderWarranties(){
 if(typeof warrantyTable==='undefined')return;
 const q=(warrantySearch.value||'').toLowerCase(),sf=warrantyStatusFilter.value||'all',mf=warrantyMachineFilter.value||'';
 const mv=warrantyMachineFilter.value;warrantyMachineFilter.innerHTML='<option value="">ทุกเครื่อง</option>'+machines.map(m=>`<option value="${m.id}">${esc(machineOptionLabel(m))}</option>`).join('');if(machines.some(m=>m.id===mv))warrantyMachineFilter.value=mv;
 const stats={active:0,expiring:0,expired:0,upcoming:0};warranties.forEach(w=>stats[warrantyState(w)]++);wActive.textContent=stats.active;wExpiring.textContent=stats.expiring;wExpired.textContent=stats.expired;wTotal.textContent=warranties.length;
 const filtered=warranties.filter(w=>{const m=machineById(w.machineId),cu=customerById(w.customerId),state=warrantyState(w);return (!q||[w.warrantyNo,cu?.name,m?.name,m?.model,m?.serial].join(' ').toLowerCase().includes(q))&&(sf==='all'||state===sf)&&(!mf||w.machineId===mf)}).sort((a,b)=>new Date(b.createdAt)-new Date(a.createdAt));
 const info=paginateList('warranty',filtered,[q,sf,mf].join('|')),list=info.list;
 warrantyTable.innerHTML=list.length?list.map(w=>{const m=machineById(w.machineId),cu=customerById(w.customerId),state=warrantyState(w),days=warrantyDaysLeft(w),caseCount=cases.filter(c=>c.machineId===w.machineId&&c.createdAt&&dateISO(String(c.createdAt).slice(0,10))>=dateISO(w.startDate)&&dateISO(String(c.createdAt).slice(0,10))<=dateISO(w.endDate)).length;return `<tr><td><b class="ticket">${esc(w.warrantyNo)}</b></td><td>${esc(cu?.name||'-')}</td><td><div class="warranty-machine">${machineMediaHTML(m)}<div><b>${esc(m?.name||'-')}</b><small>${esc(m?.model||'-')} · S/N ${esc(m?.serial||'-')}</small></div></div></td><td>${fmtDay(w.startDate)}</td><td>${fmtDay(w.endDate)}${days!==null?`<span class="warranty-days">${days>=0?`เหลือ ${days} วัน`:`เกิน ${Math.abs(days)} วัน`}</span>`:''}</td><td><span class="warranty-status ${state}">${warrantyStateLabel(state)}</span></td><td>${caseCount}</td><td><button class="mini-btn" onclick="previewWarrantyCertificate('${w.id}')">ใบรับประกัน</button>${w.attachmentData||w.attachmentUrl?` <button class="mini-btn" onclick="openWarrantyAttachment('${w.id}')">แนบ</button>`:''}</td><td><button class="mini-btn" onclick="openWarrantyDetail('${w.id}')">ดู</button> <button class="mini-btn" onclick="openWarrantyModal('${w.id}')">แก้ไข</button></td></tr>`}).join(''):`<tr><td colspan="9"><div class="empty">ยังไม่มี Warranty ตามเงื่อนไข</div></td></tr>`;
 warrantyCards.innerHTML=list.length?list.map(w=>{const m=machineById(w.machineId),cu=customerById(w.customerId),state=warrantyState(w);return `<div class="mobile-data-card"><div class="card-top"><div><h4>${esc(w.warrantyNo)}</h4><p>${esc(cu?.name||'-')} · ${esc(m?.name||'-')}</p></div><span class="warranty-status ${state}">${warrantyStateLabel(state)}</span></div><p>${esc(m?.model||'-')} · S/N ${esc(m?.serial||'-')}<br>${fmtDay(w.startDate)} → ${fmtDay(w.endDate)}</p><div class="card-actions"><button class="soft-btn" onclick="previewWarrantyCertificate('${w.id}')">ใบรับประกัน</button><button class="primary-btn" onclick="openWarrantyDetail('${w.id}')">รายละเอียด</button></div></div>`}).join(''):'<div class="empty">ยังไม่มี Warranty</div>';
 renderListPager('warranty',info,warranties.length);
}
function refreshWarrantyMachines(){if(typeof wrCustomer==='undefined'||typeof wrMachine==='undefined')return;const cur=wrMachine.value,list=machines.filter(m=>m.customerId===wrCustomer.value);wrMachine.innerHTML='<option value="">เลือกเครื่อง</option>'+list.map(m=>`<option value="${m.id}">${esc(machineOptionLabel(m))} · S/N ${esc(m.serial||'-')}</option>`).join('');if(list.some(m=>m.id===cur))wrMachine.value=cur}window.refreshWarrantyMachines=refreshWarrantyMachines;
function updateWarrantyEnd(){if(typeof wrStart==='undefined')return;wrEnd.value=addMonthsISO(wrStart.value,wrMonths.value)}window.updateWarrantyEnd=updateWarrantyEnd;
function openWarrantyModal(wid='',machinePreset=''){
 const w=warranties.find(x=>x.id===wid)||{},preset=machineById(machinePreset||w.machineId||warrantyPresetMachine),customerId=w.customerId||preset?.customerId||'';pendingWarrantyAttachment=w.attachmentData?{name:w.attachmentName,type:w.attachmentType,data:w.attachmentData}:null;
 openModal(w.id?'แก้ไข Warranty':'ลงทะเบียน Warranty','เชื่อมข้อมูลลูกค้า เครื่อง Serial และสร้างเอกสารรับประกันได้ทันที',`<form id="warrantyForm"><input id="wrId" type="hidden" value="${esc(w.id||'')}"><div class="form-grid cols2"><div class="field"><label>ลูกค้า *</label><select id="wrCustomer" required onchange="refreshWarrantyMachines()"><option value="">เลือกลูกค้า</option>${customers.map(c=>`<option value="${c.id}" ${customerId===c.id?'selected':''}>${esc(c.name)}</option>`).join('')}</select></div><div class="field"><label>เครื่องจักร *</label><select id="wrMachine" required><option value="">เลือกเครื่อง</option>${machines.filter(m=>m.customerId===customerId).map(m=>`<option value="${m.id}" ${(w.machineId||preset?.id)===m.id?'selected':''}>${esc(machineOptionLabel(m))} · S/N ${esc(m.serial||'-')}</option>`).join('')}</select></div><div class="field"><label>วันที่ซื้อ / ส่งมอบ</label><input id="wrPurchase" type="date" value="${esc(w.purchaseDate||'')}"></div><div class="field"><label>วันที่ติดตั้ง</label><input id="wrInstall" type="date" value="${esc(w.installDate||'')}"></div><div class="field"><label>เริ่มประกัน *</label><input id="wrStart" required type="date" onchange="updateWarrantyEnd()" value="${esc(w.startDate||new Date().toISOString().slice(0,10))}"></div><div class="field"><label>ระยะเวลา (เดือน)</label><input id="wrMonths" type="number" min="1" onchange="updateWarrantyEnd()" value="${Number(w.months||settings.warrantyConfig?.defaultMonths||12)}"></div><div class="field"><label>สิ้นสุดประกัน *</label><input id="wrEnd" required type="date" value="${esc(w.endDate||addMonthsISO(new Date().toISOString().slice(0,10),settings.warrantyConfig?.defaultMonths||12))}"></div><div class="field"><label>ผู้จัดทำเอกสาร</label><input id="wrIssuedBy" value="${esc(w.issuedBy||currentUser?.name||'')}"></div><div class="field full"><label>ขอบเขตการรับประกัน</label><textarea id="wrCoverage">${esc(w.coverage||'รับประกันความบกพร่องจากการผลิตและการทำงานของเครื่อง ภายใต้การใช้งานตามคู่มือและเงื่อนไขของบริษัท')}</textarea></div><div class="field full"><label>ข้อยกเว้น / ไม่ครอบคลุม</label><textarea id="wrExclusions">${esc(w.exclusions||'ไม่ครอบคลุมความเสียหายจากการใช้งานผิดวิธี อุบัติเหตุ การดัดแปลงโดยไม่ได้รับอนุญาต และวัสดุสิ้นเปลือง')}</textarea></div><div class="field full"><label>หมายเหตุ</label><textarea id="wrNote">${esc(w.note||'')}</textarea></div></div><div class="doc-upload-box"><b style="font-size:13px">เอกสารแนบ Warranty / Purchase</b><div class="doc-upload-preview" id="warrantyAttachmentInfo">${w.attachmentName?`แนบแล้ว: ${esc(w.attachmentName)}`:'ยังไม่มีไฟล์แนบ'}</div><div class="button-row" style="justify-content:flex-start;margin-top:8px"><label class="soft-btn" style="cursor:pointer">📎 เลือกไฟล์<input type="file" accept="image/*,.pdf" style="display:none" onchange="selectWarrantyAttachment(this)"></label><button type="button" class="soft-btn" onclick="clearWarrantyAttachment()">ลบไฟล์แนบ</button></div><div class="file-warning">Local Prototype จำกัดไฟล์ประมาณ 1.2 MB ต่อไฟล์ • Production แนะนำเก็บใน Google Drive / Supabase Storage</div></div><div class="button-row"><button type="button" class="soft-btn" onclick="closeModal()">ยกเลิก</button><button class="primary-btn">บันทึก Warranty</button></div></form>`);setTimeout(()=>warrantyForm.onsubmit=saveWarranty,20)
}window.openWarrantyModal=openWarrantyModal;
function selectWarrantyAttachment(input){const f=input.files?.[0];if(!f)return;if(f.size>1200000){toastMsg('ไฟล์ใหญ่เกิน 1.2 MB สำหรับ Local Prototype');input.value='';return}const r=new FileReader();r.onload=()=>{pendingWarrantyAttachment={name:f.name,type:f.type,data:r.result};warrantyAttachmentInfo.textContent='เลือกไฟล์: '+f.name};r.readAsDataURL(f)}window.selectWarrantyAttachment=selectWarrantyAttachment;
function clearWarrantyAttachment(){pendingWarrantyAttachment=null;if(typeof warrantyAttachmentInfo!=='undefined')warrantyAttachmentInfo.textContent='ยังไม่มีไฟล์แนบ'}window.clearWarrantyAttachment=clearWarrantyAttachment;
async function saveWarranty(e){e.preventDefault();const old=warranties.find(x=>x.id===wrId.value),m=machineById(wrMachine.value);if(!m){toastMsg('กรุณาเลือกเครื่อง');return}const rec={id:old?.id||uid(),warrantyNo:old?.warrantyNo||warrantyNo(),customerId:wrCustomer.value,machineId:wrMachine.value,purchaseDate:wrPurchase.value,installDate:wrInstall.value,startDate:wrStart.value,endDate:wrEnd.value,months:Number(wrMonths.value)||12,coverage:wrCoverage.value.trim(),exclusions:wrExclusions.value.trim(),note:wrNote.value.trim(),issuedBy:wrIssuedBy.value.trim(),attachmentName:pendingWarrantyAttachment?.name||'',attachmentType:pendingWarrantyAttachment?.type||'',attachmentData:pendingWarrantyAttachment?.data||'',createdAt:old?.createdAt||new Date().toISOString(),updatedAt:new Date().toISOString()};if(old)warranties=warranties.map(x=>x.id===rec.id?rec:x);else warranties.unshift(rec);m.warranty=warrantyState(rec)==='expired'?'หมดประกัน':'อยู่ในประกัน';saveLocal();await cloudUpsertWarranty(rec);closeModal();renderAll();toastMsg('บันทึก '+rec.warrantyNo+' แล้ว')}window.saveWarranty=saveWarranty;
function openWarrantyAttachment(id){const w=warranties.find(x=>x.id===id);if(!w)return;if(w.attachmentData){const a=document.createElement('a');a.href=w.attachmentData;a.target='_blank';a.rel='noopener';a.click()}else if(w.attachmentUrl)window.open(w.attachmentUrl,'_blank')}window.openWarrantyAttachment=openWarrantyAttachment;
function openWarrantyDetail(id){const w=warranties.find(x=>x.id===id);if(!w)return;const m=machineById(w.machineId),cu=customerById(w.customerId),state=warrantyState(w),cs=cases.filter(c=>c.machineId===w.machineId);openModal('Warranty: '+w.warrantyNo,`${cu?.name||'-'} · ${m?.name||'-'}`,`<div class="warranty-detail-head"><div>${machineMediaHTML(m,true)}</div><div class="warranty-detail-title"><h3>${esc(m?.name||'-')}</h3><p>${esc(m?.model||'-')} · S/N ${esc(m?.serial||'-')}</p><span class="warranty-status ${state}">${warrantyStateLabel(state)}</span><div class="warranty-timeline"><div class="datebox"><small>เริ่มประกัน</small><b>${fmtDay(w.startDate)}</b></div><div class="arrow">→</div><div class="datebox"><small>สิ้นสุดประกัน</small><b>${fmtDay(w.endDate)}</b></div></div></div></div><div class="detail-grid"><div class="detail-box"><small>ลูกค้า</small><b>${esc(cu?.name||'-')}</b></div><div class="detail-box"><small>ระยะเวลา</small><b>${w.months} เดือน</b></div><div class="detail-box"><small>วันที่ติดตั้ง</small><b>${fmtDay(w.installDate)}</b></div><div class="detail-box"><small>ผู้จัดทำ</small><b>${esc(w.issuedBy||'-')}</b></div><div class="detail-box full"><small>ขอบเขตการรับประกัน</small><b>${esc(w.coverage||'-')}</b></div><div class="detail-box full"><small>ข้อยกเว้น</small><b>${esc(w.exclusions||'-')}</b></div></div><div class="section-title" style="margin-top:14px">ประวัติ Service ของเครื่อง</div><div class="related-list">${cs.length?cs.slice(0,8).map(c=>`<div class="related-item" onclick="closeModal();openCaseDetail('${c.id}')" style="cursor:pointer"><b>${esc(c.ticket)} · ${esc(c.issue).slice(0,70)}</b><small>${esc(c.status)} · ${fmt(c.createdAt)}</small></div>`).join(''):'<div class="empty">ยังไม่มีประวัติ Service</div>'}</div><div class="button-row" style="margin-top:14px"><button class="soft-btn" onclick="previewWarrantyCertificate('${w.id}')">🖨 ใบรับประกัน</button><button class="soft-btn" onclick="closeModal();openDocsForMachine('${w.machineId}')">📁 เอกสารเครื่อง</button><button class="primary-btn" onclick="openWarrantyModal('${w.id}')">แก้ไข Warranty</button></div>`) }window.openWarrantyDetail=openWarrantyDetail;
function warrantyCertificateHTML(w){const m=machineById(w.machineId),cu=customerById(w.customerId),issued=new Date().toLocaleDateString(uiLocale(),{dateStyle:'long'});return `<div id="warrantyCertificateDoc" class="warranty-cert"><div class="warranty-cert-head"><img src="${IMODE_UI_LOGO}" class="warranty-cert-logo"><div class="warranty-cert-company"><b>${esc(settings.companyNameTh||'บริษัท ไอโมด พลัส จำกัด')}</b><br>${esc(settings.companyName||'I-MODE Plus Co., Ltd.')}<br>${esc(settings.companyAddress||'')}<br>Tel. ${esc(settings.companyPhone||'')} · ${esc(settings.companyEmail||'')}<br>Tax ID: ${esc(settings.companyTaxId||'-')}</div></div><div class="warranty-cert-title"><h2>ใบรับประกันเครื่องจักร</h2><p>MACHINE WARRANTY CERTIFICATE</p></div><div class="warranty-cert-grid"><div class="warranty-cert-box"><small>Warranty No.</small><b>${esc(w.warrantyNo)}</b></div><div class="warranty-cert-box"><small>Customer</small><b>${esc(cu?.name||'-')}</b></div><div class="warranty-cert-box"><small>Machine</small><b>${esc(m?.name||'-')} / ${esc(m?.model||'-')}</b></div><div class="warranty-cert-box"><small>Serial No.</small><b>${esc(m?.serial||'-')}</b></div><div class="warranty-cert-box"><small>Warranty Period</small><b>${fmtDay(w.startDate)} - ${fmtDay(w.endDate)}</b></div><div class="warranty-cert-box"><small>Duration</small><b>${w.months} เดือน</b></div></div><div class="warranty-cert-section"><h4>ขอบเขตการรับประกัน / WARRANTY COVERAGE</h4><div>${esc(w.coverage||'-')}</div></div><div class="warranty-cert-section"><h4>ข้อยกเว้น / EXCLUSIONS</h4><div>${esc(w.exclusions||'-')}</div></div><div class="warranty-cert-section"><h4>หมายเหตุ / REMARK</h4><div>${esc(w.note||'การรับประกันมีผลตามเงื่อนไขของบริษัท และต้องมีหมายเลข Serial No. ตรงกับเครื่องที่ลงทะเบียน')}</div></div><div class="warranty-cert-sign"><div>Customer / ผู้รับเอกสาร<br><br>วันที่ ____________________</div><div>${esc(w.issuedBy||currentUser?.name||'Authorized Person')}<br>Authorized by I-MODE Plus<br>Issued: ${issued}</div></div></div>`}
function previewWarrantyCertificate(id){const w=warranties.find(x=>x.id===id);if(!w)return;openModal('เอกสารการรับประกัน',w.warrantyNo,warrantyCertificateHTML(w)+`<div class="button-row" style="margin-top:14px"><button class="primary-btn" onclick="printWarrantyCertificate()">🖨 พิมพ์ / Save PDF</button></div>`)}window.previewWarrantyCertificate=previewWarrantyCertificate;
function printWarrantyCertificate(){const doc=document.getElementById('warrantyCertificateDoc');if(!doc)return;const w=window.open('','_blank','width=1000,height=820');w.document.write(`<!doctype html><html lang="th"><head><meta charset="utf-8"><title>Warranty Certificate</title><style>body{font-family:Arial,Tahoma,sans-serif;padding:18px;color:#1d2636}.warranty-cert{background:#fff;border:1px solid #cfd5e0;padding:22px;max-width:950px;margin:auto}.warranty-cert-head{display:grid;grid-template-columns:210px 1fr;gap:15px;align-items:center;border-bottom:2px solid #e47a22;padding-bottom:10px}.warranty-cert-logo{width:175px;height:78px;object-fit:contain}.warranty-cert-company{font-size:11px;line-height:1.55}.warranty-cert-company b{font-size:13px}.warranty-cert-title{text-align:center;margin:13px 0}.warranty-cert-title h2{margin:0;font-size:24px;color:#123d9d}.warranty-cert-title p{margin:4px 0 0;font-size:13px;letter-spacing:.08em}.warranty-cert-grid{display:grid;grid-template-columns:1fr 1fr;gap:10px}.warranty-cert-box{border:1px solid #d7dbe4;padding:10px}.warranty-cert-box small{display:block;color:#657089;font-size:10px}.warranty-cert-box b{display:block;margin-top:4px;font-size:13px}.warranty-cert-section{margin-top:12px;border:1px solid #d7dbe4}.warranty-cert-section h4{margin:0;padding:7px 10px;background:#f3f6fb;color:#123d9d;font-size:12px}.warranty-cert-section div{padding:10px;font-size:12px;line-height:1.6;min-height:55px}.warranty-cert-sign{display:grid;grid-template-columns:1fr 1fr;gap:20px;margin-top:55px}.warranty-cert-sign div{text-align:center;padding-top:9px;border-top:1px solid #555;font-size:12px}@media print{body{padding:0}.warranty-cert{border:0}}</style></head><body>${doc.outerHTML}<script>window.onload=()=>window.print()<\/script></body></html>`);w.document.close()}window.printWarrantyCertificate=printWarrantyCertificate;
function openWarrantyForMachine(mid){warrantyPresetMachine=mid;goPage('warranty');setTimeout(()=>{warrantyMachineFilter.value=mid;renderWarranties()},50)}window.openWarrantyForMachine=openWarrantyForMachine;

function docIcon(category,type=''){if(type.includes('pdf'))return'📕';if(type.startsWith('image'))return'🖼';return ({'Operating Manual':'📘','Technical Datasheet':'📄','Electrical Diagram':'⚡','Mechanical Drawing':'📐','PM Checklist':'✅','SOP / Work Instruction':'📋','Software / Firmware':'💾','Calibration / Certificate':'📜','Spare Parts List':'⚙','Training Material':'🎓'}[category]||'📁')}
function renderMachineDocuments(){
 if(typeof machineDocumentTable==='undefined')return;const q=(documentSearch.value||'').toLowerCase(),cat=documentCategoryFilter.value||'',mf=documentMachineFilter.value||'';
 const cv=documentCategoryFilter.value;documentCategoryFilter.innerHTML='<option value="">ทุกหมวดหมู่</option>'+settings.documentCategories.map(c=>`<option>${esc(c)}</option>`).join('');if(settings.documentCategories.includes(cv))documentCategoryFilter.value=cv;
 const mv=documentMachineFilter.value;documentMachineFilter.innerHTML='<option value="">ทุกเครื่อง</option>'+machines.map(m=>`<option value="${m.id}">${esc(machineOptionLabel(m))}</option>`).join('');if(machines.some(m=>m.id===mv))documentMachineFilter.value=mv;
 documentCategorySummary.innerHTML=settings.documentCategories.slice(0,10).map(c=>{const n=machineDocuments.filter(d=>d.category===c).length;return `<button class="doc-category-card" onclick="setDocumentCategory('${esc(c)}')"><span>${docIcon(c)}</span><b>${esc(c)}</b><small>${n} เอกสาร</small></button>`}).join('');
 const filtered=machineDocuments.filter(d=>{const m=machineById(d.machineId);return (!q||[d.title,d.category,d.version,d.language,m?.name,m?.model,m?.serial].join(' ').toLowerCase().includes(q))&&(!cat||d.category===cat)&&(!mf||d.machineId===mf)}).sort((a,b)=>new Date(b.updatedAt||b.createdAt)-new Date(a.updatedAt||a.createdAt));
 const info=paginateList('documents',filtered,[q,cat,mf].join('|')),list=info.list;
 machineDocumentTable.innerHTML=list.length?list.map(d=>{const m=machineById(d.machineId);return `<tr><td><div class="doc-title-cell"><span class="doc-file-icon">${docIcon(d.category,d.fileType||'')}</span><div><b>${esc(d.title)}</b><small>${esc(d.fileName||d.url||'')}</small></div></div></td><td><span class="doc-category-badge">${esc(d.category)}</span></td><td>${esc(m?.name||'เอกสารทั่วไป')}<span class="subtext">${esc(m?.model||'')}</span></td><td>${esc(d.version||'-')}</td><td>${esc(d.language||'-')}</td><td>${fmtDay(d.docDate)}</td><td>${d.fileData||d.url?`<button class="doc-link-btn" onclick="openMachineDocument('${d.id}')">เปิดเอกสาร</button>`:'-'}</td><td><button class="mini-btn" onclick="openMachineDocumentModal('${d.id}')">แก้ไข</button> <button class="mini-btn" onclick="deleteMachineDocument('${d.id}')">ลบ</button></td></tr>`}).join(''):`<tr><td colspan="8"><div class="empty">ยังไม่มีเอกสารตามเงื่อนไข</div></td></tr>`;
 machineDocumentCards.innerHTML=list.length?list.map(d=>{const m=machineById(d.machineId);return `<div class="mobile-data-card"><div class="card-top"><div><h4>${docIcon(d.category,d.fileType||'')} ${esc(d.title)}</h4><p>${esc(d.category)} · ${esc(m?.name||'เอกสารทั่วไป')}</p></div><span class="badge">${esc(d.version||'-')}</span></div><p>${esc(d.language||'-')} · ${fmtDay(d.docDate)}</p><div class="card-actions"><button class="soft-btn" onclick="openMachineDocument('${d.id}')">เปิด</button><button class="primary-btn" onclick="openMachineDocumentModal('${d.id}')">แก้ไข</button></div></div>`}).join(''):'<div class="empty">ยังไม่มีเอกสาร</div>';
 renderListPager('documents',info,machineDocuments.length);
}
function setDocumentCategory(c){documentCategoryFilter.value=c;renderMachineDocuments()}window.setDocumentCategory=setDocumentCategory;
function openMachineDocumentModal(id='',machinePreset=''){
 const d=machineDocuments.find(x=>x.id===id)||{},preset=machineById(machinePreset||d.machineId||documentPresetMachine);pendingMachineDocumentFile=d.fileData?{name:d.fileName,type:d.fileType,data:d.fileData}:null;
 openModal(d.id?'แก้ไขเอกสารเครื่อง':'เพิ่มเอกสารเครื่องจักร','เก็บคู่มือ Drawing SOP Checklist และข้อมูลใช้งานเครื่องในที่เดียว',`<form id="machineDocumentForm"><input id="mdId" type="hidden" value="${esc(d.id||'')}"><div class="form-grid cols2"><div class="field full"><label>ชื่อเอกสาร *</label><input id="mdTitle" required value="${esc(d.title||'')}"></div><div class="field"><label>หมวดหมู่ *</label><select id="mdCategory" required>${settings.documentCategories.map(c=>`<option ${d.category===c?'selected':''}>${esc(c)}</option>`).join('')}</select></div><div class="field"><label>เครื่องจักร</label><select id="mdMachine"><option value="">เอกสารทั่วไป / ทุกเครื่อง</option>${machines.map(m=>`<option value="${m.id}" ${(d.machineId||preset?.id)===m.id?'selected':''}>${esc(machineOptionLabel(m))}</option>`).join('')}</select></div><div class="field"><label>Version / Revision</label><input id="mdVersion" placeholder="Rev.01 / V2.3" value="${esc(d.version||'')}"></div><div class="field"><label>ภาษา</label><select id="mdLanguage"><option ${d.language==='ไทย'?'selected':''}>ไทย</option><option ${d.language==='English'?'selected':''}>English</option><option ${d.language==='ไทย / English'?'selected':''}>ไทย / English</option><option ${d.language==='Other'?'selected':''}>Other</option></select></div><div class="field"><label>วันที่เอกสาร</label><input id="mdDate" type="date" value="${esc(d.docDate||new Date().toISOString().slice(0,10))}"></div><div class="field full"><label>URL / Google Drive Link</label><input id="mdUrl" placeholder="https://drive.google.com/... หรือ URL เอกสาร" value="${esc(d.url||'')}"></div><div class="field full"><label>หมายเหตุ / คำอธิบาย</label><textarea id="mdNote">${esc(d.note||'')}</textarea></div></div><div class="doc-upload-box"><b style="font-size:13px">Upload File (Prototype)</b><div id="machineDocumentFileInfo" class="doc-upload-preview">${d.fileName?`ไฟล์ปัจจุบัน: ${esc(d.fileName)}`:'ยังไม่มีไฟล์ Upload — สามารถใช้ Google Drive Link ด้านบนแทนได้'}</div><div class="button-row" style="justify-content:flex-start;margin-top:8px"><label class="soft-btn" style="cursor:pointer">📎 เลือกไฟล์<input type="file" accept=".pdf,.png,.jpg,.jpeg,.webp,.doc,.docx,.xls,.xlsx,.ppt,.pptx" style="display:none" onchange="selectMachineDocumentFile(this)"></label><button type="button" class="soft-btn" onclick="clearMachineDocumentFile()">ลบไฟล์</button></div><div class="file-warning">Local Prototype จำกัดไฟล์ประมาณ 1.2 MB • สำหรับระบบจริงแนะนำ Google Drive / Supabase Storage และเก็บเพียง URL ในฐานข้อมูล</div></div><div class="button-row"><button type="button" class="soft-btn" onclick="closeModal()">ยกเลิก</button><button class="primary-btn">บันทึกเอกสาร</button></div></form>`);setTimeout(()=>machineDocumentForm.onsubmit=saveMachineDocument,20)
}window.openMachineDocumentModal=openMachineDocumentModal;
function selectMachineDocumentFile(input){const f=input.files?.[0];if(!f)return;if(f.size>1200000){toastMsg('ไฟล์ใหญ่เกิน 1.2 MB สำหรับ Local Prototype');input.value='';return}const r=new FileReader();r.onload=()=>{pendingMachineDocumentFile={name:f.name,type:f.type,data:r.result};machineDocumentFileInfo.textContent='เลือกไฟล์: '+f.name};r.readAsDataURL(f)}window.selectMachineDocumentFile=selectMachineDocumentFile;
function clearMachineDocumentFile(){pendingMachineDocumentFile=null;if(typeof machineDocumentFileInfo!=='undefined')machineDocumentFileInfo.textContent='ยังไม่มีไฟล์ Upload'}window.clearMachineDocumentFile=clearMachineDocumentFile;
async function saveMachineDocument(e){e.preventDefault();const old=machineDocuments.find(x=>x.id===mdId.value),rec={id:old?.id||uid(),title:mdTitle.value.trim(),category:mdCategory.value,machineId:mdMachine.value,customerId:machineById(mdMachine.value)?.customerId||'',version:mdVersion.value.trim(),language:mdLanguage.value,docDate:mdDate.value,url:mdUrl.value.trim(),note:mdNote.value.trim(),fileName:pendingMachineDocumentFile?.name||'',fileType:pendingMachineDocumentFile?.type||'',fileData:pendingMachineDocumentFile?.data||'',createdAt:old?.createdAt||new Date().toISOString(),updatedAt:new Date().toISOString()};if(old)machineDocuments=machineDocuments.map(x=>x.id===rec.id?rec:x);else machineDocuments.unshift(rec);saveLocal();await cloudUpsertMachineDocument(rec);closeModal();renderAll();toastMsg('บันทึกเอกสารแล้ว')}window.saveMachineDocument=saveMachineDocument;
function openMachineDocument(id){const d=machineDocuments.find(x=>x.id===id);if(!d)return;if(d.fileData){const a=document.createElement('a');a.href=d.fileData;a.target='_blank';a.rel='noopener';if(!d.fileType?.includes('pdf')&&!d.fileType?.startsWith('image'))a.download=d.fileName||d.title;a.click()}else if(d.url)window.open(d.url,'_blank','noopener');else toastMsg('เอกสารนี้ยังไม่มีไฟล์หรือ Link')}window.openMachineDocument=openMachineDocument;
function deleteMachineDocument(id){const d=machineDocuments.find(x=>x.id===id);if(!d)return;if(!confirm(settings.language==='en'?`Delete document “${d.title}”?`:`ลบเอกสาร “${d.title}” ?`))return;machineDocuments=machineDocuments.filter(x=>x.id!==id);saveLocal();renderAll();toastMsg('ลบเอกสารแล้ว')}window.deleteMachineDocument=deleteMachineDocument;
function openDocsForMachine(mid){documentPresetMachine=mid;goPage('documents');setTimeout(()=>{documentMachineFilter.value=mid;renderMachineDocuments()},50)}window.openDocsForMachine=openDocsForMachine;


// ==================== V5.9 LINE OA / QR CUSTOMER PORTAL ====================
let portalMachineToken='',portalLineProfile=null,lineRequestQuickFilter='all',fieldTechId='',pendingReportBefore=[],pendingReportAfter=[],reportParts=[];
function portalBaseUrl(){const cfg=settings.lineConfig||{};if(cfg.liffId)return `https://liff.line.me/${cfg.liffId}/`;if(cfg.publicAppUrl)return cfg.publicAppUrl;return location.href.split('#')[0].split('?')[0]}
function machinePortalUrl(mid){const m=machineById(mid);if(!m)return'';const token=m.qrToken||mid,cfg=settings.lineConfig||{};if(cfg.liffId)return `https://liff.line.me/${cfg.liffId}/?machineToken=${encodeURIComponent(token)}`;const base=(cfg.publicAppUrl||location.href.split('#')[0].split('?')[0]);return `${base}${base.includes('?')?'&':'?'}machineToken=${encodeURIComponent(token)}#/customer-portal`}
function machineByQrToken(token){return machines.find(m=>m.qrToken===token||m.id===token)}
function openMachineQR(mid){const m=machineById(mid);if(!m)return;const url=machinePortalUrl(mid);openModal('QR Code ประจำเครื่อง',`${m.name} · ${m.model||'-'} · S/N ${m.serial||'-'}`,`<div class="qr-wrap"><div><div id="machineQRCode" class="qr-box"></div><div class="button-row" style="justify-content:center;margin-top:10px"><button class="soft-btn" onclick="printMachineQR('${m.id}')">🖨 พิมพ์สติกเกอร์</button></div></div><div class="qr-meta"><h3>Customer Service QR</h3><p>ติด QR นี้บนตัวเครื่อง เมื่อลูกค้าสแกนจะเปิดหน้าแจ้งปัญหา เช็ค Warranty ประวัติ Service คู่มือ และขอราคา Warranty ของเครื่องนี้โดยตรง</p><div class="qr-url">${esc(url)}</div><div class="info-box"><b>Production:</b> แนะนำใช้ LIFF URL + Machine QR Token แบบสุ่ม และให้ Backend ตรวจสิทธิ์ก่อนคืนข้อมูลลูกค้า/เครื่อง</div><button class="primary-btn" onclick="openCustomerPortalForMachine('${m.id}')">📱 Preview Customer Portal</button></div></div>`);setTimeout(()=>renderQR('machineQRCode',url),50)}window.openMachineQR=openMachineQR;
function renderQR(el,url){const node=document.getElementById(el);if(!node)return;node.innerHTML='';if(typeof QRCode!=='undefined'){new QRCode(node,{text:url,width:190,height:190,colorDark:'#0b2f8f',colorLight:'#ffffff',correctLevel:QRCode.CorrectLevel.H})}else node.innerHTML=`<div style="font-size:11px;text-align:center;padding:20px">QR Library ยังโหลดไม่สำเร็จ<br>${esc(url)}</div>`}
function printMachineQR(mid){const m=machineById(mid);if(!m)return;const url=machinePortalUrl(mid),w=window.open('','_blank','width=600,height=700');w.document.write(`<!doctype html><html><head><meta charset="utf-8"><script src="https://cdnjs.cloudflare.com/ajax/libs/qrcodejs/1.0.0/qrcode.min.js"><\/script><style>body{font-family:Arial;text-align:center;padding:25px}.label{width:340px;margin:auto;border:2px solid #0b2f8f;border-radius:20px;padding:18px}.logo{width:190px;height:80px;object-fit:contain}.qr{display:flex;justify-content:center;margin:12px}.title{font-size:20px;font-weight:bold;color:#0b2f8f}.small{font-size:12px;line-height:1.5}.serial{margin:8px 0;font-weight:bold}</style></head><body><div class="label"><img class="logo" src="${IMODE_UI_LOGO}"><div class="title">SCAN FOR SERVICE</div><div class="small">แจ้งปัญหา • เช็ค Warranty • ประวัติ Service • คู่มือเครื่อง</div><div id="q" class="qr"></div><b>${esc(m.name)}</b><div>${esc(m.model||'-')}</div><div class="serial">S/N ${esc(m.serial||'-')}</div><div class="small">I-MODE Plus Service & Maintenance</div></div><script>window.onload=()=>{new QRCode(document.getElementById('q'),{text:${JSON.stringify(url)},width:210,height:210});setTimeout(()=>window.print(),500)}<\/script></body></html>`);w.document.close()}window.printMachineQR=printMachineQR;
function openCustomerPortalForMachine(mid){const m=machineById(mid);if(!m)return;portalMachineToken=m.qrToken||m.id;goPage('customer-portal');renderCustomerPortal()}window.openCustomerPortalForMachine=openCustomerPortalForMachine;
function previewCustomerPortal(){const m=machines[0];if(!m){toastMsg('กรุณาเพิ่มเครื่องจักรก่อน');return}openCustomerPortalForMachine(m.id)}window.previewCustomerPortal=previewCustomerPortal;
function exitCustomerPortal(){document.body.classList.remove('customer-portal-mode');goPage('customers')}window.exitCustomerPortal=exitCustomerPortal;
function portalMachine(){return machineByQrToken(portalMachineToken)}
function renderCustomerPortal(){if(typeof portalMachineHero==='undefined')return;const m=portalMachine();if(!m){portalMachineHero.innerHTML='<div class="portal-content"><b>ไม่พบข้อมูลเครื่องจาก QR Code</b><p>กรุณาติดต่อ I-MODE Plus Service</p></div>';return}const cu=customerById(m.customerId),w=latestWarrantyForMachine(m.id),state=w?warrantyState(w):'none';portalLineIdentity.innerHTML=portalLineProfile?`LINE: ${esc(portalLineProfile.displayName||'')} • เชื่อมต่อ Customer Portal แล้ว`:'Customer Portal • สามารถใช้งานผ่าน QR หรือ LIFF';const ownPhoto=String(m.photo||'').trim(),refPhoto=ownPhoto?'':machineFamilyPhoto(m),photoSrc=ownPhoto||refPhoto;const photoOnError="this.onerror=null;this.replaceWith(Object.assign(document.createElement('div'),{className:'portal-machine-noimg',textContent:'No Image'}))";portalMachineHero.innerHTML=`<div class="portal-machine-card">${photoSrc?`<img class="${refPhoto?'is-reference':''}" src="${esc(photoSrc)}" alt="${esc(machinePrimaryName(m)||'Machine')}" loading="lazy" onerror="${photoOnError}">`:`<div class="portal-machine-noimg">No Image</div>`}<div><h3>${esc(m.name)}</h3><p>${esc(m.model||'-')} · S/N ${esc(m.serial||'-')}</p><p>${esc(cu?.name||'-')}</p>${refPhoto?`<p class="portal-machine-refnote">${settings.language==='en'?'Reference image of this model':'ภาพอ้างอิงของรุ่นนี้'}</p>`:''}<span class="warranty-status ${state}">${w?warrantyStateLabel(state):'ยังไม่มี Warranty'}</span></div></div>`;portalContent.innerHTML='<b>เลือกบริการด้านบนได้เลย</b><p style="font-size:11px;color:var(--muted)">ข้อมูลทั้งหมดจะอ้างอิงจาก QR ประจำเครื่องนี้</p>'}
function openPortalIssueForm(){const m=portalMachine(),cu=customerById(m?.customerId);if(!m)return;portalContent.innerHTML=`<form id="portalIssueForm" class="portal-form"><h3>🔧 แจ้งปัญหาเครื่อง</h3><div class="field"><label>ผู้ติดต่อ</label><input id="piContact" required value="${esc(cu?.contact||portalLineProfile?.displayName||'')}"></div><div class="field"><label>โทรศัพท์</label><input id="piPhone" required value="${esc(cu?.phone||'')}"></div><div class="field"><label>ระดับความเร่งด่วน</label><select id="piPriority"><option>ปกติ</option><option>สูง</option><option>ด่วนมาก</option></select></div><div class="field"><label>อาการ / ปัญหา *</label><textarea id="piIssue" required placeholder="อธิบายอาการ Error เสียงผิดปกติ หรือสิ่งที่พบ"></textarea></div><button class="primary-btn" style="width:100%">ส่งแจ้งปัญหา</button></form>`;setTimeout(()=>portalIssueForm.onsubmit=submitPortalIssue,20)}window.openPortalIssueForm=openPortalIssueForm;
async function submitPortalIssue(e){e.preventDefault();const m=portalMachine(),cu=customerById(m.customerId),req={id:uid(),type:'service',status:'ใหม่',customerId:cu?.id||'',machineId:m.id,contact:piContact.value.trim(),phone:piPhone.value.trim(),priority:piPriority.value,message:piIssue.value.trim(),lineUserId:portalLineProfile?.userId||'',lineDisplayName:portalLineProfile?.displayName||'',createdAt:new Date().toISOString()};lineRequests.unshift(req);const c={id:uid(),ticket:genTicket(),createdAt:new Date().toISOString(),updatedAt:new Date().toISOString(),customerId:cu?.id||'',customer:cu?.name||'',contact:req.contact,phone:req.phone,email:cu?.email||'',location:cu?.address||cu?.location||'',branch:cu?.branch||'',mapUrl:cu?.mapUrl||'',latitude:cu?.latitude||'',longitude:cu?.longitude||'',machineId:m.id,machine:machinePrimaryName(m),machineTh:machineNameTh(m),machineEn:machineNameEn(m),model:m.model||'',serial:m.serial||'',machineSize:m.size||'',warranty:m.warranty||'ไม่ทราบ',channel:'LINE OA',serviceType:'เข้าบริการหน้างาน',priority:req.priority,issue:req.message,note:'รับแจ้งจาก Customer Portal / Machine QR',status:'เคสใหม่',serviceTeam:'Technical',assignee:'',appointment:''};cases.unshift(c);req.caseId=c.id;if(cu&&req.lineUserId){cu.lineUserId=req.lineUserId;cu.lineDisplayName=req.lineDisplayName;cu.lineLinkedAt=new Date().toISOString();cloudUpsertCustomer(cu)}saveLocal();await sendPortalBackend(req);await cloudUpsertLineRequest(req);await cloudUpsertCase(c);portalContent.innerHTML=`<div style="text-align:center;padding:15px"><div style="font-size:36px">✅</div><h3>รับแจ้งปัญหาแล้ว</h3><p>เลขเคส <b>${esc(c.ticket)}</b></p><p style="font-size:11px;color:var(--muted)">ทีม Service จะติดต่อกลับเพื่อยืนยันนัดหมาย</p></div>`;renderAll()}window.submitPortalIssue=submitPortalIssue;
function showPortalWarranty(){const m=portalMachine(),w=latestWarrantyForMachine(m?.id);if(!m)return;if(!w){portalContent.innerHTML='<h3>🛡 Warranty</h3><p>ยังไม่พบข้อมูลการรับประกันของเครื่องนี้</p><button class="primary-btn" onclick="openPortalWarrantyRequest()">ขอซื้อ Warranty</button>';return}const state=warrantyState(w),days=warrantyDaysLeft(w);portalContent.innerHTML=`<h3>🛡 Warranty Status</h3><div class="portal-warranty-box"><span class="warranty-status ${state}">${warrantyStateLabel(state)}</span><div class="big">${days!==null?days+' วัน':'-'}</div><small>คงเหลือ</small><div>Warranty No. <b>${esc(w.warrantyNo)}</b></div><div>เริ่ม ${fmtDay(w.startDate)} → สิ้นสุด ${fmtDay(w.endDate)}</div><div style="font-size:11px;color:var(--muted)">${esc(w.coverage||'')}</div></div>`}window.showPortalWarranty=showPortalWarranty;
function openPortalWarrantyRequest(){const m=portalMachine(),cu=customerById(m?.customerId);if(!m)return;portalContent.innerHTML=`<form id="portalWarrantyForm" class="portal-form"><h3>฿ ขอราคา Warranty</h3><div class="field"><label>แพ็กเกจที่สนใจ</label><select id="pwMonths"><option value="6">6 เดือน</option><option value="12" selected>12 เดือน</option><option value="24">24 เดือน</option></select></div><div class="field"><label>ผู้ติดต่อ</label><input id="pwContact" required value="${esc(cu?.contact||portalLineProfile?.displayName||'')}"></div><div class="field"><label>โทรศัพท์</label><input id="pwPhone" required value="${esc(cu?.phone||'')}"></div><div class="field"><label>หมายเหตุ</label><textarea id="pwNote" placeholder="ระบุความต้องการเพิ่มเติม"></textarea></div><button class="primary-btn" style="width:100%">ขอใบเสนอราคา</button></form>`;setTimeout(()=>portalWarrantyForm.onsubmit=submitPortalWarrantyRequest,20)}window.openPortalWarrantyRequest=openPortalWarrantyRequest;
function openPortalServiceQuoteRequest(){const m=portalMachine(),cu=customerById(m.customerId);portalContent.innerHTML=`<form id="portalServiceQuoteForm" onsubmit="submitPortalServiceQuoteRequest(event)"><h3>💰 ขอใบเสนอราคา Service</h3><div class="field"><label>ชื่อลูกค้า / ผู้ติดต่อ</label><input id="psqContact" value="${esc(cu?.contact||portalLineProfile?.displayName||'')}" required></div><div class="field"><label>เบอร์โทร</label><input id="psqPhone" value="${esc(cu?.phone||'')}" required></div><div class="field"><label>ประเภทบริการที่ต้องการ</label><select id="psqServiceMode"><option value="OS">Onsite Service</option><option value="WS">Workshop Service</option><option value="DG">Diagnosis Only</option></select></div><div class="field"><label>รายละเอียดเพิ่มเติม</label><textarea id="psqNote" placeholder="เช่น ต้องการให้ทีมประเมินค่าบริการ, ขอราคาอะไหล่, ต้องการเข้าตรวจเช็กเบื้องต้น"></textarea></div><div class="button-row"><button type="button" class="soft-btn" onclick="renderCustomerPortal()">กลับ</button><button class="primary-btn action-3d-orange">ส่งคำขอ</button></div></form>`}window.openPortalServiceQuoteRequest=openPortalServiceQuoteRequest;
async function submitPortalServiceQuoteRequest(e){e.preventDefault();const m=portalMachine(),cu=customerById(m.customerId),req={id:uid(),type:'service_quote',status:'ใหม่',customerId:cu?.id||'',machineId:m.id,contact:psqContact.value.trim(),phone:psqPhone.value.trim(),serviceMode:psqServiceMode.value,message:psqNote.value.trim(),lineUserId:portalLineProfile?.userId||'',lineDisplayName:portalLineProfile?.displayName||'',createdAt:new Date().toISOString()};lineRequests.unshift(req);saveLocal();await sendPortalBackend(req);await cloudUpsertLineRequest(req);portalContent.innerHTML='<div style="text-align:center;padding:15px"><div style="font-size:36px">🧾</div><h3>ส่งคำขอใบเสนอราคา Service แล้ว</h3><p style="font-size:11px;color:var(--muted)">ทีม I-MODE Plus จะตรวจสอบข้อมูลเครื่องและจัดทำใบเสนอราคาให้</p></div>';renderAll()}window.submitPortalServiceQuoteRequest=submitPortalServiceQuoteRequest;
async function submitPortalWarrantyRequest(e){e.preventDefault();const m=portalMachine(),cu=customerById(m.customerId),req={id:uid(),type:'warranty_quote',status:'ใหม่',customerId:cu?.id||'',machineId:m.id,contact:pwContact.value.trim(),phone:pwPhone.value.trim(),months:Number(pwMonths.value)||12,message:pwNote.value.trim(),lineUserId:portalLineProfile?.userId||'',lineDisplayName:portalLineProfile?.displayName||'',createdAt:new Date().toISOString()};lineRequests.unshift(req);saveLocal();await sendPortalBackend(req);await cloudUpsertLineRequest(req);portalContent.innerHTML='<div style="text-align:center;padding:15px"><div style="font-size:36px">📨</div><h3>ส่งคำขอใบเสนอราคาแล้ว</h3><p style="font-size:11px;color:var(--muted)">ทีม I-MODE Plus จะจัดทำราคา Warranty และติดต่อกลับ</p></div>';renderAll()}window.submitPortalWarrantyRequest=submitPortalWarrantyRequest;
function showPortalHistory(){const m=portalMachine();if(!m)return;const cs=cases.filter(c=>c.machineId===m.id).sort((a,b)=>new Date(b.createdAt)-new Date(a.createdAt));portalContent.innerHTML=`<h3>🕘 ประวัติ Service</h3>${cs.length?cs.map(c=>{const r=serviceReports.find(x=>x.caseId===c.id),parts=r?.parts||[];return `<div class="portal-history-item"><b>${esc(c.ticket)} · ${esc(c.serviceType||'Service')}</b><small>${fmt(c.createdAt)} · ${esc(c.status)}</small><small>${esc(c.issue||'')}</small>${parts.length?`<small>อะไหล่: ${parts.map(p=>esc(p.name)+' x'+(p.qty||1)).join(', ')}</small>`:''}</div>`}).join(''):'<p>ยังไม่มีประวัติ Service</p>'}` }window.showPortalHistory=showPortalHistory;
function showPortalDocuments(){const m=portalMachine();if(!m)return;const docs=machineDocuments.filter(d=>d.machineId===m.id);portalContent.innerHTML=`<h3>📘 คู่มือและเอกสาร</h3>${docs.length?docs.map(d=>`<div class="portal-doc-item"><div><b>${esc(d.title)}</b><small style="display:block;color:var(--muted)">${esc(d.category)} · ${esc(d.version||'-')}</small></div><button class="mini-btn" onclick="openMachineDocument('${d.id}')">เปิด</button></div>`).join(''):'<p>ยังไม่มีเอกสารสำหรับเครื่องนี้</p>'}` }window.showPortalDocuments=showPortalDocuments;
function contactServiceFromPortal(){const tel=(settings.companyPhone||'').replace(/[^0-9+]/g,'');if(settings.lineConfig?.addFriendUrl)window.open(settings.lineConfig.addFriendUrl,'_blank');else if(tel)location.href='tel:'+tel;else portalContent.innerHTML='<p>กรุณาติดต่อ Service ตามข้อมูลบริษัท</p>'}window.contactServiceFromPortal=contactServiceFromPortal;
async function sendPortalBackend(req){const url=settings.lineConfig?.backendEndpoint;if(!url)return false;try{const res=await fetch(url,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action:'customer_request',request:req})});return res.ok}catch(e){return false}}
function filterLineRequests(v){lineRequestQuickFilter=v;if(v==='new'){lineRequestStatusFilter.value='ใหม่';lineRequestTypeFilter.value='all'}else if(v==='warranty_quote'){lineRequestTypeFilter.value='warranty_quote';lineRequestStatusFilter.value='all'}else{lineRequestTypeFilter.value='all';lineRequestStatusFilter.value='all'}renderLineRequests()}window.filterLineRequests=filterLineRequests;
function renderLineRequests(){if(typeof lineRequestTable==='undefined')return;lineLinkedCustomers.textContent=customers.filter(customerLineLinked).length;lineNewRequests.textContent=lineRequests.filter(r=>r.status==='ใหม่').length;lineQrMachines.textContent=machines.filter(m=>m.qrToken).length;lineWarrantyRequests.textContent=lineRequests.filter(r=>(r.type==='warranty_quote'||r.type==='service_quote')&&r.status!=='เสร็จสิ้น').length;const q=(lineRequestSearch.value||'').toLowerCase(),tf=lineRequestTypeFilter.value||'all',sf=lineRequestStatusFilter.value||'all';const list=lineRequests.filter(r=>{const m=machineById(r.machineId),cu=customerById(r.customerId);return(!q||[r.contact,r.phone,r.message,m?.name,m?.serial,cu?.name,r.serviceMode].join(' ').toLowerCase().includes(q))&&(tf==='all'||r.type===tf)&&(sf==='all'||r.status===sf)}).sort((a,b)=>new Date(b.createdAt)-new Date(a.createdAt));const typeLabel=r=>r.type==='service'?'แจ้งปัญหา':r.type==='service_quote'?'ขอราคา Service':r.type==='warranty_quote'?'ขอราคา Warranty':'เช็ค Warranty';lineRequestTable.innerHTML=list.length?list.map(r=>`<tr><td>${fmt(r.createdAt)}</td><td><span class="line-type ${r.type}">${typeLabel(r)}</span></td><td>${esc(customerById(r.customerId)?.name||r.contact||'-')}</td><td>${esc(machineById(r.machineId)?.name||'-')}<span class="subtext">${esc(machineById(r.machineId)?.serial||'')}</span></td><td>${esc(r.message||'-').slice(0,90)}${r.serviceMode?`<span class="subtext">ประเภทบริการ: ${esc(r.serviceMode==='WS'?'Workshop Service':r.serviceMode==='DG'?'Diagnosis Only':'Onsite Service')}</span>`:''}</td><td><span class="line-status ${r.status==='ใหม่'?'new':r.status==='เสร็จสิ้น'?'done':''}">${esc(r.status)}</span></td><td>${r.type==='service'&&r.caseId?`<button class="mini-btn" onclick="openCaseDetail('${r.caseId}')">เปิดเคส</button>`:''}${r.type==='service_quote'?` <button class="mini-btn" onclick="prepareServiceQuoteFromRequest('${r.id}')">ทำใบเสนอราคา</button>`:''}${r.type==='warranty_quote'?` <button class="mini-btn" onclick="prepareWarrantyQuoteFromRequest('${r.id}')">ทำใบเสนอราคา</button>`:''} <button class="mini-btn" onclick="setLineRequestStatus('${r.id}')">สถานะ</button></td></tr>`).join(''):`<tr><td colspan="7"><div class="empty">ยังไม่มีคำขอจาก LINE OA</div></td></tr>`;lineRequestCards.innerHTML=''}
function setLineRequestStatus(id){const r=lineRequests.find(x=>x.id===id);if(!r)return;openModal('สถานะคำขอจาก LINE',r.contact||'',`<div class="field"><label>สถานะ</label><select id="lrStatus"><option ${r.status==='ใหม่'?'selected':''}>ใหม่</option><option ${r.status==='กำลังดำเนินการ'?'selected':''}>กำลังดำเนินการ</option><option ${r.status==='เสร็จสิ้น'?'selected':''}>เสร็จสิ้น</option></select></div><div class="button-row"><button class="primary-btn" onclick="saveLineRequestStatus('${r.id}')">บันทึก</button></div>`,true)}window.setLineRequestStatus=setLineRequestStatus;
function saveLineRequestStatus(id){const r=lineRequests.find(x=>x.id===id);if(!r)return;r.status=lrStatus.value;saveLocal();cloudUpsertLineRequest(r);closeModal();renderLineRequests()}window.saveLineRequestStatus=saveLineRequestStatus;
function prepareServiceQuoteFromRequest(id){const r=lineRequests.find(x=>x.id===id);if(!r)return;const m=machineById(r.machineId),cu=customerById(r.customerId);goPage('quotation');setTimeout(()=>{qCustomer.value=r.customerId||'';quoteCustomerChanged();quoteMachineLines=r.machineId?[r.machineId]:[];qService.value=r.serviceMode||'OS';if(typeof qWarranty!=='undefined')qWarranty.value='none';qContact.value=r.contact||cu?.contact||'';qLocation.value=cu?.address||cu?.location||'';renderQuoteMachineLines();calcQuote();toastMsg('เตรียมใบเสนอราคา Service แล้ว')},80)}window.prepareServiceQuoteFromRequest=prepareServiceQuoteFromRequest;
function prepareWarrantyQuoteFromRequest(id){const r=lineRequests.find(x=>x.id===id);if(!r)return;const m=machineById(r.machineId);goPage('quotation');setTimeout(()=>{qCustomer.value=r.customerId;quoteCustomerChanged();quoteMachineLines=[r.machineId];qService.value='WP';qWarrantyMonths.value=String(r.months||12);qContact.value=r.contact||'';renderQuoteMachineLines();calcQuote();toastMsg('เตรียมใบเสนอราคา Warranty '+(r.months||12)+' เดือนแล้ว')},80)}window.prepareWarrantyQuoteFromRequest=prepareWarrantyQuoteFromRequest;
async function initLiffPortal(){const cfg=settings.lineConfig||{};if(!cfg.liffId||typeof liff==='undefined')return;try{await liff.init({liffId:cfg.liffId});if(!liff.isLoggedIn()&&!liff.isInClient()){liff.login();return}if(liff.isLoggedIn()){const p=await liff.getProfile();portalLineProfile={userId:p.userId,displayName:p.displayName,pictureUrl:p.pictureUrl}}}catch(e){}}
async function initPortalFromUrl(){if(settings.lineConfig?.liffId)await initLiffPortal();const params=new URLSearchParams(location.search);const token=params.get('machineToken');if(token){portalMachineToken=token;goPage('customer-portal');renderCustomerPortal();return}if(location.hash.includes('customer-portal')){portalMachineToken=params.get('machineToken')||machines[0]?.qrToken||'';goPage('customer-portal');renderCustomerPortal()}}

// ==================== V5.9 TECHNICIAN FIELD SERVICE ====================

const FIELD_STATUSES=['กำลังเดินทาง','ถึงหน้างาน','เริ่มตรวจเช็ก','กำลัง PM / Maintenance','กำลังซ่อม Service','รออะไหล่','ทดสอบเครื่อง','รอลูกค้าตรวจรับ','จบงาน'];
function inferWorkType(c){
 const s=String(c?.serviceType||'').toLowerCase();
 if(s.includes('pm')||s.includes('preventive'))return'PM';
 if(s.includes('maintenance'))return'Maintenance';
 return'Service'
}
function checklistTemplate(type='Service'){
 const th={
  PM:['ตรวจสภาพภายนอก โครงสร้าง ฝาครอบ และจุดยึด','ตรวจระบบไฟฟ้า สายไฟ ปลั๊ก ขั้วต่อ และสายดิน','ตรวจอุปกรณ์นิรภัย ปุ่มหยุดฉุกเฉิน อินเตอร์ล็อก และเซนเซอร์นิรภัย','ทำความสะอาดภายใน ภายนอก และจุดสะสมฝุ่นหรือคราบ','ตรวจระบบกลไก การเคลื่อนที่ สายพาน ลูกปืน รางนำ และสกรู','ตรวจและเติมสารหล่อลื่นตามจุดที่กำหนด','ตรวจระบบลม สุญญากาศ ระบบระบายความร้อน ไส้กรอง และพัดลม (ถ้ามี)','ตรวจเซนเซอร์ สวิตช์ ลิมิต เอนโค้ดเดอร์ และสัญญาณผิดปกติ','ตรวจวัสดุสิ้นเปลืองและชิ้นส่วนสึกหรอ พร้อมประเมินอายุการใช้งาน','ทดสอบการทำงาน สอบเทียบ และทดลองเดินเครื่องหลัง PM','ตรวจบันทึกข้อผิดพลาดและสัญญาณเตือน พร้อมบันทึกค่าที่สำคัญ','ติดป้าย PM และกำหนดวัน PM ครั้งถัดไป'],
  Maintenance:['ตรวจสภาพเครื่องและอาการผิดปกติก่อนบำรุงรักษา','ตรวจชิ้นส่วนกลไก แนวศูนย์ ระยะห่าง และจุดยึด','ตรวจระบบไฟฟ้า แหล่งจ่ายไฟ ระบบควบคุม และขั้วต่อ','ตรวจระบบลม สุญญากาศ และระบบระบายความร้อน','ตรวจเซนเซอร์ อุปกรณ์ขับเคลื่อน มอเตอร์ และชุดควบคุม','ทำความสะอาดและหล่อลื่นจุดสำคัญ','ปรับตั้ง แนวศูนย์ และขันยึดตามความจำเป็น','ตรวจชิ้นส่วนสึกหรอและวางแผนรายการที่ควรเปลี่ยน','ตรวจการสอบเทียบ ค่าพารามิเตอร์ และซอฟต์แวร์ (ถ้ามี)','ทดสอบการทำงานและทดสอบภาระหลังบำรุงรักษา','บันทึกคำแนะนำและแผนบำรุงรักษาครั้งถัดไป'],
  Service:['ยืนยันอาการเสียกับลูกค้าและตรวจรับอาการ','ตรวจรหัสข้อผิดพลาด สัญญาณเตือน บันทึกเหตุการณ์ และอาการที่เกิดขึ้น','ตรวจระบบไฟฟ้า แหล่งจ่ายไฟ ขั้วต่อ ฟิวส์ และเบรกเกอร์','ตรวจระบบกลไก การเคลื่อนที่ เซนเซอร์ และอุปกรณ์ขับเคลื่อน','วิเคราะห์สาเหตุของปัญหา','ดำเนินการซ่อมและปรับตั้ง','ตรวจและบันทึกอะไหล่ที่เปลี่ยน หมายเลขอะไหล่ จำนวน และหมายเลขประจำชิ้น','ทดสอบการทำงานหลังซ่อม','ทดสอบร่วมกับลูกค้าและยืนยันว่าแก้ปัญหาแล้ว','แจ้งคำแนะนำและแนวทางป้องกันปัญหาซ้ำ','ส่งมอบงานและบันทึกการรับทราบของลูกค้า']
 };
 const en={
  PM:['Inspect external condition, frame, covers and mounting points','Inspect electrical system, cables, plugs, connectors, terminals and grounding','Inspect safety devices / Emergency Stop / Interlocks / safety sensors','Clean internal and external areas and remove dust / contamination','Inspect mechanical movement / belts / bearings / guides / screws','Inspect and apply lubrication at specified points','Inspect air / vacuum / cooling / filters / fans (if applicable)','Inspect sensors / switches / limits / encoders and abnormal signals','Inspect consumables / wear parts and remaining service life','Function test / calibration / test run after PM','Review error logs / alarms and record key readings','Apply PM label / set next PM date'],
  Maintenance:['Inspect machine condition and abnormalities before maintenance','Inspect mechanical parts / alignment / clearance / fasteners','Inspect electrical / power supply / control / connectors','Inspect pneumatic / air / vacuum / cooling system','Inspect sensors / actuators / motors / drives','Clean and lubricate critical points','Adjust / align / tighten as required','Inspect wear parts and plan replacement items','Calibration / parameters / software check (if applicable)','Function test / load test after maintenance','Record recommendations and next maintenance plan'],
  Service:['Confirm the reported symptom with the customer','Check error codes / alarms / logs / observed symptoms','Inspect power / electrical / connectors / fuses / breakers','Inspect mechanical movement / sensors / actuators','Analyze root cause / diagnosis','Perform repair / adjustment / corrective action','Record replaced parts, Part No. / Qty / S/N','Function test after repair','Test with customer / confirm problem resolved','Provide recommendations to prevent recurrence','Handover and record customer acknowledgement']
 };
 const templates=settings.language==='en'?en:th;
 return (templates[type]||templates.Service).map(label=>({label,result:'OK',note:''}))
}
function reportChecklistForType(type,old=[]){return checklistTemplate(type).map((x,i)=>{const prev=old.find(o=>o.label===x.label)||old[i];return prev?{...x,result:prev.result||'OK',note:prev.note||''}:x})}
function caseContextLinks(c){
 const warranty=warranties.find(w=>w.machineId===c.machineId),docs=machineDocuments.filter(d=>!d.machineId||d.machineId===c.machineId);
 return `<div class="context-link-bar"><button type="button" class="context-link" onclick="closeModal();openCaseDetail('${c.id}')">📋 Service Case</button><button type="button" class="context-link" onclick="closeModal();openCustomerDetail('${c.customerId}')">👥 ลูกค้า</button><button type="button" class="context-link" onclick="closeModal();openMachineDetail('${c.machineId}')">⚙ เครื่องจักร</button><button type="button" class="context-link map" onclick="openMapForCase('${c.id}')">🗺 Google Maps</button><button type="button" class="context-link" onclick="closeModal();goWarrantyForMachine('${c.machineId}')">🛡 Warranty${warranty?' ✓':''}</button><button type="button" class="context-link" onclick="closeModal();goDocumentsForMachine('${c.machineId}')">📁 Documents (${docs.length})</button></div>`
}
let pendingFieldStatusMedia=[];
function fieldStatusLabel(c){return c.fieldStatus||((c.checkInAt)?'ถึงหน้างาน':'ยังไม่อัปเดต')}
function openFieldStatusModal(cid){if(!requirePermission('field.status'))return;
 const c=cases.find(x=>x.id===cid);if(!c)return;pendingFieldStatusMedia=[];
 openModal('อัปเดตสถานะงาน',`${c.ticket} · ${c.customer}`,`${caseContextLinks(c)}<div class="form-grid cols2" style="margin-top:14px"><div class="field"><label>สถานะหน้างาน</label><select id="fieldStatusSelect">${FIELD_STATUSES.map(s=>`<option ${c.fieldStatus===s?'selected':''}>${s}</option>`).join('')}</select></div><div class="field"><label>เวลา</label><input value="${new Date().toLocaleString(uiLocale())}" disabled></div><div class="field full"><label>รายละเอียด / สิ่งที่พบ</label><textarea id="fieldStatusNote" placeholder="เช่น ตรวจพบเสียงผิดปกติบริเวณ Motor..."></textarea></div></div><div class="section-title">หลักฐานสถานะงาน (รูป / วิดีโอ)</div><label class="soft-btn" style="display:inline-block;cursor:pointer">📎 เพิ่มรูปหรือวิดีโอ<input type="file" accept="image/*,video/*" multiple style="display:none" onchange="addFieldStatusMedia(this)"></label><div id="fieldStatusMediaGrid" class="status-media-grid"></div><div class="file-warning">Prototype: รูปจะถูกย่ออัตโนมัติ • วิดีโอแนะนำไม่เกิน 2.5 MB/ไฟล์ และสูงสุด 2 ไฟล์</div><div class="button-row" style="margin-top:14px"><button type="button" class="soft-btn" onclick="closeModal()">ยกเลิก</button><button type="button" class="primary-btn" onclick="saveFieldStatus('${c.id}')">บันทึกสถานะ</button></div>`,true)
}window.openFieldStatusModal=openFieldStatusModal;
function readFileData(file){return new Promise(resolve=>{const r=new FileReader();r.onload=()=>resolve(r.result);r.readAsDataURL(file)})}
async function addFieldStatusMedia(input){
 for(const f of [...input.files]){
   if(f.type.startsWith('image/'))pendingFieldStatusMedia.push({type:'image',name:f.name,data:await compressPhoto(f),size:f.size});
   else if(f.type.startsWith('video/')){
     if(pendingFieldStatusMedia.filter(x=>x.type==='video').length>=2){toastMsg('วิดีโอสูงสุด 2 ไฟล์ต่อสถานะ');continue}
     if(f.size>(settings.mediaConfig?.maxVideoMB||2.5)*1024*1024){toastMsg('วิดีโอ '+f.name+' ใหญ่เกิน 2.5 MB');continue}
     pendingFieldStatusMedia.push({type:'video',name:f.name,data:await readFileData(f),size:f.size})
   }
 }
 renderFieldStatusMedia()
}window.addFieldStatusMedia=addFieldStatusMedia;
function renderFieldStatusMedia(){if(typeof fieldStatusMediaGrid==='undefined')return;fieldStatusMediaGrid.innerHTML=pendingFieldStatusMedia.map((m,i)=>m.type==='image'?`<div class="status-media-item"><img src="${m.data}"><button onclick="removeFieldStatusMedia(${i})">×</button></div>`:`<div class="status-media-item"><video src="${m.data}" controls></video><button onclick="removeFieldStatusMedia(${i})">×</button><small>${esc(m.name)}</small></div>`).join('')}
function removeFieldStatusMedia(i){pendingFieldStatusMedia.splice(i,1);renderFieldStatusMedia()}window.removeFieldStatusMedia=removeFieldStatusMedia;
async function saveFieldStatus(cid){
 const c=cases.find(x=>x.id===cid);if(!c)return;
 const entry={id:uid(),status:fieldStatusSelect.value,note:fieldStatusNote.value.trim(),media:JSON.parse(JSON.stringify(pendingFieldStatusMedia)),createdAt:new Date().toISOString(),techId:c.assignee||fieldTechId};
 c.fieldStatus=entry.status;c.fieldStatusLog=[...(c.fieldStatusLog||[]),entry];c.updatedAt=new Date().toISOString();
 if(entry.status==='ถึงหน้างาน'&&!c.checkInAt)c.checkInAt=entry.createdAt;
 if(entry.status==='รออะไหล่')c.status='รออะไหล่';else if(entry.status==='จบงาน')c.status='รอส่งงาน';else if(!['เสร็จสิ้น','ปิดเคส'].includes(c.status))c.status='กำลังดำเนินการ';
 saveLocal();await cloudUpsertCase(c);closeModal();renderAll();toastMsg('อัปเดตสถานะ: '+entry.status)
}window.saveFieldStatus=saveFieldStatus;

function defaultChecklist(){return checklistTemplate('PM')}
function reportNo(){const d=new Date(),p=`SR-${d.getFullYear()}${String(d.getMonth()+1).padStart(2,'0')}-`;let n=1;serviceReports.forEach(r=>{if(r.reportNo?.startsWith(p))n=Math.max(n,(parseInt(r.reportNo.split('-').pop())||0)+1)});return p+String(n).padStart(3,'0')}
function reportForCase(cid){return serviceReports.find(r=>r.caseId===cid)}
function openFieldService(tid=''){fieldTechId=tid||fieldTechId||technicians.find(t=>t.name===currentUser?.name)?.id||technicians[0]?.id||'';goPage('field-service');renderFieldService()}window.openFieldService=openFieldService;
function renderFieldService(){if(typeof fieldTechSelect==='undefined')return;const cur=fieldTechSelect.value||fieldTechId||technicians[0]?.id||'';fieldTechSelect.innerHTML=technicians.map(t=>`<option value="${t.id}">${esc(t.name)} · ${esc(t.team||'Technical')}</option>`).join('');fieldTechSelect.value=technicians.some(t=>t.id===cur)?cur:(technicians[0]?.id||'');fieldTechId=fieldTechSelect.value;const now=new Date(),today=now.toDateString(),week=new Date(Date.now()+7*86400000);const jobs=cases.filter(c=>c.assignee===fieldTechId&&!['ปิดเคส'].includes(c.status));const scheduled=jobs.filter(c=>c.appointment).sort((a,b)=>new Date(a.appointment)-new Date(b.appointment));fsToday.textContent=scheduled.filter(c=>new Date(c.appointment).toDateString()===today).length;fsUpcoming.textContent=scheduled.filter(c=>new Date(c.appointment)>=now&&new Date(c.appointment)<=week).length;fsOpen.textContent=jobs.length;fsReportPending.textContent=jobs.filter(c=>['รอส่งงาน','เสร็จสิ้น'].includes(c.status)&&!reportForCase(c.id)).length;fieldQueue.innerHTML=scheduled.length?scheduled.slice(0,10).map(fieldJobCard).join(''):'<div class="empty">ยังไม่มีคิวนัดหมาย</div>';fieldJobs.innerHTML=jobs.length?jobs.map(fieldJobCard).join(''):'<div class="empty">ไม่มีงานที่รับผิดชอบ</div>'}
function fieldJobCard(c){
 const cu=customerById(c.customerId),r=reportForCase(c.id),mapReady=!!customerMapDestination(cu);
 return `<div class="field-job-card"><div class="field-job-top"><span class="job-time">${c.appointment?fmt(c.appointment):'ยังไม่นัดหมาย'}</span><span class="field-status-chip">${esc(fieldStatusLabel(c))}</span></div><h4>${esc(c.ticket)} · ${esc(c.customer)}</h4><p>${esc(c.machine)} · ${esc(inferWorkType(c))} · ${esc(c.status)}</p><p>${esc(c.issue).slice(0,90)}</p><p class="field-location">📍 ${esc(cu?.address||cu?.location||c.location||'ยังไม่มี Location')}</p><div class="field-actions"><button class="soft-btn" onclick="openFieldStatusModal('${c.id}')">📡 สถานะงาน</button><button class="soft-btn" onclick="fieldCheckIn('${c.id}')">📍 Check-in</button><button class="soft-btn ${mapReady?'':'muted-btn'}" onclick="openMapForCase('${c.id}')">🗺 นำทาง</button><button class="soft-btn" onclick="callCaseCustomer('${c.id}')">📞 โทร</button><button class="soft-btn" onclick="sendCustomerReminder('${c.id}')">🔔 เตือนลูกค้า</button><button class="primary-btn" onclick="openServiceReport('${c.id}')">${r?'📝 แก้ไขใบตรวจ':'＋ ใบตรวจ / Report'}</button></div></div>`
}
function fieldCheckIn(cid){
 const c=cases.find(x=>x.id===cid);if(!c)return;
 const finalize=(lat='',lng='')=>{c.checkInAt=new Date().toISOString();c.checkInLat=lat;c.checkInLng=lng;c.fieldStatus='ถึงหน้างาน';c.fieldStatusLog=[...(c.fieldStatusLog||[]),{id:uid(),status:'ถึงหน้างาน',note:'Check-in จากอุปกรณ์ทีมช่าง',media:[],createdAt:c.checkInAt,techId:c.assignee||fieldTechId,latitude:lat,longitude:lng}];c.status='กำลังดำเนินการ';c.updatedAt=new Date().toISOString();saveLocal();cloudUpsertCase(c);renderFieldService();toastMsg('Check-in หน้างานแล้ว')};
 if(!navigator.geolocation){finalize();return}
 navigator.geolocation.getCurrentPosition(pos=>finalize(pos.coords.latitude,pos.coords.longitude),()=>{finalize();toastMsg('Check-in แล้ว แต่ไม่ได้พิกัด GPS')})
}window.fieldCheckIn=fieldCheckIn;
function openMapForCase(cid){
 const c=cases.find(x=>x.id===cid);if(!c)return;const cu=customerById(c.customerId);
 const direct=cu?.mapUrl||c.mapUrl;if(direct){window.open(direct,'_blank');return}
 const dest=(cu&&customerMapDestination(cu))||(c.latitude&&c.longitude?`${c.latitude},${c.longitude}`:c.location||c.branch||c.customer);
 if(!dest){toastMsg('ยังไม่มี Location ของลูกค้า');return}
 window.open(`https://www.google.com/maps/dir/?api=1&destination=${encodeURIComponent(dest)}`,'_blank')
}window.openMapForCase=openMapForCase;
function callCaseCustomer(cid){const c=cases.find(x=>x.id===cid);if(!c)return;location.href='tel:'+String(c.phone||'').replace(/[^0-9+]/g,'')}window.callCaseCustomer=callCaseCustomer;
async function sendCustomerReminder(cid){const c=cases.find(x=>x.id===cid),cu=customerById(c?.customerId);if(!c)return;notifications.unshift({id:uid(),icon:'🔔',title:'แจ้งเตือนนัดหมายลูกค้า',message:`${c.ticket} · ${c.customer} · ${fmt(c.appointment)}`,createdAt:new Date().toISOString(),read:false,caseId:c.id});saveLocal();if(settings.lineConfig?.backendEndpoint&&cu?.lineUserId){try{await fetch(settings.lineConfig.backendEndpoint,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action:'appointment_reminder',lineUserId:cu.lineUserId,case:c})})}catch(e){}}renderNotifications();toastMsg(cu?.lineUserId?'ส่งคำขอแจ้งเตือน LINE แล้ว':'บันทึกการแจ้งเตือนแล้ว • ลูกค้ายังไม่เชื่อม LINE')}window.sendCustomerReminder=sendCustomerReminder;
function partRowHTML(p={},i=0){return `<div class="part-row"><input placeholder="Part No." value="${esc(p.partNo||'')}" oninput="reportParts[${i}].partNo=this.value"><input placeholder="ชื่ออะไหล่" value="${esc(p.name||'')}" oninput="reportParts[${i}].name=this.value"><input type="number" min="1" value="${Number(p.qty)||1}" oninput="reportParts[${i}].qty=Number(this.value)||1"><input placeholder="หมายเหตุ / S/N" value="${esc(p.note||'')}" oninput="reportParts[${i}].note=this.value"><button type="button" class="quote-remove" onclick="removeReportPart(${i})">×</button></div>`}
function addReportPart(){reportParts.push({partNo:'',name:'',qty:1,note:''});renderReportParts()}window.addReportPart=addReportPart;function removeReportPart(i){reportParts.splice(i,1);renderReportParts()}window.removeReportPart=removeReportPart;function renderReportParts(){if(typeof reportPartsBox!=='undefined')reportPartsBox.innerHTML=reportParts.length?reportParts.map(partRowHTML).join(''):'<div class="empty">ยังไม่มีอะไหล่ที่เปลี่ยน</div>'}
let pendingReportVideos=[],customerSignatureData='',techSignatureData='';
function inspectionResultLabel(v){const th=settings.language!=='en';const map={OK:th?'ปกติ':'OK',Cleaned:th?'ทำความสะอาดแล้ว':'Cleaned',Adjusted:th?'ปรับตั้งแล้ว':'Adjusted',Replaced:th?'เปลี่ยนแล้ว':'Replaced',NG:th?'ไม่ผ่าน':'NG','N/A':th?'ไม่เกี่ยวข้อง':'N/A'};return map[v]||v}
function renderChecklistEditor(type,existing=[]){const list=reportChecklistForType(type,existing),opts=['OK','Cleaned','Adjusted','Replaced','NG','N/A'];return list.map((x,i)=>`<div class="checklist-row"><b>${i+1}. ${esc(x.label)}</b><select id="src${i}">${opts.map(v=>`<option value="${v}" ${x.result===v?'selected':''}>${esc(inspectionResultLabel(v))}</option>`).join('')}</select><input id="srn${i}" placeholder="หมายเหตุ / ค่าที่ตรวจวัด" value="${esc(x.note||'')}"></div>`).join('')}
function changeReportWorkType(){const type=srWorkType.value,old=reportForCase(srCaseId.value)||{};checklistEditor.innerHTML=renderChecklistEditor(type,old.workType===type?old.checklist:[])}window.changeReportWorkType=changeReportWorkType;
function signaturePadHTML(id,label){return `<div class="signature-box"><div class="signature-title">${label}</div><canvas id="${id}" class="signature-pad" width="560" height="180"></canvas><div class="signature-actions"><span>ใช้นิ้วหรือเมาส์เซ็นในกรอบ</span><button type="button" class="mini-btn" onclick="clearSignaturePad('${id}')">ล้างลายเซ็น</button></div></div>`}
function initSignaturePad(id,initial=''){const canvas=document.getElementById(id);if(!canvas)return;const ctx=canvas.getContext('2d');ctx.lineWidth=3;ctx.lineCap='round';ctx.strokeStyle='#173b8f';if(initial){const im=new Image();im.onload=()=>ctx.drawImage(im,0,0,canvas.width,canvas.height);im.src=initial}let drawing=false;const pos=e=>{const r=canvas.getBoundingClientRect(),p=e;return{x:(p.clientX-r.left)*(canvas.width/r.width),y:(p.clientY-r.top)*(canvas.height/r.height)}};canvas.onpointerdown=e=>{drawing=true;const p=pos(e);ctx.beginPath();ctx.moveTo(p.x,p.y);canvas.setPointerCapture?.(e.pointerId)};canvas.onpointermove=e=>{if(!drawing)return;const p=pos(e);ctx.lineTo(p.x,p.y);ctx.stroke()};canvas.onpointerup=canvas.onpointerleave=()=>drawing=false}
function clearSignaturePad(id){const c=document.getElementById(id);if(c)c.getContext('2d').clearRect(0,0,c.width,c.height)}window.clearSignaturePad=clearSignaturePad;
function signatureData(id){const c=document.getElementById(id);return c?c.toDataURL('image/png'):''}
async function addReportVideos(input){for(const f of [...input.files]){if(pendingReportVideos.length>=(settings.mediaConfig?.maxVideos??2)){toastMsg('วิดีโอสูงสุด 2 ไฟล์ต่อ Report');break}if(f.size>(settings.mediaConfig?.maxVideoMB||2.5)*1024*1024){toastMsg('วิดีโอ '+f.name+' ใหญ่เกิน 2.5 MB');continue}pendingReportVideos.push({name:f.name,type:f.type,size:f.size,data:await readFileData(f)})}renderReportVideos()}window.addReportVideos=addReportVideos;
function renderReportVideos(){if(typeof reportVideoGrid==='undefined')return;reportVideoGrid.innerHTML=pendingReportVideos.map((v,i)=>`<div class="report-video-item"><video src="${v.data}" controls></video><div><small>${esc(v.name)}</small><button type="button" class="mini-btn" onclick="removeReportVideo(${i})">ลบ</button></div></div>`).join('')}
function removeReportVideo(i){pendingReportVideos.splice(i,1);renderReportVideos()}window.removeReportVideo=removeReportVideo;

function openServiceReport(cid){if(!requirePermission('field.report'))return;
 const c=cases.find(x=>x.id===cid);if(!c)return;const old=reportForCase(cid)||{},tech=techById(c.assignee),workType=old.workType||inferWorkType(c),check=reportChecklistForType(workType,old.checklist||[]);
 pendingReportBefore=[...(old.beforePhotos||[])];pendingReportAfter=[...(old.afterPhotos||[])];pendingReportVideos=JSON.parse(JSON.stringify(old.videos||[]));reportParts=JSON.parse(JSON.stringify(old.parts||[]));customerSignatureData=old.customerSignature||'';techSignatureData=old.techSignature||'';
 openModal(old.id?'ใบตรวจ / Service Report '+old.reportNo:'สร้างใบตรวจงานช่าง',`${c.ticket} · ${c.customer} · ${c.machine}`,`<form id="serviceReportForm"><input type="hidden" id="srCaseId" value="${c.id}">${caseContextLinks(c)}<div class="detail-grid" style="margin-top:14px"><div class="detail-box"><small>ประเภทงานใน Case</small><b>${esc(c.serviceType)}</b></div><div class="detail-box"><small>ช่าง</small><b>${esc(tech?.name||'-')}</b></div><div class="detail-box"><small>สถานะหน้างาน</small><b>${esc(fieldStatusLabel(c))}</b></div><div class="detail-box"><small>Check-in</small><b>${fmt(c.checkInAt)}</b></div><div class="detail-box full"><small>Location ลูกค้า</small><b>${esc(customerById(c.customerId)?.address||c.location||'-')}</b></div></div><div class="report-type-strip"><b>ประเภทใบตรวจ</b><select id="srWorkType" onchange="changeReportWorkType()"><option ${workType==='PM'?'selected':''}>PM</option><option ${workType==='Maintenance'?'selected':''}>Maintenance</option><option ${workType==='Service'?'selected':''}>Service</option></select><span>Checklist จะเปลี่ยนตามประเภทงาน</span></div><div class="section-title" style="margin-top:14px">Checklist จุดตรวจเครื่อง</div><div id="checklistEditor" class="checklist-grid">${renderChecklistEditor(workType,check)}</div><div class="section-title" style="margin-top:14px">งานที่ดำเนินการ</div><div class="field"><label>การวิเคราะห์ / ผลการตรวจพบ</label><textarea id="srDiagnosis">${esc(old.diagnosis||'')}</textarea></div><div class="field"><label>งานที่ดำเนินการ</label><textarea id="srWork">${esc(old.workPerformed||'')}</textarea></div><div class="field"><label>คำแนะนำ / งานที่ควรทำต่อ</label><textarea id="srRecommend">${esc(old.recommendation||'')}</textarea></div><div class="section-title">อะไหล่ที่เปลี่ยน</div><div id="reportPartsBox" class="parts-editor"></div><button type="button" class="soft-btn" onclick="addReportPart()">＋ เพิ่มอะไหล่</button><div class="section-title" style="margin-top:14px">รูปก่อนทำงาน</div><label class="soft-btn" style="display:inline-block;cursor:pointer">📷 เพิ่มรูปก่อนทำงาน<input type="file" accept="image/*" multiple style="display:none" onchange="addReportPhotos(this,'before')"></label><div id="beforePhotoGrid" class="report-photo-grid"></div><div class="section-title" style="margin-top:14px">รูปหลังทำงาน</div><label class="soft-btn" style="display:inline-block;cursor:pointer">📷 เพิ่มรูปหลังทำงาน<input type="file" accept="image/*" multiple style="display:none" onchange="addReportPhotos(this,'after')"></label><div id="afterPhotoGrid" class="report-photo-grid"></div><div class="section-title" style="margin-top:14px">วิดีโอประกอบงาน</div><label class="soft-btn" style="display:inline-block;cursor:pointer">🎥 เพิ่มวิดีโอ<input type="file" accept="video/*" multiple style="display:none" onchange="addReportVideos(this)"></label><div id="reportVideoGrid" class="report-video-grid"></div><div class="file-warning">ระบบทดลองจำกัดวิดีโอ 2 ไฟล์ ไฟล์ละไม่เกิน 2.5 MB</div><div class="section-title" style="margin-top:14px">สถานะการทำงานของช่าง</div><div class="status-timeline">${(c.fieldStatusLog||[]).length?(c.fieldStatusLog||[]).map(x=>`<div class="status-timeline-item"><span></span><div><b>${esc(x.status)}</b><small>${fmt(x.createdAt)} · ${esc(techById(x.techId)?.name||'-')}</small><p>${esc(x.note||'')}</p></div></div>`).join(''):'<div class="empty">ยังไม่มี Timeline</div>'}</div><div class="form-grid cols2" style="margin-top:14px"><div class="field"><label>ผู้รับมอบงาน / ลูกค้า</label><input id="srCustomerAccept" value="${esc(old.customerAccept||c.contact||'')}"></div><div class="field"><label>PM ครั้งถัดไป</label><input id="srNextPm" type="date" value="${esc(old.nextPm||'')}"></div></div><div class="signature-grid">${signaturePadHTML('srCustomerSignPad','ลายเซ็นลูกค้า / ผู้รับมอบงาน')}${signaturePadHTML('srTechSignPad','ลายเซ็นทีมช่าง')}</div><div class="button-row"><button type="button" class="soft-btn" onclick="closeModal()">ยกเลิก</button><button type="button" class="soft-btn" onclick="saveServiceReport(false,true)">ดูตัวอย่าง Report</button><button class="primary-btn">บันทึก / จบงาน</button></div></form>`);
 setTimeout(()=>{renderReportParts();renderReportPhotos();renderReportVideos();initSignaturePad('srCustomerSignPad',customerSignatureData);initSignaturePad('srTechSignPad',techSignatureData);serviceReportForm.onsubmit=e=>{e.preventDefault();saveServiceReport(true,false)}},40)
}window.openServiceReport=openServiceReport;

async function compressPhoto(file){return new Promise(resolve=>{const r=new FileReader();r.onload=()=>{const img=new Image();img.onload=()=>{const max=1000,s=Math.min(1,max/Math.max(img.width,img.height)),c=document.createElement('canvas');c.width=Math.round(img.width*s);c.height=Math.round(img.height*s);c.getContext('2d').drawImage(img,0,0,c.width,c.height);resolve(c.toDataURL('image/jpeg',.78))};img.src=r.result};r.readAsDataURL(file)})}
async function addReportPhotos(input,type){const max=settings.mediaConfig?.maxPhotosEach||settings.fieldServiceConfig?.maxPhotosEach||4,arr=type==='before'?pendingReportBefore:pendingReportAfter;for(const f of [...input.files].slice(0,max-arr.length)){arr.push(await compressPhoto(f))}renderReportPhotos()}window.addReportPhotos=addReportPhotos;
function renderReportPhotos(){if(typeof beforePhotoGrid!=='undefined')beforePhotoGrid.innerHTML=pendingReportBefore.map((x,i)=>`<img class="report-photo" src="${x}" onclick="removeReportPhoto('before',${i})">`).join('');if(typeof afterPhotoGrid!=='undefined')afterPhotoGrid.innerHTML=pendingReportAfter.map((x,i)=>`<img class="report-photo" src="${x}" onclick="removeReportPhoto('after',${i})">`).join('')}
function removeReportPhoto(type,i){(type==='before'?pendingReportBefore:pendingReportAfter).splice(i,1);renderReportPhotos()}window.removeReportPhoto=removeReportPhoto;

async function saveServiceReport(doSave=true,preview=false){
 const c=cases.find(x=>x.id===srCaseId.value);if(!c)return;const old=reportForCase(c.id),workType=srWorkType.value,template=checklistTemplate(workType);
 const check=template.map((x,i)=>({label:x.label,result:document.getElementById('src'+i)?.value||'OK',note:document.getElementById('srn'+i)?.value||''}));
 const rec={id:old?.id||uid(),reportNo:old?.reportNo||reportNo(),caseId:c.id,customerId:c.customerId,machineId:c.machineId,techId:c.assignee,serviceType:c.serviceType,workType,checkInAt:c.checkInAt||'',appointment:c.appointment||'',checklist:check,diagnosis:srDiagnosis.value.trim(),workPerformed:srWork.value.trim(),recommendation:srRecommend.value.trim(),parts:JSON.parse(JSON.stringify(reportParts)),beforePhotos:[...pendingReportBefore],afterPhotos:[...pendingReportAfter],videos:JSON.parse(JSON.stringify(pendingReportVideos)),statusLog:JSON.parse(JSON.stringify(c.fieldStatusLog||[])),customerAccept:srCustomerAccept.value.trim(),customerSignature:signatureData('srCustomerSignPad'),techSignature:signatureData('srTechSignPad'),nextPm:srNextPm.value,lineSentAt:old?.lineSentAt||'',lineSendStatus:old?.lineSendStatus||'',status:'Completed',createdAt:old?.createdAt||new Date().toISOString(),updatedAt:new Date().toISOString()};
 if(doSave){if(old)serviceReports=serviceReports.map(x=>x.id===rec.id?rec:x);else serviceReports.unshift(rec);c.fieldStatus='จบงาน';c.fieldStatusLog=[...(c.fieldStatusLog||[]),{id:uid(),status:'จบงาน',note:'บันทึกใบตรวจและลงลายเซ็นจบงาน',media:[],createdAt:new Date().toISOString(),techId:c.assignee||fieldTechId}];c.status='รอส่งงาน';c.updatedAt=new Date().toISOString();saveLocal();await cloudUpsertServiceReport(rec);await cloudUpsertCase(c);closeModal();renderAll();toastMsg('บันทึก '+rec.reportNo+' และจบงานแล้ว')}
 if(preview)previewServiceReportRecord(rec)
}window.saveServiceReport=saveServiceReport;

function serviceReportHTML(r){const c=cases.find(x=>x.id===r.caseId),m=machineById(r.machineId),cu=customerById(r.customerId),t=techById(r.techId),th=settings.language!=='en';const L=(a,b)=>th?a:b;return `<div id="serviceReportPrint" class="report-doc"><div class="report-doc-head"><img src="${IMODE_LOGO}"><div><h2>${th?'ใบตรวจ / รายงานการบริการ':'SERVICE INSPECTION / REPORT'}</h2><div style="text-align:right;font-size:11px">${esc(r.reportNo)}<br>${new Date(r.updatedAt).toLocaleDateString(uiLocale(),{dateStyle:'long'})}</div></div></div><div class="report-doc-meta"><div><b>${L('ลูกค้า:','Customer:')}</b> ${esc(cu?.name||c?.customer||'-')}</div><div><b>${L('ผู้ติดต่อ:','Contact:')}</b> ${esc(c?.contact||'-')}</div><div><b>${L('เครื่องจักร:','Machine:')}</b> ${esc(m?.name||c?.machine||'-')} / ${esc(m?.model||'-')}</div><div><b>${L('หมายเลขเครื่อง:','Serial:')}</b> ${esc(m?.serial||'-')}</div><div><b>${L('ประเภทงาน:','Work Type:')}</b> ${esc(r.workType||'-')}</div><div><b>${L('ประเภทบริการ:','Service Type:')}</b> ${esc(r.serviceType||'-')}</div><div><b>${L('ช่างผู้รับผิดชอบ:','Technician:')}</b> ${esc(t?.name||'-')}</div><div><b>${L('นัดหมาย:','Appointment:')}</b> ${fmt(r.appointment)}</div><div><b>${L('เช็กอิน:','Check-in:')}</b> ${fmt(r.checkInAt)}</div><div><b>${L('สถานที่:','Location:')}</b> ${esc(cu?.address||c?.location||'-')}</div></div><h4>${L('รายการตรวจเช็ก '+esc(r.workType||'Service'),esc(r.workType||'Service')+' Checklist')}</h4><table><thead><tr><th>${L('เลขที่','No.')}</th><th>${L('จุดตรวจ','Inspection Point')}</th><th>${L('ผลตรวจ','Result')}</th><th>${L('หมายเหตุ','Remark')}</th></tr></thead><tbody>${r.checklist.map((x,i)=>`<tr><td>${i+1}</td><td>${esc(x.label)}</td><td>${esc(inspectionResultLabel(x.result))}</td><td>${esc(x.note||'')}</td></tr>`).join('')}</tbody></table><h4>${L('การวิเคราะห์ / ผลการตรวจพบ','Diagnosis / Findings')}</h4><p>${esc(r.diagnosis||'-')}</p><h4>${L('งานที่ดำเนินการ','Work Performed')}</h4><p>${esc(r.workPerformed||'-')}</p><h4>${L('อะไหล่ที่เปลี่ยน','Spare Parts Replaced')}</h4><table><thead><tr><th>${L('หมายเลขอะไหล่','Part No.')}</th><th>${L('รายละเอียด','Description')}</th><th>${L('จำนวน','Qty')}</th><th>${L('หมายเหตุ','Remark')}</th></tr></thead><tbody>${r.parts.length?r.parts.map(p=>`<tr><td>${esc(p.partNo||'')}</td><td>${esc(p.name||'')}</td><td>${p.qty||1}</td><td>${esc(p.note||'')}</td></tr>`).join(''):'<tr><td colspan="4">ไม่มีการเปลี่ยนอะไหล่</td></tr>'}</tbody></table><div class="report-photo-section"><div><h4>${L('รูปก่อนทำงาน','Before')}</h4><div class="report-print-photo-grid">${r.beforePhotos.map(x=>`<img src="${x}">`).join('')||'-'}</div></div><div><h4>${L('รูปหลังทำงาน','After')}</h4><div class="report-print-photo-grid">${r.afterPhotos.map(x=>`<img src="${x}">`).join('')||'-'}</div></div></div>${(r.videos||[]).length?`<h4>${L('วิดีโอประกอบงาน','Video Evidence')}</h4><p>${r.videos.map(v=>esc(v.name)).join(' • ')}</p>`:''}<h4>${L('คำแนะนำ / งานที่ควรทำต่อ','Recommendation')}</h4><p>${esc(r.recommendation||'-')}</p><h4>${L('สถานะงาน / Time line','Work Status / Time line')}</h4><table><thead><tr><th>${L('เวลา','Time')}</th><th>${L('สถานะ','Status')}</th><th>${L('ผู้ดำเนินการ','Operator')}</th><th>${L('หมายเหตุ','Remark')}</th></tr></thead><tbody>${(r.statusLog||[]).length?(r.statusLog||[]).map(x=>`<tr><td>${fmt(x.createdAt)}</td><td>${esc(x.status)}</td><td>${esc(techById(x.techId)?.name||'-')}</td><td>${esc(x.note||'')}</td></tr>`).join(''):'<tr><td colspan="4">-</td></tr>'}</tbody></table><div class="report-sign"><div><b>${L('การยอมรับงานของลูกค้า','Customer Acceptance')}</b>${r.customerSignature?`<img class="report-signature-img" src="${r.customerSignature}">`:'<br><br>'}${esc(r.customerAccept||'-')}<br>${L('วันที่','Date')}: ${new Date(r.updatedAt).toLocaleDateString(uiLocale())}</div><div><b>${L('ช่างผู้ให้บริการ','Service Technician')}</b>${r.techSignature?`<img class="report-signature-img" src="${r.techSignature}">`:'<br><br>'}${esc(t?.name||'-')}<br>I-MODE Plus</div></div></div>`}
function previewServiceReportRecord(r){openModal('ตัวอย่าง Service Report',r.reportNo,serviceReportHTML(r)+`<div class="button-row" style="margin-top:12px"><button class="soft-btn" onclick="sendServiceReportToLine('${r.id}')">💬 ส่ง Report เข้า LINE</button><button class="primary-btn" onclick="printServiceReport()">🖨 Print / Save PDF</button></div>`)}
function previewServiceReport(id){const r=serviceReports.find(x=>x.id===id);if(r)previewServiceReportRecord(r)}window.previewServiceReport=previewServiceReport;
async function sendServiceReportToLine(id){const r=serviceReports.find(x=>x.id===id);if(!r)return;const cu=customerById(r.customerId),m=machineById(r.machineId);if(!cu?.lineUserId){toastMsg('ลูกค้ายังไม่เชื่อม LINE OA / QR');return}if(!settings.lineConfig?.backendEndpoint){toastMsg('ยังไม่ได้ตั้ง LINE Backend Endpoint');return}try{const res=await fetch(settings.lineConfig.backendEndpoint,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action:'send_service_report',lineUserId:cu.lineUserId,report:{id:r.id,reportNo:r.reportNo,workType:r.workType,customer:cu.name,machine:m?.name||'',serial:m?.serial||'',updatedAt:r.updatedAt,status:r.status,portalUrl:portalUrlForMachine(r.machineId),data:r}})});if(!res.ok)throw new Error('LINE backend error');r.lineSentAt=new Date().toISOString();r.lineSendStatus='Sent';saveLocal();await cloudUpsertServiceReport(r);renderServiceReports();toastMsg('ส่ง Service Report เข้า LINE แล้ว')}catch(e){r.lineSendStatus='Failed';saveLocal();toastMsg('ส่ง LINE ไม่สำเร็จ กรุณาตรวจ Backend')}}window.sendServiceReportToLine=sendServiceReportToLine;
function printServiceReport(){const doc=document.getElementById('serviceReportPrint');if(!doc)return;const w=window.open('','_blank','width=1000,height=850');w.document.write(`<!doctype html><html lang="th"><head><meta charset="utf-8"><title>Service Report</title><style>body{font-family:Arial,Tahoma,sans-serif;padding:20px;color:#1c2433}.report-doc{max-width:950px;margin:auto}.report-doc-head{display:grid;grid-template-columns:190px 1fr;gap:15px;border-bottom:2px solid #1748a5;padding-bottom:9px}.report-doc-head img{width:170px;height:75px;object-fit:contain}.report-doc-head h2{text-align:right;color:#102a6b}.report-doc-meta{display:grid;grid-template-columns:1fr 1fr;gap:7px;margin:12px 0}.report-doc-meta div{border:1px solid #ccc;padding:8px;font-size:11px}table{width:100%;border-collapse:collapse}th,td{border:1px solid #bbb;padding:7px;font-size:10px}.report-photo-section{display:grid;grid-template-columns:1fr 1fr;gap:12px}.report-print-photo-grid{display:grid;grid-template-columns:1fr 1fr;gap:6px}.report-print-photo-grid img{width:100%;height:150px;object-fit:cover}.report-sign{display:grid;grid-template-columns:1fr 1fr;gap:12px;margin-top:12px}.report-sign>div{min-height:110px;border:1px solid #bbb;padding:10px;text-align:center;font-size:11px}.report-signature-img{display:block;max-width:220px;max-height:70px;object-fit:contain;margin:6px auto}@media print{body{padding:0}}</style></head><body>${doc.outerHTML}<script>window.onload=()=>window.print()<\/script></body></html>`);w.document.close()}window.printServiceReport=printServiceReport;
function renderServiceReports(){if(typeof serviceReportTable==='undefined')return;const list=[...serviceReports].sort((a,b)=>new Date(b.updatedAt)-new Date(a.updatedAt));serviceReportTable.innerHTML=list.length?list.map(r=>{const c=cases.find(x=>x.id===r.caseId);return `<tr><td><b>${esc(r.reportNo)}</b><span class="subtext">${esc(r.workType||'Service')}</span></td><td>${esc(c?.ticket||'-')}</td><td>${esc(customerById(r.customerId)?.name||'-')}</td><td>${esc(machineById(r.machineId)?.name||'-')}</td><td>${esc(techById(r.techId)?.name||'-')}</td><td>${fmt(r.updatedAt)}</td><td><span class="badge status-เสร็จสิ้น">${esc(r.status)}</span>${r.lineSentAt?`<span class="subtext">LINE ✓ ${fmt(r.lineSentAt)}</span>`:''}</td><td><button class="mini-btn" onclick="previewServiceReport('${r.id}')">PDF / Print</button> <button class="mini-btn" onclick="sendServiceReportToLine('${r.id}')">LINE</button></td></tr>`}).join(''):`<tr><td colspan="8"><div class="empty">ยังไม่มี Service Report</div></td></tr>`;serviceReportCards.innerHTML=''}

// LINE OA Settings
function openLineOASettingModal(){const c=settings.lineConfig||defaults.lineConfig,w=settings.warrantyConfig||defaults.warrantyConfig;openModal('LINE OA Customer Portal','ตั้งค่าเฉพาะข้อมูล Public / ID เท่านั้น ห้ามใส่ Channel Secret หรือ Channel Access Token ในหน้า Web',`<form id="lineOAForm"><div class="info-box">ใช้ LIFF / LINE MINI App เป็นหน้า Customer Portal และใช้ Backend/Webhook สำหรับรับส่งข้อมูลกับ Messaging API อย่างปลอดภัย</div><div class="form-grid cols2"><div class="field"><label>LINE Official Account ID</label><input id="lnOA" placeholder="@youraccount" value="${esc(c.officialAccountId||'')}"></div><div class="field"><label>LIFF ID</label><input id="lnLiff" placeholder="1234567890-AbcdEfgh" value="${esc(c.liffId||'')}"></div><div class="field full"><label>Public App URL / Endpoint URL</label><input id="lnPublic" placeholder="https://service.imode.co.th/" value="${esc(c.publicAppUrl||'')}"></div><div class="field full"><label>Backend API Endpoint</label><input id="lnBackend" placeholder="https://api.imode.co.th/line/customer-request" value="${esc(c.backendEndpoint||'')}"></div><div class="field full"><label>Webhook URL (สำหรับ IT อ้างอิง)</label><input id="lnWebhook" placeholder="https://api.imode.co.th/line/webhook" value="${esc(c.webhookUrl||'')}"></div><div class="field full"><label>Add Friend URL</label><input id="lnFriend" placeholder="https://lin.ee/..." value="${esc(c.addFriendUrl||'')}"></div></div><div class="section-title">ราคา Extended Warranty สำหรับใบเสนอราคา</div><div class="form-grid cols3"><div class="field"><label>6 เดือน</label><input id="wp6" type="number" value="${w.packagePrices?.[6]||0}"></div><div class="field"><label>12 เดือน</label><input id="wp12" type="number" value="${w.packagePrices?.[12]||0}"></div><div class="field"><label>24 เดือน</label><input id="wp24" type="number" value="${w.packagePrices?.[24]||0}"></div></div><div class="button-row"><button type="button" class="soft-btn" onclick="closeModal()">ยกเลิก</button><button class="primary-btn">บันทึก</button></div></form>`,true);setTimeout(()=>lineOAForm.onsubmit=e=>{e.preventDefault();settings.lineConfig={...c,officialAccountId:lnOA.value.trim(),liffId:lnLiff.value.trim(),publicAppUrl:lnPublic.value.trim(),backendEndpoint:lnBackend.value.trim(),webhookUrl:lnWebhook.value.trim(),addFriendUrl:lnFriend.value.trim()};settings.warrantyConfig={...w,packagePrices:{6:+wp6.value||0,12:+wp12.value||0,24:+wp24.value||0}};saveLocal();cloudSaveSettings();closeModal();renderSettings();toastMsg('บันทึก LINE OA Customer Portal แล้ว')},20)}window.openLineOASettingModal=openLineOASettingModal;

function buildNotifications(){
 const out=[],now=new Date(),cfg=settings.notificationConfig||defaults.notificationConfig,lead=cfg.leadHours||settings.notificationLeadHours||24;
 cases.forEach(c=>{
  if(cfg.appointment&&c.appointment&&!['เสร็จสิ้น','ปิดเคส'].includes(c.status)){const diff=(new Date(c.appointment)-now)/3600000;if(diff>=0&&diff<=lead)out.push({key:'auto_visit_'+c.id,icon:'📅',title:'ใกล้ถึงเวลานัดลูกค้า',message:`${c.ticket} · ${c.customer} · ${fmt(c.appointment)}`,createdAt:c.appointment,caseId:c.id,read:false})}
  if(cfg.slaRisk&&c.priority==='ด่วนมาก'&&!['เสร็จสิ้น','ปิดเคส'].includes(c.status))out.push({key:'auto_urgent_'+c.id,icon:'⚠️',title:'เคสด่วนมาก',message:`${c.ticket} · ${c.customer} · ${c.machine}`,createdAt:c.updatedAt,caseId:c.id,read:false});
  if(cfg.partsWaiting&&c.status==='รออะไหล่')out.push({key:'auto_part_'+c.id,icon:'📦',title:'งานรออะไหล่',message:`${c.ticket} · ${c.customer}`,createdAt:c.updatedAt,caseId:c.id,read:false});
  if(cfg.submitWaiting&&c.status==='รอส่งงาน')out.push({key:'auto_submit_'+c.id,icon:'📝',title:'รอส่งงาน / รายงาน',message:`${c.ticket} · ${c.customer}`,createdAt:c.updatedAt,caseId:c.id,read:false})
 });warranties.forEach(w=>{const state=warrantyState(w),m=machineById(w.machineId),cu=customerById(w.customerId),days=warrantyDaysLeft(w);if(state==='expiring')out.push({key:'auto_warranty_'+w.id,icon:'🛡️',title:'Warranty ใกล้หมดอายุ',message:`${w.warrantyNo} · ${cu?.name||'-'} · ${m?.name||'-'} · เหลือ ${days} วัน`,createdAt:new Date().toISOString(),warrantyId:w.id,read:false});});notifications.forEach(n=>out.push({key:'n_'+n.id,icon:n.icon||'ℹ️',...n}));return out.sort((a,b)=>new Date(b.createdAt)-new Date(a.createdAt))
}
function renderNotifications(){const list=buildNotifications();const unread=list.filter(x=>!x.read).length;topNotifyBadge.textContent=unread;sideNotifyBadge.textContent=unread;notificationList.innerHTML=list.length?list.map(n=>`<div class="notification-row ${n.read?'':'unread'}" onclick="openNotificationDetail('${n.key}')" style="cursor:pointer"><span class="stack-icon">${n.icon}</span><div><h4>${esc(n.title)}</h4><p>${esc(n.message)}</p></div><small>${fmt(n.createdAt)}</small></div>`).join(''):'<div class="empty">ไม่มีการแจ้งเตือน</div>'}
function renderReports(){const open=openCases();rStatus.textContent=cases.length;rTech.textContent=open.filter(c=>c.assignee).length;rCustomer.textContent=new Set(cases.map(c=>c.customerId).filter(Boolean)).size;rMachine.textContent=new Set(cases.map(c=>c.machineId).filter(Boolean)).size;const done=cases.filter(c=>['เสร็จสิ้น','ปิดเคส'].includes(c.status)).length;const urgent=open.filter(c=>c.priority==='ด่วนมาก').length;const waiting=cases.filter(c=>c.status==='รออะไหล่').length;const submit=cases.filter(c=>c.status==='รอส่งงาน').length;reportSummary.innerHTML=`<div class="summary-box"><small>เคสทั้งหมด</small><b>${cases.length}</b></div><div class="summary-box"><small>ปิดแล้ว</small><b>${done}</b></div><div class="summary-box"><small>ด่วนมาก</small><b>${urgent}</b></div><div class="summary-box"><small>รออะไหล่ / ส่งงาน</small><b>${waiting+submit}</b></div>`}

function modalHasDataEntry(){return !!modalBody.querySelector('form,input:not([type="hidden"]),select,textarea,[contenteditable="true"],canvas')}
function openModal(title,sub,body,small=false){
 modalTitle.textContent=title;modalSub.textContent=sub||'';modalBody.innerHTML=body;modalPanel.className='modal-panel'+(small?' small':'');
 modal.classList.add('open');setTimeout(()=>applyLanguageTo(modal),0)
}
function closeModal(){modal.classList.remove('open')}window.closeModal=closeModal;
function openMetricPopup(type){let title='รายการเคส',list=cases;if(type==='open'){title='เคสที่เปิดอยู่';list=openCases()}if(type==='urgent'){title='เคสด่วนมาก';list=openCases().filter(c=>c.priority==='ด่วนมาก')}if(type==='assigned'){title='งานที่มอบหมาย';list=openCases().filter(c=>c.assignee)}if(type==='done'){title='งานที่ปิดแล้ว';list=cases.filter(c=>['เสร็จสิ้น','ปิดเคส'].includes(c.status))}openModal(title,`${list.length} รายการ`,`<div class="related-list">${list.length?list.map(c=>`<div class="related-item" onclick="closeModal();openCaseDetail('${c.id}')" style="cursor:pointer"><b>${esc(c.ticket)} · ${esc(c.customer)}</b><small>${esc(c.machine)} · ${esc(c.status)} · ${esc(c.priority)}</small></div>`).join(''):'<div class="empty">ไม่มีรายการ</div>'}</div>`,true)}window.openMetricPopup=openMetricPopup;

function caseMachineSearchLabel(m){return `${machinePrimaryName(m)||'-'}${m.model?` · ${m.model}`:''}${m.serial?` · S/N ${m.serial}`:''}`}
function caseForm(c={}){
 const selectedCustomer=customerById(c.customerId),selectedMachine=machineById(c.machineId);
 const customerOpts=customers.map(x=>`<option value="${esc(x.name)}"></option>`).join('');
 const machineList=machines.filter(m=>!c.customerId||m.customerId===c.customerId),machineOpts=machineList.map(m=>`<option value="${esc(caseMachineSearchLabel(m))}"></option>`).join('');
 return `<form id="caseForm"><input type="hidden" id="fCaseId" value="${esc(c.id||'')}"><input type="hidden" id="fCustomer" value="${esc(c.customerId||'')}"><input type="hidden" id="fMachine" value="${esc(c.machineId||'')}">
 <div class="modal-section"><div class="section-title">1. ลูกค้าและเครื่องจักร</div><div class="form-grid cols2"><div class="field searchable-field"><label>ลูกค้า *</label><input id="fCustomerSearch" list="caseCustomerList" autocomplete="off" required value="${esc(selectedCustomer?.name||'')}" placeholder="พิมพ์ชื่อลูกค้าเพื่อค้นหา..." oninput="caseCustomerSearchChanged()" onblur="caseCustomerSearchChanged(true)"><datalist id="caseCustomerList">${customerOpts}</datalist></div><div class="field searchable-field"><label>เครื่องจักร *</label><input id="fMachineSearch" list="caseMachineList" autocomplete="off" required value="${esc(selectedMachine?caseMachineSearchLabel(selectedMachine):'')}" placeholder="พิมพ์ชื่อเครื่อง / Model / Serial..." oninput="caseMachineSearchChanged()" onblur="caseMachineSearchChanged(true)"><datalist id="caseMachineList">${machineOpts}</datalist></div><div class="field"><label>ผู้ติดต่อ</label><input id="fContact" value="${esc(c.contact||'')}"></div><div class="field"><label>โทรศัพท์</label><input id="fPhone" value="${esc(c.phone||'')}"></div></div><div class="button-row"><button type="button" class="soft-btn" onclick="openCustomerModal('',true)">＋ เพิ่มลูกค้า</button><button type="button" class="soft-btn" onclick="openMachineModal('',true)">＋ เพิ่มเครื่อง</button></div></div>
 <div class="modal-section"><div class="section-title">2. รายละเอียดเคส</div><div class="form-grid cols3"><div class="field"><label>ช่องทางรับแจ้ง</label><select id="fChannel"><option ${c.channel==='LINE OA'?'selected':''}>LINE OA</option><option ${c.channel==='โทรศัพท์'?'selected':''}>โทรศัพท์</option><option ${c.channel==='อีเมล'?'selected':''}>อีเมล</option><option ${c.channel==='ฝ่ายขาย'?'selected':''}>ฝ่ายขาย</option></select></div><div class="field"><label>ประเภทงาน</label><select id="fServiceType">${settings.serviceTypes.map(x=>`<option ${c.serviceType===x?'selected':''}>${esc(x)}</option>`).join('')}</select></div><div class="field"><label>Priority</label><select id="fPriority">${settings.priorities.map(x=>`<option ${c.priority===x?'selected':''}>${esc(x)}</option>`).join('')}</select></div><div class="field full"><label>อาการ / รายละเอียด *</label><textarea id="fIssue" required>${esc(c.issue||'')}</textarea></div><div class="field full"><label>หมายเหตุภายใน</label><textarea id="fNote">${esc(c.note||'')}</textarea></div></div></div>
 <div class="modal-section"><div class="section-title">3. มอบหมาย / นัดหมาย</div><div class="form-grid cols4"><div class="field"><label>สถานะ</label><select id="fStatus">${settings.statuses.map(x=>`<option ${c.status===x?'selected':''}>${esc(x)}</option>`).join('')}</select></div><div class="field"><label>ทีมรับผิดชอบ</label><select id="fServiceTeam" onchange="refreshCaseAssignee()"><option value="Technical" ${(c.serviceTeam||techById(c.assignee)?.team||'Technical')==='Technical'?'selected':''}>ทีม Technical</option><option value="R&D" ${(c.serviceTeam||techById(c.assignee)?.team||'Technical')==='R&D'?'selected':''}>ทีม R&D</option></select></div><div class="field"><label>ช่างผู้รับผิดชอบ</label><select id="fAssignee"><option value="">ยังไม่มอบหมาย</option>${filteredTechOptions(c.serviceTeam||techById(c.assignee)?.team||'Technical').map(t=>`<option value="${t.id}" ${c.assignee===t.id?'selected':''}>${esc(t.name)} · ${esc(t.team||'Technical')}</option>`).join('')}</select></div><div class="field"><label>วันและเวลานัดหมาย</label>${datetimeSplitHTML('fAppointment',c.appointment||'',false)}</div></div></div>
 <div class="button-row"><button type="button" class="soft-btn" onclick="closeModal()">ยกเลิก</button><button class="primary-btn">บันทึกเคส</button></div></form>`
}
function openCaseModal(cid=''){const c=cases.find(x=>x.id===cid)||{};openModal(c.id?'แก้ไข '+c.ticket:'รับเคสบริการใหม่','เชื่อมลูกค้า → เครื่อง → ช่าง → ปฏิทิน → แจ้งเตือน',caseForm(c));setTimeout(()=>caseFormEl=document.getElementById('caseForm'),0);setTimeout(()=>document.getElementById('caseForm').onsubmit=saveCase,20)}window.openCaseModal=openCaseModal;
function resolveCustomerSearchValue(v=''){const q=String(v).trim().toLowerCase();if(!q)return null;const exact=customers.find(x=>String(x.name||'').trim().toLowerCase()===q);if(exact)return exact;const matches=customers.filter(x=>String(x.name||'').toLowerCase().includes(q));return matches.length===1?matches[0]:null}
function caseCustomerSearchChanged(force=false){const cu=resolveCustomerSearchValue(fCustomerSearch.value);fCustomer.value=cu?.id||'';if(cu){if(!fContact.value||force)fContact.value=cu.contact||'';if(!fPhone.value||force)fPhone.value=cu.phone||''}refreshMachines();if(!cu&&force&&fCustomerSearch.value)toastMsg('กรุณาเลือกลูกค้าจากรายการค้นหา')}window.caseCustomerSearchChanged=caseCustomerSearchChanged;
function refreshMachines(){const cu=fCustomer.value,cur=fMachine.value,list=machines.filter(m=>m.customerId===cu);caseMachineList.innerHTML=list.map(m=>`<option value="${esc(caseMachineSearchLabel(m))}"></option>`).join('');if(!list.some(m=>m.id===cur)){fMachine.value='';if(typeof fMachineSearch!=='undefined'&&!list.some(m=>caseMachineSearchLabel(m)===fMachineSearch.value))fMachineSearch.value=''}}window.refreshMachines=refreshMachines;
function caseMachineSearchChanged(force=false){const cu=fCustomer.value,q=String(fMachineSearch.value||'').trim().toLowerCase(),list=machines.filter(m=>m.customerId===cu);let m=list.find(x=>caseMachineSearchLabel(x).toLowerCase()===q);if(!m){const matches=list.filter(x=>[machinePrimaryName(x),x.model,x.serial].join(' ').toLowerCase().includes(q));if(matches.length===1)m=matches[0]}fMachine.value=m?.id||'';if(!m&&force&&q)toastMsg('กรุณาเลือกเครื่องจากรายการค้นหา')}window.caseMachineSearchChanged=caseMachineSearchChanged;
function filteredTechOptions(team){return technicians.filter(t=>!team||team==='all'||(t.team||'Technical')===team)}
function refreshCaseAssignee(){if(typeof fAssignee==='undefined'||typeof fServiceTeam==='undefined')return;const cur=fAssignee.value,list=filteredTechOptions(fServiceTeam.value);fAssignee.innerHTML='<option value="">ยังไม่มอบหมาย</option>'+list.map(t=>`<option value="${t.id}">${esc(t.name)} · ${esc(t.team||'Technical')}</option>`).join('');if(list.some(t=>t.id===cur))fAssignee.value=cur}window.refreshCaseAssignee=refreshCaseAssignee;
function refreshScheduleTechs(){if(typeof sTech==='undefined'||typeof sTeam==='undefined')return;const cur=sTech.value,list=filteredTechOptions(sTeam.value);sTech.innerHTML='<option value="">เลือกช่าง</option>'+list.map(t=>`<option value="${t.id}">${esc(t.name)} · ${esc(t.team||'Technical')}</option>`).join('');if(list.some(t=>t.id===cur))sTech.value=cur}window.refreshScheduleTechs=refreshScheduleTechs;
async function saveCase(e){e.preventDefault();const old=cases.find(c=>c.id===fCaseId.value),cu=customerById(fCustomer.value),m=machineById(fMachine.value);if(!cu||!m){toastMsg('กรุณาเลือกลูกค้าและเครื่อง');return}const obj={id:old?.id||uid(),ticket:old?.ticket||genTicket(),createdAt:old?.createdAt||new Date().toISOString(),updatedAt:new Date().toISOString(),customerId:cu.id,customer:cu.name,contact:fContact.value||cu.contact||'',phone:fPhone.value||cu.phone||'',email:cu.email||'',location:cu.address||cu.location||'',branch:cu.branch||'',mapUrl:cu.mapUrl||'',latitude:cu.latitude||'',longitude:cu.longitude||'',machineId:m.id,machine:machinePrimaryName(m),machineTh:machineNameTh(m),machineEn:machineNameEn(m),model:m.model||'',serial:m.serial||'',machineSize:m.size||'',warranty:m.warranty||'ไม่ทราบ',channel:fChannel.value,serviceType:fServiceType.value,priority:fPriority.value,issue:fIssue.value.trim(),note:fNote.value.trim(),status:fStatus.value,serviceTeam:fServiceTeam.value,assignee:fAssignee.value,appointment:fAppointment.value};if(old)cases=cases.map(c=>c.id===obj.id?obj:c);else cases.unshift(obj);notifications.unshift({id:uid(),icon:'📋',title:old?'อัปเดตเคส':'รับเคสใหม่',message:`${obj.ticket} · ${obj.customer} · ${obj.machine}`,createdAt:new Date().toISOString(),read:false,caseId:obj.id});saveLocal();await cloudUpsertCase(obj);closeModal();renderAll();toastMsg(old?'อัปเดตเคสเรียบร้อย':'สร้างเคส '+obj.ticket+' แล้ว')}
function openCaseDetail(cid,tab='summary'){const c=cases.find(x=>x.id===cid);if(!c)return;const cu=customerById(c.customerId),m=machineById(c.machineId),t=techById(c.assignee);let content='';
 if(tab==='summary')content=`<div class="detail-grid">${[['ลูกค้า',c.customer],['เครื่อง',c.machine],['Model',c.model||'-'],['Serial',c.serial||'-'],['ประเภทงาน',c.serviceType],['ทีมรับผิดชอบ',c.serviceTeam||t?.team||'-'],['Priority',c.priority],['สถานะ',c.status],['ช่าง',t?.name||'-'],['นัดหมาย',fmt(c.appointment)]].map(x=>`<div class="detail-box"><small>${x[0]}</small><b>${esc(x[1])}</b></div>`).join('')}<div class="detail-box full"><small>อาการ / รายละเอียด</small><b>${esc(c.issue)}</b></div><div class="detail-box full"><small>หมายเหตุ</small><b>${esc(c.note||'-')}</b></div></div>`;
 if(tab==='related')content=`<div class="related-list"><div class="related-item" onclick="closeModal();openCustomerDetail('${cu?.id||''}')" style="cursor:pointer"><b>👥 ${esc(cu?.name||c.customer)}</b><small>เปิดข้อมูลลูกค้าและประวัติ Service</small></div><div class="related-item" onclick="closeModal();openMachineDetail('${m?.id||''}')" style="cursor:pointer"><b>⚙ ${esc(m?.name||c.machine)}</b><small>${esc(m?.model||c.model||'')} · S/N ${esc(m?.serial||c.serial||'-')}</small></div>${t?`<div class="related-item" onclick="closeModal();openTechnicianDetail('${t.id}')" style="cursor:pointer"><b>👨‍🔧 ${esc(t.name)}</b><small>ดูงานของช่าง</small></div>`:''}</div>`;
 if(tab==='activity'){const ns=notifications.filter(n=>n.caseId===c.id);content=`<div class="related-list">${ns.length?ns.map(n=>`<div class="related-item"><b>${esc(n.title)}</b><small>${esc(n.message)} · ${fmt(n.createdAt)}</small></div>`).join(''):'<div class="empty">ยังไม่มีกิจกรรม</div>'}</div>`}
 openModal(`${c.ticket} · ${c.customer}`,`อัปเดตล่าสุด ${fmt(c.updatedAt)}`,`<div class="tabs"><button class="tab-btn ${tab==='summary'?'active':''}" onclick="openCaseDetail('${c.id}','summary')">สรุปเคส</button><button class="tab-btn ${tab==='related'?'active':''}" onclick="openCaseDetail('${c.id}','related')">ข้อมูลเชื่อมโยง</button><button class="tab-btn ${tab==='activity'?'active':''}" onclick="openCaseDetail('${c.id}','activity')">กิจกรรม</button></div>${content}<div class="button-row" style="margin-top:14px"><button class="soft-btn" onclick="prepareQuotation('${c.id}')">฿ ทำใบเสนอราคา</button><button class="soft-btn" onclick="openScheduleModal('${c.id}')">📅 นัดหมาย</button><button class="soft-btn" onclick="openCaseModal('${c.id}')">แก้ไข</button><button class="primary-btn" onclick="advanceCase('${c.id}')">สถานะถัดไป</button></div>`)
}
window.openCaseDetail=openCaseDetail;
async function advanceCase(cid){const c=cases.find(x=>x.id===cid),i=settings.statuses.indexOf(c.status);if(!c||i<0||i>=settings.statuses.length-1){toastMsg('อยู่สถานะสุดท้ายแล้ว');return}c.status=settings.statuses[i+1];c.updatedAt=new Date().toISOString();notifications.unshift({id:uid(),icon:'ℹ️',title:'อัปเดตสถานะ',message:`${c.ticket} → ${c.status}`,createdAt:new Date().toISOString(),read:false,caseId:c.id});saveLocal();await cloudUpsertCase(c);openCaseDetail(c.id);renderAll();toastMsg('เปลี่ยนเป็น '+c.status)}window.advanceCase=advanceCase;

function openScheduleModal(cid=''){if(!requirePermission('calendar.edit'))return;const c=cases.find(x=>x.id===cid)||{};const team=c.serviceTeam||techById(c.assignee)?.team||'Technical';openModal('เพิ่ม / แก้ไขนัดหมาย','งานจะไปแสดงในปฏิทินช่างและสร้างแจ้งเตือน',`<form id="scheduleForm"><div class="field"><label>เลือกเคส *</label><select id="sCase" required><option value="">เลือกเคส</option>${openCases().map(x=>`<option value="${x.id}" ${x.id===c.id?'selected':''}>${esc(x.ticket)} · ${esc(x.customer)} · ${esc(x.machine)}</option>`).join('')}</select></div><div class="form-grid cols3"><div class="field"><label>ทีม</label><select id="sTeam" onchange="refreshScheduleTechs()"><option value="Technical" ${team==='Technical'?'selected':''}>ทีม Technical</option><option value="R&D" ${team==='R&D'?'selected':''}>ทีม R&D</option></select></div><div class="field"><label>ช่าง *</label><select id="sTech" required><option value="">เลือกช่าง</option>${filteredTechOptions(team).map(t=>`<option value="${t.id}" ${c.assignee===t.id?'selected':''}>${esc(t.name)} · ${esc(t.team||'Technical')}</option>`).join('')}</select></div><div class="field"><label>วันและเวลา *</label>${datetimeSplitHTML('sDate',c.appointment||'',true)}</div></div><div class="button-row"><button type="button" class="soft-btn" onclick="closeModal()">ยกเลิก</button><button class="primary-btn">บันทึกนัดหมาย</button></div></form>`,true);setTimeout(()=>scheduleForm.onsubmit=saveSchedule,20)}window.openScheduleModal=openScheduleModal;
async function saveSchedule(e){e.preventDefault();const c=cases.find(x=>x.id===sCase.value);if(!c)return;c.serviceTeam=sTeam.value;c.assignee=sTech.value;c.appointment=sDate.value;c.status=settings.statuses.includes('นัดหมายแล้ว')?'นัดหมายแล้ว':c.status;c.updatedAt=new Date().toISOString();notifications.unshift({id:uid(),icon:'📅',title:'สร้างนัดหมาย',message:`${c.ticket} · ${c.customer} · ${fmt(c.appointment)}`,createdAt:new Date().toISOString(),read:false,caseId:c.id});saveLocal();await cloudUpsertCase(c);closeModal();renderAll();toastMsg('บันทึกนัดหมายแล้ว')}


function customerMapDestination(cu){
 if(!cu)return'';
 if(cu.latitude!==''&&cu.longitude!==''&&cu.latitude!=null&&cu.longitude!=null)return`${cu.latitude},${cu.longitude}`;
 return cu.address||cu.location||cu.branch||cu.name||''
}
function customerDirectionsUrl(cu){
 if(!cu)return'';
 if(cu.mapUrl)return cu.mapUrl;
 const dest=customerMapDestination(cu);
 return dest?`https://www.google.com/maps/dir/?api=1&destination=${encodeURIComponent(dest)}`:''
}
function openCustomerMap(cid){
 const cu=customerById(cid);if(!cu)return;
 const url=customerDirectionsUrl(cu);
 if(!url){toastMsg('ลูกค้ายังไม่มีข้อมูล Location');return}
 window.open(url,'_blank')
}window.openCustomerMap=openCustomerMap;
function captureCustomerLocation(){
 if(!navigator.geolocation){toastMsg('อุปกรณ์นี้ไม่รองรับ Location');return}
 navigator.geolocation.getCurrentPosition(pos=>{
   if(typeof cuLat!=='undefined')cuLat.value=pos.coords.latitude.toFixed(6);
   if(typeof cuLng!=='undefined')cuLng.value=pos.coords.longitude.toFixed(6);
   if(typeof cuMapUrl!=='undefined')cuMapUrl.value=`https://www.google.com/maps/search/?api=1&query=${pos.coords.latitude},${pos.coords.longitude}`;
   toastMsg('บันทึกพิกัดจากอุปกรณ์แล้ว')
 },()=>toastMsg('ไม่สามารถอ่านตำแหน่งได้ กรุณาอนุญาต Location'))
}window.captureCustomerLocation=captureCustomerLocation;
function previewCustomerMap(){
 const temp={mapUrl:cuMapUrl?.value?.trim()||'',latitude:cuLat?.value||'',longitude:cuLng?.value||'',address:cuAddress?.value?.trim()||'',location:cuLocation?.value?.trim()||'',branch:cuBranch?.value?.trim()||'',name:cuName?.value?.trim()||''};
 const url=customerDirectionsUrl(temp);if(!url){toastMsg('กรุณาใส่ที่อยู่หรือพิกัดก่อน');return}window.open(url,'_blank')
}window.previewCustomerMap=previewCustomerMap;

function openCustomerModal(cid='',returnToCase=false){if(!requirePermission('customer.edit'))return;
 const c=customerById(cid)||{};
 openModal(c.id?'แก้ไขลูกค้า':'เพิ่มลูกค้าใหม่','ข้อมูลลูกค้า + ที่อยู่หน้างาน + Google Maps ใช้ร่วมกับ Case และ Field Service',`<form id="customerForm"><input id="cuId" type="hidden" value="${esc(c.id||'')}">
 <div class="form-grid cols2">
  <div class="field"><label>บริษัทลูกค้า *</label><input id="cuName" required value="${esc(c.name||'')}"></div>
  <div class="field"><label>สาขา</label><input id="cuBranch" value="${esc(c.branch||'')}"></div>
  <div class="field"><label>ผู้ติดต่อ</label><input id="cuContact" value="${esc(c.contact||'')}"></div>
  <div class="field"><label>โทรศัพท์</label><input id="cuPhone" value="${esc(c.phone||'')}"></div>
  <div class="field"><label>อีเมล</label><input id="cuEmail" value="${esc(c.email||'')}"></div>
  <div class="field"><label>จังหวัด / พื้นที่</label><input id="cuLocation" value="${esc(c.location||'')}"></div>
  <div class="field full"><label>ที่อยู่หน้างานแบบเต็ม</label><textarea id="cuAddress" placeholder="ที่อยู่สำหรับทีมช่าง / ใช้ค้นหาใน Google Maps">${esc(c.address||'')}</textarea></div>
  <div class="field full"><label>Google Maps Link</label><input id="cuMapUrl" placeholder="https://maps.google.com/... หรือ https://goo.gl/maps/..." value="${esc(c.mapUrl||'')}"></div>
  <div class="field"><label>Latitude</label><input id="cuLat" inputmode="decimal" placeholder="13.756331" value="${esc(c.latitude??'')}"></div>
  <div class="field"><label>Longitude</label><input id="cuLng" inputmode="decimal" placeholder="100.501762" value="${esc(c.longitude??'')}"></div>
  <div class="field full"><div class="map-action-row"><button type="button" class="soft-btn" onclick="captureCustomerLocation()">📍 ใช้ตำแหน่งอุปกรณ์นี้</button><button type="button" class="soft-btn" onclick="previewCustomerMap()">🗺 เปิด Google Maps</button></div><div class="setting-help">ทีมช่างจะดึง Location จาก ฐานข้อมูลลูกค้า อัตโนมัติเมื่อเปิดคิวงานและกดนำทาง</div></div>
  <div class="field full"><label>หมายเหตุ</label><textarea id="cuNote">${esc(c.note||'')}</textarea></div>
 </div>
 <div class="button-row"><button type="button" class="soft-btn" onclick="closeModal()">ยกเลิก</button><button class="primary-btn">บันทึกลูกค้า</button></div></form>`);
 setTimeout(()=>customerForm.onsubmit=e=>saveCustomer(e,returnToCase),20)
}window.openCustomerModal=openCustomerModal;

async function saveCustomer(e,returnToCase){
 e.preventDefault();const old=customerById(cuId.value);
 const obj={id:old?.id||uid(),name:cuName.value.trim(),branch:cuBranch.value.trim(),contact:cuContact.value.trim(),phone:cuPhone.value.trim(),email:cuEmail.value.trim(),location:cuLocation.value.trim(),address:cuAddress.value.trim(),mapUrl:cuMapUrl.value.trim(),latitude:cuLat.value.trim(),longitude:cuLng.value.trim(),note:cuNote.value.trim(),lineUserId:old?.lineUserId||'',lineDisplayName:old?.lineDisplayName||'',lineLinkedAt:old?.lineLinkedAt||''};
 if(old)customers=customers.map(x=>x.id===obj.id?obj:x);else customers.push(obj);
 saveLocal();await cloudUpsertCustomer(obj);closeModal();renderAll();toastMsg('บันทึกลูกค้าและ Location แล้ว');if(returnToCase)setTimeout(()=>openCaseModal(),100)
}

function openCustomerDetail(cid){
 const cu=customerById(cid);if(!cu)return;const ms=machines.filter(m=>m.customerId===cu.id),cs=cases.filter(c=>c.customerId===cu.id);
 openModal('ลูกค้า: '+cu.name,`${cu.contact||'-'} · ${cu.phone||'-'}`,`<div class="detail-grid"><div class="detail-box"><small>ผู้ติดต่อ</small><b>${esc(cu.contact||'-')}</b></div><div class="detail-box"><small>โทรศัพท์</small><b>${esc(cu.phone||'-')}</b></div><div class="detail-box"><small>อีเมล</small><b>${esc(cu.email||'-')}</b></div><div class="detail-box full"><small>สถานที่ / ที่อยู่หน้างาน</small><b>${esc([cu.branch,cu.address||cu.location].filter(Boolean).join(' / ')||'-')}</b></div>${cu.latitude&&cu.longitude?`<div class="detail-box full"><small>GPS</small><b>${esc(cu.latitude)}, ${esc(cu.longitude)}</b></div>`:''}</div><div class="context-link-bar" style="margin-top:12px"><button class="context-link map" onclick="openCustomerMap('${cu.id}')">🗺 Google Maps</button><button class="context-link" onclick="openCaseForCustomer('${cu.id}')">📋 เปิดเคส</button></div><div class="section-title" style="margin-top:14px">เครื่องจักร</div><div class="related-list">${ms.length?ms.map(m=>`<div class="related-item" onclick="closeModal();openMachineDetail('${m.id}')" style="cursor:pointer"><b>${esc(machinePrimaryName(m))}</b><small>${esc(machineSecondaryName(m))} · ${esc(m.model||'')} · S/N ${esc(m.serial||'-')}</small></div>`).join(''):'<div class="empty">ยังไม่มีเครื่อง</div>'}</div><div class="section-title" style="margin-top:14px">ประวัติ Service</div><div class="related-list">${cs.length?cs.map(c=>`<div class="related-item" onclick="closeModal();openCaseDetail('${c.id}')" style="cursor:pointer"><b>${esc(c.ticket)} · ${esc(c.machine)}</b><small>${esc(c.status)} · ${fmt(c.createdAt)}</small></div>`).join(''):'<div class="empty">ยังไม่มีเคส</div>'}</div><div class="button-row" style="margin-top:14px"><button class="soft-btn" onclick="openMachineModal('','','${cu.id}')">＋ เพิ่มเครื่อง</button><button class="primary-btn" onclick="openCaseForCustomer('${cu.id}')">＋ เปิดเคสใหม่</button></div>`)
}window.openCustomerDetail=openCustomerDetail;
function openCaseForCustomer(cid){const m=machines.find(x=>x.customerId===cid);closeModal();openCaseModal();setTimeout(()=>{fCustomer.value=cid;refreshMachines();if(m)fMachine.value=m.id},50)}window.openCaseForCustomer=openCaseForCustomer;

let pendingMachinePhoto="";
function openMachineModal(mid='',returnToCase=false,customerPreset=''){if(!requirePermission('machine.edit'))return;const m=machineById(mid)||{};pendingMachinePhoto=m.photo||'';openModal(m.id?'แก้ไขเครื่องจักร':'เพิ่มเครื่องจักร','Machine Master ใช้ร่วมกับเคส ประวัติซ่อม และใบเสนอราคา',`<form id="machineForm"><input id="maId" type="hidden" value="${esc(m.id||'')}"><div class="machine-photo-line"><div id="machinePhotoPreview">${m.photo?`<img class="machine-photo-preview" src="${m.photo}" alt="Machine">`:`<div class="machine-photo-placeholder">Machine Photo</div>`}</div><div style="flex:1"><b style="font-size:14px">รูปภาพเครื่องจักร</b><div class="setting-help">เพิ่มรูปเครื่องเพื่อให้หน้า Machine Master อ่านง่ายขึ้น และใช้ประกอบรายละเอียดเครื่อง</div><div class="button-row" style="justify-content:flex-start;margin-top:8px"><label class="soft-btn" style="cursor:pointer">📷 เลือกรูป<input type="file" accept="image/*" style="display:none" onchange="previewMachinePhoto(this)"></label><button type="button" class="soft-btn" onclick="removeMachinePhoto()">ลบรูป</button></div></div></div><div class="form-grid cols2"><div class="field"><label>ลูกค้า *</label><select id="maCustomer" required><option value="">เลือกลูกค้า</option>${customers.map(c=>`<option value="${c.id}" ${(m.customerId||customerPreset)===c.id?'selected':''}>${esc(c.name)}</option>`).join('')}</select></div><div class="field"><label>Model</label><input id="maModel" value="${esc(m.model||'')}"></div><div class="field"><label>ชื่อเครื่องภาษาไทย *</label><input id="maNameTh" required value="${esc(machineNameTh(m)||'')}"></div><div class="field"><label>Machine Name (English) *</label><input id="maNameEn" required value="${esc(machineNameEn(m)||'')}"></div><div class="field"><label>Serial No.</label><input id="maSerial" value="${esc(m.serial||'')}"></div><div class="field"><label>ปีออกเครื่อง</label><input id="maIssueYear" inputmode="numeric" value="${esc(m.issueYear||'')}"></div><div class="field"><label>สถานะ Service</label><select id="maServiceStatus"><option ${m.serviceStatus==='ต่อ Service'?'selected':''}>ต่อ Service</option><option ${m.serviceStatus==='ไม่ต่อ Service'?'selected':''}>ไม่ต่อ Service</option><option ${m.serviceStatus==='ไม่ระบุ'?'selected':''}>ไม่ระบุ</option></select></div><div class="field"><label>ขนาดเครื่อง</label><select id="maSize"><option value="">ไม่ระบุ</option><option value="S" ${m.size==='S'?'selected':''}>S - เครื่องเล็ก</option><option value="M" ${m.size==='M'?'selected':''}>M - เครื่องขนาดกลาง</option><option value="L" ${m.size==='L'?'selected':''}>L - เครื่องใหญ่</option></select></div><div class="field"><label>การรับประกัน</label><select id="maWarranty"><option ${m.warranty==='ไม่ทราบ'?'selected':''}>ไม่ทราบ</option><option ${m.warranty==='อยู่ในประกัน'?'selected':''}>อยู่ในประกัน</option><option ${m.warranty==='หมดประกัน'?'selected':''}>หมดประกัน</option></select></div><div class="field"><label>กำหนด PM ถัดไป</label><input id="maPmDue" type="date" value="${esc(m.pmDue||'')}"></div><div class="field full"><label>หมายเหตุ</label><textarea id="maNote">${esc(m.note||'')}</textarea></div></div><div class="button-row"><button type="button" class="soft-btn" onclick="closeModal()">ยกเลิก</button><button class="primary-btn">บันทึกเครื่อง</button></div></form>`);setTimeout(()=>machineForm.onsubmit=e=>saveMachine(e,returnToCase),20)}window.openMachineModal=openMachineModal;
function previewMachinePhoto(input){const file=input.files?.[0];if(!file)return;if(!file.type.startsWith('image/')){toastMsg('กรุณาเลือกไฟล์รูปภาพ');return}const reader=new FileReader();reader.onload=()=>{const img=new Image();img.onload=()=>{const max=900,scale=Math.min(1,max/Math.max(img.width,img.height)),w=Math.max(1,Math.round(img.width*scale)),h=Math.max(1,Math.round(img.height*scale));const c=document.createElement('canvas');c.width=w;c.height=h;c.getContext('2d').drawImage(img,0,0,w,h);pendingMachinePhoto=c.toDataURL('image/jpeg',.84);machinePhotoPreview.innerHTML=`<img class="machine-photo-preview" src="${pendingMachinePhoto}" alt="Machine preview">`};img.src=reader.result};reader.readAsDataURL(file)}window.previewMachinePhoto=previewMachinePhoto;
function removeMachinePhoto(){pendingMachinePhoto='';if(typeof machinePhotoPreview!=='undefined')machinePhotoPreview.innerHTML=`<div class="machine-photo-placeholder">Machine Photo</div>`}window.removeMachinePhoto=removeMachinePhoto;
async function saveMachine(e,returnToCase){e.preventDefault();const old=machineById(maId.value),manualSize=normalizeMachineSize(maSize.value),obj={id:old?.id||uid(),customerId:maCustomer.value,name:maNameTh.value.trim(),nameTh:maNameTh.value.trim(),nameEn:maNameEn.value.trim(),model:maModel.value.trim(),serial:maSerial.value.trim(),issueYear:maIssueYear.value.trim(),serviceStatus:maServiceStatus.value,size:manualSize,sizeStatus:manualSize?'Manual Confirm':(old?.sizeStatus||''),serviceSize:manualSize==='S'?'Small Machine':manualSize==='M'?'Medium Machine':manualSize==='L'?'Large Machine':(old?.serviceSize||''),sizeBasis:manualSize?'กำหนดจาก Machine Master โดยผู้ใช้งาน':(old?.sizeBasis||''),warranty:maWarranty.value,pmDue:maPmDue.value,note:maNote.value.trim(),photo:pendingMachinePhoto||'',source:old?.source||''};if(old)machines=machines.map(x=>x.id===obj.id?obj:x);else machines.push(obj);saveLocal();await cloudUpsert('machines',obj);closeModal();renderAll();toastMsg('บันทึกเครื่องจักรแล้ว');if(returnToCase)setTimeout(()=>openCaseModal(),100)}
function openMachineDetail(mid){const m=machineById(mid);if(!m)return;const cu=customerById(m.customerId),cs=cases.filter(c=>c.machineId===m.id);openModal(machinePrimaryName(m),`${machineSecondaryName(m)} · ${m.model||'-'} · S/N ${m.serial||'-'}`,`<div class="machine-detail-layout"><div>${machineMediaHTML(m,true)}</div><div><div class="detail-grid"><div class="detail-box full bilingual-machine-detail"><small>ชื่อเครื่อง / Machine Name</small><b>${esc(machineNameTh(m))}</b><span>${esc(machineNameEn(m))}</span></div><div class="detail-box"><small>ลูกค้า</small><b>${esc(cu?.name||'-')}</b></div><div class="detail-box"><small>Model</small><b>${esc(m.model||'-')}</b></div><div class="detail-box"><small>Serial No.</small><b>${esc(m.serial||'-')}</b></div><div class="detail-box"><small>ปีออกเครื่อง</small><b>${esc(m.issueYear||'-')}</b></div><div class="detail-box"><small>สถานะ Service</small><b>${esc(m.serviceStatus||'-')}</b></div><div class="detail-box"><small>ขนาด Service</small><b>${esc(m.size||'รอ Confirm')} ${m.serviceSize?`· ${esc(m.serviceSize)}`:''}</b><span class="subtext">${esc(m.sizeStatus||'')}</span></div><div class="detail-box"><small>ประกัน</small><b>${esc(m.warranty||'-')}</b></div><div class="detail-box"><small>PM ถัดไป</small><b>${esc(m.pmDue||'-')}</b></div><div class="detail-box"><small>จำนวนเคส</small><b>${cs.length}</b></div><div class="detail-box full"><small>หมายเหตุ</small><b>${esc(m.note||'-')}</b></div></div><div class="section-title" style="margin-top:14px">ประวัติ Service</div><div class="related-list">${cs.length?cs.map(c=>`<div class="related-item" onclick="closeModal();openCaseDetail('${c.id}')" style="cursor:pointer"><b>${esc(c.ticket)} · ${esc(c.issue).slice(0,70)}</b><small>${esc(c.status)} · ${fmt(c.createdAt)}</small></div>`).join(''):'<div class="empty">ยังไม่มีประวัติ</div>'}</div><div class="button-row" style="margin-top:14px"><button class="soft-btn" onclick="closeModal();openWarrantyForMachine('${m.id}')">🛡 Warranty</button><button class="soft-btn" onclick="closeModal();openDocsForMachine('${m.id}')">📁 เอกสารเครื่อง</button><button class="soft-btn" onclick="closeModal();openCustomerDetail('${cu?.id||''}')">ดูลูกค้า</button><button class="primary-btn" onclick="openCaseForMachine('${m.id}')">＋ เปิดเคสเครื่องนี้</button></div></div></div>`)}window.openMachineDetail=openMachineDetail;
function openCaseForMachine(mid){const m=machineById(mid);if(!m)return;closeModal();openCaseModal();setTimeout(()=>{fCustomer.value=m.customerId;refreshMachines();fMachine.value=m.id},50)}window.openCaseForMachine=openCaseForMachine;

let pendingTechPhoto='';
function openTechnicianModal(tid=''){
 const t=techById(tid)||{};pendingTechPhoto=t.photo||'';
 const initial=esc((t.name||'TECH').split(' ').map(x=>x[0]).join('').slice(0,2));
 openModal(t.id?'แก้ไขพนักงาน Service Maintenance':'เพิ่มพนักงาน Service Maintenance','กำหนดทีม รูปโปรไฟล์ ทักษะ และสถานะสำหรับใช้ในระบบ Service',`<form id="techForm"><input id="tId" type="hidden" value="${esc(t.id||'')}">
 <div class="tech-photo-line"><div id="techPhotoPreview">${t.photo?`<img class="tech-photo-preview" src="${t.photo}" alt="Technician">`:`<div class="tech-photo-placeholder">${initial}</div>`}</div><div style="flex:1"><b style="font-size:13px">รูปประจำตัวช่าง</b><div class="setting-help">รองรับ JPG / PNG / WEBP ระบบจะย่อรูปอัตโนมัติเพื่อให้ Web ทำงานได้เร็วขึ้น</div><div class="button-row" style="justify-content:flex-start;margin-top:8px"><label class="soft-btn" style="cursor:pointer">📷 เลือกรูป<input type="file" accept="image/*" style="display:none" onchange="previewTechPhoto(this)"></label><button type="button" class="soft-btn" onclick="removeTechPhoto()">ลบรูป</button></div></div></div>
 <div class="form-grid cols2">
  <div class="field"><label>ชื่อ-นามสกุล *</label><input id="tName" required value="${esc(t.name||'')}"></div>
  <div class="field"><label>ตำแหน่ง</label><input id="tRole" value="${esc(t.role||'Service Technician')}"></div>
  <div class="field"><label>ทีม</label><select id="tTeam"><option value="Technical" ${(t.team||'Technical')==='Technical'?'selected':''}>ทีม Technical</option><option value="R&D" ${(t.team||'Technical')==='R&D'?'selected':''}>ทีม R&D</option></select></div>
  <div class="field"><label>สถานะ</label><select id="tStatus"><option ${t.status==='พร้อมรับงาน'?'selected':''}>พร้อมรับงาน</option><option ${t.status==='กำลังปฏิบัติงาน'?'selected':''}>กำลังปฏิบัติงาน</option><option ${t.status==='ลงพื้นที่'?'selected':''}>ลงพื้นที่</option><option ${t.status==='ไม่พร้อม'?'selected':''}>ไม่พร้อม</option></select></div>
  <div class="field"><label>โทรศัพท์</label><input id="tPhone" value="${esc(t.phone||'')}"></div>
  <div class="field"><label>อีเมล</label><input id="tEmail" value="${esc(t.email||'')}"></div>
  <div class="field"><label>สีในปฏิทิน</label><select id="tColor"><option value="blue" ${t.color==='blue'?'selected':''}>น้ำเงิน</option><option value="orange" ${t.color==='orange'?'selected':''}>ส้ม</option><option value="green" ${t.color==='green'?'selected':''}>เขียว</option><option value="purple" ${t.color==='purple'?'selected':''}>ม่วง</option></select></div>
  <div class="field full"><label>ทักษะ / Skills</label><input id="tSkills" placeholder="Laser, CNC, Electrical, R&D, Testing..." value="${esc(t.skills||'')}"></div>
 </div><div class="button-row"><button type="button" class="soft-btn" onclick="closeModal()">ยกเลิก</button><button class="primary-btn">บันทึกช่าง</button></div></form>`,true);
 setTimeout(()=>techForm.onsubmit=saveTech,20)
}window.openTechnicianModal=openTechnicianModal;

function previewTechPhoto(input){
 const file=input.files?.[0];if(!file)return;
 if(!file.type.startsWith('image/')){toastMsg('กรุณาเลือกไฟล์รูปภาพ');return}
 const reader=new FileReader();
 reader.onload=()=>{const img=new Image();img.onload=()=>{
   const max=420,scale=Math.min(1,max/Math.max(img.width,img.height)),w=Math.max(1,Math.round(img.width*scale)),h=Math.max(1,Math.round(img.height*scale));
   const c=document.createElement('canvas');c.width=w;c.height=h;c.getContext('2d').drawImage(img,0,0,w,h);
   pendingTechPhoto=c.toDataURL('image/jpeg',.82);
   techPhotoPreview.innerHTML=`<img class="tech-photo-preview" src="${pendingTechPhoto}" alt="Technician preview">`
 };img.src=reader.result};reader.readAsDataURL(file)
}window.previewTechPhoto=previewTechPhoto;
function removeTechPhoto(){pendingTechPhoto='';const initials=esc((tName?.value||'TECH').split(' ').map(x=>x[0]).join('').slice(0,2));techPhotoPreview.innerHTML=`<div class="tech-photo-placeholder">${initials}</div>`}window.removeTechPhoto=removeTechPhoto;

async function saveTech(e){
 e.preventDefault();const old=techById(tId.value),obj={id:old?.id||'T'+Date.now(),name:tName.value.trim(),role:tRole.value.trim(),team:tTeam.value,phone:tPhone.value.trim(),email:tEmail.value.trim(),status:tStatus.value,skills:tSkills.value.trim(),color:tColor.value,photo:pendingTechPhoto||''};
 if(old)technicians=technicians.map(x=>x.id===obj.id?obj:x);else technicians.push(obj);
 saveLocal();await cloudUpsert('technicians',obj);closeModal();renderAll();toastMsg('บันทึกพนักงาน Service Maintenance แล้ว')
}
function openTechnicianDetail(tid){
 const t=techById(tid);if(!t)return;const cs=cases.filter(c=>c.assignee===t.id).sort((a,b)=>new Date(b.createdAt)-new Date(a.createdAt));
 openModal('ช่าง: '+t.name,`${t.role||''} · ${t.phone||'-'}`,`<div class="tech-profile-head">${techAvatarHTML(t,true)}<div><h3 style="margin:0">${esc(t.name)}</h3><p style="margin:3px 0;color:var(--muted);font-size:12px">${esc(t.role||'Service Technician')}</p>${teamBadge(t.team)} <span class="tech-status-dot ${techStatusClass(t.status)}">${esc(t.status||'พร้อมรับงาน')}</span>${t.skills?`<div class="skill-tags">${techSkillTags(t)}</div>`:''}</div></div><div class="detail-grid"><div class="detail-box"><small>ทีม</small><b>${esc(t.team||'Technical')}</b></div><div class="detail-box"><small>งานเปิด</small><b>${cs.filter(x=>!['เสร็จสิ้น','ปิดเคส'].includes(x.status)).length}</b></div><div class="detail-box"><small>นัดหมาย</small><b>${cs.filter(x=>x.appointment).length}</b></div><div class="detail-box"><small>อีเมล</small><b>${esc(t.email||'-')}</b></div></div><div class="related-list">${cs.length?cs.map(c=>`<div class="related-item" onclick="closeModal();openCaseDetail('${c.id}')" style="cursor:pointer"><b>${esc(c.ticket)} · ${esc(c.customer)}</b><small>${esc(c.machine)} · ${fmt(c.appointment)} · ${esc(c.status)}</small></div>`).join(''):'<div class="empty">ยังไม่มีงานของช่างคนนี้</div>'}</div><div class="button-row" style="margin-top:14px"><button class="soft-btn" onclick="openScheduleModal()">📅 เพิ่มนัดหมาย</button><button class="primary-btn" onclick="openTechnicianModal('${t.id}')">แก้ไขข้อมูล</button></div>`)
}window.openTechnicianDetail=openTechnicianDetail;

function openNotificationDetail(key){if(key.startsWith('auto_warranty_')){const id=key.replace('auto_warranty_','');openWarrantyDetail(id);return}const n=buildNotifications().find(x=>x.key===key);if(!n)return;openModal(n.title,fmt(n.createdAt),`<div class="detail-box full"><small>รายละเอียด</small><b>${esc(n.message)}</b></div><div class="button-row" style="margin-top:14px">${n.caseId?`<button class="primary-btn" onclick="closeModal();openCaseDetail('${n.caseId}')">เปิดเคสที่เกี่ยวข้อง</button>`:''}</div>`,true)}window.openNotificationDetail=openNotificationDetail;
function boolSwitch(id,label,sub,val){
 return `<div class="toggle-row"><div><b>${label}</b><small>${sub||''}</small></div><label class="switch"><input id="${id}" type="checkbox" ${val?'checked':''}><span></span></label></div>`
}
function openLanguageSettingModal(){openModal('ภาษาและการแสดงผล','เลือกภาษาหลักของระบบ และสลับได้จากปุ่ม TH / EN ด้านบน',`<form id="languageSettingForm"><div class="language-choice-grid"><label class="language-choice ${settings.language==='th'?'active':''}"><input type="radio" name="sysLang" value="th" ${settings.language==='th'?'checked':''}><span>🇹🇭</span><b>ภาษาไทย</b><small>ค่าเริ่มต้นสำหรับผู้ดูแลระบบและทีมช่าง</small></label><label class="language-choice ${settings.language==='en'?'active':''}"><input type="radio" name="sysLang" value="en" ${settings.language==='en'?'checked':''}><span>🇬🇧</span><b>English</b><small>English interface</small></label></div><div class="button-row"><button type="button" class="soft-btn" onclick="closeModal()">ยกเลิก</button><button class="primary-btn">บันทึกภาษา</button></div></form>`,true);setTimeout(()=>languageSettingForm.onsubmit=e=>{e.preventDefault();const v=new FormData(languageSettingForm).get('sysLang')||'th';closeModal();setLanguage(v)},20)}window.openLanguageSettingModal=openLanguageSettingModal;
function openFieldServiceSettingModal(){const f=settings.fieldServiceConfig;openModal('งานภาคสนามและใบตรวจช่าง','กำหนด Reminder, Check-in, รูป และเงื่อนไขจบงาน',`<form id="fieldSettingForm"><div class="form-grid cols2"><div class="field"><label>แจ้งเตือนก่อนนัดหมาย (ชั่วโมง)</label><input id="fsReminder" type="number" min="1" value="${f.appointmentReminderHours}"></div><div class="field"><label>จำนวนรูปสูงสุดต่อชุด</label><input id="fsPhotos" type="number" min="1" value="${f.maxPhotosEach}"></div></div>${boolSwitch('fsAccept','ต้องมีผู้รับมอบงาน','บังคับกรอกชื่อก่อนจบงาน',f.requireCustomerAcceptance)}${boolSwitch('fsTechSign','ต้องมีลายเซ็นทีมช่าง','บังคับลายเซ็นก่อนจบงาน',f.requireTechSignature)}${boolSwitch('fsCheckin','ต้อง Check-in ก่อนทำงาน','ควบคุมลำดับ Field Service',f.checkInRequired)}<div class="button-row"><button type="button" class="soft-btn" onclick="closeModal()">ยกเลิก</button><button class="primary-btn">บันทึก</button></div></form>`,true);setTimeout(()=>fieldSettingForm.onsubmit=e=>{e.preventDefault();settings.fieldServiceConfig={...f,appointmentReminderHours:+fsReminder.value||24,maxPhotosEach:+fsPhotos.value||4,requireCustomerAcceptance:fsAccept.checked,requireTechSignature:fsTechSign.checked,checkInRequired:fsCheckin.checked};saveLocal();cloudSaveSettings();closeModal();renderSettings()},20)}window.openFieldServiceSettingModal=openFieldServiceSettingModal;
function openLocationSettingModal(){const m=settings.mapConfig;openModal('ตำแหน่งลูกค้าและ Google Maps','กำหนด GPS, Check-in, จุดเริ่มต้น I-MODE และการคำนวณระยะทาง',`<form id="locationSettingForm">${boolSwitch('mapEnabled','เปิดใช้งาน Google Maps','แสดงปุ่มนำทาง',m.enabled)}${boolSwitch('mapAutoQuote','คำนวณระยะทางในใบเสนอราคาอัตโนมัติ','เมื่อเลือกลูกค้าให้คำนวณ I-MODE → ลูกค้า',m.autoQuoteDistance!==false)}${boolSwitch('mapCoord','ใช้พิกัด GPS ก่อนที่อยู่','ใช้ Latitude / Longitude เป็นปลายทางหลัก',m.useCoordinatesFirst)}${boolSwitch('mapGps','บันทึก GPS ตอน Check-in','บันทึกตำแหน่งอุปกรณ์ช่าง',m.checkInGps)}${boolSwitch('mapDir','เปิดโหมดนำทาง','เปิด Directions ไปยังลูกค้า',m.openDirections)}<div class="section-title" style="margin-top:12px">Google Maps Routes</div><div class="form-grid cols2"><div class="field full"><label>จุดเริ่มต้น I-MODE Plus</label><input id="mapOriginAddress" value="${esc(m.originAddress||settings.companyAddress||'')}" placeholder="ที่อยู่บริษัท ไอโมด พลัส จำกัด"></div><div class="field"><label>Origin Latitude (ถ้ามี)</label><input id="mapOriginLat" value="${esc(m.originLat||'')}"></div><div class="field"><label>Origin Longitude (ถ้ามี)</label><input id="mapOriginLng" value="${esc(m.originLng||'')}"></div><div class="field full"><label>Google Maps API Key</label><input id="mapApiKey" type="password" value="${esc(m.googleMapsApiKey||'')}" placeholder="AIza..."><small class="field-help">สำหรับ Demo/Browser ให้จำกัด API Key ด้วย Website/HTTP Referrer และเปิด Maps JavaScript API + Routes API</small></div></div><div class="button-row"><button type="button" class="soft-btn" onclick="closeModal()">ยกเลิก</button><button class="primary-btn">บันทึก</button></div></form>`,true);setTimeout(()=>locationSettingForm.onsubmit=e=>{e.preventDefault();settings.mapConfig={...m,enabled:mapEnabled.checked,autoQuoteDistance:mapAutoQuote.checked,useCoordinatesFirst:mapCoord.checked,checkInGps:mapGps.checked,openDirections:mapDir.checked,originAddress:mapOriginAddress.value.trim(),originLat:mapOriginLat.value.trim(),originLng:mapOriginLng.value.trim(),googleMapsApiKey:mapApiKey.value.trim()};saveLocal();cloudSaveSettings();closeModal();renderSettings();toastMsg('บันทึกการตั้งค่า Google Maps แล้ว')},20)}window.openLocationSettingModal=openLocationSettingModal;
function openWarrantyModuleSettingModal(){const w=settings.warrantyConfig;openModal('ระบบประกันเครื่อง','ระยะประกัน การเตือน และราคาแพ็กเกจ',`<form id="warrantyModuleForm"><div class="form-grid cols2"><div class="field"><label>ระยะประกันมาตรฐาน (เดือน)</label><input id="wmMonths" type="number" value="${w.defaultMonths}"></div><div class="field"><label>แจ้งเตือนก่อนหมดประกัน (วัน)</label><input id="wmReminder" type="number" value="${w.reminderDays}"></div><div class="field"><label>แพ็กเกจ 6 เดือน</label><input id="wm6" type="number" value="${w.packagePrices?.[6]||0}"></div><div class="field"><label>แพ็กเกจ 12 เดือน</label><input id="wm12" type="number" value="${w.packagePrices?.[12]||0}"></div><div class="field"><label>แพ็กเกจ 24 เดือน</label><input id="wm24" type="number" value="${w.packagePrices?.[24]||0}"></div></div><div class="button-row"><button type="button" class="soft-btn" onclick="closeModal()">ยกเลิก</button><button class="primary-btn">บันทึก</button></div></form>`,true);setTimeout(()=>warrantyModuleForm.onsubmit=e=>{e.preventDefault();settings.warrantyConfig={...w,defaultMonths:+wmMonths.value||12,reminderDays:+wmReminder.value||30,packagePrices:{6:+wm6.value||0,12:+wm12.value||0,24:+wm24.value||0}};saveLocal();cloudSaveSettings();closeModal();renderSettings()},20)}window.openWarrantyModuleSettingModal=openWarrantyModuleSettingModal;
function openDocumentModuleSettingModal(){const d=settings.documentConfig;openModal('คลังเอกสารเครื่องจักร','แหล่งเอกสารและข้อจำกัดไฟล์',`<form id="documentModuleForm">${boolSwitch('docUpload','อนุญาต Upload File','อัปโหลดจากอุปกรณ์',d.allowUpload)}${boolSwitch('docDrive','อนุญาต Google Drive / URL','เก็บลิงก์ Cloud',d.allowDriveLink)}<div class="field"><label>ขนาดไฟล์ Local สูงสุด (MB)</label><input id="docMaxMb" type="number" value="${d.maxLocalFileMB}"></div><div class="button-row"><button type="button" class="soft-btn" onclick="openMasterSettingModal('documentCategories')">แก้ไขหมวดเอกสาร</button><button class="primary-btn">บันทึก</button></div></form>`,true);setTimeout(()=>documentModuleForm.onsubmit=e=>{e.preventDefault();settings.documentConfig={allowUpload:docUpload.checked,allowDriveLink:docDrive.checked,maxLocalFileMB:+docMaxMb.value||3};saveLocal();cloudSaveSettings();closeModal();renderSettings()},20)}window.openDocumentModuleSettingModal=openDocumentModuleSettingModal;
function openMediaSettingModal(){const m=settings.mediaConfig;openModal('รูปภาพ วิดีโอ และลายเซ็น','จำนวนไฟล์ ขนาดวิดีโอ และหลักฐาน',`<form id="mediaSettingForm"><div class="form-grid cols2"><div class="field"><label>รูปสูงสุดต่อ Before / After</label><input id="mdPhotos" type="number" value="${m.maxPhotosEach}"></div><div class="field"><label>วิดีโอสูงสุดต่อ Report</label><input id="mdVideos" type="number" value="${m.maxVideos}"></div><div class="field"><label>ขนาดวิดีโอสูงสุด (MB)</label><input id="mdVideoMb" type="number" step="0.5" value="${m.maxVideoMB}"></div><div class="field"><label>คุณภาพบีบอัดรูป (%)</label><input id="mdQuality" type="number" value="${m.compressQuality}"></div></div>${boolSwitch('mdBeforeAfter','บังคับ Before / After','ต้องมีรูปก่อน-หลัง',m.requireBeforeAfter)}<div class="button-row"><button type="button" class="soft-btn" onclick="closeModal()">ยกเลิก</button><button class="primary-btn">บันทึก</button></div></form>`,true);setTimeout(()=>mediaSettingForm.onsubmit=e=>{e.preventDefault();settings.mediaConfig={maxPhotosEach:+mdPhotos.value||4,maxVideos:+mdVideos.value||0,maxVideoMB:+mdVideoMb.value||2.5,compressQuality:+mdQuality.value||78,requireBeforeAfter:mdBeforeAfter.checked};settings.fieldServiceConfig.maxPhotosEach=settings.mediaConfig.maxPhotosEach;saveLocal();cloudSaveSettings();closeModal();renderSettings()},20)}window.openMediaSettingModal=openMediaSettingModal;
function openReportExportSettingModal(){const r=settings.reportConfig,th=settings.language!=='en';openModal(th?'รายงานและการส่งออกไฟล์':'Reports & File Export',th?'รูปแบบ Report และข้อมูลในไฟล์ส่งลูกค้า':'Report format and exported content for customers',`<form id="reportExportForm"><div class="field"><label>${th?'รูปแบบหลัก':'Primary format'}</label><select id="rpFormat"><option value="PDF" ${r.defaultFormat==='PDF'?'selected':''}>PDF</option><option value="CSV" ${r.defaultFormat==='CSV'?'selected':''}>CSV</option><option value="Excel" ${r.defaultFormat==='Excel'?'selected':''}>Excel (.xls)</option></select></div>${boolSwitch('rpPhotos',th?'รวมรูป Before / After':'Include before / after photos',th?'แสดงรูปในไฟล์รายงาน':'Show photos in report files',r.includePhotos)}${boolSwitch('rpSign',th?'รวมลายเซ็น':'Include signatures',th?'แสดงลายเซ็นลูกค้าและช่าง':'Show customer and technician signatures',r.includeSignatures)}${boolSwitch('rpTimeline',th?'รวม Timeline สถานะ':'Include status timeline',th?'แสดงประวัติการทำงาน':'Show work history',r.includeTimeline)}${boolSwitch('rpLine',th?'อนุญาตส่ง Report เข้า LINE':'Allow sending reports to LINE',th?'เปิดปุ่ม LINE OA':'Enable LINE OA action',r.lineSendEnabled)}<div class="button-row"><button type="button" class="soft-btn" onclick="closeModal()">${th?'ยกเลิก':'Cancel'}</button><button class="primary-btn">${th?'บันทึก':'Save'}</button></div></form>`,true);setTimeout(()=>reportExportForm.onsubmit=e=>{e.preventDefault();settings.reportConfig={defaultFormat:rpFormat.value,includePhotos:rpPhotos.checked,includeSignatures:rpSign.checked,includeTimeline:rpTimeline.checked,lineSendEnabled:rpLine.checked};saveLocal();cloudSaveSettings();closeModal();renderSettings();toastMsg(th?'บันทึกการตั้งค่าการส่งออกแล้ว':'Report export settings saved')},20)}window.openReportExportSettingModal=openReportExportSettingModal;
function openNotificationSettingModal(){
 const n=settings.notificationConfig||defaults.notificationConfig;
 openModal('ตั้งค่าการแจ้งเตือน','กำหนด Channel และ Event ที่ต้องการให้ระบบแจ้งเตือน',`<form id="notiForm"><div class="field"><label>แจ้งเตือนก่อนนัดหมาย (ชั่วโมง)</label><input id="leadHours" type="number" min="1" max="168" value="${n.leadHours||24}"></div><div class="section-title">ช่องทางแจ้งเตือน</div><div class="toggle-list">${boolSwitch('nLine','LINE OA','เปิด/ปิดช่องทาง LINE OA',n.lineOA)}${boolSwitch('nEmail','Email','ส่งอีเมลแจ้งเตือน',n.email)}${boolSwitch('nPush','Push Notification','แจ้งเตือนบน Web App',n.push)}</div><div class="section-title" style="margin-top:13px">Event ที่ต้องแจ้ง</div><div class="toggle-list">${boolSwitch('nAppointment','ใกล้ถึงเวลานัดหมาย','แจ้งตาม Lead Time ด้านบน',n.appointment)}${boolSwitch('nSla','SLA / เคสด่วน','แจ้งงาน Priority สูง',n.slaRisk)}${boolSwitch('nParts','งานรออะไหล่','แจ้งเตือนเมื่อสถานะรออะไหล่',n.partsWaiting)}${boolSwitch('nSubmit','รอส่งงาน / รายงาน','แจ้งงานที่ช่างทำเสร็จแต่ยังไม่ปิด',n.submitWaiting)}</div><div class="button-row" style="margin-top:14px"><button type="button" class="soft-btn" onclick="closeModal()">ยกเลิก</button><button class="primary-btn">บันทึก</button></div></form>`,true);
 setTimeout(()=>notiForm.onsubmit=e=>{e.preventDefault();settings.notificationConfig={leadHours:Number(leadHours.value)||24,lineOA:nLine.checked,email:nEmail.checked,push:nPush.checked,appointment:nAppointment.checked,slaRisk:nSla.checked,partsWaiting:nParts.checked,submitWaiting:nSubmit.checked};settings.notificationLeadHours=settings.notificationConfig.leadHours;saveLocal();cloudSaveSettings();closeModal();renderAll();toastMsg('บันทึกการแจ้งเตือนแล้ว')},20)
}window.openNotificationSettingModal=openNotificationSettingModal;

function openMasterHubModal(){
 openModal('Master Data','เลือกข้อมูลกลางที่ต้องการแก้ไข',`<div class="settings-list"><button onclick="openMasterSettingModal('serviceTypes')">🧰<div><b>ประเภทงาน Service</b><small>${settings.serviceTypes.length} รายการ</small></div><strong>›</strong></button><button onclick="openMasterSettingModal('statuses')">📌<div><b>Workflow / สถานะเคส</b><small>${settings.statuses.length} สถานะ</small></div><strong>›</strong></button><button onclick="openMasterSettingModal('priorities')">⚠️<div><b>Priority</b><small>${settings.priorities.length} ระดับ</small></div><strong>›</strong></button></div>`,true)
}window.openMasterHubModal=openMasterHubModal;

function openMasterSettingModal(type){const map={serviceTypes:['ประเภทงาน Service','serviceTypes'],statuses:['Workflow / สถานะเคส','statuses'],priorities:['Priority','priorities'],documentCategories:['หมวดเอกสารเครื่องจักร','documentCategories']},[title,key]=map[type]||map.serviceTypes;openModal(title,'เพิ่ม ลบ และจัดรายการที่ใช้ร่วมกันในทุกโมดูล',`<div id="configList" class="config-list">${settings[key].map(x=>`<div class="config-row"><input value="${esc(x)}"><button class="danger-btn" onclick="this.parentElement.remove()">ลบ</button></div>`).join('')}</div><div class="button-row" style="margin-top:12px"><button class="soft-btn" onclick="addConfigRow()">＋ เพิ่มรายการ</button><button class="primary-btn" onclick="saveConfig('${key}')">บันทึก</button></div>`,true)}window.openMasterSettingModal=openMasterSettingModal;
function addConfigRow(){configList.insertAdjacentHTML('beforeend','<div class="config-row"><input value=""><button class="danger-btn" onclick="this.parentElement.remove()">ลบ</button></div>')}window.addConfigRow=addConfigRow;
function saveConfig(key){const vals=[...configList.querySelectorAll('input')].map(x=>x.value.trim()).filter(Boolean);if(!vals.length){toastMsg('ต้องมีอย่างน้อย 1 รายการ');return}settings[key]=vals;saveLocal();cloudSaveSettings();closeModal();renderAll();toastMsg('บันทึก Master Data แล้ว')}window.saveConfig=saveConfig;

function permissionLabel(item){return settings.language==='en'?item[2]:item[1]}
function permissionGroupLabel(g){return settings.language==='en'?g.groupEn:g.group}
function rolePermissionSummary(perms=[]){const total=allPermissionKeys().length;return settings.language==='en'?`${perms.length}/${total} permissions`:`${perms.length}/${total} สิทธิ์`}
function roleCard(r={}){
 const perms=migrateLegacyPermissions(r.permissions,r.name||'');
 const lang=settings.language||'th',th=lang!=='en';
 const scopedCount=roleUserCount(r);
 return `<div class="role-card"><div class="role-card-head compact"><div class="role-name-wrap"><label>${th?'ชื่อบทบาท / Role':'Role name'}</label><input data-k="name" value="${esc(r.name||'')}" oninput="updateRoleUserCount(this)"></div><div class="role-meta-grid"><div class="role-meta-field"><label>${th?'ทีมที่ใช้':'Team scope'}</label><select data-k="teamScope" onchange="updateRoleUserCount(this)">${roleSelectOptions(ROLE_TEAM_OPTIONS,r.teamScope||'','team')}</select></div><div class="role-meta-field"><label>${th?'ตำแหน่ง':'Position scope'}</label><select data-k="positionScope" onchange="updateRoleUserCount(this)">${roleSelectOptions(ROLE_POSITION_OPTIONS,r.positionScope||'','position')}</select></div></div><div class="role-auto-count editable"><small>${th?'จำนวนผู้ใช้งาน':'User count'}</small><input data-k="users" class="role-user-count-input" type="number" min="0" value="${Number.isFinite(Number(r.users))?Number(r.users):scopedCount}"><span>${th?'บัญชีที่พบจริง':'Actual matched accounts'} <b data-actual-user-count>${scopedCount}</b></span></div><div class="role-head-actions"><button class="soft-btn" type="button" onclick="applyStandardRolePreset(this)">${th?'ชุดสิทธิ์มาตรฐาน':'Standard preset'}</button><button class="danger-btn" type="button" onclick="this.closest('.role-card').remove()">${th?'ลบ Role':'Delete role'}</button></div></div><div class="role-perm-toolbar"><b>${th?'กำหนดสิทธิ์การใช้งาน':'Permissions'}</b><span data-perm-summary>${rolePermissionSummary(perms)}</span><button type="button" class="mini-btn" onclick="toggleRoleAll(this,true)">${th?'เลือกทั้งหมด':'Select all'}</button><button type="button" class="mini-btn" onclick="toggleRoleAll(this,false)">${th?'ล้างทั้งหมด':'Clear'}</button></div><div class="permission-groups compact">${PERMISSION_CATALOG.map(g=>`<div class="permission-group"><h4>${permissionGroupLabel(g)}</h4><div class="permission-grid compact">${g.items.map(item=>`<label class="permission-check compact"><input type="checkbox" data-perm="${item[0]}" ${perms.includes(item[0])?'checked':''} onchange="updatePermissionSummary(this)"><span><b>${permissionLabel(item)}</b><small>${item[0]}</small></span></label>`).join('')}</div></div>`).join('')}</div></div>`
}

function openRolesSettingModal(){
 const th=settings.language!=='en';
 openModal(th?'ผู้ใช้งานและสิทธิ์':'Users & Roles',th?'กำหนดสิทธิ์ด้วย Checkbox แยกตามโมดูล พร้อมแยกทีมและตำแหน่งของช่างได้':'Configure module permissions with compact checkboxes and separate technician team / position scopes.',`<div class="roles-top-note"><div><b>${th?'การบังคับสิทธิ์ตาม Role':'Role permission enforcement'}</b><small>${th?'เมื่อเปิด ระบบจะซ่อนเมนูที่ไม่มีสิทธิ์และตรวจสิทธิ์ก่อนเข้าใช้งาน':'When enabled, unavailable modules are hidden and page access is checked.'}</small></div><label class="toggle"><input id="roleEnforceToggle" type="checkbox" ${settings.systemBehavior.enforceRolePermissions?'checked':''}><span></span></label></div><div class="info-box">${th?'จำนวนผู้ใช้งานสามารถแก้ไขได้ และระบบจะแสดงจำนวนบัญชี Login ที่พบจริงตามทีม/ตำแหน่งไว้ให้เปรียบเทียบ • การป้องกันข้อมูลระดับ Server ยังต้องใช้ Authentication + RLS ตอน Production':'User count can be edited; the system also shows the actual matched login accounts by team / position. Server-side protection still requires Authentication + RLS in production.'}</div><div id="roleList" class="role-card-list">${settings.roles.map(r=>roleCard(r)).join('')}</div><div class="button-row"><button type="button" class="soft-btn" onclick="addRoleRow()">＋ ${th?'เพิ่ม Role':'Add Role'}</button><button type="button" class="primary-btn" onclick="saveRoles()">${th?'บันทึกสิทธิ์':'Save permissions'}</button></div>`)
}window.openRolesSettingModal=openRolesSettingModal;
function addRoleRow(){roleList.insertAdjacentHTML('beforeend',roleCard({name:settings.language==='en'?'New Role':'Role ใหม่',teamScope:'',positionScope:'',permissions:['dashboard.view']}))}window.addRoleRow=addRoleRow;
function updateRoleUserCount(input){const card=input.closest('.role-card');const role={name:card.querySelector('[data-k=name]')?.value.trim()||'',teamScope:card.querySelector('[data-k=teamScope]')?.value||'',positionScope:card.querySelector('[data-k=positionScope]')?.value||''};card.querySelector('[data-actual-user-count]').textContent=roleUserCount(role)}window.updateRoleUserCount=updateRoleUserCount;
function updatePermissionSummary(el){const card=el.closest('.role-card'),p=[...card.querySelectorAll('[data-perm]:checked')].map(x=>x.dataset.perm);card.querySelector('[data-perm-summary]').textContent=rolePermissionSummary(p)}window.updatePermissionSummary=updatePermissionSummary;
function toggleRoleAll(btn,state){const card=btn.closest('.role-card');card.querySelectorAll('[data-perm]').forEach(x=>x.checked=state);updatePermissionSummary(card.querySelector('[data-perm]'))}window.toggleRoleAll=toggleRoleAll;
function applyStandardRolePreset(btn){const card=btn.closest('.role-card'),name=card.querySelector('[data-k=name]').value.trim(),perms=permissionPresetForRoleName(name);card.querySelectorAll('[data-perm]').forEach(x=>x.checked=perms.includes(x.dataset.perm));updatePermissionSummary(card.querySelector('[data-perm]'));toastMsg(settings.language==='en'?'Standard permission preset applied':'ใช้ชุดสิทธิ์มาตรฐานแล้ว')}window.applyStandardRolePreset=applyStandardRolePreset;
function saveRoles(){
 const rows=[...roleList.querySelectorAll('.role-card')].map(card=>{const name=card.querySelector('[data-k=name]').value.trim(),teamScope=card.querySelector('[data-k=teamScope]')?.value||'',positionScope=card.querySelector('[data-k=positionScope]')?.value||'',permissions=[...card.querySelectorAll('[data-perm]:checked')].map(x=>x.dataset.perm);return{name,teamScope,positionScope,users:Math.max(0,Number(card.querySelector('[data-k=users]')?.value)||0),permissions}}).filter(x=>x.name);
 if(!rows.length){toastMsg(settings.language==='en'?'At least one role is required':'ต้องมีอย่างน้อย 1 Role');return}
 if(rows.some(r=>!r.permissions.length)){toastMsg(settings.language==='en'?'Every role needs at least one permission':'ทุก Role ต้องมีสิทธิ์อย่างน้อย 1 รายการ');return}
 settings.roles=rows;settings.systemBehavior={...settings.systemBehavior,enforceRolePermissions:!!roleEnforceToggle.checked};saveLocal();cloudSaveSettings();closeModal();renderAll();toastMsg(settings.language==='en'?'Roles & permissions saved':'บันทึกผู้ใช้งานและสิทธิ์แล้ว')
}window.saveRoles=saveRoles;

function openSlaSettingModal(){
 const s=settings.sla;
 openModal('SLA & Workflow','กำหนดเวลาตอบรับ การแก้ไข การ Escalate และเวลาทำการ',`<form id="slaForm"><div class="form-grid cols3"><div class="field"><label>Urgent Response (นาที)</label><input id="slaUrgent" type="number" min="0" value="${s.urgentResponseMin}"></div><div class="field"><label>Resolution Target (ชม.)</label><input id="slaResolution" type="number" min="0" step=".5" value="${s.resolutionHours}"></div><div class="field"><label>Auto Escalate (นาที)</label><input id="slaEscalate" type="number" min="0" value="${s.autoEscalateMin}"></div><div class="field"><label>เริ่มเวลาทำการ</label><input id="slaStart" type="time" value="${esc(s.workStart)}"></div><div class="field"><label>สิ้นสุดเวลาทำการ</label><input id="slaEnd" type="time" value="${esc(s.workEnd)}"></div><div class="field"><label>Warranty งานซ่อม (วัน)</label><input id="slaWarranty" type="number" min="0" value="${s.repairWarrantyDays}"></div></div><div class="button-row"><button type="button" class="soft-btn" onclick="closeModal()">ยกเลิก</button><button class="primary-btn">บันทึก SLA</button></div></form>`,true);
 setTimeout(()=>slaForm.onsubmit=e=>{e.preventDefault();settings.sla={urgentResponseMin:+slaUrgent.value||0,resolutionHours:+slaResolution.value||0,autoEscalateMin:+slaEscalate.value||0,workStart:slaStart.value,workEnd:slaEnd.value,repairWarrantyDays:+slaWarranty.value||0};saveLocal();cloudSaveSettings();closeModal();renderAll();toastMsg('บันทึก SLA & Workflow แล้ว')},20)
}window.openSlaSettingModal=openSlaSettingModal;

function openCompanySettingModal(){
 openModal('ข้อมูลบริษัทและชื่อระบบ','ข้อมูลนี้ใช้เป็นค่ากลางและนำไปแสดงในเอกสาร/ใบเสนอราคา',`<form id="companyForm"><div class="form-grid cols2"><div class="field full"><label>ชื่อระบบ</label><input id="coSystem" value="${esc(settings.systemName)}"></div><div class="field"><label>ชื่อบริษัท (EN)</label><input id="coName" value="${esc(settings.companyName)}"></div><div class="field"><label>ชื่อบริษัท (TH)</label><input id="coNameTh" value="${esc(settings.companyNameTh||'')}"></div><div class="field"><label>อีเมล Service</label><input id="coEmail" value="${esc(settings.companyEmail)}"></div><div class="field"><label>โทรศัพท์</label><input id="coPhone" value="${esc(settings.companyPhone||'')}"></div><div class="field full"><label>ที่อยู่</label><textarea id="coAddress">${esc(settings.companyAddress||'')}</textarea></div><div class="field"><label>Tax ID</label><input id="coTax" value="${esc(settings.companyTaxId||'')}"></div><div class="field"><label>ภาษา / Locale</label><select id="coLocale"><option value="th-TH" ${settings.locale==='th-TH'?'selected':''}>ไทย (th-TH)</option><option value="en-US" ${settings.locale==='en-US'?'selected':''}>English (en-US)</option></select></div></div><div class="button-row"><button type="button" class="soft-btn" onclick="closeModal()">ยกเลิก</button><button class="primary-btn">บันทึก</button></div></form>`,true);
 setTimeout(()=>companyForm.onsubmit=e=>{e.preventDefault();settings.systemName=coSystem.value.trim();settings.companyName=coName.value.trim();settings.companyNameTh=coNameTh.value.trim();settings.companyEmail=coEmail.value.trim();settings.companyPhone=coPhone.value.trim();settings.companyAddress=coAddress.value.trim();settings.companyTaxId=coTax.value.trim();settings.locale=coLocale.value;saveLocal();cloudSaveSettings();closeModal();renderAll();toastMsg('บันทึกข้อมูลบริษัทแล้ว')},20)
}window.openCompanySettingModal=openCompanySettingModal;

function openServiceRateSettingModal(){
 const c=quoteCfg();
 openModal('Service Rate','แก้ไขอัตราค่าบริการที่ใช้คำนวณใบเสนอราคา',`<form id="serviceRateForm"><div class="form-grid cols2"><div class="field"><label>Onsite S</label><input id="srOnS" type="number" value="${c.onsiteS}"></div><div class="field"><label>Onsite L</label><input id="srOnL" type="number" value="${c.onsiteL}"></div><div class="field"><label>Workshop S</label><input id="srWsS" type="number" value="${c.workshopS}"></div><div class="field"><label>Workshop L</label><input id="srWsL" type="number" value="${c.workshopL}"></div><div class="field"><label>Diagnosis S</label><input id="srDgS" type="number" value="${c.diagnosisS}"></div><div class="field"><label>Diagnosis L</label><input id="srDgL" type="number" value="${c.diagnosisL}"></div><div class="field"><label>เครื่องถัดไป S</label><input id="srNxS" type="number" value="${c.nextS}"></div><div class="field"><label>เครื่องถัดไป L</label><input id="srNxL" type="number" value="${c.nextL}"></div><div class="field"><label>Extra Labor S / ชม.</label><input id="srLbS" type="number" value="${c.laborS}"></div><div class="field"><label>Extra Labor L / ชม.</label><input id="srLbL" type="number" value="${c.laborL}"></div></div><div class="button-row"><button type="button" class="soft-btn" onclick="closeModal()">ยกเลิก</button><button class="primary-btn">บันทึก Rate</button></div></form>`,true);
 setTimeout(()=>serviceRateForm.onsubmit=e=>{e.preventDefault();settings.quoteConfig={...c,onsiteS:+srOnS.value||0,onsiteL:+srOnL.value||0,workshopS:+srWsS.value||0,workshopL:+srWsL.value||0,diagnosisS:+srDgS.value||0,diagnosisL:+srDgL.value||0,nextS:+srNxS.value||0,nextL:+srNxL.value||0,laborS:+srLbS.value||0,laborL:+srLbL.value||0};saveLocal();cloudSaveSettings();closeModal();renderAll();toastMsg('บันทึก Service Rate แล้ว')},20)
}window.openServiceRateSettingModal=openServiceRateSettingModal;

function travelRow(z={}){return `<div class="travel-row"><input data-k="name" placeholder="Zone" value="${esc(z.name||'')}"><input data-k="min" type="number" min="0" placeholder="Min km" value="${z.min??0}"><input data-k="max" type="number" min="0" placeholder="Max km" value="${z.max??''}"><input data-k="fee" type="number" min="0" placeholder="Fee" value="${z.fee??0}"><input data-k="perKm" type="number" min="0" placeholder="บาท/km" value="${z.perKm??0}"><button class="danger-btn" onclick="this.parentElement.remove()">ลบ</button></div>`}
function openTravelSettingModal(){openModal('Travel Zone','แก้ไขช่วงระยะทาง ค่าเดินทางแบบเหมาจ่าย หรือบาทต่อกิโลเมตร',`<div class="setting-help" style="margin-bottom:9px">ถ้าใส่ “บาท/km” มากกว่า 0 ระบบจะใช้ระยะทาง × บาท/km แทนค่าเหมาจ่ายของ Zone นั้น</div><div id="travelList">${settings.travelZones.map(z=>travelRow(z)).join('')}</div><div class="button-row"><button class="soft-btn" onclick="addTravelZone()">＋ เพิ่ม Zone</button><button class="primary-btn" onclick="saveTravelZones()">บันทึก Travel Zone</button></div>`)}window.openTravelSettingModal=openTravelSettingModal;
function addTravelZone(){travelList.insertAdjacentHTML('beforeend',travelRow({name:'Z'+travelList.children.length,min:0,max:null,fee:0,perKm:0}))}window.addTravelZone=addTravelZone;
function saveTravelZones(){const rows=[...travelList.querySelectorAll('.travel-row')].map(r=>({name:r.querySelector('[data-k=name]').value.trim()||'Zone',min:+r.querySelector('[data-k=min]').value||0,max:r.querySelector('[data-k=max]').value===''?null:+r.querySelector('[data-k=max]').value,fee:+r.querySelector('[data-k=fee]').value||0,perKm:+r.querySelector('[data-k=perKm]').value||0})).sort((a,b)=>a.min-b.min);if(!rows.length){toastMsg('ต้องมี Travel Zone อย่างน้อย 1 รายการ');return}settings.travelZones=rows;saveLocal();cloudSaveSettings();closeModal();renderAll();toastMsg('บันทึก Travel Zone แล้ว')}window.saveTravelZones=saveTravelZones;

function openEmergencySettingModal(){
 const c=quoteCfg(),s=settings.sla;
 openModal('Emergency / Warranty','กำหนดค่าบวกงานเร่งด่วนและเงื่อนไข Warranty',`<form id="emergencyForm"><div class="form-grid cols2"><div class="field"><label>Urgent เพิ่ม (%)</label><input id="emUrgent" type="number" min="0" value="${c.urgentPct}"></div><div class="field"><label>Emergency เพิ่ม (%)</label><input id="emEmergency" type="number" min="0" value="${c.emergencyPct}"></div><div class="field"><label>Critical Pricing</label><select id="emCritical"><option value="manual" ${c.criticalMode==='manual'?'selected':''}>Manual Quote</option><option value="emergency" ${c.criticalMode==='emergency'?'selected':''}>ใช้ราคา Emergency</option></select></div><div class="field"><label>Repair Warranty (วัน)</label><input id="emWarranty" type="number" min="0" value="${s.repairWarrantyDays}"></div></div><div class="button-row"><button type="button" class="soft-btn" onclick="closeModal()">ยกเลิก</button><button class="primary-btn">บันทึก</button></div></form>`,true);
 setTimeout(()=>emergencyForm.onsubmit=e=>{e.preventDefault();settings.quoteConfig={...c,urgentPct:+emUrgent.value||0,emergencyPct:+emEmergency.value||0,criticalMode:emCritical.value};settings.sla={...s,repairWarrantyDays:+emWarranty.value||0};saveLocal();cloudSaveSettings();closeModal();renderAll();toastMsg('บันทึก Emergency / Warranty แล้ว')},20)
}window.openEmergencySettingModal=openEmergencySettingModal;

function openQuotationSettingModal(){
 const c=quoteCfg();
 openModal('Quotation / Parts','VAT, อายุใบเสนอราคา และ Margin Guideline ของอะไหล่',`<form id="quoteSettingForm"><div class="form-grid cols2"><div class="field"><label>VAT (%)</label><input id="setVat" type="number" min="0" value="${c.vatPct}"></div><div class="field"><label>อายุใบเสนอราคา (วัน)</label><input id="setValidity" type="number" min="1" value="${c.validityDays}"></div><div class="field"><label>Stock Markup (%)</label><input id="setMkStock" type="number" min="0" value="${c.markupStock}"></div><div class="field"><label>Special Markup (%)</label><input id="setMkSpecial" type="number" min="0" value="${c.markupSpecial}"></div><div class="field"><label>Imported Markup (%)</label><input id="setMkImported" type="number" min="0" value="${c.markupImported}"></div><div class="field"><label>High Value Markup (%)</label><input id="setMkHigh" type="number" min="0" value="${c.markupHigh}"></div></div><div class="button-row"><button type="button" class="soft-btn" onclick="closeModal()">ยกเลิก</button><button class="primary-btn">บันทึก</button></div></form>`,true);
 setTimeout(()=>quoteSettingForm.onsubmit=e=>{e.preventDefault();settings.quoteConfig={...c,vatPct:+setVat.value||0,validityDays:+setValidity.value||30,markupStock:+setMkStock.value||0,markupSpecial:+setMkSpecial.value||0,markupImported:+setMkImported.value||0,markupHigh:+setMkHigh.value||0};saveLocal();cloudSaveSettings();closeModal();renderAll();toastMsg('บันทึก Quotation / Parts แล้ว')},20)
}window.openQuotationSettingModal=openQuotationSettingModal;

function integrationLabel(v){const th=settings.language!=='en';return ({connected:th?'เชื่อมต่อแล้ว':'Connected',ready:th?'พร้อมเชื่อมต่อ':'Ready',active:th?'ใช้งานอยู่':'Active',plan:th?'วางแผนเชื่อมต่อ':'Planned',disabled:th?'ปิดใช้งาน':'Disabled'}[v]||v)}
function integrationSelect(id,val){const L=(th,en)=>settings.language==='en'?en:th;return `<select id="${id}"><option value="connected" ${val==='connected'?'selected':''}>${L('เชื่อมต่อแล้ว','Connected')}</option><option value="ready" ${val==='ready'?'selected':''}>${L('พร้อมเชื่อมต่อ','Ready')}</option><option value="active" ${val==='active'?'selected':''}>${L('ใช้งานอยู่','Active')}</option><option value="plan" ${val==='plan'?'selected':''}>${L('วางแผนเชื่อมต่อ','Planned')}</option><option value="disabled" ${val==='disabled'?'selected':''}>${L('ปิดใช้งาน','Disabled')}</option></select>`}
function openIntegrationSettingModal(){
 const i=settings.integrations;
 openModal('Integration','กำหนดสถานะ Integration ที่ใช้กับ Service System',`<form id="integrationForm"><div class="form-grid cols2"><div class="field"><label>LINE OA</label>${integrationSelect('intLine',i.lineOA)}</div><div class="field"><label>Google Maps</label>${integrationSelect('intMaps',i.googleMaps)}</div><div class="field"><label>Google Drive</label>${integrationSelect('intDrive',i.googleDrive)}</div><div class="field"><label>ERP / Accounting</label>${integrationSelect('intErp',i.erp)}</div></div><div class="info-box">สถานะนี้เป็น Configuration ฝั่ง UI สำหรับเตรียม Integration จริง โดย Token/API Credential ควรให้ IT จัดการใน Server/Cloud อย่างปลอดภัย</div><div class="button-row"><button type="button" class="soft-btn" onclick="closeModal()">ยกเลิก</button><button class="primary-btn">บันทึก</button></div></form>`,true);
 setTimeout(()=>integrationForm.onsubmit=e=>{e.preventDefault();settings.integrations={lineOA:intLine.value,googleMaps:intMaps.value,googleDrive:intDrive.value,erp:intErp.value};saveLocal();cloudSaveSettings();closeModal();renderAll();toastMsg('บันทึก Integration แล้ว')},20)
}window.openIntegrationSettingModal=openIntegrationSettingModal;

function openSystemBehaviorSettingModal(){
 const s=settings.systemBehavior,L=(th,en)=>settings.language==='en'?en:th;
 openModal(L('พฤติกรรมและความปลอดภัยระบบ','System Behavior & Security'),L('กำหนดการบันทึกอัตโนมัติ Audit การ Export และการบังคับสิทธิ์','Configure autosave, audit, export and permission enforcement'),`<form id="behaviorForm"><div class="toggle-list">${boolSwitch('bhAudit',L('บันทึกประวัติการแก้ไข','Audit Trail'),L('เก็บประวัติการเปลี่ยนแปลงข้อมูล','Track data changes'),s.auditTrail)}${boolSwitch('bhOffline',L('บันทึก Draft แบบ Offline','Offline Draft'),L('เก็บ Draft เมื่ออินเทอร์เน็ตไม่พร้อม','Keep draft when offline'),s.offlineDraft)}${boolSwitch('bhAutosave',L('บันทึกอัตโนมัติ','Autosave'),L('บันทึกข้อมูลระหว่างกรอกแบบอัตโนมัติ','Autosave while editing'),s.autosave)}${boolSwitch('bhGuard',L('เตือนข้อมูลที่ยังไม่บันทึก','Unsaved Guard'),L('เตือนก่อนออกจากหน้าที่ยังไม่บันทึก','Warn before leaving unsaved form'),s.unsavedGuard)}${boolSwitch('bhError',L('ติดตามข้อผิดพลาด','Error Tracking'),L('เก็บ Error ID เพื่อ Trace ปัญหา','Track error IDs'),s.errorTracking)}${boolSwitch('bhExport',L('อนุญาต Export','Allow Export'),L('อนุญาต PDF / CSV / Print','Allow PDF / CSV / Print'),s.exportEnabled)}${boolSwitch('bhRole',L('บังคับสิทธิ์ตาม Role','Enforce Role Permissions'),L('ซ่อนเมนูและตรวจสิทธิ์ตาม Checkbox ใน Users & Roles','Use permissions configured in Users & Roles'),s.enforceRolePermissions)}</div><div class="button-row" style="margin-top:14px"><button type="button" class="soft-btn" onclick="closeModal()">${L('ยกเลิก','Cancel')}</button><button class="primary-btn">${L('บันทึก','Save')}</button></div></form>`,true);
 setTimeout(()=>behaviorForm.onsubmit=e=>{e.preventDefault();settings.systemBehavior={auditTrail:bhAudit.checked,offlineDraft:bhOffline.checked,autosave:bhAutosave.checked,unsavedGuard:bhGuard.checked,errorTracking:bhError.checked,exportEnabled:bhExport.checked,enforceRolePermissions:bhRole.checked};saveLocal();cloudSaveSettings();closeModal();renderAll();toastMsg(L('บันทึกพฤติกรรมระบบแล้ว','System behavior saved'))},20)
}window.openSystemBehaviorSettingModal=openSystemBehaviorSettingModal;

function exportSettings(){
 const blob=new Blob([JSON.stringify(settings,null,2)],{type:'application/json'}),u=URL.createObjectURL(blob),a=document.createElement('a');a.href=u;a.download='I-MODE_Service_Settings.json';a.click();setTimeout(()=>URL.revokeObjectURL(u),1000)
}window.exportSettings=exportSettings;

function openReportPopup(type){let title='',items=[];if(type==='status'){title='เคสตามสถานะ';items=settings.statuses.map(s=>[s,cases.filter(c=>c.status===s).length])}if(type==='technician'){title='ภาระงาน Service Maintenance';items=technicians.map(t=>[t.name,openCases().filter(c=>c.assignee===t.id).length])}if(type==='customer'){title='เคสตามลูกค้า';items=customers.map(cu=>[cu.name,cases.filter(c=>c.customerId===cu.id).length]).sort((a,b)=>b[1]-a[1])}if(type==='machine'){title='เคสตามเครื่อง';items=machines.map(m=>[machinePrimaryName(m),cases.filter(c=>c.machineId===m.id).length]).sort((a,b)=>b[1]-a[1])}openModal(title,'สรุปข้อมูลจากระบบ',`<div class="related-list">${items.map(x=>`<div class="related-item"><b>${esc(x[0])}</b><small>${x[1]} รายการ</small></div>`).join('')}</div>`,true)}window.openReportPopup=openReportPopup;

function openGlobalSearch(){openModal('ค้นหาทั้งระบบ','ค้นหาเคส ลูกค้า เครื่อง และช่าง',`<div class="field"><input id="globalQ" placeholder="พิมพ์คำค้น..." oninput="runGlobalSearch()"></div><div id="globalResults" class="related-list"><div class="empty">เริ่มพิมพ์เพื่อค้นหา</div></div>`,true)}window.openGlobalSearch=openGlobalSearch;
function runGlobalSearch(){const q=globalQ.value.trim().toLowerCase();if(!q){globalResults.innerHTML='<div class="empty">เริ่มพิมพ์เพื่อค้นหา</div>';return}let r=[];cases.filter(c=>[c.ticket,c.customer,c.machine,c.serial,c.issue].join(' ').toLowerCase().includes(q)).slice(0,5).forEach(c=>r.push(`<div class="related-item" onclick="closeModal();openCaseDetail('${c.id}')" style="cursor:pointer"><b>📋 ${esc(c.ticket)} · ${esc(c.customer)}</b><small>${esc(c.machine)}</small></div>`));customers.filter(c=>c.name.toLowerCase().includes(q)).slice(0,5).forEach(c=>r.push(`<div class="related-item" onclick="closeModal();openCustomerDetail('${c.id}')" style="cursor:pointer"><b>👥 ${esc(c.name)}</b><small>${esc(c.contact||'')}</small></div>`));machines.filter(m=>[machineNameTh(m),machineNameEn(m),m.model,m.serial].join(' ').toLowerCase().includes(q)).slice(0,5).forEach(m=>r.push(`<div class="related-item" onclick="closeModal();openMachineDetail('${m.id}')" style="cursor:pointer"><b>⚙ ${esc(machinePrimaryName(m))}</b><small>${esc(m.model||'')} · ${esc(m.serial||'')}</small></div>`));quotations.filter(x=>[x.id,x.customer,x.caseTicket].join(' ').toLowerCase().includes(q)).slice(0,5).forEach(x=>r.push(`<div class="related-item" onclick="closeModal();loadQuotation('${x.id}')" style="cursor:pointer"><b>฿ ${esc(x.id)} · ${esc(x.customer)}</b><small>${money(x.grand)} · ${esc(x.status)}</small></div>`));globalResults.innerHTML=r.length?r.join(''):'<div class="empty">ไม่พบข้อมูล</div>'}window.runGlobalSearch=runGlobalSearch;
function openMoreSheet(){openModal('เมนูเพิ่มเติม','เลือกโมดูลที่ต้องการ',`<div class="more-sheet-grid"><button onclick="closeModal();goPage('quotation')">฿<b>ทำใบเสนอราคา</b><small>Service + Travel + Parts</small></button><button onclick="closeModal();goPage('customers')">👥<b>ลูกค้า</b><small>ฐานข้อมูลลูกค้า</small></button><button onclick="closeModal();goPage('machines')">⚙<b>เครื่องจักร</b><small>Machine Master</small></button><button onclick="closeModal();goPage('technicians')">👨‍🔧<b>ทีมช่าง</b><small>จัดการทีม</small></button><button onclick="closeModal();goPage('warranty')">🛡<b>ระบบประกันเครื่อง</b><small>Warranty / Certificate</small></button><button onclick="closeModal();goPage('documents')">📁<b>เอกสารเครื่องจักร</b><small>Manual / Drawing / SOP</small></button><button onclick="closeModal();goPage('notifications')">🔔<b>แจ้งเตือน</b><small>งานที่ต้องติดตาม</small></button><button onclick="closeModal();goPage('reports')">📊<b>รายงาน</b><small>KPI / Summary</small></button><button onclick="closeModal();goPage('settings')">⚙<b>ตั้งค่า</b><small>ระบบและ Cloud</small></button></div>`,true)}window.openMoreSheet=openMoreSheet;

function exportCSV(){if(!cases.length){toastMsg('ยังไม่มีข้อมูล');return}const cols=[['เลขเคส','ticket'],['วันที่รับ','createdAt'],['ลูกค้า','customer'],['เครื่อง','machine'],['Model','model'],['Serial','serial'],['ประเภทงาน','serviceType'],['Priority','priority'],['สถานะ','status'],['ช่าง','assignee'],['นัดหมาย','appointment'],['อาการ','issue']];const q=v=>`"${String(v??'').replace(/"/g,'""')}"`;const csv='\ufeff'+[cols.map(x=>q(x[0])).join(','),...cases.map(c=>cols.map(x=>q(x[1]==='assignee'?techById(c.assignee)?.name||'':c[x[1]])).join(','))].join('\n');const u=URL.createObjectURL(new Blob([csv],{type:'text/csv;charset=utf-8'})),a=document.createElement('a');a.href=u;a.download=`I-MODE_Service_${new Date().toISOString().slice(0,10)}.csv`;a.click();URL.revokeObjectURL(u)}window.exportCSV=exportCSV;
function exportExcel(){if(!cases.length){toastMsg(settings.language==='en'?'No data to export':'ยังไม่มีข้อมูล');return}const cols=[['Case No.','ticket'],['Created Date','createdAt'],['Customer','customer'],['Machine','machine'],['Model','model'],['Serial No.','serial'],['Service Type','serviceType'],['Priority','priority'],['Status','status'],['Technician','assignee'],['Appointment','appointment'],['Issue','issue']];const head=cols.map(c=>`<th style="background:#eaf0ff;border:1px solid #cfd9f0;padding:8px;text-align:left">${c[0]}</th>`).join('');const rows=cases.map(c=>`<tr>${cols.map(col=>{const val=col[1]==='assignee'?(techById(c.assignee)?.name||''):(c[col[1]]||'');return `<td style="border:1px solid #dbe3f2;padding:7px">${esc(String(val))}</td>`}).join('')}</tr>`).join('');const html=`<html><head><meta charset="utf-8"></head><body><table>${'<tr>'+head+'</tr>'}${rows}</table></body></html>`;const blob=new Blob(['\ufeff'+html],{type:'application/vnd.ms-excel;charset=utf-8'});const u=URL.createObjectURL(blob),a=document.createElement('a');a.href=u;a.download=`I-MODE_Service_${new Date().toISOString().slice(0,10)}.xls`;a.click();URL.revokeObjectURL(u)}window.exportExcel=exportExcel;



// ==================== V5.1 QUOTATION MODULE ====================
let quoteMachineLines=[];
let quotePartLines=[];
let quoteEditingId='';
let lastQuote={};

function money(n){
  return new Intl.NumberFormat(settings.language==='en'?'en-US':'th-TH',{minimumFractionDigits:2,maximumFractionDigits:2}).format(Math.max(0,Number(n)||0))+(settings.language==='en'?' THB':' บาท')
}
function quoteCfg(){ return settings.quoteConfig || defaults.quoteConfig }
function quoteNo(){
  const d=new Date(), p=`QT-SRV-${d.getFullYear()}${String(d.getMonth()+1).padStart(2,'0')}-`;
  let n=1; quotations.forEach(q=>{if(q.id?.startsWith(p))n=Math.max(n,(parseInt(q.id.split('-').pop())||0)+1)});
  return p+String(n).padStart(3,'0')
}
function quoteStatusPill(s){
  const cls=s==='อนุมัติ'?'approved':s==='ส่งแล้ว'||s==='รออนุมัติ'?'sent':s==='ไม่อนุมัติ'?'rejected':'';
  return `<span class="quote-status ${cls}">${esc(s||'ร่าง')}</span>`
}
function getQuoteTravel(distance){
  const d=Math.max(0,Number(distance)||0),zones=(settings.travelZones||defaults.travelZones).slice().sort((a,b)=>a.min-b.min);
  const z=zones.find(x=>d>=Number(x.min||0)&&(x.max===null||x.max===''||d<=Number(x.max)))||zones[zones.length-1]||{name:'Z0',min:0,max:null,fee:0,perKm:0};
  const fee=Number(z.perKm)>0?d*Number(z.perKm):Number(z.fee)||0;
  return {zone:String(z.name||'Z0').replace(/^Z/i,''),name:z.name||'Z0',fee}
}
function normalizeMachineSize(v){const s=String(v||'').trim().toUpperCase();return s==='S'||s==='M'||s==='L'?s:''}
function inferredMachineSize(m){
  const direct=normalizeMachineSize(m?.size);if(direct)return {size:direct,inferred:false,status:m?.sizeStatus||'Machine Master'};
  const model=String(m?.model||'').toUpperCase(),th=String(m?.nameTh||m?.name||''),en=String(m?.nameEn||'').toLowerCase(),key=(model+' '+th+' '+en).toLowerCase();
  if(en.includes('hydrogen gas generator')||th.includes('ไฮโดรเจน'))return {size:'L',inferred:true,status:'Hydrogen family'};
  if(en.includes('nitrogen gas generator')||th.includes('ไนโตรเจน'))return {size:'L',inferred:true,status:'Nitrogen family'};
  if(en.includes('laser marking')||th.includes('มาร์กเลเซอร์'))return {size:'S',inferred:true,status:'Laser Marking category'};
  if(en.includes('laser welding')||th.includes('เชื่อมเลเซอร์')||en==='welding machine'||th==='เครื่องเชื่อม')return {size:'L',inferred:true,status:'Laser/Welding category'};
  if(en.includes('centrifugal disc')||en.includes('gyratory polishing')||th.includes('ขัดระบบไจเรต'))return {size:'L',inferred:true,status:'Production polishing family'};
  if(en.includes('cnc')||key.includes('cnc')||key.includes('bh-mc300nstl'))return {size:'L',inferred:true,status:'CNC/Specialist family'};
  if(key.includes('mini auto diamond'))return {size:'S',inferred:true,status:'Provisional Small'};
  return {size:'',inferred:false,status:'Technical Confirm'};
}
function machineRate(m,service,isNext=false){
  const c=quoteCfg(), size=inferredMachineSize(m).size;if(service==='WP')return 0;
  if(size!=='S'&&size!=='L')return 0;
  if(service==='OS'&&isNext)return size==='L'?c.nextL:c.nextS;
  if(service==='OS')return size==='L'?c.onsiteL:c.onsiteS;
  if(service==='WS')return size==='L'?c.workshopL:c.workshopS;
  return size==='L'?c.diagnosisL:c.diagnosisS;
}
function setQuoteMachineSize(mid,size){const m=machineById(mid);if(!m)return;m.size=normalizeMachineSize(size);m.sizeStatus='Manual Confirm';m.serviceSize=m.size==='S'?'Small Machine':m.size==='M'?'Medium Machine':m.size==='L'?'Large Machine':'';m.sizeBasis='กำหนดจากหน้าใบเสนอราคาโดยผู้ใช้งาน';saveLocal();renderQuoteMachineLines();calcQuote();renderMachines()}window.setQuoteMachineSize=setQuoteMachineSize;
function quoteMarkup(type){
  const c=quoteCfg();
  return ({stock:c.markupStock,special:c.markupSpecial,imported:c.markupImported,high:c.markupHigh}[type]||0)/100;
}
function renderQuotations(){
  if(!document.getElementById('qCase'))return;
  const caseVal=qCase.value, customerVal=qCustomer.value;
  qCase.innerHTML='<option value="">ไม่อ้างอิงเคส</option>'+cases.map(c=>`<option value="${c.id}">${esc(c.ticket)} · ${esc(c.customer)} · ${esc(c.machine)}</option>`).join('');
  qCustomer.innerHTML='<option value="">เลือกลูกค้า</option>'+customers.map(c=>`<option value="${c.id}">${esc(c.name)}</option>`).join('');
  if(cases.some(c=>c.id===caseVal))qCase.value=caseVal;
  if(customers.some(c=>c.id===customerVal))qCustomer.value=customerVal;
  if(!quoteMachineLines.length && machines.length){
    const first=machines.find(m=>!qCustomer.value||m.customerId===qCustomer.value)||machines[0];
    if(first)quoteMachineLines=[first.id];
  }
  ensureQuoteSignerDefaults();quoteWarrantyChanged();renderQuoteMachineLines();renderQuotePartLines();renderQuotationList();calcQuote();
}
function renderQuotationList(){
  if(!document.getElementById('quotationTable'))return;
  const filtered=[...quotations].sort((a,b)=>new Date(b.updatedAt||b.createdAt)-new Date(a.updatedAt||a.createdAt));
  const info=paginateList('quotation',filtered,''),list=info.list;
  quotationTable.innerHTML=list.length?list.map(q=>`<tr><td><b class="ticket">${esc(q.id)}</b></td><td>${fmt(q.createdAt)}</td><td>${esc(q.caseTicket||'-')}</td><td>${esc(q.customer||'-')}</td><td><b>${money(q.grand)}</b></td><td>${quoteStatusPill(q.status)}</td><td><div class="row-actions"><button class="mini-btn" onclick="loadQuotation('${q.id}')">เปิด</button><button class="mini-btn" onclick="openQuotationStatus('${q.id}')">สถานะ</button></div></td></tr>`).join(''):`<tr><td colspan="7"><div class="empty">ยังไม่มีใบเสนอราคา</div></td></tr>`;
  quotationCards.innerHTML=list.length?list.map(q=>`<div class="mobile-data-card"><div class="card-top"><div><h4>${esc(q.id)}</h4><p>${esc(q.customer||'-')} · ${esc(q.caseTicket||'ไม่อ้างอิงเคส')}</p></div>${quoteStatusPill(q.status)}</div><p><b>${money(q.grand)}</b></p><div class="card-actions"><button class="soft-btn" onclick="loadQuotation('${q.id}')">เปิด</button><button class="primary-btn" onclick="openQuotationStatus('${q.id}')">เปลี่ยนสถานะ</button></div></div>`).join(''):'<div class="empty">ยังไม่มีใบเสนอราคา</div>';
  renderListPager('quotation',info,quotations.length);
}
let quoteDistanceSource='manual',quoteRouteDurationMinutes=0,googleMapsLoaderPromise=null;
function quoteTodayISO(){return new Date().toISOString().slice(0,10)}
function ensureQuoteSignerDefaults(){if(typeof qPreparedBy==='undefined')return;if(!qPreparedBy.value)qPreparedBy.value=currentUser?.name||'';if(!qAuthorizedBy.value)qAuthorizedBy.value=currentUser?.name||'';if(!qPreparedDate.value)qPreparedDate.value=quoteTodayISO();if(!qAuthorizedDate.value)qAuthorizedDate.value=quoteTodayISO()}
function quoteWarrantyChanged(){const mode=qWarranty.value||'none';if(typeof qWarrantyMonthsWrap!=='undefined')qWarrantyMonthsWrap.style.display=mode==='extend'?'block':'none';calcQuote()}window.quoteWarrantyChanged=quoteWarrantyChanged;
function markQuoteDistanceManual(){quoteDistanceSource='manual';quoteRouteDurationMinutes=0;if(typeof qRouteStatus!=='undefined')qRouteStatus.textContent='ระยะทางแก้ไขด้วยตนเอง'}window.markQuoteDistanceManual=markQuoteDistanceManual;
function googleRouteDestination(cu){if(!cu)return'';if(settings.mapConfig?.useCoordinatesFirst&&cu.latitude&&cu.longitude){const lat=Number(cu.latitude),lng=Number(cu.longitude);if(Number.isFinite(lat)&&Number.isFinite(lng))return{lat,lng}}return cu.address||cu.location||''}
function googleRouteOrigin(){const m=settings.mapConfig||{};const lat=Number(m.originLat),lng=Number(m.originLng);if(m.useCoordinatesFirst&&Number.isFinite(lat)&&Number.isFinite(lng)&&m.originLat!==''&&m.originLng!=='')return{lat,lng};return m.originAddress||settings.companyAddress||'241/46 Kanchanaphisek Rd., Dokmai, Prawes, Bangkok 10250'}
function quoteMapsPoint(p){return p&&typeof p==='object'?`${p.lat},${p.lng}`:String(p||'')}
function quoteDirectionsUrl(){const cu=customerById(qCustomer.value);if(!cu)return'';if(cu.mapUrl)return cu.mapUrl;const dest=quoteMapsPoint(googleRouteDestination(cu));if(!dest)return'';const origin=quoteMapsPoint(googleRouteOrigin());return `https://www.google.com/maps/dir/?api=1&travelmode=driving&destination=${encodeURIComponent(dest)}${origin?`&origin=${encodeURIComponent(origin)}`:''}`}
function openQuoteDirections(){const url=quoteDirectionsUrl();if(!url)return false;window.open(url,'_blank','noopener');return true}window.openQuoteDirections=openQuoteDirections;
function loadGoogleMapsRoutes(){const key=settings.mapConfig?.googleMapsApiKey||'';if(!key)return Promise.reject(new Error('ยังไม่ได้ตั้ง Google Maps API Key'));if(window.google?.maps?.importLibrary)return Promise.resolve(window.google.maps);if(googleMapsLoaderPromise)return googleMapsLoaderPromise;googleMapsLoaderPromise=new Promise((resolve,reject)=>{const existing=document.querySelector('script[data-imode-google-maps]');if(existing){existing.addEventListener('load',()=>resolve(window.google.maps),{once:true});existing.addEventListener('error',()=>reject(new Error('โหลด Google Maps ไม่สำเร็จ')),{once:true});return}const sc=document.createElement('script');sc.dataset.imodeGoogleMaps='1';sc.async=true;sc.defer=true;sc.src=`https://maps.googleapis.com/maps/api/js?key=${encodeURIComponent(key)}&v=weekly&loading=async`;sc.onload=()=>window.google?.maps?resolve(window.google.maps):reject(new Error('Google Maps Library ไม่พร้อม'));sc.onerror=()=>reject(new Error('โหลด Google Maps ไม่สำเร็จ'));document.head.appendChild(sc)});return googleMapsLoaderPromise}
async function calculateQuoteDistance(showToast=false){const cu=customerById(qCustomer.value);if(!cu){if(typeof qRouteStatus!=='undefined')qRouteStatus.textContent='กรุณาเลือกลูกค้าก่อน';if(showToast)toastMsg('กรุณาเลือกลูกค้าก่อน');return}const dest=googleRouteDestination(cu),origin=googleRouteOrigin();if(!dest){if(typeof qRouteStatus!=='undefined')qRouteStatus.textContent='ลูกค้ายังไม่มีที่อยู่/พิกัด';if(showToast)toastMsg('ลูกค้ายังไม่มีที่อยู่หรือพิกัด');return}if(!(settings.mapConfig?.googleMapsApiKey||'')){
   if(!showToast){if(typeof qRouteStatus!=='undefined')qRouteStatus.innerHTML=`กรอกระยะทางเอง หรือกดปุ่ม 📍 Google Maps เพื่อดูเส้นทาง · <button type="button" class="link-button" onclick="openLocationSettingModal()">ตั้งค่า API Key เพื่อคำนวณอัตโนมัติ</button>`;return}
   const opened=openQuoteDirections();
   if(typeof qRouteStatus!=='undefined')qRouteStatus.innerHTML=opened?`เปิด Google Maps ในแท็บใหม่แล้ว · อ่านระยะทางแล้วกรอกในช่อง (km) · <button type="button" class="link-button" onclick="openLocationSettingModal()">ตั้งค่า API Key เพื่อคำนวณอัตโนมัติ</button>`:`เปิด Google Maps ไม่ได้ · <button type="button" class="link-button" onclick="openLocationSettingModal()">ตั้งค่า Google Maps</button>`;
   toastMsg(opened?'เปิด Google Maps แล้ว · กรอกระยะทางที่ได้ในช่อง (km)':'เปิด Google Maps ไม่ได้ กรุณาตรวจสอบ Pop-up blocker');
   return}if(typeof qRouteStatus!=='undefined')qRouteStatus.textContent='กำลังคำนวณระยะทางจาก Google Maps...';try{await loadGoogleMapsRoutes();const {Route}=await google.maps.importLibrary('routes');const {routes}=await Route.computeRoutes({origin,destination:dest,travelMode:'DRIVING',routingPreference:'TRAFFIC_AWARE',fields:['distanceMeters','durationMillis']});const route=routes?.[0];if(!route||!Number.isFinite(route.distanceMeters))throw new Error('ไม่พบเส้นทาง');const km=Math.round((route.distanceMeters/1000)*10)/10;qDistance.value=km;quoteDistanceSource='google_maps';quoteRouteDurationMinutes=Math.round((Number(route.durationMillis)||0)/60000);if(typeof qRouteStatus!=='undefined')qRouteStatus.textContent=`Google Maps: ${km.toLocaleString('th-TH')} km เที่ยวเดียว${quoteRouteDurationMinutes?` · ประมาณ ${quoteRouteDurationMinutes} นาที`:''}`;calcQuote();if(showToast)toastMsg(`คำนวณระยะทาง ${km} km จาก Google Maps แล้ว`)}catch(err){quoteDistanceSource='manual';quoteRouteDurationMinutes=0;if(typeof qRouteStatus!=='undefined')qRouteStatus.innerHTML=`คำนวณอัตโนมัติไม่ได้: ${esc(err.message)} · <button type="button" class="link-button" onclick="openQuoteDirections()">เปิด Google Maps</button> · <button type="button" class="link-button" onclick="openLocationSettingModal()">ตั้งค่า Google Maps</button>`;if(showToast)toastMsg('Google Maps: '+err.message)}}window.calculateQuoteDistance=calculateQuoteDistance;
function quoteCustomerChanged(){
  const cu=customerById(qCustomer.value);
  if(cu){qContact.value=cu.contact||'';qLocation.value=cu.address||[cu.branch,cu.location].filter(Boolean).join(' / ')}
  const list=machines.filter(m=>m.customerId===qCustomer.value);
  quoteMachineLines=list.length?[list[0].id]:[];
  renderQuoteMachineLines();ensureQuoteSignerDefaults();calcQuote();
  if(cu&&settings.mapConfig?.enabled&&settings.mapConfig?.autoQuoteDistance!==false)calculateQuoteDistance(false)
}
window.quoteCustomerChanged=quoteCustomerChanged;

function loadCaseIntoQuote(cid){
  const c=cases.find(x=>x.id===cid);if(!c)return;
  qCustomer.value=c.customerId||'';
  qContact.value=c.contact||'';
  qLocation.value=customerById(c.customerId)?.address||[c.branch,c.location].filter(Boolean).join(' / ');
  const s=(c.serviceType||'').toLowerCase();
  qService.value=s.includes('workshop')||s.includes('ส่งเครื่อง')?'WS':s.includes('diagnosis')?'DG':'OS';
  qUrgency.value=c.priority==='ด่วนมาก'?'emergency':c.priority==='สูง'?'urgent':'normal';
  qWarranty.value=c.warranty==='อยู่ในประกัน'?'existing':'none';quoteWarrantyChanged();
  quoteMachineLines=c.machineId?[c.machineId]:[];
  renderQuoteMachineLines();ensureQuoteSignerDefaults();calcQuote();if(qCustomer.value&&settings.mapConfig?.enabled&&settings.mapConfig?.autoQuoteDistance!==false)calculateQuoteDistance(false)
}
window.loadCaseIntoQuote=loadCaseIntoQuote;

function prepareQuotation(cid=''){
  if(!requirePermission('quotation.view')||!requirePermission('quotation.create'))return;
  if(cid&&!cases.some(c=>c.id===cid)){toastMsg('ไม่พบเคส');return}
  if(cid&&(quoteEditingId||qCustomer.value||quoteMachineLines.length||quotePartLines.length)&&!confirm(settings.language==='en'?'Start a new quotation from this case? Unsaved changes in the quotation builder will be replaced.':'เริ่มใบเสนอราคาใหม่จากเคสนี้หรือไม่? ข้อมูลในฟอร์มใบเสนอราคาที่ยังไม่บันทึกจะถูกแทนที่'))return;
  closeModal();goPage('quotation');
  if(cid){resetQuote();qCase.value=cid;loadCaseIntoQuote(cid)}
}
window.prepareQuotation=prepareQuotation;

function addQuoteMachine(mid=''){
  let options=machines.filter(m=>!qCustomer.value||m.customerId===qCustomer.value);
  const m=mid||options[0]?.id||machines[0]?.id;
  if(!m){toastMsg('ยังไม่มีเครื่องจักรใน Machine Master');return}
  quoteMachineLines.push(m);renderQuoteMachineLines();calcQuote()
}
window.addQuoteMachine=addQuoteMachine;
function removeQuoteMachine(i){
  quoteMachineLines.splice(i,1);renderQuoteMachineLines();calcQuote()
}
window.removeQuoteMachine=removeQuoteMachine;
function setQuoteMachine(i,val){quoteMachineLines[i]=val;renderQuoteMachineLines();calcQuote()}window.setQuoteMachine=setQuoteMachine;

function renderQuoteMachineLines(){
  if(!document.getElementById('quoteMachineLines'))return;
  const options=machines.filter(m=>!qCustomer.value||m.customerId===qCustomer.value);
  quoteMachineLines=quoteMachineLines.filter(mid=>machines.some(m=>m.id===mid));
  document.getElementById('quoteMachineLines').innerHTML=quoteMachineLines.length?quoteMachineLines.map((mid,i)=>{
    const m=machineById(mid)||machines[0], info=inferredMachineSize(m), rate=machineRate(m,qService.value,i>0);
    const sizeCell=info.size?`<div class="quote-size ${info.size}" title="${esc(m?.sizeStatus||info.status||'')}">${esc(info.size)}</div>`:`<div class="quote-size missing"><select title="เลือก Size ก่อนคำนวณราคา" onchange="setQuoteMachineSize('${mid}',this.value)"><option value="">S/M/L</option><option value="S">S</option><option value="M">M</option><option value="L">L</option></select></div>`;
    return `<div class="quote-machine-line"><b>${i+1}</b><select onchange="setQuoteMachine(${i},this.value)">${options.map(x=>`<option value="${x.id}" ${x.id===mid?'selected':''}>${esc(machinePrimaryName(x))} · ${esc(x.model||'-')} · S/N ${esc(x.serial||'-')}</option>`).join('')}</select>${sizeCell}<div class="quote-machine-price">${money(rate)}</div><button class="quote-remove" onclick="removeQuoteMachine(${i})">×</button></div>`
  }).join(''):'<div class="empty">ยังไม่มีเครื่องในใบเสนอราคา</div>'
}

function quotePartsLocked(){return typeof qService!=='undefined'&&qService.value==='DG'}
function quoteServiceChanged(){const locked=quotePartsLocked();if(typeof quotePartsSection!=='undefined')quotePartsSection.classList.toggle('quote-parts-locked',locked);if(typeof quoteAddPartBtn!=='undefined')quoteAddPartBtn.disabled=locked;if(typeof quotePartsLockNote!=='undefined')quotePartsLockNote.style.display=locked?'block':'none';renderQuotePartLines();calcQuote()}window.quoteServiceChanged=quoteServiceChanged;
function addQuotePart(){if(quotePartsLocked()){toastMsg('Diagnosis Only ไม่อนุญาตให้แก้ไขหรือเพิ่ม Spare Parts');return}quotePartLines.push({name:'',cost:0,type:'stock'});renderQuotePartLines();calcQuote()}window.addQuotePart=addQuotePart;
function removeQuotePart(i){quotePartLines.splice(i,1);renderQuotePartLines();calcQuote()}window.removeQuotePart=removeQuotePart;
function updateQuotePart(i,key,val){quotePartLines[i][key]=key==='cost'?Number(val)||0:val;renderQuotePartLines();calcQuote()}window.updateQuotePart=updateQuotePart;
function renderQuotePartLines(){
  if(!document.getElementById('quotePartLines'))return;const locked=quotePartsLocked(),dis=locked?'disabled':'';
  document.getElementById('quotePartLines').innerHTML=quotePartLines.length?quotePartLines.map((p,i)=>{const sale=(Number(p.cost)||0)*(1+quoteMarkup(p.type));return `<div class="quote-part-line ${locked?'locked':''}"><input ${dis} placeholder="ชื่ออะไหล่" value="${esc(p.name)}" oninput="quotePartLines[${i}].name=this.value"><input ${dis} type="number" min="0" placeholder="ต้นทุน" value="${Number(p.cost)||0}" oninput="quotePartLines[${i}].cost=Number(this.value)||0;calcQuote()"><select ${dis} onchange="quotePartLines[${i}].type=this.value;calcQuote()"><option value="stock" ${p.type==='stock'?'selected':''}>Stock ${quoteCfg().markupStock}%</option><option value="special" ${p.type==='special'?'selected':''}>Special ${quoteCfg().markupSpecial}%</option><option value="imported" ${p.type==='imported'?'selected':''}>Imported ${quoteCfg().markupImported}%</option><option value="high" ${p.type==='high'?'selected':''}>High Value ${quoteCfg().markupHigh}%</option></select><b class="quote-part-price">${locked?money(0):money(sale)}</b><button ${dis} class="quote-remove" onclick="removeQuotePart(${i})">×</button></div>`}).join(''):`<div class="empty">${locked?'Diagnosis Only — ไม่ใช้รายการอะไหล่':'ยังไม่มีรายการอะไหล่'}</div>`
}
function calcQuote(){
  if(!document.getElementById('qService'))return;
  const cfg=quoteCfg(), service=qService.value, distance=Number(qDistance.value)||0, urgency=qUrgency.value, warrantyMode=qWarranty.value||'none', warranty=(warrantyMode==='existing'||warrantyMode==='repair'), vatOn=qVat.value==='yes';
  const t=getQuoteTravel(distance), warnings=[];
  let serviceFee=0;
  quoteMachineLines.forEach((mid,i)=>{
    const m=machineById(mid),info=inferredMachineSize(m);
    if(!info.size)warnings.push(`เครื่อง ${machinePrimaryName(m)||mid} ยังไม่ระบุ Size S/L — กรุณาเลือก S หรือ L ที่บรรทัดเครื่องก่อนออกใบเสนอราคา`);
    else if(info.inferred)warnings.push(`เครื่อง ${machinePrimaryName(m)||mid}: ระบบประเมิน Size ${info.size} อัตโนมัติ (${info.status}) — ควรยืนยันใน Machine Master`);
    else if(String(m?.sizeStatus||'').includes('Technical Confirm'))warnings.push(`เครื่อง ${machinePrimaryName(m)||mid}: Size ${info.size} เป็นค่าชั่วคราว — รอ Technical Confirm ก่อนส่งราคาจริง`);
    serviceFee+=machineRate(m,service,i>0)
  });
  const warrantyMonths=Number(qWarrantyMonths?.value||12);if(warrantyMode==='extend'){serviceFee=(Number(settings.warrantyConfig?.packagePrices?.[warrantyMonths])||0)*Math.max(1,quoteMachineLines.length);if(!serviceFee)warnings.push(`แพ็กเกจต่อประกัน ${warrantyMonths} เดือนยังไม่ได้ตั้งราคาใน Settings`)}
  const pct=urgency==='urgent'?cfg.urgentPct/100:urgency==='emergency'?cfg.emergencyPct/100:0;
  let emergency=warrantyMode==='extend'?0:serviceFee*pct, travel=(service==='OS'&&warrantyMode!=='extend')?t.fee:0;
  const pickupMode=Number(qPickup.value)||0;let pickup=pickupMode===3?t.fee*2:(pickupMode===1||pickupMode===2?t.fee:0);
  const isLarge=quoteMachineLines.some(mid=>inferredMachineSize(machineById(mid)).size==='L');
  let labor=(Number(qExtraHours.value)||0)*(Number(qExtraTechs.value)||0)*(isLarge?cfg.laborL:cfg.laborS);
  const parts=service==='DG'?0:quotePartLines.reduce((s,p)=>s+(Number(p.cost)||0)*(1+quoteMarkup(p.type)),0);if(service==='DG')warnings.push('Diagnosis Only: ล็อก Spare Parts และไม่คิดค่าอะไหล่ในใบเสนอราคา');
  const other=(Number(qToll.value)||0)+(Number(qOther.value)||0);
  const diagnosis=Number(qDiagnosisPaid.value)||0;
  if(warranty){serviceFee=0;emergency=0;travel=0;labor=0;warnings.push(warrantyMode==='repair'?`Warranty งานซ่อม ${settings.sla.repairWarrantyDays} วัน: Service / Travel / Labor = 0 แต่ค่าอะไหล่ยังไม่ยกเว้นอัตโนมัติ`:'อยู่ในประกันเดิมของเครื่อง: Service / Travel / Labor = 0 ตาม Logic ปัจจุบัน กรุณาตรวจ Coverage ก่อนส่งราคา')}if(warrantyMode==='extend'){emergency=0;travel=0;pickup=0;labor=0}
  const beforeDiscount=Math.max(0,serviceFee+emergency+travel+pickup+labor+parts+other-diagnosis);
  const discountPct=Math.min(100,Math.max(0,Number(qDiscount.value)||0));
  const discount=beforeDiscount*discountPct/100, subtotal=beforeDiscount-discount;
  const vat=vatOn?subtotal*(cfg.vatPct/100):0, grand=subtotal+vat;
  if(urgency==='critical')warnings.push('Critical Emergency: ต้องเสนอราคาเป็นกรณีและให้ผู้มีอำนาจอนุมัติ');
  qZoneBadge.textContent=`Zone ${t.zone} • ${money(t.fee)}`;
  qsService.textContent=money(serviceFee);qsEmergency.textContent=money(emergency);qsTravel.textContent=money(travel);
  qsPickup.textContent=money(pickup);qsLabor.textContent=money(labor);qsParts.textContent=money(parts);qsOther.textContent=money(other);
  qsDiagnosis.textContent='-'+money(diagnosis);qsDiscount.textContent='-'+money(discount);qsVat.textContent=money(vat);qsGrand.textContent=money(grand);
  qGrandBig.textContent='฿'+new Intl.NumberFormat('th-TH',{minimumFractionDigits:2,maximumFractionDigits:2}).format(grand);
  quoteWarnings.innerHTML=warnings.map(w=>`<div class="${w.startsWith('Critical')?'quote-error':'quote-warning'}">${esc(w)}</div>`).join('');
  const cu=customerById(qCustomer.value), c=cases.find(x=>x.id===qCase.value);
  lastQuote={caseId:qCase.value,caseTicket:c?.ticket||'',customerId:qCustomer.value,customer:cu?.name||'',contact:qContact.value,location:qLocation.value,service,warrantyMode,warrantyMonths:Number(qWarrantyMonths?.value||12),distance,distanceSource:quoteDistanceSource,routeDurationMinutes:quoteRouteDurationMinutes,pickupMode,urgency,warranty,vatOn,delivery:qDelivery?.value||'ตามตกลง',payment:qPayment?.value||'ตามเงื่อนไข',purchaserName:qPurchaserName?.value||'',purchaserDate:qPurchaserDate?.value||'',preparedBy:qPreparedBy?.value||currentUser?.name||'',preparedDate:qPreparedDate?.value||quoteTodayISO(),authorizedBy:qAuthorizedBy?.value||currentUser?.name||'',authorizedDate:qAuthorizedDate?.value||quoteTodayISO(),machineIds:[...quoteMachineLines],machineSizes:Object.fromEntries(quoteMachineLines.map(mid=>[mid,inferredMachineSize(machineById(mid)).size])),parts:JSON.parse(JSON.stringify(quotePartLines)),extraHours:Number(qExtraHours.value)||0,extraTechs:Number(qExtraTechs.value)||0,diagnosis,toll:Number(qToll.value)||0,otherExpense:Number(qOther.value)||0,discountPct,serviceFee,emergency,travel,pickup,labor,partsTotal:parts,other,discount,subtotal,vat,grand}
  return lastQuote
}
window.calcQuote=calcQuote;

function resetQuote(){
  quoteEditingId='';qCase.value='';qCustomer.value='';qContact.value='';qLocation.value='';qService.value='OS';qWarrantyMonths.value='12';qDistance.value=0;qPickup.value=0;qUrgency.value='normal';qWarranty.value='none';qVat.value='yes';quoteDistanceSource='manual';quoteRouteDurationMinutes=0;if(typeof qRouteStatus!=='undefined')qRouteStatus.textContent='เลือก customer เพื่อคำนวณระยะทางอัตโนมัติ';
  qExtraHours.value=0;qExtraTechs.value=0;qDiagnosisPaid.value=0;qToll.value=0;qOther.value=0;qDiscount.value=0;qDelivery.value='ตามตกลง';qPayment.value='ตามเงื่อนไข';qPurchaserName.value='';qPurchaserDate.value='';qPreparedBy.value=currentUser?.name||'';qAuthorizedBy.value=currentUser?.name||'';qPreparedDate.value=quoteTodayISO();qAuthorizedDate.value=quoteTodayISO();quoteMachineLines=[];quotePartLines=[];quoteEditLabel.textContent='ใบเสนอราคาใหม่';quoteWarrantyChanged();renderQuoteMachineLines();quoteServiceChanged();calcQuote()
}
window.resetQuote=resetQuote;

async function saveQuotation(){if(!requirePermission('quotation.create'))return;
  const q=calcQuote(), cu=customerById(q.customerId);
  if(!cu){toastMsg('กรุณาเลือกลูกค้า');return}
  if(!q.machineIds.length){toastMsg('กรุณาเพิ่มเครื่องจักรอย่างน้อย 1 เครื่อง');return}
  const old=quotations.find(x=>x.id===quoteEditingId);
  const rec={...q,id:old?.id||quoteNo(),status:old?.status||'ร่าง',createdAt:old?.createdAt||new Date().toISOString(),updatedAt:new Date().toISOString()};
  if(old)quotations=quotations.map(x=>x.id===rec.id?rec:x);else quotations.unshift(rec);
  quoteEditingId=rec.id;quoteEditLabel.textContent=rec.id+' · '+rec.status;
  notifications.unshift({id:uid(),icon:'฿',title:old?'อัปเดตใบเสนอราคา':'สร้างใบเสนอราคา',message:`${rec.id} · ${rec.customer} · ${money(rec.grand)}`,createdAt:new Date().toISOString(),read:false,caseId:rec.caseId||''});
  saveLocal();await cloudUpsertQuotation(rec);renderQuotationList();renderNotifications();toastMsg('บันทึก '+rec.id+' เรียบร้อย')
}
window.saveQuotation=saveQuotation;

function loadQuotation(id){
  const q=quotations.find(x=>x.id===id);if(!q)return;
  goPage('quotation');
  setTimeout(()=>{
    quoteEditingId=q.id;qCase.value=q.caseId||'';qCustomer.value=q.customerId||'';qContact.value=q.contact||'';qLocation.value=q.location||'';
    qService.value=q.service==='WP'?'OS':(q.service||'OS');qWarrantyMonths.value=String(q.warrantyMonths||12);qDistance.value=q.distance||0;quoteDistanceSource=q.distanceSource||'manual';quoteRouteDurationMinutes=q.routeDurationMinutes||0;qPickup.value=q.pickupMode||0;qUrgency.value=q.urgency||'normal';qWarranty.value=q.warrantyMode||(q.service==='WP'?'extend':(q.warranty?'repair':'none'));qVat.value=q.vatOn?'yes':'no';qDelivery.value=q.delivery||'ตามตกลง';qPayment.value=q.payment||'ตามเงื่อนไข';qPurchaserName.value=q.purchaserName||'';qPurchaserDate.value=q.purchaserDate||'';qPreparedBy.value=q.preparedBy||currentUser?.name||'';qPreparedDate.value=q.preparedDate||quoteTodayISO();qAuthorizedBy.value=q.authorizedBy||currentUser?.name||'';qAuthorizedDate.value=q.authorizedDate||quoteTodayISO();quoteWarrantyChanged();if(typeof qRouteStatus!=='undefined')qRouteStatus.textContent=quoteDistanceSource==='google_maps'?`Google Maps: ${q.distance||0} km เที่ยวเดียว${quoteRouteDurationMinutes?` · ประมาณ ${quoteRouteDurationMinutes} นาที`:''}`:'ระยะทางบันทึก/แก้ไขด้วยตนเอง';
    qExtraHours.value=q.extraHours||0;qExtraTechs.value=q.extraTechs||0;qDiagnosisPaid.value=q.diagnosis||0;qToll.value=q.toll||0;qOther.value=q.otherExpense||0;qDiscount.value=q.discountPct||0;
    quoteMachineLines=[...(q.machineIds||[])];quotePartLines=JSON.parse(JSON.stringify(q.parts||[]));quoteEditLabel.textContent=q.id+' · '+q.status;renderQuoteMachineLines();quoteServiceChanged();calcQuote();window.scrollTo({top:0,behavior:'smooth'})
  },80)
}
window.loadQuotation=loadQuotation;

function openQuotationStatus(id){if(!requirePermission('quotation.approve'))return;
  const q=quotations.find(x=>x.id===id);if(!q)return;
  openModal('สถานะใบเสนอราคา '+q.id,q.customer,`<div class="field"><label>สถานะ</label><select id="quoteStatusSelect">${settings.quotationStatuses.map(s=>`<option ${s===q.status?'selected':''}>${esc(s)}</option>`).join('')}</select></div><div class="button-row"><button class="soft-btn" onclick="closeModal()">ยกเลิก</button><button class="primary-btn" onclick="saveQuotationStatus('${q.id}')">บันทึกสถานะ</button></div>`,true)
}
window.openQuotationStatus=openQuotationStatus;
async function saveQuotationStatus(id){
  const q=quotations.find(x=>x.id===id);if(!q)return;q.status=quoteStatusSelect.value;q.updatedAt=new Date().toISOString();saveLocal();await cloudUpsertQuotation(q);closeModal();renderQuotationList();toastMsg('เปลี่ยนสถานะเป็น '+q.status)
}
window.saveQuotationStatus=saveQuotationStatus;

function quotationDocHTML(q,quoteId='DRAFT'){
  const cfg=quoteCfg(), lang=settings.language||'th', serviceName={OS:'Onsite Service',WS:'Workshop Service',DG:'Diagnosis Only'}[q.service]||q.service;
  const rows=[];
  (q.machineIds||[]).forEach((mid,i)=>{const m=machineById(mid);let fee=q.warranty?0:machineRate(m,q.service,i>0),desc=`${serviceName} - ${machinePrimaryName(m)||'-'}${m?.model?` / ${m.model}`:''}${inferredMachineSize(m).size?` / Size ${inferredMachineSize(m).size}`:''}`;if(q.warrantyMode==='extend'){fee=Number(settings.warrantyConfig?.packagePrices?.[Number(q.warrantyMonths||12)])||0;desc=`Extended Warranty ${q.warrantyMonths||12} Months - ${machinePrimaryName(m)||'-'}${m?.model?` / ${m.model}`:''}`};rows.push({no:i+1,desc,qty:1,unit:fee,amount:fee})});
  if(q.travel)rows.push({no:'',desc:'Travel & Fuel Charge',qty:1,unit:q.travel,amount:q.travel});
  if(q.pickup)rows.push({no:'',desc:'Pickup / Delivery',qty:1,unit:q.pickup,amount:q.pickup});
  if(q.labor)rows.push({no:'',desc:'Additional Labor',qty:1,unit:q.labor,amount:q.labor});
  if(q.service!=='DG')(q.parts||[]).forEach((p,idx)=>{const sale=(Number(p.cost)||0)*(1+quoteMarkup(p.type));if(sale)rows.push({no:'',desc:`Spare Part - ${p.name||'อะไหล่'}`,qty:1,unit:sale,amount:sale})});
  if(q.other)rows.push({no:'',desc:'Other Expense',qty:1,unit:q.other,amount:q.other});
  const rowHTML=rows.length?rows.map(r=>`<tr><td style="text-align:center">${esc(r.no)}</td><td>${esc(r.desc)}</td><td style="text-align:center">${esc(r.qty)}</td><td class="right">${money(r.unit)}</td><td class="right">${money(r.amount)}</td></tr>`).join(''):`<tr><td style="text-align:center">1</td><td>-</td><td style="text-align:center">1</td><td class="right">0.00 บาท</td><td class="right">0.00 บาท</td></tr>`;
  const formatQuoteDate=v=>{if(!v)return'...............';const d=new Date(v+'T00:00:00');return d.toLocaleDateString(uiLocale(),{day:'2-digit',month:'long',year:'numeric'})};const issueDate=formatQuoteDate(q.preparedDate);const preparedDate=formatQuoteDate(q.preparedDate),authorizedDate=formatQuoteDate(q.authorizedDate),purchaserDate=formatQuoteDate(q.purchaserDate);
  const validDate=new Date(Date.now()+cfg.validityDays*86400000).toLocaleDateString(uiLocale(),{day:'2-digit',month:'long',year:'numeric'});
  return `<div id="quotePreviewDoc" class="quote-doc official"><div class="quote-paper-header"><div><img src="${IMODE_LOGO}" class="quote-paper-logo"></div><div class="quote-paper-company"><b>${esc(lang==='en'?(settings.companyName||'I-MODE Plus Co., Ltd.'):(settings.companyNameTh||'บริษัท ไอโมด พลัส จำกัด'))}</b>${lang==='en'?'':`<br>${esc(settings.companyName||'I-MODE Plus Co., Ltd.')}`}<br>${esc(settings.companyAddress||'241/46 Kanchanaphisek Rd., Dok Mai Subdistrict, Prawet District, Bangkok 10250')}<br>Tel. ${esc(settings.companyPhone||'02-727-0130')} &nbsp; E-mail : ${esc(settings.companyEmail||'sales@imode.co.th')} &nbsp; URL : www.imode.co.th<br>${lang==='en'?'Head Office / Tax ID.':'สำนักงานใหญ่ / เลขประจำตัวผู้เสียภาษีอากร :'} ${esc(settings.companyTaxId||'-')}</div></div><div class="quote-paper-title">QUOTATION</div><div class="quote-paper-meta"><div class="quote-paper-block"><div class="row"><div class="label">Customer Name :</div><div class="value">${esc(q.contact||'-')}</div></div><div class="row"><div class="label">Company Name :</div><div class="value">${esc(q.customer||'-')}</div></div><div class="row"><div class="label">Address :</div><div class="value">${esc(q.location||'-')}</div></div><div class="row"><div class="label">Tax ID :</div><div class="value">-</div></div><div class="row"><div class="label">Email :</div><div class="value">-</div></div></div><div class="quote-paper-block"><div class="row"><div class="label">No :</div><div class="value">${esc(quoteId)}</div></div><div class="row"><div class="label">Date :</div><div class="value">${issueDate}</div></div><div class="row"><div class="label">Ref :</div><div class="value">${esc(currentUser?.name||'Service Department')}</div></div><div class="row"><div class="label">Email :</div><div class="value">${esc(settings.companyEmail||'sales@imode.co.th')}</div></div><div class="row"><div class="label">Tel :</div><div class="value">${esc(settings.companyPhone||'02-727-0130')}</div></div></div></div><div class="quote-paper-table"><table><thead><tr><th style="width:56px">No.</th><th>Description</th><th style="width:70px">Qty</th><th style="width:150px">Unit Price</th><th style="width:160px">Amount Total</th></tr></thead><tbody>${rowHTML}</tbody></table></div><div class="quote-paper-remark"><div class="label">Remark :</div><div>${lang==='en'?`Reference case ${esc(q.caseTicket||'-')} • Distance ${Number(q.distance||0).toLocaleString('en-US')} km (${q.distanceSource==='google_maps'?'Google Maps':'Manual'}) • ${q.warrantyMode==='extend'?`Extended Warranty ${q.warrantyMonths} months`:q.warrantyMode==='repair'?`Repair warranty ${settings.sla.repairWarrantyDays} days`:q.warrantyMode==='existing'?'Machine warranty active':'No warranty'} • Quotation valid until ${validDate}`:`อ้างอิงเคส ${esc(q.caseTicket||'-')} • ระยะทาง ${Number(q.distance||0).toLocaleString('th-TH')} km (${q.distanceSource==='google_maps'?'Google Maps':'Manual'}) • ${q.warrantyMode==='extend'?`ต่อประกัน ${q.warrantyMonths} เดือน`:q.warrantyMode==='repair'?`Warranty งานซ่อม ${settings.sla.repairWarrantyDays} วัน`:q.warrantyMode==='existing'?'อยู่ในประกันเดิมของเครื่อง':'ไม่เกี่ยวกับ Warranty'} • ใบเสนอราคามีอายุถึง ${validDate}`}</div></div><div class="quote-paper-summary"><div class="sum-row"><div style="text-align:right">Total</div><div><b>${money(q.subtotal)}</b></div></div><div class="sum-row"><div style="text-align:right">VAT ${cfg.vatPct}%</div><div><b>${money(q.vat)}</b></div></div><div class="sum-row total"><div style="text-align:right">Grand Total</div><div><b>${money(q.grand)}</b></div></div></div><div class="quote-paper-sign"><div><b>Term & Condition</b><div>Delivery : ${esc(q.delivery||'ตามตกลง')}</div><div>Payment : ${esc(q.payment||'ตามเงื่อนไข')}</div></div><div><div class="sign-line">Purchaser Signature</div><div style="text-align:center;margin-top:6px">${esc(q.purchaserName||'')}</div><div style="text-align:center;margin-top:4px">Date ${purchaserDate}</div></div><div><div class="sign-line">Customer Service</div><div style="text-align:center;margin-top:6px">${esc(q.preparedBy||currentUser?.name||'')}</div><div style="text-align:center">Date ${preparedDate}</div></div><div><div class="sign-line">Authorizer</div><div style="text-align:center;margin-top:6px">${esc(q.authorizedBy||currentUser?.name||'')}</div><div style="text-align:center">Date ${authorizedDate}</div></div></div><div class="quote-paper-footer">${lang==='en'?`We hope our proposal meets with your approval and look forward to receiving your confirmation.<br>Please sign and return this quotation to ${esc(settings.companyEmail||'sales@imode.co.th')} to confirm your order.`:`We certainly hope our proposal meet with your approval and hope to receive your confirmation soon.<br>กรุณาลงนามและส่งกลับมายัง ${esc(settings.companyEmail||'sales@imode.co.th')} เพื่อยืนยันใบเสนอราคานี้`}</div></div>`
}
function previewQuotation(){
  const q=calcQuote();if(!q.customerId){toastMsg('กรุณาเลือกลูกค้าก่อน');return}
  const id=quoteEditingId||'DRAFT';
  openModal('ตัวอย่างใบเสนอราคา',id,quotationDocHTML(q,id)+`<div class="button-row" style="margin-top:14px"><button class="soft-btn" onclick="printQuotation()">🖨 พิมพ์ / Save PDF</button><button class="primary-btn" onclick="closeModal();saveQuotation()">💾 บันทึก</button></div>`)
}
window.previewQuotation=previewQuotation;
function printQuotation(){
  const doc=document.getElementById('quotePreviewDoc');if(!doc)return;
  const w=window.open('','_blank','width=1100,height=860');
  w.document.write(`<!doctype html><html lang="th"><head><meta charset="utf-8"><title>Quotation</title><style>body{font-family:Arial,Tahoma,sans-serif;padding:18px;color:#1d232f}.quote-doc.official{max-width:1020px;margin:auto;border:1px solid #dadce8;padding:18px 22px;background:#fff}.quote-paper-header{display:grid;grid-template-columns:240px 1fr;gap:16px;align-items:start;padding-bottom:10px;border-bottom:2px solid #1748a5}.quote-paper-logo{width:210px;height:92px;object-fit:contain}.quote-paper-company{font-size:12px;line-height:1.5;color:#222}.quote-paper-company b{font-size:14px;color:#000}.quote-paper-title{text-align:center;font-size:22px;font-weight:800;letter-spacing:.02em;margin:6px 0 10px;color:#111}.quote-paper-meta{display:grid;grid-template-columns:1.2fr .9fr;gap:18px;margin-bottom:8px}.quote-paper-block .row{display:grid;grid-template-columns:140px 1fr;gap:8px;margin-bottom:6px;font-size:13px;align-items:start}.quote-paper-block .label{font-weight:700;color:#21306e}.quote-paper-block .value{color:#111;min-height:18px}.quote-paper-table table{width:100%;border-collapse:collapse;border:1px solid #444}.quote-paper-table th,.quote-paper-table td{border:1px solid #444;padding:7px 8px;font-size:12px;vertical-align:top}.quote-paper-table th{background:#f7f7fb;text-align:center;color:#111;font-weight:700}.quote-paper-table td.right{text-align:right}.quote-paper-remark{display:grid;grid-template-columns:90px 1fr;border-left:1px solid #444;border-right:1px solid #444;border-bottom:1px solid #444}.quote-paper-remark div{padding:8px;font-size:12px}.quote-paper-remark .label{font-weight:700;color:#21306e;border-right:1px solid #444}.quote-paper-summary{margin-left:auto;width:340px;border-left:1px solid #444;border-right:1px solid #444;border-bottom:1px solid #444}.quote-paper-summary .sum-row{display:grid;grid-template-columns:1fr 140px}.quote-paper-summary .sum-row>div{padding:6px 8px;border-top:1px solid #444;font-size:12px}.quote-paper-summary .sum-row b{text-align:right;display:block}.quote-paper-summary .sum-row.total{font-weight:800;color:#21306e}.quote-paper-sign{display:grid;grid-template-columns:1.1fr .9fr .9fr .9fr;border:1px solid #444;margin-top:12px}.quote-paper-sign>div{min-height:122px;padding:10px;border-right:1px solid #444;font-size:12px}.quote-paper-sign>div:last-child{border-right:0}.quote-paper-sign .sign-line{margin-top:42px;text-align:center;font-weight:700}.quote-paper-footer{margin-top:8px;font-size:11px;line-height:1.55;color:#333}@media print{body{padding:0}.quote-doc.official{border:0;max-width:none;margin:0}}</style></head><body>${doc.outerHTML}<script>window.onload=()=>window.print()<\/script></body></html>`);
  w.document.close()
}
window.printQuotation=printQuotation;

function openQuotationSettingModal(){
  const c=quoteCfg();
  openModal('ตั้งค่าใบเสนอราคา','แก้ไขอัตราที่ใช้คำนวณในโมดูลใบเสนอราคา',`<form id="quoteSettingForm"><div class="section-title">Service Rate</div><div class="form-grid cols2"><div class="field"><label>Onsite S</label><input id="setOnsiteS" type="number" value="${c.onsiteS}"></div><div class="field"><label>Onsite L</label><input id="setOnsiteL" type="number" value="${c.onsiteL}"></div><div class="field"><label>Workshop S</label><input id="setWorkshopS" type="number" value="${c.workshopS}"></div><div class="field"><label>Workshop L</label><input id="setWorkshopL" type="number" value="${c.workshopL}"></div><div class="field"><label>Diagnosis S</label><input id="setDiagnosisS" type="number" value="${c.diagnosisS}"></div><div class="field"><label>Diagnosis L</label><input id="setDiagnosisL" type="number" value="${c.diagnosisL}"></div><div class="field"><label>เครื่องถัดไป S</label><input id="setNextS" type="number" value="${c.nextS}"></div><div class="field"><label>เครื่องถัดไป L</label><input id="setNextL" type="number" value="${c.nextL}"></div></div><div class="section-title" style="margin-top:12px">Emergency / VAT</div><div class="form-grid cols3"><div class="field"><label>Urgent %</label><input id="setUrgent" type="number" value="${c.urgentPct}"></div><div class="field"><label>Emergency %</label><input id="setEmergency" type="number" value="${c.emergencyPct}"></div><div class="field"><label>VAT %</label><input id="setVat" type="number" value="${c.vatPct}"></div><div class="field"><label>อายุใบเสนอราคา (วัน)</label><input id="setValidity" type="number" value="${c.validityDays}"></div></div><div class="button-row"><button type="button" class="soft-btn" onclick="closeModal()">ยกเลิก</button><button class="primary-btn">บันทึก</button></div></form>`,true);
  setTimeout(()=>quoteSettingForm.onsubmit=e=>{e.preventDefault();settings.quoteConfig={...c,onsiteS:+setOnsiteS.value||0,onsiteL:+setOnsiteL.value||0,workshopS:+setWorkshopS.value||0,workshopL:+setWorkshopL.value||0,diagnosisS:+setDiagnosisS.value||0,diagnosisL:+setDiagnosisL.value||0,nextS:+setNextS.value||0,nextL:+setNextL.value||0,urgentPct:+setUrgent.value||0,emergencyPct:+setEmergency.value||0,vatPct:+setVat.value||0,validityDays:+setValidity.value||30};saveLocal();cloudSaveSettings();closeModal();renderQuotations();toastMsg('บันทึกอัตราใบเสนอราคาแล้ว')},20)
}
window.openQuotationSettingModal=openQuotationSettingModal;

async function cloudUpsertQuotation(q){
  if(!supa)return;
  await cloudUpsert('quotations',{id:q.id,case_id:q.caseId||null,case_ticket:q.caseTicket||'',customer_id:q.customerId||null,customer:q.customer||'',contact:q.contact||'',location:q.location||'',service:q.service,warranty_mode:q.warrantyMode||'none',warranty_months:q.warrantyMonths||12,distance:q.distance,distance_source:q.distanceSource||'manual',route_duration_minutes:q.routeDurationMinutes||0,pickup_mode:q.pickupMode,urgency:q.urgency,warranty:q.warranty,vat_on:q.vatOn,delivery:q.delivery||'',payment:q.payment||'',purchaser_name:q.purchaserName||'',purchaser_date:q.purchaserDate||null,prepared_by:q.preparedBy||'',prepared_date:q.preparedDate||null,authorized_by:q.authorizedBy||'',authorized_date:q.authorizedDate||null,machine_ids:q.machineIds||[],parts:q.parts||[],extra_hours:q.extraHours||0,extra_techs:q.extraTechs||0,diagnosis:q.diagnosis||0,toll:q.toll||0,other_expense:q.otherExpense||0,discount_pct:q.discountPct||0,subtotal:q.subtotal||0,vat:q.vat||0,grand:q.grand||0,status:q.status||'ร่าง',created_at:q.createdAt,updated_at:q.updatedAt})
}
function fromQuotationDb(q){
  return {id:q.id,caseId:q.case_id||'',caseTicket:q.case_ticket||'',customerId:q.customer_id||'',customer:q.customer||'',contact:q.contact||'',location:q.location||'',service:q.service||'OS',warrantyMode:q.warranty_mode||(q.warranty?'repair':'none'),warrantyMonths:q.warranty_months||12,distance:q.distance||0,distanceSource:q.distance_source||'manual',routeDurationMinutes:q.route_duration_minutes||0,pickupMode:q.pickup_mode||0,urgency:q.urgency||'normal',warranty:!!q.warranty,vatOn:q.vat_on!==false,delivery:q.delivery||'',payment:q.payment||'',purchaserName:q.purchaser_name||'',purchaserDate:q.purchaser_date||'',preparedBy:q.prepared_by||'',preparedDate:q.prepared_date||'',authorizedBy:q.authorized_by||'',authorizedDate:q.authorized_date||'',machineIds:q.machine_ids||[],parts:q.parts||[],extraHours:q.extra_hours||0,extraTechs:q.extra_techs||0,diagnosis:q.diagnosis||0,toll:q.toll||0,otherExpense:q.other_expense||0,discountPct:q.discount_pct||0,subtotal:q.subtotal||0,vat:q.vat||0,grand:q.grand||0,status:q.status||'ร่าง',createdAt:q.created_at,updatedAt:q.updated_at}
}


async function cloudUpsertWarranty(w){if(!supa)return;await cloudUpsert('machine_warranties',{id:w.id,warranty_no:w.warrantyNo,customer_id:w.customerId||null,machine_id:w.machineId||null,purchase_date:w.purchaseDate||null,install_date:w.installDate||null,start_date:w.startDate||null,end_date:w.endDate||null,months:w.months||0,coverage:w.coverage||'',exclusions:w.exclusions||'',note:w.note||'',issued_by:w.issuedBy||'',attachment_name:w.attachmentName||'',attachment_type:w.attachmentType||'',attachment_data:w.attachmentData||'',created_at:w.createdAt,updated_at:w.updatedAt})}
function fromWarrantyDb(w){return{id:w.id,warrantyNo:w.warranty_no,customerId:w.customer_id||'',machineId:w.machine_id||'',purchaseDate:w.purchase_date||'',installDate:w.install_date||'',startDate:w.start_date||'',endDate:w.end_date||'',months:w.months||0,coverage:w.coverage||'',exclusions:w.exclusions||'',note:w.note||'',issuedBy:w.issued_by||'',attachmentName:w.attachment_name||'',attachmentType:w.attachment_type||'',attachmentData:w.attachment_data||'',createdAt:w.created_at,updatedAt:w.updated_at}}
async function cloudUpsertMachineDocument(d){if(!supa)return;await cloudUpsert('machine_documents',{id:d.id,title:d.title,category:d.category,machine_id:d.machineId||null,customer_id:d.customerId||null,version:d.version||'',language:d.language||'',doc_date:d.docDate||null,url:d.url||'',note:d.note||'',file_name:d.fileName||'',file_type:d.fileType||'',file_data:d.fileData||'',created_at:d.createdAt,updated_at:d.updatedAt})}
function fromMachineDocumentDb(d){return{id:d.id,title:d.title,category:d.category,machineId:d.machine_id||'',customerId:d.customer_id||'',version:d.version||'',language:d.language||'',docDate:d.doc_date||'',url:d.url||'',note:d.note||'',fileName:d.file_name||'',fileType:d.file_type||'',fileData:d.file_data||'',createdAt:d.created_at,updatedAt:d.updated_at}}


async function cloudUpsertLineRequest(r){if(!supa)return;await cloudUpsert('line_customer_requests',{id:r.id,type:r.type,status:r.status,customer_id:r.customerId||null,machine_id:r.machineId||null,case_id:r.caseId||null,contact:r.contact||'',phone:r.phone||'',priority:r.priority||'',months:r.months||0,message:r.message||'',line_user_id:r.lineUserId||'',line_display_name:r.lineDisplayName||'',created_at:r.createdAt})}
async function cloudUpsertServiceReport(r){if(!supa)return;await cloudUpsert('service_reports',{id:r.id,report_no:r.reportNo,case_id:r.caseId||null,customer_id:r.customerId||null,machine_id:r.machineId||null,tech_id:r.techId||null,service_type:r.serviceType||'',work_type:r.workType||'',check_in_at:r.checkInAt||null,appointment:r.appointment||null,checklist:r.checklist||[],diagnosis:r.diagnosis||'',work_performed:r.workPerformed||'',recommendation:r.recommendation||'',parts:r.parts||[],before_photos:r.beforePhotos||[],after_photos:r.afterPhotos||[],videos:r.videos||[],status_log:r.statusLog||[],customer_accept:r.customerAccept||'',customer_signature:r.customerSignature||'',tech_signature:r.techSignature||'',next_pm:r.nextPm||null,line_sent_at:r.lineSentAt||null,line_send_status:r.lineSendStatus||'',status:r.status||'Completed',created_at:r.createdAt,updated_at:r.updatedAt})}
function fromLineRequestDb(r){return{id:r.id,type:r.type,status:r.status,customerId:r.customer_id||'',machineId:r.machine_id||'',caseId:r.case_id||'',contact:r.contact||'',phone:r.phone||'',priority:r.priority||'',months:r.months||0,message:r.message||'',lineUserId:r.line_user_id||'',lineDisplayName:r.line_display_name||'',createdAt:r.created_at}}
function fromServiceReportDb(r){return{id:r.id,reportNo:r.report_no,caseId:r.case_id||'',customerId:r.customer_id||'',machineId:r.machine_id||'',techId:r.tech_id||'',serviceType:r.service_type||'',workType:r.work_type||'',checkInAt:r.check_in_at||'',appointment:r.appointment||'',checklist:r.checklist||[],diagnosis:r.diagnosis||'',workPerformed:r.work_performed||'',recommendation:r.recommendation||'',parts:r.parts||[],beforePhotos:r.before_photos||[],afterPhotos:r.after_photos||[],videos:r.videos||[],statusLog:r.status_log||[],customerAccept:r.customer_accept||'',customerSignature:r.customer_signature||'',techSignature:r.tech_signature||'',nextPm:r.next_pm||'',lineSentAt:r.line_sent_at||'',lineSendStatus:r.line_send_status||'',status:r.status||'Completed',createdAt:r.created_at,updatedAt:r.updated_at}}
async function initCloud(){if(!cloudSettings.url||!cloudSettings.key||typeof window.supabase==='undefined'){updateCloudUI(false);return}try{supa=window.supabase.createClient(cloudSettings.url,cloudSettings.key);const {error}=await supa.from('service_cases').select('id').limit(1);if(error)throw error;updateCloudUI(true);await syncCloud(true)}catch(e){supa=null;updateCloudUI(false,e.message)}}
function updateCloudUI(ok,msg=''){if(typeof cloudDot!=='undefined')cloudDot.className='cloud-dot '+(ok?'online':'offline');if(typeof cloudLabel!=='undefined')cloudLabel.textContent=ok?'Cloud Connected':'Local Mode';if(typeof cloudSub!=='undefined')cloudSub.textContent=ok?'ข้อมูลกลางพร้อมใช้งาน':'ข้อมูลอยู่บนอุปกรณ์นี้';systemStatus.innerHTML=`<div class="status-box"><small>โหมดข้อมูล</small><b>${ok?'Cloud + Local Cache':'Local Storage'}</b></div><div class="status-box"><small>เคส</small><b>${cases.length}</b></div><div class="status-box"><small>ลูกค้า</small><b>${customers.length}</b></div><div class="status-box"><small>เครื่อง</small><b>${machines.length}</b></div><div class="status-box"><small>ช่าง</small><b>${technicians.length}</b></div><div class="status-box"><small>ใบเสนอราคา</small><b>${quotations.length}</b></div><div class="status-box"><small>Warranty</small><b>${warranties.length}</b></div><div class="status-box"><small>เอกสาร</small><b>${machineDocuments.length}</b></div><div class="status-box"><small>LINE Requests</small><b>${lineRequests.length}</b></div><div class="status-box"><small>Service Reports</small><b>${serviceReports.length}</b></div>`;cloudResult.textContent=msg?`เชื่อมต่อไม่สำเร็จ: ${msg}`:(ok?'เชื่อมต่อ Cloud สำเร็จ':'ยังไม่ได้เชื่อม Cloud')}
async function saveCloudSettings(){const url=supabaseUrl.value.trim(),key=supabaseKey.value.trim();if(!url||!key){toastMsg('กรุณากรอก URL และ Key');return}cloudSettings={url,key};localStorage.setItem(K.cloud,JSON.stringify(cloudSettings));await initCloud();toastMsg(supa?'เชื่อม Cloud สำเร็จ':'เชื่อม Cloud ไม่สำเร็จ')}window.saveCloudSettings=saveCloudSettings;
function disconnectCloud(){supa=null;cloudSettings={};localStorage.removeItem(K.cloud);supabaseUrl.value='';supabaseKey.value='';updateCloudUI(false);toastMsg('กลับมาใช้ Local แล้ว')}window.disconnectCloud=disconnectCloud;

async function cloudUpsertCustomer(c){if(!supa)return;await cloudUpsert('customers',{id:c.id,name:c.name,branch:c.branch||'',contact:c.contact||'',phone:c.phone||'',email:c.email||'',location:c.location||'',address:c.address||'',map_url:c.mapUrl||'',latitude:c.latitude===''?null:Number(c.latitude),longitude:c.longitude===''?null:Number(c.longitude),note:c.note||'',line_user_id:c.lineUserId||'',line_display_name:c.lineDisplayName||'',line_linked_at:c.lineLinkedAt||null})}
function fromCustomerDb(c){return{id:c.id,name:c.name,branch:c.branch||'',contact:c.contact||'',phone:c.phone||'',email:c.email||'',location:c.location||'',address:c.address||'',mapUrl:c.map_url||'',latitude:c.latitude??'',longitude:c.longitude??'',note:c.note||'',lineUserId:c.line_user_id||'',lineDisplayName:c.line_display_name||'',lineLinkedAt:c.line_linked_at||''}}

async function cloudUpsert(table,obj){if(!supa)return;try{const {error}=await supa.from(table).upsert(obj);if(error)throw error}catch(e){console.warn(e)}}
async function cloudUpsertCase(c){if(!supa)return;await cloudUpsert('service_cases',{id:c.id,ticket:c.ticket,created_at:c.createdAt,updated_at:c.updatedAt,customer_id:c.customerId,customer:c.customer,contact:c.contact,phone:c.phone,email:c.email,location:c.location,branch:c.branch,map_url:c.mapUrl||'',latitude:c.latitude===''?null:Number(c.latitude),longitude:c.longitude===''?null:Number(c.longitude),field_status:c.fieldStatus||'',field_status_log:c.fieldStatusLog||[],machine_id:c.machineId,machine:c.machine,model:c.model,serial:c.serial,machine_size:c.machineSize,warranty:c.warranty,channel:c.channel,service_type:c.serviceType,priority:c.priority,issue:c.issue,note:c.note,status:c.status,assignee:c.assignee||null,appointment:c.appointment||null})}
async function cloudSaveSettings(){if(!supa)return;await cloudUpsert('system_settings',{id:'main',data:settings,updated_at:new Date().toISOString()})}
function fromCaseDb(c){return {id:c.id,ticket:c.ticket,createdAt:c.created_at,updatedAt:c.updated_at,customerId:c.customer_id,customer:c.customer,contact:c.contact,phone:c.phone,email:c.email,location:c.location,branch:c.branch,mapUrl:c.map_url||'',latitude:c.latitude??'',longitude:c.longitude??'',fieldStatus:c.field_status||'',fieldStatusLog:c.field_status_log||[],machineId:c.machine_id,machine:c.machine,model:c.model,serial:c.serial,machineSize:c.machine_size,warranty:c.warranty,channel:c.channel,serviceType:c.service_type,priority:c.priority,issue:c.issue,note:c.note,status:c.status,assignee:c.assignee||'',appointment:c.appointment||''}}
async function syncCloud(silent=false){if(!supa){if(!silent)toastMsg('ยังไม่ได้เชื่อม Cloud');return}try{const [a,b,c,d,e,f,g,h,i,j,k]=await Promise.all([supa.from('service_cases').select('*'),supa.from('technicians').select('*'),supa.from('customers').select('*'),supa.from('machines').select('*'),supa.from('notifications').select('*'),supa.from('system_settings').select('*').eq('id','main').maybeSingle(),supa.from('quotations').select('*'),supa.from('machine_warranties').select('*'),supa.from('machine_documents').select('*'),supa.from('line_customer_requests').select('*'),supa.from('service_reports').select('*')]);if(!a.error&&a.data.length)cases=a.data.map(fromCaseDb);if(!b.error&&b.data.length)technicians=b.data.map(t=>({photo:'',status:'พร้อมรับงาน',skills:'',team:'Technical',...t}));if(!c.error&&c.data.length)customers=c.data.map(fromCustomerDb);if(!d.error&&d.data.length)machines=d.data.map(m=>({photo:'',...m}));if(!e.error&&e.data.length)notifications=e.data.map(n=>({id:n.id,icon:n.icon,title:n.title,message:n.message,createdAt:n.created_at,read:n.is_read,caseId:n.case_id}));if(!f.error&&f.data?.data)settings=mergeSettings(f.data.data);if(!g.error&&g.data.length)quotations=g.data.map(fromQuotationDb);if(!h.error&&h.data.length)warranties=h.data.map(fromWarrantyDb);if(!i.error&&i.data.length)machineDocuments=i.data.map(fromMachineDocumentDb);if(!j.error&&j.data.length)lineRequests=j.data.map(fromLineRequestDb);if(!k.error&&k.data.length)serviceReports=k.data.map(fromServiceReportDb);saveLocal();renderAll();if(!silent)toastMsg('ซิงก์ Cloud แล้ว')}catch(e){if(!silent)toastMsg('ซิงก์ไม่สำเร็จ')}}
window.syncCloud=syncCloud;

function renderSettings(){supabaseUrl.value=cloudSettings.url||'';supabaseKey.value=cloudSettings.key||'';const c=quoteCfg(),s=settings.sla,n=settings.notificationConfig,i=settings.integrations,b=settings.systemBehavior,lang=settings.language||'th',L=(th,en)=>lang==='en'?en:th;const roleUsers=settings.roles.reduce((sum,r)=>sum+(Number(r.users)||0),0);if(typeof setRolesSummary!=='undefined')setRolesSummary.textContent=L(`${settings.roles.length} บทบาท · ${roleUsers} ผู้ใช้งาน`,`${settings.roles.length} roles · ${roleUsers} users`);if(typeof setSlaSummary!=='undefined')setSlaSummary.textContent=L(`งานด่วน ${s.urgentResponseMin} นาที · แก้ไข ${s.resolutionHours} ชม.`,`Urgent ${s.urgentResponseMin} min · Resolution ${s.resolutionHours} hr`);if(typeof setMasterSummary!=='undefined')setMasterSummary.textContent=L(`${settings.serviceTypes.length} ประเภทงาน · ${settings.statuses.length} สถานะ · ${settings.priorities.length} ระดับ`,`${settings.serviceTypes.length} service types · ${settings.statuses.length} statuses · ${settings.priorities.length} priorities`);if(typeof setLanguageSummary!=='undefined')setLanguageSummary.textContent=lang==='en'?'English · Thai available':'ภาษาไทย · สลับ English ได้';if(typeof setTypographySummary!=='undefined'){const fs=currentFontScale();setTypographySummary.textContent=`${fs}% · ${fontScaleLabel(fs)}`;}if(typeof setCompanySummary!=='undefined')setCompanySummary.textContent=settings.companyNameTh||settings.companyName;if(typeof setRateSummary!=='undefined')setRateSummary.textContent=`Onsite S ${Number(c.onsiteS).toLocaleString()} · L ${Number(c.onsiteL).toLocaleString()} ${L('บาท','THB')}`;if(typeof setTravelSummary!=='undefined')setTravelSummary.textContent=L(`${settings.travelZones.length} โซน · ${settings.travelZones.at(-1)?.perKm||0} บาท/km`,`${settings.travelZones.length} zones · ${settings.travelZones.at(-1)?.perKm||0} THB/km`);if(typeof setEmergencySummary!=='undefined')setEmergencySummary.textContent=`Urgent +${c.urgentPct}% · Emergency +${c.emergencyPct}% · Warranty ${s.repairWarrantyDays} ${L('วัน','days')}`;if(typeof setQuoteSummary!=='undefined')setQuoteSummary.textContent=`VAT ${c.vatPct}% · ${c.validityDays} ${L('วัน','days')} · Imported +${c.markupImported}%`;if(typeof setFieldSummary!=='undefined')setFieldSummary.textContent=L(`เตือน ${settings.fieldServiceConfig.appointmentReminderHours} ชม. · รูป ${settings.fieldServiceConfig.maxPhotosEach}/ชุด`,`Reminder ${settings.fieldServiceConfig.appointmentReminderHours} hr · ${settings.fieldServiceConfig.maxPhotosEach} photos/set`);if(typeof setLocationSummary!=='undefined')setLocationSummary.textContent=`Maps ${settings.mapConfig.enabled?'ON':'OFF'} · GPS ${settings.mapConfig.checkInGps?'ON':'OFF'}`;if(typeof setWarrantyModuleSummary!=='undefined')setWarrantyModuleSummary.textContent=L(`${settings.warrantyConfig.defaultMonths} เดือน · เตือน ${settings.warrantyConfig.reminderDays} วัน`,`${settings.warrantyConfig.defaultMonths} months · remind ${settings.warrantyConfig.reminderDays} days`);if(typeof setDocumentModuleSummary!=='undefined')setDocumentModuleSummary.textContent=L(`${settings.documentCategories.length} หมวด · Upload ${settings.documentConfig.allowUpload?'ON':'OFF'}`,`${settings.documentCategories.length} categories · Upload ${settings.documentConfig.allowUpload?'ON':'OFF'}`);if(typeof setMediaSummary!=='undefined')setMediaSummary.textContent=L(`รูป ${settings.mediaConfig.maxPhotosEach}/ชุด · วิดีโอ ${settings.mediaConfig.maxVideos} · ${settings.mediaConfig.maxVideoMB} MB`,`Photos ${settings.mediaConfig.maxPhotosEach}/set · Videos ${settings.mediaConfig.maxVideos} · ${settings.mediaConfig.maxVideoMB} MB`);if(typeof setReportExportSummary!=='undefined')setReportExportSummary.textContent=`${settings.reportConfig.defaultFormat} · LINE ${settings.reportConfig.lineSendEnabled?'ON':'OFF'} · Signature ${settings.reportConfig.includeSignatures?'ON':'OFF'}`;if(typeof setNotificationSummary!=='undefined')setNotificationSummary.textContent=L(`เตือน ${n.leadHours} ชม. · LINE ${n.lineOA?'ON':'OFF'} · Email ${n.email?'ON':'OFF'}`,`Lead ${n.leadHours} hr · LINE ${n.lineOA?'ON':'OFF'} · Email ${n.email?'ON':'OFF'}`);if(typeof setIntegrationSummary!=='undefined')setIntegrationSummary.textContent=`LINE ${integrationLabel(i.lineOA)} · Maps ${integrationLabel(i.googleMaps)} · Drive ${integrationLabel(i.googleDrive)}`;if(typeof setLineOASummary!=='undefined')setLineOASummary.textContent=settings.lineConfig?.liffId?`LIFF ${settings.lineConfig.liffId} · QR ${L('พร้อม','ready')}`:L('ยังไม่ได้ตั้ง LIFF ID','LIFF ID not configured');const onCount=Object.values(b).filter(Boolean).length;if(typeof setBehaviorSummary!=='undefined')setBehaviorSummary.textContent=L(`${onCount}/${Object.keys(b).length} ฟังก์ชันเปิด`,`${onCount}/${Object.keys(b).length} features enabled`);if(typeof settingsSnapshot!=='undefined')settingsSnapshot.innerHTML=`<div class="snapshot-item"><small>${L('ภาษา','Language')}</small><b>${lang==='en'?'English':'ภาษาไทย'}</b></div><div class="snapshot-item"><small>${L('ขนาดตัวอักษร','Font Size')}</small><b>${currentFontScale()}%</b></div><div class="snapshot-item"><small>${L('ค่าบริการ','Service Rate')}</small><b>S ${Number(c.onsiteS).toLocaleString()} / L ${Number(c.onsiteL).toLocaleString()}</b></div><div class="snapshot-item"><small>${L('ค่าเดินทาง','Travel')}</small><b>${settings.travelZones.length} ${L('โซน','zones')}</b></div><div class="snapshot-item"><small>VAT</small><b>${c.vatPct}%</b></div><div class="snapshot-item"><small>Field Service</small><b>${settings.fieldServiceConfig.appointmentReminderHours}h</b></div>`;pageInfo=PAGE_INFO[lang];const active=document.querySelector('.page.active')?.id?.replace('page-','')||'dashboard';if(pageInfo[active]){pageTitle.textContent=pageInfo[active][0];pageDesc.textContent=pageInfo[active][1]};if(typeof setCloudState!=='undefined'){setCloudState.textContent=supa?'Cloud Connected':'Local Mode';setCloudState.className='setting-state'+(supa?' online':'')}updateCloudUI(!!supa);applyLanguageTo(document.body)}
function applyOfficialImodeLogo(){document.querySelectorAll('.brand-logo-shell img').forEach(img=>{img.src='./assets/imode-ui-logo-v532.png';img.alt='I-MODE';img.onerror=()=>{img.onerror=null;img.src=IMODE_UI_LOGO}});document.querySelectorAll('.portal-header img').forEach(img=>{img.src=IMODE_UI_LOGO;img.alt='I-MODE'});document.querySelectorAll('.hero-logo').forEach(img=>{img.src=IMODE_LOGO;img.alt='I-MODE'})}window.applyOfficialImodeLogo=applyOfficialImodeLogo;
function renderAll(){applyFontScale();ensureMasters();applyOfficialImodeLogo();renderDashboard();renderCases();renderCustomers();renderMachines();renderQc();renderTechnicians();renderCalendar();renderNotifications();renderReports();renderSettings();renderQuotations();renderWarranties();renderMachineDocuments();renderLineRequests();renderFieldService();renderServiceReports();renderUserCard();applyRoleVisibility()}
window.addEventListener('load',async()=>{renderAll();await initCloud();renderAll();await initPortalFromUrl();if('serviceWorker' in navigator)navigator.serviceWorker.register('sw.js').catch(()=>{})});


/* ==================== V6.2 SALES EASY IMPROVEMENTS ==================== */
(function(){
 const uiStyle=document.createElement('style');
 uiStyle.textContent=`
 #page-crm .plain-module-intro h2,#page-crm .hero-digital h2,#page-crm .crm-hero-title,#page-crm .panel-head h3{font-size:calc(28px * var(--ui-font-scale))!important;line-height:1.15}
 #page-crm .plain-module-intro p,#page-crm .hero-digital p,#page-crm .panel-head p{font-size:calc(14px * var(--ui-font-scale))!important}
 #page-crm .crm-step-chip,#page-crm .tab-btn,#page-crm .crm-stage-chip,#page-crm .soft-btn,#page-crm .primary-btn,#page-crm .orange-btn{font-size:calc(14px * var(--ui-font-scale))!important}
 #page-crm .mobile-data-card h4,#page-crm .customer-card h4,#page-crm .customer-name,#page-crm .customer-name b,#page-crm .crm-mini-card b{font-size:calc(16px * var(--ui-font-scale))!important}
 #page-crm .mobile-data-card p,#page-crm .customer-card p,#page-crm .customer-card small,#page-crm .subtext{font-size:calc(12px * var(--ui-font-scale))!important}
 .modal-panel.large{max-width:1180px;width:min(1180px,96vw)}
 .modal-panel.quote-a4-preview{max-width:1120px;width:min(1120px,96vw)}
 .sales-quote-form-head{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:12px}
 .sales-quote-items{display:grid;gap:10px}
 .sales-quote-item-row.v62{display:grid;grid-template-columns:1.1fr 1.6fr .7fr .8fr .9fr .9fr 40px;gap:8px;align-items:end;padding:12px;border:1px solid #e3e8f4;border-radius:16px;background:#fbfcff}
 .sales-quote-item-row.v62 .field label{min-height:18px}
 .sales-quote-item-row.v62 .sales-line-amount{padding:0 6px 10px;text-align:right}
 .sales-quote-item-row.v62 .sales-line-amount small{display:block;color:var(--muted);font-size:11px}
 .sales-quote-item-row.v62 .sales-line-amount b{font-size:15px;color:var(--blue3)}
 .sales-line-remove{align-self:center}
 .sales-quote-toolbar{display:flex;gap:10px;justify-content:space-between;align-items:center;flex-wrap:wrap;margin-bottom:8px}
 .sales-quote-toolbar .button-row{margin:0}
 .sales-quote-helper{display:flex;gap:12px;flex-wrap:wrap;align-items:center;color:#5f6f88;font-size:12px}
 .sales-attachment-box{border:1px dashed #cdd8ec;background:#f8fbff;border-radius:14px;padding:12px}
 .sales-attachment-list{display:grid;gap:8px;margin-top:8px}
 .sales-attachment-item{display:flex;justify-content:space-between;gap:10px;align-items:center;padding:9px 10px;border:1px solid #e2e9f7;border-radius:12px;background:white}
 .sales-attachment-item b{font-size:13px;color:var(--blue3)}
 .sales-attachment-item small{display:block;color:var(--muted)}
 .sales-inline-note{background:#fff8ea;border:1px solid #f4d8aa;color:#915d00;border-radius:12px;padding:8px 10px;font-size:12px}
 .roles-split-grid{display:grid;gap:14px}
 .roles-section-title{display:flex;align-items:center;justify-content:space-between;gap:10px;margin:14px 0 6px}
 .roles-section-title b{font-size:16px;color:var(--blue3)}
 .roles-section-title small{display:block;color:var(--muted);font-size:12px}
 .permission-grid.compact.v62{grid-template-columns:repeat(3,minmax(0,1fr));gap:7px}
 .permission-check.compact.v62 b{font-size:12px}.permission-check.compact.v62 small{font-size:8.3px}
 .user-permission-card .role-card-head{grid-template-columns:minmax(220px,1fr) minmax(220px,1fr) 150px auto}
 .user-permission-badge{display:inline-flex;align-items:center;gap:6px;padding:6px 10px;border-radius:999px;background:#eef4ff;color:#1844a7;font-size:12px;font-weight:700}
 .portal-upload-grid{display:grid;gap:10px}
 .portal-upload-preview{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:8px}
 .portal-upload-preview .preview-card{border:1px solid #dce6f6;border-radius:12px;padding:8px;background:#fff}
 .portal-upload-preview img,.portal-upload-preview video{width:100%;height:96px;object-fit:cover;border-radius:10px;background:#eef3ff}
 .a4-sheet{width:210mm;min-height:297mm;margin:0 auto;background:#fff;padding:12mm 12mm 10mm;border:1px solid #cbd5e7;box-shadow:0 12px 30px rgba(16,33,74,.08);box-sizing:border-box;color:#1f2d4a}
 .a4-sheet *{box-sizing:border-box}
 .sq-doc-head{display:grid;grid-template-columns:210px 1fr;gap:18px;align-items:start;border-bottom:2px solid #173f99;padding-bottom:10px}
 .sq-doc-head img{max-width:190px;max-height:82px;object-fit:contain}
 .sq-doc-head h1{margin:8px 0 0;font-size:18px;letter-spacing:.08em;text-align:center;color:#0f295f}
 .sq-company-name{font-size:20px;font-weight:800;color:#173f99;margin-bottom:4px}
 .sq-company-block{font-size:11.5px;line-height:1.45}
 .sq-info-grid{display:grid;grid-template-columns:1.2fr .9fr;gap:14px;margin:12px 0}
 .sq-info-card{border:1px solid #bfcde6;padding:10px 12px;min-height:140px}
 .sq-info-row{display:grid;grid-template-columns:110px 1fr;gap:8px;font-size:12px;padding:3px 0}
 .sq-info-row b{font-weight:800}
 .sq-title{font-size:22px;font-weight:900;text-align:center;letter-spacing:.06em;margin:8px 0 12px;color:#102761}
 .sq-table{width:100%;border-collapse:collapse;margin-top:4px}
 .sq-table th,.sq-table td{border:1px solid #516789;padding:7px 8px;font-size:12px;vertical-align:top}
 .sq-table th{background:#f1f5fb;font-weight:800;text-align:center}
 .sq-table td.num{text-align:center}.sq-table td.money{text-align:right;white-space:nowrap}
 .sq-note-box{border:1px solid #516789;border-top:0;padding:8px 10px;font-size:11.5px;min-height:48px}
 .sq-bottom-grid{display:grid;grid-template-columns:1.1fr .9fr;gap:0;margin-top:0}
 .sq-term-box{border:1px solid #516789;border-top:0;padding:10px;font-size:11.5px;min-height:92px}
 .sq-total-box table{width:100%;border-collapse:collapse}.sq-total-box td{border:1px solid #516789;border-top:0;padding:8px 10px;font-size:12px}.sq-total-box td:last-child{text-align:right;font-weight:800}.sq-total-box .grand td{font-size:14px;color:#102761;background:#f4f7fd}
 .sq-attachment-note{margin-top:8px;font-size:11px;color:#5f6f88}
 .sq-sign-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:0;margin-top:12px}
 .sq-sign-cell{border:1px solid #516789;min-height:94px;padding:10px;text-align:center;font-size:11.5px}
 .sq-sign-cell b{display:block;margin-top:34px;font-size:12.5px}
 .sq-foot{margin-top:10px;font-size:11px;color:#42516f;line-height:1.45}
 @media print{body{background:#fff}.a4-sheet{margin:0;border:0;box-shadow:none}.no-print{display:none!important}}
 @media(max-width:980px){.sales-quote-form-head{grid-template-columns:1fr 1fr}.sales-quote-item-row.v62{grid-template-columns:1fr 1fr 1fr 1fr}.sales-quote-item-row.v62 .item-product,.sales-quote-item-row.v62 .item-desc{grid-column:span 2}.sales-quote-item-row.v62 .sales-line-amount{grid-column:span 1;text-align:left;padding:0}.permission-grid.compact.v62{grid-template-columns:repeat(2,minmax(0,1fr))}.sq-doc-head,.sq-info-grid,.sq-bottom-grid,.sq-sign-grid{grid-template-columns:1fr}}
 @media(max-width:640px){.sales-quote-form-head,.sales-quote-item-row.v62{grid-template-columns:1fr}.permission-grid.compact.v62{grid-template-columns:1fr}.portal-upload-preview{grid-template-columns:1fr 1fr}}
 `;
 document.head.appendChild(uiStyle);

 const PRODUCT_CATALOG_DEFAULT=[
  {code:'MC-LASER-WELD',name:'เครื่อง Laser Welding',description:'เครื่อง Laser Welding พร้อมอุปกรณ์มาตรฐาน',unit:'เครื่อง',price:0},
  {code:'MC-LASER-MARK',name:'เครื่อง Laser Marking',description:'เครื่อง Laser Marking พร้อมติดตั้ง',unit:'เครื่อง',price:0},
  {code:'MC-HO500WT',name:'เครื่องผลิตแก๊สไฮโดรเจน HO500WT',description:'เครื่องผลิตแก๊สไฮโดรเจน รุ่น HO500WT',unit:'เครื่อง',price:0},
  {code:'SRV-INSTALL',name:'ค่าติดตั้งเครื่อง',description:'Installation & Commissioning Service',unit:'งาน',price:0},
  {code:'SRV-TRAIN',name:'ค่าอบรมการใช้งาน',description:'Operator Training Service',unit:'งาน',price:0},
  {code:'SRV-PM',name:'Preventive Maintenance',description:'บริการ PM / Preventive Maintenance',unit:'งาน',price:0},
  {code:'WRTY-12M',name:'Extended Warranty 12M',description:'แพ็กเกจขยายการรับประกัน 12 เดือน',unit:'ชุด',price:Number(settings?.warrantyConfig?.packagePrices?.[12]||0)},
  {code:'DOC-MANUAL',name:'เอกสารคู่มือ / Drawing',description:'Manual / Drawing / SOP / เอกสารประกอบการขาย',unit:'ชุด',price:0}
 ];
 function ensureV62(){
  settings.typographyConfig=settings.typographyConfig||{};
  if((Number(settings.typographyConfig.defaultScale)||100)<115)settings.typographyConfig.defaultScale=115;
  settings.quoteConfig={...(defaults.quoteConfig||{}),...(settings.quoteConfig||{})};
  const c=settings.quoteConfig;
  if(c.onsiteM==null)c.onsiteM=Math.round(((Number(c.onsiteS)||0)+(Number(c.onsiteL)||0))/2);
  if(c.workshopM==null)c.workshopM=Math.round(((Number(c.workshopS)||0)+(Number(c.workshopL)||0))/2);
  if(c.diagnosisM==null)c.diagnosisM=Math.round(((Number(c.diagnosisS)||0)+(Number(c.diagnosisL)||0))/2);
  if(c.nextM==null)c.nextM=Math.round(((Number(c.nextS)||0)+(Number(c.nextL)||0))/2);
  if(c.laborM==null)c.laborM=Math.round(((Number(c.laborS)||0)+(Number(c.laborL)||0))/2);
  settings.userPermissions=settings.userPermissions||{};
  settings.productCatalog=(Array.isArray(settings.productCatalog)&&settings.productCatalog.length)?settings.productCatalog:PRODUCT_CATALOG_DEFAULT;
  try{if(typeof fontPreferences!=='undefined' && typeof fontUserKey==='function' && typeof saveFontPreference==='function'){const key=fontUserKey();if(!fontPreferences[key]||Number(fontPreferences[key])<115)saveFontPreference(115);}}catch(e){}
 }
 ensureV62();

 function permissionUserKey(u){return String(u?.id||u?.name||'').trim()}
 function allLoginUsers(){
  const seen=new Map();
  [...(Array.isArray(demoUsers)?demoUsers:[]),currentUser].filter(Boolean).forEach(u=>{const k=permissionUserKey(u);if(k&&!seen.has(k))seen.set(k,u)});
  return [...seen.values()];
 }
 function userPermissionEntry(u){return settings.userPermissions?.[permissionUserKey(u)]||null}
 function defaultUserPerms(u){
  const existing=userPermissionEntry(u);
  if(existing?.permissions?.length)return existing.permissions;
  const roleName=resolvePermissionRoleName(u);
  return migrateLegacyPermissions([],roleName);
 }
 function userPermissionCard(u){
  const th=settings.language!=='en';
  const k=permissionUserKey(u),entry=userPermissionEntry(u),enabled=!!entry?.enabled,perms=defaultUserPerms(u);
  return `<div class="role-card user-permission-card" data-user-key="${esc(k)}"><div class="role-card-head compact"><div class="role-name-wrap"><label>${th?'ผู้ใช้งาน':'User'}</label><input value="${esc(u?.name||u?.email||k)}" readonly></div><div class="role-meta-grid"><div class="role-meta-field"><label>${th?'บทบาท':'Role'}</label><input value="${esc(resolvePermissionRoleName(u)||u?.role||'-')}" readonly></div><div class="role-meta-field"><label>${th?'ทีม':'Team'}</label><input value="${esc(u?.team||'-')}" readonly></div></div><div class="role-auto-count"><small>${th?'โหมดสิทธิ์':'Permission mode'}</small><b>${enabled?(th?'รายบุคคล':'Individual'):(th?'ตาม Role':'By role')}</b><span>${th?'สิทธิ์':'Permissions'} ${perms.length}/${allPermissionKeys().length}</span></div><div class="role-head-actions"><label class="toggle"><input data-user-enable type="checkbox" ${enabled?'checked':''} onchange="toggleUserPermissionMode(this)"><span></span></label></div></div><div class="role-perm-toolbar"><b>${th?'สิทธิ์รายบุคคล':'Individual permissions'}</b><span data-user-perm-summary>${perms.length}/${allPermissionKeys().length} ${th?'สิทธิ์':'permissions'}</span><button type="button" class="mini-btn" onclick="toggleUserAll(this,true)">${th?'เลือกทั้งหมด':'Select all'}</button><button type="button" class="mini-btn" onclick="toggleUserAll(this,false)">${th?'ล้างทั้งหมด':'Clear'}</button></div><div class="permission-groups compact">${PERMISSION_CATALOG.map(g=>`<div class="permission-group"><h4>${permissionGroupLabel(g)}</h4><div class="permission-grid compact v62">${g.items.map(item=>`<label class="permission-check compact v62"><input type="checkbox" data-user-perm="${item[0]}" ${perms.includes(item[0])?'checked':''} onchange="updateUserPermissionSummary(this)"><span><b>${permissionLabel(item)}</b><small>${item[0]}</small></span></label>`).join('')}</div></div>`).join('')}</div></div>`;
 }
 window.toggleUserPermissionMode=function(el){const card=el.closest('.user-permission-card');if(!card)return;const b=card.querySelector('.role-auto-count b');if(b)b.textContent=el.checked?(settings.language==='en'?'Individual':'รายบุคคล'):(settings.language==='en'?'By role':'ตาม Role')}
 window.updateUserPermissionSummary=function(el){const card=el.closest('.user-permission-card');if(!card)return;const p=[...card.querySelectorAll('[data-user-perm]:checked')].map(x=>x.dataset.userPerm);const sum=card.querySelector('[data-user-perm-summary]');if(sum)sum.textContent=`${p.length}/${allPermissionKeys().length} ${settings.language==='en'?'permissions':'สิทธิ์'}`;const span=card.querySelector('.role-auto-count span');if(span)span.textContent=`${settings.language==='en'?'Permissions':'สิทธิ์'} ${p.length}/${allPermissionKeys().length}`}
 window.toggleUserAll=function(btn,state){const card=btn.closest('.user-permission-card');card?.querySelectorAll('[data-user-perm]').forEach(x=>x.checked=state);const one=card?.querySelector('[data-user-perm]');if(one)updateUserPermissionSummary(one)}
 const originalSaveRoles=window.saveRoles;
 window.openRolesSettingModal=function(){
  const th=settings.language!=='en';
  openModal(th?'ผู้ใช้งานและสิทธิ์':'Users & Roles',th?'กำหนดสิทธิ์ได้ทั้งตาม Role และรายบุคคล พร้อมขนาดตัวอักษรที่อ่านง่ายขึ้น':'Configure permissions by role and per individual user with larger readable text.',`<div class="roles-top-note"><div><b>${th?'การบังคับสิทธิ์ของระบบ':'Permission enforcement'}</b><small>${th?'เมื่อเปิดใช้งาน ระบบจะซ่อนเมนูที่ไม่มีสิทธิ์และตรวจสิทธิ์ก่อนเข้าใช้งานทุกโมดูล':'When enabled, unavailable menus are hidden and access is checked before entering each module.'}</small></div><label class="toggle"><input id="roleEnforceToggle" type="checkbox" ${settings.systemBehavior.enforceRolePermissions?'checked':''}><span></span></label></div><div class="roles-split-grid"><div><div class="roles-section-title"><div><b>${th?'สิทธิ์ตามบทบาท (Role-based)':'Role-based permissions'}</b><small>${th?'กำหนดสิทธิ์มาตรฐานของแต่ละตำแหน่งงาน':'Standard permissions for each role'}</small></div></div><div id="roleList" class="role-card-list">${settings.roles.map(r=>roleCard(r)).join('')}</div></div><div><div class="roles-section-title"><div><b>${th?'สิทธิ์รายบุคคล':'Individual permissions'}</b><small>${th?'เปิดเฉพาะผู้ที่ต้องการสิทธิ์ต่างจากบทบาทหลัก เช่น ฝ่ายขายบางคน, หัวหน้าช่าง, ผู้อนุมัติ':'Enable only for users who need permissions different from their main role.'}</small></div><span class="user-permission-badge">${allLoginUsers().length} ${th?'บัญชี':'accounts'}</span></div><div class="sales-inline-note">${th?'หากไม่เปิดโหมดรายบุคคล ระบบจะใช้สิทธิ์ตาม Role โดยอัตโนมัติ':'If individual mode is off, the user automatically inherits permissions from the assigned role.'}</div><div id="userPermissionList" class="role-card-list" style="margin-top:10px">${allLoginUsers().map(userPermissionCard).join('')}</div></div></div><div class="button-row"><button type="button" class="soft-btn" onclick="addRoleRow()">＋ ${th?'เพิ่ม Role':'Add Role'}</button><button type="button" class="primary-btn" onclick="saveRoles()">${th?'บันทึกสิทธิ์ทั้งหมด':'Save all permissions'}</button></div>`);
  modalPanel.classList.add('large');
 };
 window.saveRoles=function(){
  const rows=[...roleList.querySelectorAll('.role-card:not(.user-permission-card)')].map(card=>{const name=card.querySelector('[data-k=name]')?.value.trim(),teamScope=card.querySelector('[data-k=teamScope]')?.value||'',positionScope=card.querySelector('[data-k=positionScope]')?.value||'',permissions=[...card.querySelectorAll('[data-perm]:checked')].map(x=>x.dataset.perm);return{name,teamScope,positionScope,users:Math.max(0,Number(card.querySelector('[data-k=users]')?.value)||0),permissions}}).filter(x=>x.name);
  if(!rows.length){toastMsg(settings.language==='en'?'At least one role is required':'ต้องมีอย่างน้อย 1 Role');return}
  if(rows.some(r=>!r.permissions.length)){toastMsg(settings.language==='en'?'Every role needs at least one permission':'ทุก Role ต้องมีสิทธิ์อย่างน้อย 1 รายการ');return}
  const individual={};
  [...(document.querySelectorAll('#userPermissionList .user-permission-card')||[])].forEach(card=>{const enabled=!!card.querySelector('[data-user-enable]')?.checked;const perms=[...card.querySelectorAll('[data-user-perm]:checked')].map(x=>x.dataset.userPerm);const key=card.dataset.userKey;if(key)individual[key]={enabled,permissions:perms}});
  settings.roles=rows;settings.userPermissions=individual;settings.systemBehavior={...settings.systemBehavior,enforceRolePermissions:!!roleEnforceToggle.checked};saveLocal();cloudSaveSettings();closeModal();renderAll();toastMsg(settings.language==='en'?'Roles & permissions saved':'บันทึกผู้ใช้งานและสิทธิ์แล้ว')
 };
 window.canPermission=function(key){
  if(!(settings.systemBehavior?.enforceRolePermissions))return true;
  const entry=settings.userPermissions?.[permissionUserKey(currentUser||{})];
  if(entry?.enabled)return (entry.permissions||[]).includes(key);
  const r=currentRoleConfig();if(!r)return false;return (r.permissions||[]).includes(key)
 };
 window.requirePermission=function(key){if(canPermission(key))return true;toastMsg(settings.language==='en'?'You do not have permission for this action':'คุณไม่มีสิทธิ์ใช้งานฟังก์ชันนี้');return false};

 function ensureQuoteItemShape(it){return {productCode:it?.productCode||'',description:it?.description||'',qty:Number(it?.qty||1),unit:it?.unit||'หน่วย',unitPrice:Number(it?.unitPrice||0)}}
 function productCatalog(){return (Array.isArray(settings.productCatalog)&&settings.productCatalog.length?settings.productCatalog:PRODUCT_CATALOG_DEFAULT)}
 function productOptionsHTML(code=''){return '<option value="">เลือกสินค้า / บริการ</option>'+productCatalog().map(p=>`<option value="${esc(p.code)}" ${code===p.code?'selected':''}>${esc(p.name)}</option>`).join('')}
 window.setSalesQuoteProduct=function(i,code){const p=productCatalog().find(x=>x.code===code);if(!salesQuoteDraftItems[i])return;salesQuoteDraftItems[i].productCode=code;if(p){if(!salesQuoteDraftItems[i].description||salesQuoteDraftItems[i].description===salesQuoteDraftItems[i]._lastAutoDesc)salesQuoteDraftItems[i].description=p.description; if(!salesQuoteDraftItems[i].unit||salesQuoteDraftItems[i].unit==='หน่วย'||salesQuoteDraftItems[i].unit===salesQuoteDraftItems[i]._lastAutoUnit)salesQuoteDraftItems[i].unit=p.unit; if(!Number(salesQuoteDraftItems[i].unitPrice))salesQuoteDraftItems[i].unitPrice=Number(p.price||0); salesQuoteDraftItems[i]._lastAutoDesc=p.description; salesQuoteDraftItems[i]._lastAutoUnit=p.unit;} renderSalesQuoteItems();calcSalesQuoteDraft()}
 window.renderSalesQuoteItems=function(){
  if(typeof salesQuoteItems==='undefined')return;
  salesQuoteItems.innerHTML=salesQuoteDraftItems.map((raw,i)=>{const it=ensureQuoteItemShape(raw);salesQuoteDraftItems[i]=it;return `<div class="sales-quote-item-row v62"><div class="field item-product"><label>${i===0?'สินค้า / บริการ':''}</label><select onchange="setSalesQuoteProduct(${i},this.value)">${productOptionsHTML(it.productCode||'')}</select></div><div class="field item-desc"><label>${i===0?'รายการ / รุ่น / รายละเอียด':''}</label><input value="${esc(it.description||'')}" placeholder="เช่น เครื่อง Laser Welding รุ่น ..." oninput="updateSalesQuoteItem(${i},'description',this.value)"></div><div class="field"><label>${i===0?'จำนวน':''}</label><input type="number" min="0" step="1" value="${Number(it.qty||1)}" oninput="updateSalesQuoteItem(${i},'qty',this.value)"></div><div class="field"><label>${i===0?'หน่วย':''}</label><input value="${esc(it.unit||'หน่วย')}" oninput="updateSalesQuoteItem(${i},'unit',this.value)"></div><div class="field"><label>${i===0?'ราคาต่อหน่วย':''}</label><input type="number" min="0" step="0.01" value="${Number(it.unitPrice||0)}" oninput="updateSalesQuoteItem(${i},'unitPrice',this.value)"></div><div class="sales-line-amount"><small>${i===0?'รวม':''}</small><b>${crmMoney((Number(it.qty)||0)*(Number(it.unitPrice)||0))}</b></div><button type="button" class="sales-line-remove" onclick="removeSalesQuoteItem(${i})" ${salesQuoteDraftItems.length<=1?'disabled':''}>×</button></div>`}).join('')
 }
 window.addSalesQuoteItem=function(){salesQuoteDraftItems.push({productCode:'',description:'',qty:1,unit:'หน่วย',unitPrice:0});renderSalesQuoteItems();calcSalesQuoteDraft()}
 window.updateSalesQuoteItem=function(i,k,v){if(!salesQuoteDraftItems[i])return;salesQuoteDraftItems[i][k]=['qty','unitPrice'].includes(k)?Number(v)||0:v;const row=salesQuoteItems.children[i];if(row){const b=row.querySelector('.sales-line-amount b');if(b)b.textContent=crmMoney((Number(salesQuoteDraftItems[i].qty)||0)*(Number(salesQuoteDraftItems[i].unitPrice)||0))}calcSalesQuoteDraft()}
 let salesQuoteDraftAttachments=[];
 window.renderSalesQuoteAttachments=function(){if(typeof salesQuoteAttachmentList==='undefined')return;salesQuoteAttachmentList.innerHTML=salesQuoteDraftAttachments.length?salesQuoteDraftAttachments.map((f,i)=>`<div class="sales-attachment-item"><div><b>${esc(f.name)}</b><small>${esc(f.type||'file')} · ${Math.round((Number(f.size)||0)/1024)} KB</small></div><div class="row-actions"><button type="button" class="mini-btn" onclick="openDraftAttachment(${i})">เปิด</button><button type="button" class="mini-btn" onclick="removeDraftAttachment(${i})">ลบ</button></div></div>`).join(''):`<div class="empty">ยังไม่มีเอกสารแนบ — รองรับ PDF / รูปภาพ / เอกสาร Office (แนะนำไฟล์ไม่เกิน 900 KB ต่อไฟล์เพื่อให้ Browser Demo ทำงานลื่น)</div>`}
 window.removeDraftAttachment=function(i){salesQuoteDraftAttachments.splice(i,1);renderSalesQuoteAttachments()}
 window.openDraftAttachment=function(i){const f=salesQuoteDraftAttachments[i];if(f?.data)window.open(f.data,'_blank')}
 window.pickSalesQuoteAttachments=async function(input){const files=[...(input.files||[])];for(const file of files){if(salesQuoteDraftAttachments.length>=3){toastMsg('แนบไฟล์ได้สูงสุด 3 ไฟล์');break}if(file.size>900*1024){toastMsg('ไฟล์ '+file.name+' ใหญ่เกิน 900 KB');continue}const data=await new Promise((res,rej)=>{const r=new FileReader();r.onload=()=>res(r.result);r.onerror=rej;r.readAsDataURL(file)});salesQuoteDraftAttachments.push({name:file.name,type:file.type,size:file.size,data:String(data)})}input.value='';renderSalesQuoteAttachments()}
 function currentSalesQuoteDraftRecord(){
  const c=salesQuoteCustomer(sqCustomer?.value)||{};
  const clean=salesQuoteDraftItems.map(ensureQuoteItemShape).filter(i=>String(i.description||'').trim()&&(Number(i.qty)||0)>0);
  const rec={id:salesQuoteEditingId||'DRAFT',customerId:c.id||'',customerName:c.name||'',contact:sqContact?.value?.trim?.()||'',quoteDate:sqDate?.value||new Date().toISOString().slice(0,10),status:sqStatus?.value||'ร่าง',preparedBy:sqPreparedBy?.value?.trim?.()||currentUser?.name||'',items:clean,discountPct:Number(sqDiscount?.value||0),vatOn:sqVat?.value!=='no',vatPct:Number(settings.quoteConfig?.vatPct||7),note:sqNote?.value?.trim?.()||'',attachments:salesQuoteDraftAttachments,opportunityId:sqOpportunity?.value||'',createdAt:new Date().toISOString(),updatedAt:new Date().toISOString()};Object.assign(rec,salesQuoteTotals(rec));return rec
 }
 window.previewSalesQuoteDraft=function(){const rec=currentSalesQuoteDraftRecord();openSalesQuotePreviewRecord(rec,true)}
 window.openSalesQuoteModal=function(id='',presetCustomer='',presetOpp=''){
  if(!requirePermission('quotation.create'))return;
  const old=salesQuotes.find(q=>q.id===id)||{};
  salesQuoteEditingId=old.id||'';
  salesQuoteDraftItems=JSON.parse(JSON.stringify((old.items?.length?old.items:[{productCode:'',description:'',qty:1,unit:'หน่วย',unitPrice:0}]).map(ensureQuoteItemShape)));
  salesQuoteDraftAttachments=JSON.parse(JSON.stringify(old.attachments||[]));
  const cid=old.customerId||presetCustomer||'',oppid=old.opportunityId||presetOpp||'';
  openModal(old.id?'แก้ไขใบเสนอราคาฝ่ายขาย':'สร้างใบเสนอราคาฝ่ายขาย','ฟอร์มสำหรับฝ่ายขาย — เลือกลูกค้า เลือกสินค้า ใส่จำนวน/หน่วย/ราคา แนบเอกสาร และเปิดตัวอย่าง A4 ได้ทันที',`<form id="salesQuoteForm"><div class="sales-quote-form-head"><div class="field"><label>ลูกค้า *</label><select id="sqCustomer" required onchange="salesQuoteCustomerChanged()"><option value="">เลือกลูกค้า</option>${customers.map(c=>`<option value="${c.id}" ${cid===c.id?'selected':''}>${esc(c.name)}</option>`).join('')}</select></div><div class="field"><label>งานขายที่เกี่ยวข้อง</label><select id="sqOpportunity"><option value="">ไม่ระบุ</option>${crmOpportunities.map(o=>`<option value="${o.id}" ${oppid===o.id?'selected':''}>${esc(o.id+' · '+o.title)}</option>`).join('')}</select></div><div class="field"><label>ผู้ติดต่อ</label><input id="sqContact" value="${esc(old.contact||'')}"></div><div class="field"><label>วันที่</label><input id="sqDate" type="date" value="${esc(old.quoteDate||new Date().toISOString().slice(0,10))}"></div><div class="field"><label>สถานะ</label><select id="sqStatus">${['ร่าง','ส่งแล้ว','รอลูกค้าตอบ','รอ PO','อนุมัติ','ไม่สำเร็จ'].map(s=>`<option ${old.status===s?'selected':''}>${s}</option>`).join('')}</select></div><div class="field"><label>ผู้จัดทำ</label><input id="sqPreparedBy" value="${esc(old.preparedBy||currentUser?.name||'')}"></div></div><div class="sales-quote-toolbar"><div class="sales-quote-helper"><span>• เลือกสินค้า/บริการจากรายการมาตรฐานได้</span><span>• ใส่จำนวน + หน่วย + ราคาต่อหน่วยได้</span><span>• แนบเอกสารประกอบการขายได้</span></div><div class="button-row"><button type="button" class="soft-btn" onclick="previewSalesQuoteDraft()">🧾 ดูตัวอย่างใบเสนอราคา A4</button><button type="button" class="soft-btn" onclick="addSalesQuoteItem()">＋ เพิ่มรายการ</button></div></div><div id="salesQuoteItems" class="sales-quote-items"></div><div class="sales-quote-bottom"><div class="sales-quote-options"><div class="field"><label>ส่วนลด %</label><input id="sqDiscount" type="number" min="0" max="100" value="${Number(old.discountPct||0)}" oninput="calcSalesQuoteDraft()"></div><div class="field"><label>VAT</label><select id="sqVat" onchange="calcSalesQuoteDraft()"><option value="yes" ${old.vatOn!==false?'selected':''}>VAT ${(old.vatPct||settings.quoteConfig?.vatPct||7)}%</option><option value="no" ${old.vatOn===false?'selected':''}>ไม่คิด VAT</option></select></div><div class="field full"><label>เงื่อนไข / หมายเหตุ</label><textarea id="sqNote">${esc(old.note||'')}</textarea></div><div class="field full"><label>เอกสารประกอบการขาย</label><div class="sales-attachment-box"><div class="button-row" style="justify-content:flex-start"><label class="soft-btn" style="cursor:pointer">📎 แนบเอกสาร<input type="file" accept=".pdf,.png,.jpg,.jpeg,.doc,.docx,.xls,.xlsx" multiple style="display:none" onchange="pickSalesQuoteAttachments(this)"></label><div class="sales-inline-note">ตัวอย่างเช่น Catalog, Drawing, รูปเครื่อง, TOR, Spec Sheet</div></div><div id="salesQuoteAttachmentList" class="sales-attachment-list"></div></div></div></div><div class="sales-quote-draft-summary"><div><span>รวมรายการ</span><b id="sqSubtotal">0</b></div><div><span>ส่วนลด</span><b id="sqDiscountAmt">0</b></div><div><span>VAT</span><b id="sqVatAmt">0</b></div><div class="grand"><span>ยอดรวมสุทธิ</span><b id="sqGrand">0</b></div></div></div><div class="button-row"><button type="button" class="soft-btn" onclick="closeModal()">ยกเลิก</button><button class="primary-btn action-3d-orange">💾 บันทึกใบเสนอราคา</button></div></form>`,true);
  modalPanel.classList.add('large');
  setTimeout(()=>{salesQuoteCustomerChanged();if(oppid&&[...sqOpportunity.options].some(o=>o.value===oppid))sqOpportunity.value=oppid;renderSalesQuoteItems();renderSalesQuoteAttachments();calcSalesQuoteDraft();salesQuoteForm.onsubmit=e=>{e.preventDefault();saveSalesQuote()}},20)
 }
 window.saveSalesQuote=function(){
  const c=salesQuoteCustomer(sqCustomer.value);if(!c){toastMsg('กรุณาเลือกลูกค้า');return}
  const clean=salesQuoteDraftItems.map(ensureQuoteItemShape).filter(i=>String(i.description||'').trim()&&(Number(i.qty)||0)>0);if(!clean.length){toastMsg('กรุณาใส่รายการเสนอขายอย่างน้อย 1 รายการ');return}
  const old=salesQuotes.find(q=>q.id===salesQuoteEditingId),vatPct=Number(settings.quoteConfig?.vatPct||7);
  const rec={id:old?.id||salesQuoteNo(),customerId:c.id,customerName:c.name,opportunityId:sqOpportunity.value,contact:sqContact.value.trim(),quoteDate:sqDate.value,status:sqStatus.value,preparedBy:sqPreparedBy.value.trim(),items:clean,discountPct:Number(sqDiscount.value)||0,vatOn:sqVat.value!=='no',vatPct,note:sqNote.value.trim(),attachments:JSON.parse(JSON.stringify(salesQuoteDraftAttachments)),isDemo:false,createdAt:old?.createdAt||new Date().toISOString(),updatedAt:new Date().toISOString()};
  Object.assign(rec,salesQuoteTotals(rec));if(old)salesQuotes=salesQuotes.map(q=>q.id===rec.id?rec:q);else salesQuotes.unshift(rec);
  if(rec.opportunityId){const o=crmOpportunity(rec.opportunityId);if(o){o.quotationId=rec.id;if(['Prospect','Contacted','Needs Analysis','Demo'].includes(o.stage)){o.stage='Quotation';o.probability=CRM_STAGE_PROB.Quotation}o.updatedAt=new Date().toISOString()}}
  saveLocal();closeModal();renderCrm();toastMsg('บันทึก '+rec.id+' แล้ว');setTimeout(()=>previewSalesQuote(rec.id),80)
 }
 window.salesQuoteDocHTML=function(q){
  const cu=salesQuoteCustomer(q.customerId)||{};
  const companyName=settings.companyNameTh||settings.companyName||'I-MODE Plus Co., Ltd.';
  const companyAddress=settings.companyAddress||'';
  const t=salesQuoteTotals(q);const items=(q.items||[]);
  const headImg=typeof IMODE_LOGO!=='undefined'?`<img src="${IMODE_LOGO}" alt="I-MODE Logo">`:'<div style="font-size:32px;font-weight:900;color:#173f99">I-MODE</div>';
  const dateTxt=crmDateOnly(q.quoteDate||q.createdAt||new Date().toISOString());
  const amountRows=items.map((it,idx)=>`<tr><td class="num">${idx+1}</td><td>${esc(it.description||'-')}</td><td class="num">${crmMoney(Number(it.qty)||0)}</td><td class="num">${esc(it.unit||'หน่วย')}</td><td class="money">${crmMoney(Number(it.unitPrice)||0)} บาท</td><td class="money">${crmMoney((Number(it.qty)||0)*(Number(it.unitPrice)||0))} บาท</td></tr>`).join('');
  const attachmentText=(q.attachments||[]).length?`${(q.attachments||[]).map(x=>x.name).join(' • ')}`:'-';
  return `<div class="a4-sheet" id="salesQuotePrint"><div class="sq-doc-head"><div>${headImg}</div><div><div class="sq-company-name">${esc(companyName)}</div><div class="sq-company-block">${esc(companyAddress).replace(/\n/g,'<br>')}<br>Tel. ${esc(settings.companyPhone||'-')} &nbsp; E-mail : ${esc(settings.companyEmail||'-')}<br>Tax ID : ${esc(settings.companyTaxId||'-')}</div><div class="sq-title">QUOTATION</div></div></div><div class="sq-info-grid"><div class="sq-info-card"><div class="sq-info-row"><b>Customer Name :</b><div>${esc(q.contact||cu.contact||'-')}</div></div><div class="sq-info-row"><b>Company Name :</b><div>${esc(cu.name||q.customerName||'-')}</div></div><div class="sq-info-row"><b>Address :</b><div>${esc(cu.address||cu.location||'-')}</div></div><div class="sq-info-row"><b>Tax ID :</b><div>${esc(cu.taxId||'-')}</div></div><div class="sq-info-row"><b>Email :</b><div>${esc(cu.email||'-')}</div></div></div><div class="sq-info-card"><div class="sq-info-row"><b>No :</b><div>${esc(q.id||'DRAFT')}</div></div><div class="sq-info-row"><b>Date :</b><div>${esc(dateTxt)}</div></div><div class="sq-info-row"><b>Ref :</b><div>${esc(q.preparedBy||'-')}</div></div><div class="sq-info-row"><b>Email :</b><div>${esc(settings.companyEmail||'-')}</div></div><div class="sq-info-row"><b>Tel :</b><div>${esc(settings.companyPhone||'-')}</div></div><div class="sq-info-row"><b>Status :</b><div>${esc(q.status||'ร่าง')}</div></div></div></div><table class="sq-table"><thead><tr><th style="width:52px">No.</th><th>Description</th><th style="width:76px">Qty</th><th style="width:76px">Unit</th><th style="width:130px">Unit Price</th><th style="width:140px">Amount Total</th></tr></thead><tbody>${amountRows||'<tr><td colspan="6" style="text-align:center">-</td></tr>'}</tbody></table><div class="sq-note-box"><b>Remark :</b> ${esc(q.note||'-')}</div><div class="sq-bottom-grid"><div class="sq-term-box"><b>Term & Condition</b><div style="margin-top:6px">Delivery : ตามตกลง<br>Payment : ตามเงื่อนไข<br>Attachments : ${esc(attachmentText)}</div><div class="sq-attachment-note">Prototype นี้รองรับการแนบ Catalog / Drawing / Spec Sheet / รูปเครื่อง เพื่อส่งประกอบการขาย</div></div><div class="sq-total-box"><table><tr><td>Total</td><td>${crmMoney(t.subtotal)} บาท</td></tr><tr><td>Discount</td><td>${crmMoney(t.discount)} บาท</td></tr><tr><td>VAT ${q.vatOn?(q.vatPct||7)+'%':'0%'}</td><td>${crmMoney(q.vatOn?t.vat:0)} บาท</td></tr><tr class="grand"><td>ยอดรวม</td><td>${crmMoney(t.grand)} บาท</td></tr></table></div></div><div class="sq-sign-grid"><div class="sq-sign-cell"><b>Purchaser Signature</b><div>Date ....................</div></div><div class="sq-sign-cell"><b>Customer Service</b><div>${esc(q.preparedBy||'-')}</div><div>Date ${esc(dateTxt)}</div></div><div class="sq-sign-cell"><b>Sales</b><div>${esc(q.preparedBy||'-')}</div><div>Date ${esc(dateTxt)}</div></div><div class="sq-sign-cell"><b>Authorizer</b><div>${esc(currentUser?.name||'-')}</div><div>Date ${esc(dateTxt)}</div></div></div><div class="sq-foot">We certainly hope our proposal meets with your approval and hope to receive your confirmation soon.<br>กรุณาลงนามและส่งกลับมายัง ${esc(settings.companyEmail||'-')} เพื่อยืนยันใบเสนอราคา</div></div>`
 }
 function openSalesQuotePreviewRecord(q,isDraft=false){
  const body=`${salesQuoteDocHTML(q)}<div class="button-row no-print" style="margin-top:12px"><button type="button" class="soft-btn" onclick="closeModal()">ปิด</button><button type="button" class="soft-btn" onclick="printCurrentSalesQuote()">🖨 พิมพ์ A4</button>${!isDraft&&q?.id?`<button type="button" class="primary-btn" onclick="openSalesQuoteModal('${q.id}')">แก้ไข</button>`:''}</div>`;
  openModal(isDraft?'ตัวอย่างใบเสนอราคา A4':'ดูใบเสนอราคาฝ่ายขาย',isDraft?'ตัวอย่างฟอร์มมาตรฐาน A4 ของใบเสนอราคา CRM':(q.customerName||'-'),body,true);modalPanel.classList.add('quote-a4-preview','large')
 }
 window.previewSalesQuote=function(id){const q=salesQuotes.find(x=>x.id===id);if(!q)return;openSalesQuotePreviewRecord(q,false)}
 window.printCurrentSalesQuote=function(){const doc=document.getElementById('salesQuotePrint');if(!doc)return;const w=window.open('','_blank','width=1200,height=900');w.document.write(`<!doctype html><html lang="th"><head><meta charset="utf-8"><title>Sales Quotation</title><style>${uiStyle.textContent}</style></head><body>${doc.outerHTML}<script>window.onload=()=>window.print()<\/script></body></html>`);w.document.close()}

 window.openCrmLeadModal=function(id='',preset=''){
  if(!requirePermission('crm.lead'))return;const old=crmLead(id)||{},th=settings.language!=='en',customerId=old.customerId||'';
  openModal(old.id?(th?'แก้ไขลูกค้าเป้าหมาย':'Edit Lead'):(th?'เพิ่มลูกค้าเป้าหมาย':'Add Lead'),th?'เลือกได้ทั้งลูกค้าใหม่และเลือกลูกค้าจากฐานข้อมูลเดิมเพื่อสร้างงานขายต่อ':'Use for both new prospects and existing customers from the database.',`<form id="crmLeadForm"><input id="clId" type="hidden" value="${esc(old.id||'')}"><div class="form-grid cols2"><div class="field full"><label>${th?'เลือกลูกค้าจากฐานข้อมูล (ถ้ามี)':'Choose existing customer (optional)'}</label><select id="clCustomerRef" onchange="crmLeadCustomerRefChanged()"><option value="">${th?'— ลูกค้าใหม่ / ยังไม่เลือก —':'— New lead / not selected —'}</option>${customers.map(c=>`<option value="${c.id}" ${customerId===c.id?'selected':''}>${esc(c.name)}${c.contact?` · ${esc(c.contact)}`:''}</option>`).join('')}</select></div><div class="field"><label>${th?'บริษัท / ชื่อลูกค้า *':'Company / Prospect *'}</label><input id="clCompany" required value="${esc(old.company||'')}"></div><div class="field"><label>${th?'ผู้ติดต่อ':'Contact'}</label><input id="clContact" value="${esc(old.contact||'')}"></div><div class="field"><label>${th?'โทรศัพท์':'Phone'}</label><input id="clPhone" value="${esc(old.phone||'')}"></div><div class="field"><label>Email</label><input id="clEmail" value="${esc(old.email||'')}"></div><div class="field"><label>Source</label><select id="clSource">${['LINE OA','Website / QR','Exhibition','Referral','Sales','Service'].map(x=>`<option ${old.source===x?'selected':''}>${x}</option>`).join('')}</select></div><div class="field"><label>${th?'สถานะ':'Status'}</label><select id="clStatus"><option value="New" ${old.status==='New'?'selected':''}>ลูกค้าใหม่</option><option value="Contacted" ${old.status==='Contacted'?'selected':''}>ติดต่อแล้ว</option><option value="Qualified" ${old.status==='Qualified'?'selected':''}>สนใจจริง / พร้อมทำงานขาย</option><option value="Converted" ${old.status==='Converted'?'selected':''}>ยืนยันเป็นลูกค้าแล้ว</option><option value="Lost" ${old.status==='Lost'?'selected':''}>ไม่สนใจ / ยกเลิก</option></select></div><div class="field full"><label>${th?'สินค้า / บริการที่สนใจ':'Product / Service Interest'}</label><input id="clInterest" value="${esc(old.interest||preset||'')}"></div><div class="field"><label>${th?'งบประมาณ':'Budget'}</label><input id="clBudget" type="number" min="0" value="${Number(old.budget||0)}"></div><div class="field"><label>ระดับความสนใจ %</label><input id="clScore" type="number" min="0" max="100" value="${Number(old.score||50)}"></div><div class="field"><label>ผู้รับผิดชอบ</label><input id="clOwner" value="${esc(old.owner||currentUser?.name||'Sales Team')}"></div><div class="field"><label>วัน/เวลาติดตามครั้งถัดไป</label><input id="clNext" type="datetime-local" value="${old.nextFollowUp?old.nextFollowUp.slice(0,16):''}"></div><div class="field full"><label>${th?'หมายเหตุ':'Note'}</label><textarea id="clNote">${esc(old.note||'')}</textarea></div></div><div class="button-row"><button type="button" class="soft-btn" onclick="closeModal()">${th?'ยกเลิก':'Cancel'}</button><button class="primary-btn">${th?'บันทึกลูกค้าเป้าหมาย':'Save Prospect'}</button></div></form>`,true);
  setTimeout(()=>{crmLeadForm.onsubmit=e=>{e.preventDefault();saveCrmLead()};if(customerId)crmLeadCustomerRefChanged()},20)
 }
 window.crmLeadCustomerRefChanged=function(){const c=customers.find(x=>x.id===clCustomerRef.value);if(!c)return;if(!clCompany.value)clCompany.value=c.name||'';if(!clContact.value)clContact.value=c.contact||'';if(!clPhone.value)clPhone.value=c.phone||'';if(!clEmail.value)clEmail.value=c.email||'';if(!clStatus.value||clStatus.value==='New')clStatus.value='Qualified'}
 window.saveCrmLead=function(){const old=crmLead(clId.value),obj={id:old?.id||'LEAD-'+String(crmLeads.length+1).padStart(4,'0'),customerId:clCustomerRef.value||'',company:clCompany.value.trim(),contact:clContact.value.trim(),phone:clPhone.value.trim(),email:clEmail.value.trim(),source:clSource.value,status:clStatus.value,interest:clInterest.value.trim(),budget:Number(clBudget.value)||0,score:Number(clScore.value)||0,owner:clOwner.value.trim(),nextFollowUp:clNext.value?new Date(clNext.value).toISOString():'',note:clNote.value.trim(),createdAt:old?.createdAt||new Date().toISOString(),updatedAt:new Date().toISOString()};if(old)crmLeads=crmLeads.map(x=>x.id===obj.id?obj:x);else crmLeads.unshift(obj);saveLocal();closeModal();renderCrm();toastMsg(crmL('บันทึกลูกค้าเป้าหมายแล้ว','Lead saved'))}

 window.openPortalIssueForm=function(){const m=portalMachine(),cu=customerById(m?.customerId);if(!m)return;portalContent.innerHTML=`<form id="portalIssueForm" class="portal-form"><h3>🔧 แจ้งปัญหาเครื่อง</h3><div class="field"><label>ผู้ติดต่อ</label><input id="piContact" required value="${esc(cu?.contact||portalLineProfile?.displayName||'')}"></div><div class="field"><label>โทรศัพท์</label><input id="piPhone" required value="${esc(cu?.phone||'')}"></div><div class="field"><label>ระดับความเร่งด่วน</label><select id="piPriority"><option>ปกติ</option><option>สูง</option><option>ด่วนมาก</option></select></div><div class="field"><label>อาการ / ปัญหา *</label><textarea id="piIssue" required placeholder="อธิบายอาการ Error เสียงผิดปกติ หรือสิ่งที่พบ"></textarea></div><div class="field"><label>แนบรูปภาพ / วิดีโอ</label><div class="portal-upload-grid"><label class="soft-btn" style="cursor:pointer;width:max-content">📷 / 🎥 เพิ่มไฟล์<input id="piMediaInput" type="file" accept="image/*,video/*" multiple style="display:none" onchange="portalIssueMediaChanged(this)"></label><div id="portalIssueMediaPreview" class="portal-upload-preview"></div></div></div><button class="primary-btn" style="width:100%">ส่งแจ้งปัญหา</button></form>`;window.portalIssuePendingMedia=[];setTimeout(()=>portalIssueForm.onsubmit=submitPortalIssue,20)}
 window.portalIssueMediaChanged=async function(input){window.portalIssuePendingMedia=window.portalIssuePendingMedia||[];for(const file of [...(input.files||[])]){if(window.portalIssuePendingMedia.length>=4){toastMsg('แนบได้สูงสุด 4 ไฟล์');break}if(file.size>3*1024*1024){toastMsg('ไฟล์ '+file.name+' ใหญ่เกิน 3 MB');continue}const data=await new Promise((res,rej)=>{const r=new FileReader();r.onload=()=>res(r.result);r.onerror=rej;r.readAsDataURL(file)});window.portalIssuePendingMedia.push({name:file.name,type:file.type,size:file.size,data:String(data)})}input.value='';const box=document.getElementById('portalIssueMediaPreview');if(box)box.innerHTML=window.portalIssuePendingMedia.map((f,i)=>`<div class="preview-card">${String(f.type||'').startsWith('video/')?`<video src="${f.data}" controls></video>`:`<img src="${f.data}" alt="upload">`}<small style="display:block;margin-top:6px;color:var(--muted)">${esc(f.name)}</small><button type="button" class="mini-btn" onclick="removePortalIssueMedia(${i})" style="margin-top:6px">ลบ</button></div>`).join('')}
 window.removePortalIssueMedia=function(i){window.portalIssuePendingMedia.splice(i,1);window.portalIssueMediaChanged({files:[]})}
 window.submitPortalIssue=async function(e){e.preventDefault();const m=portalMachine(),cu=customerById(m.customerId),req={id:uid(),type:'service',status:'ใหม่',customerId:cu?.id||'',machineId:m.id,contact:piContact.value.trim(),phone:piPhone.value.trim(),priority:piPriority.value,message:piIssue.value.trim(),media:JSON.parse(JSON.stringify(window.portalIssuePendingMedia||[])),lineUserId:portalLineProfile?.userId||'',lineDisplayName:portalLineProfile?.displayName||'',createdAt:new Date().toISOString()};lineRequests.unshift(req);const c={id:uid(),ticket:genTicket(),createdAt:new Date().toISOString(),updatedAt:new Date().toISOString(),customerId:cu?.id||'',customer:cu?.name||'',contact:req.contact,phone:req.phone,email:cu?.email||'',location:cu?.address||cu?.location||'',branch:cu?.branch||'',mapUrl:cu?.mapUrl||'',latitude:cu?.latitude||'',longitude:cu?.longitude||'',machineId:m.id,machine:machinePrimaryName(m),machineTh:machineNameTh(m),machineEn:machineNameEn(m),model:m.model||'',serial:m.serial||'',machineSize:m.size||'',warranty:m.warranty||'ไม่ทราบ',channel:'LINE OA',serviceType:'เข้าบริการหน้างาน',priority:req.priority,issue:req.message,note:'รับแจ้งจาก Customer Portal / Machine QR',status:'เคสใหม่',serviceTeam:'Technical',assignee:'',appointment:'',media:req.media};cases.unshift(c);req.caseId=c.id;if(cu&&req.lineUserId){cu.lineUserId=req.lineUserId;cu.lineDisplayName=req.lineDisplayName;cu.lineLinkedAt=new Date().toISOString();cloudUpsertCustomer(cu)}saveLocal();await sendPortalBackend(req);await cloudUpsertLineRequest(req);await cloudUpsertCase(c);portalContent.innerHTML=`<div style="text-align:center;padding:15px"><div style="font-size:36px">✅</div><h3>รับแจ้งปัญหาแล้ว</h3><p>เลขเคส <b>${esc(c.ticket)}</b></p><p style="font-size:11px;color:var(--muted)">ทีม Service จะติดต่อกลับเพื่อยืนยันนัดหมาย</p></div>`;renderAll()}

 window.quoteCfg=function(){const c=settings.quoteConfig||{};return {...(defaults.quoteConfig||{}),...c,onsiteM:c.onsiteM??Math.round(((Number(c.onsiteS)||0)+(Number(c.onsiteL)||0))/2),workshopM:c.workshopM??Math.round(((Number(c.workshopS)||0)+(Number(c.workshopL)||0))/2),diagnosisM:c.diagnosisM??Math.round(((Number(c.diagnosisS)||0)+(Number(c.diagnosisL)||0))/2),nextM:c.nextM??Math.round(((Number(c.nextS)||0)+(Number(c.nextL)||0))/2),laborM:c.laborM??Math.round(((Number(c.laborS)||0)+(Number(c.laborL)||0))/2)}}
 window.normalizeMachineSize=function(v){const s=String(v||'').trim().toUpperCase();return ['S','M','L'].includes(s)?s:''}
 window.inferredMachineSize=function(m){const size=normalizeMachineSize(m?.size||m?.machineSize||m?.serviceSize||'');if(size)return{size,label:size==='S'?'Small Machine':size==='M'?'Medium Machine':'Large Machine'};const txt=[m?.name,m?.nameEn,m?.nameTh,m?.model,m?.machine].filter(Boolean).join(' ').toLowerCase();if(/\bsmall\b|เครื่องเล็ก/.test(txt))return{size:'S',label:'Small Machine'};if(/\bmedium\b|เครื่องกลาง/.test(txt))return{size:'M',label:'Medium Machine'};if(/\blarge\b|เครื่องใหญ่/.test(txt))return{size:'L',label:'Large Machine'};return{size:'S',label:'Small Machine'}}
 window.machineRate=function(m,service,isNext=false){const c=quoteCfg(),size=inferredMachineSize(m).size;if(service==='WP')return 0;const key=(prefix)=>c[prefix+size]??0;if(service==='OS'&&isNext)return key('next');if(service==='OS')return key('onsite');if(service==='WS')return key('workshop');return key('diagnosis')}
 window.openServiceRateSettingModal=function(){const c=quoteCfg();openModal('Service Rate','แก้ไขอัตราค่าบริการให้เป็น S / M / L เพื่อใช้ในใบเสนอราคาและ CRM',`<form id="serviceRateForm"><div class="section-title">Onsite / Workshop / Diagnosis</div><div class="form-grid cols3"><div class="field"><label>Onsite S</label><input id="srOnS" type="number" value="${c.onsiteS}"></div><div class="field"><label>Onsite M</label><input id="srOnM" type="number" value="${c.onsiteM}"></div><div class="field"><label>Onsite L</label><input id="srOnL" type="number" value="${c.onsiteL}"></div><div class="field"><label>Workshop S</label><input id="srWsS" type="number" value="${c.workshopS}"></div><div class="field"><label>Workshop M</label><input id="srWsM" type="number" value="${c.workshopM}"></div><div class="field"><label>Workshop L</label><input id="srWsL" type="number" value="${c.workshopL}"></div><div class="field"><label>Diagnosis S</label><input id="srDgS" type="number" value="${c.diagnosisS}"></div><div class="field"><label>Diagnosis M</label><input id="srDgM" type="number" value="${c.diagnosisM}"></div><div class="field"><label>Diagnosis L</label><input id="srDgL" type="number" value="${c.diagnosisL}"></div></div><div class="section-title" style="margin-top:12px">เครื่องถัดไป / Extra Labor</div><div class="form-grid cols3"><div class="field"><label>เครื่องถัดไป S</label><input id="srNxS" type="number" value="${c.nextS}"></div><div class="field"><label>เครื่องถัดไป M</label><input id="srNxM" type="number" value="${c.nextM}"></div><div class="field"><label>เครื่องถัดไป L</label><input id="srNxL" type="number" value="${c.nextL}"></div><div class="field"><label>Extra Labor S / ชม.</label><input id="srLbS" type="number" value="${c.laborS}"></div><div class="field"><label>Extra Labor M / ชม.</label><input id="srLbM" type="number" value="${c.laborM}"></div><div class="field"><label>Extra Labor L / ชม.</label><input id="srLbL" type="number" value="${c.laborL}"></div></div><div class="button-row"><button type="button" class="soft-btn" onclick="closeModal()">ยกเลิก</button><button class="primary-btn">บันทึก Rate</button></div></form>`,true);setTimeout(()=>serviceRateForm.onsubmit=e=>{e.preventDefault();settings.quoteConfig={...c,onsiteS:+srOnS.value||0,onsiteM:+srOnM.value||0,onsiteL:+srOnL.value||0,workshopS:+srWsS.value||0,workshopM:+srWsM.value||0,workshopL:+srWsL.value||0,diagnosisS:+srDgS.value||0,diagnosisM:+srDgM.value||0,diagnosisL:+srDgL.value||0,nextS:+srNxS.value||0,nextM:+srNxM.value||0,nextL:+srNxL.value||0,laborS:+srLbS.value||0,laborM:+srLbM.value||0,laborL:+srLbL.value||0};saveLocal();cloudSaveSettings();closeModal();renderAll();toastMsg('บันทึก Service Rate แล้ว')},20)};

 const originalRenderSettings=window.renderSettings;
 window.renderSettings=function(){originalRenderSettings();const c=quoteCfg(),L=(th,en)=>(settings.language==='en'?en:th);if(typeof setRateSummary!=='undefined')setRateSummary.textContent=`Onsite S ${Number(c.onsiteS).toLocaleString()} · M ${Number(c.onsiteM).toLocaleString()} · L ${Number(c.onsiteL).toLocaleString()} ${L('บาท','THB')}`;if(typeof settingsSnapshot!=='undefined'){const blocks=[...settingsSnapshot.querySelectorAll('.snapshot-item')];if(blocks[2])blocks[2].innerHTML=`<small>${L('ค่าบริการ','Service Rate')}</small><b>S ${Number(c.onsiteS).toLocaleString()} / M ${Number(c.onsiteM).toLocaleString()} / L ${Number(c.onsiteL).toLocaleString()}</b>`}}

 const originalOpenMachineModal=window.openMachineModal;
 if(originalOpenMachineModal){window.openMachineModal=function(mid='',returnToCase=false,customerPreset=''){originalOpenMachineModal(mid,returnToCase,customerPreset);setTimeout(()=>{const sel=document.getElementById('maSize');if(sel && ![...sel.options].some(o=>o.value==='M')){const opt=document.createElement('option');opt.value='M';opt.textContent='M - เครื่องกลาง';sel.insertBefore(opt, sel.querySelector('option[value="L"]')||null);const m=machineById(mid)||{};if(m.size==='M')sel.value='M'}},30)}}

 window.addEventListener('load',()=>{ensureV62();try{applyFontScale()}catch(e){}}, {once:true});
})();



// ==================== V6.6 MACHINE QC + DATA BACKUP ====================
const QC_CHECKLIST_MASTER=[
 {id:'physical',th:'ตรวจสภาพภายนอกและความเสียหาย',en:'Visual condition / damage'},
 {id:'identity',th:'ตรวจ Model / Serial / Nameplate',en:'Model / Serial / Nameplate'},
 {id:'electrical',th:'ตรวจ Power / Plug / Breaker / Ground',en:'Power / Plug / Breaker / Ground'},
 {id:'safety',th:'ตรวจระบบ Safety / Interlock / Emergency Stop',en:'Safety / Interlock / Emergency Stop'},
 {id:'mechanical',th:'ตรวจ Mechanical / Movement / Fastener',en:'Mechanical / Movement / Fastener'},
 {id:'sensor',th:'ตรวจ Sensor / Switch / Connector',en:'Sensor / Switch / Connector'},
 {id:'function',th:'Function Test / ทดสอบการทำงานจริง',en:'Function test'},
 {id:'alarm',th:'ตรวจ Alarm / Error Code / Log',en:'Alarm / Error Code / Log'},
 {id:'accessory',th:'ตรวจอุปกรณ์และอะไหล่ประกอบครบถ้วน',en:'Accessories / supplied parts completeness'},
 {id:'cleaning',th:'ตรวจความสะอาดและสภาพก่อนส่งมอบ',en:'Cleaning / appearance'},
 {id:'document',th:'ตรวจ Manual / Drawing / เอกสารที่ต้องส่ง',en:'Manual / Drawing / documents'},
 {id:'release',th:'Final QC Release / อนุมัติปล่อยเครื่อง',en:'Final QC release'}
];
let qcEditingId='',qcDraftChecklist=[],qcPendingMedia=[];
function qcNo(){const y=new Date().getFullYear(),m=String(new Date().getMonth()+1).padStart(2,'0');let max=0;qcRecords.forEach(q=>{const mt=String(q.qcNo||'').match(/QC-\d{6}-(\d+)/);if(mt)max=Math.max(max,Number(mt[1])||0)});return `QC-${y}${m}-${String(max+1).padStart(3,'0')}`}
function qcRecord(id){return qcRecords.find(x=>x.id===id||x.qcNo===id)}
function qcStatusClass(s){return s==='Pass'?'pass':s==='Fail'?'fail':s==='Conditional'?'conditional':s==='In Progress'?'progress':''}
function qcChecklistStats(q){const a=q?.checklist||[],done=a.filter(x=>x.result&&x.result!=='Pending').length,pass=a.filter(x=>x.result==='Pass').length,fail=a.filter(x=>x.result==='Fail').length;return{total:a.length||QC_CHECKLIST_MASTER.length,done,pass,fail,pct:Math.round(done/Math.max(1,a.length||QC_CHECKLIST_MASTER.length)*100)}}
function setQcStatusFilter(s='all'){if(typeof qcStatusFilter!=='undefined')qcStatusFilter.value=s;renderQc()}window.setQcStatusFilter=setQcStatusFilter;
function renderQc(){if(typeof qcTable==='undefined')return;const q=(qcSearch?.value||'').toLowerCase(),tf=qcTypeFilter?.value||'all',sf=qcStatusFilter?.value||'all';qcKpiAll.textContent=qcRecords.length;qcKpiDraft.textContent=qcRecords.filter(x=>x.status==='Draft').length;qcKpiProgress.textContent=qcRecords.filter(x=>x.status==='In Progress').length;qcKpiPass.textContent=qcRecords.filter(x=>x.status==='Pass').length;qcKpiFail.textContent=qcRecords.filter(x=>['Fail','Conditional'].includes(x.status)).length;const list=[...qcRecords].filter(x=>{const m=machineById(x.machineId),cu=customerById(x.customerId);return(!q||[x.qcNo,x.type,x.inspector,cu?.name,machinePrimaryName(m),m?.model,m?.serial].join(' ').toLowerCase().includes(q))&&(tf==='all'||x.type===tf)&&(sf==='all'||x.status===sf)}).sort((a,b)=>new Date(b.qcDate||b.updatedAt)-new Date(a.qcDate||a.updatedAt));qcTable.innerHTML=list.length?list.map(x=>{const m=machineById(x.machineId),cu=customerById(x.customerId),st=qcChecklistStats(x);return `<tr><td><b>${esc(x.qcNo)}</b>${x.isDemo?'<span class="subtext">TEST DATA</span>':''}</td><td>${x.qcDate?new Date(x.qcDate+'T00:00:00').toLocaleDateString(uiLocale()):'-'}</td><td>${esc(x.type||'-')}</td><td>${esc(cu?.name||'-')}</td><td>${esc(machinePrimaryName(m)||'-')}<span class="subtext">${esc(m?.model||'-')} · S/N ${esc(m?.serial||'-')}</span></td><td>${esc(x.inspector||'-')}</td><td><span class="qc-result-badge ${qcStatusClass(x.status)}">${esc(x.status||'Draft')}</span></td><td><div class="qc-check-progress"><small>${st.done}/${st.total}</small><div class="qc-meter"><i style="width:${st.pct}%"></i></div></div></td><td><button class="mini-btn" onclick="previewQc('${x.id}')">ดู / พิมพ์</button> <button class="mini-btn" onclick="openQcModal('${x.id}')">แก้ไข</button></td></tr>`}).join(''):`<tr><td colspan="9"><div class="empty">ยังไม่มีรายการ QC เครื่อง</div></td></tr>`;qcCards.innerHTML=list.map(x=>{const m=machineById(x.machineId),cu=customerById(x.customerId),st=qcChecklistStats(x);return `<div class="mobile-data-card"><div class="card-top"><div><h4>${esc(x.qcNo)}</h4><p>${esc(cu?.name||'-')}</p></div><span class="qc-result-badge ${qcStatusClass(x.status)}">${esc(x.status)}</span></div><p><b>${esc(machinePrimaryName(m)||'-')}</b><br>${esc(m?.model||'-')} · S/N ${esc(m?.serial||'-')}</p><p>${esc(x.type||'-')} · Checklist ${st.done}/${st.total}</p><div class="card-actions"><button class="soft-btn" onclick="previewQc('${x.id}')">ดู / พิมพ์</button><button class="primary-btn" onclick="openQcModal('${x.id}')">แก้ไข</button></div></div>`}).join('')}
window.renderQc=renderQc;
function qcCustomerChanged(selectedMachine=''){const cid=qcCustomer.value;const list=machines.filter(m=>!cid||m.customerId===cid);qcMachine.innerHTML='<option value="">เลือกเครื่อง</option>'+list.map(m=>`<option value="${m.id}">${esc(machinePrimaryName(m))} · ${esc(m.model||'-')} · S/N ${esc(m.serial||'-')}</option>`).join('');if(selectedMachine&&list.some(m=>m.id===selectedMachine))qcMachine.value=selectedMachine}window.qcCustomerChanged=qcCustomerChanged;
function qcCaseOptions(cid='',mid='',selected=''){const list=cases.filter(c=>(!cid||c.customerId===cid)&&(!mid||c.machineId===mid));return '<option value="">ไม่ผูก Service Case</option>'+list.map(c=>`<option value="${c.id}" ${selected===c.id?'selected':''}>${esc(c.ticket)} · ${esc(c.status||'')}</option>`).join('')}
function renderQcChecklist(){if(typeof qcChecklistBox==='undefined')return;qcChecklistBox.innerHTML=qcDraftChecklist.map((it,i)=>`<div class="qc-check-row"><div class="qc-check-no">${i+1}</div><div class="qc-check-name"><b>${esc(it.labelTh)}</b><small>${esc(it.labelEn)}</small></div><select onchange="qcDraftChecklist[${i}].result=this.value;updateQcSummary()"><option value="Pending" ${it.result==='Pending'?'selected':''}>รอตรวจ</option><option value="Pass" ${it.result==='Pass'?'selected':''}>ผ่าน / PASS</option><option value="Fail" ${it.result==='Fail'?'selected':''}>ไม่ผ่าน / FAIL</option><option value="N/A" ${it.result==='N/A'?'selected':''}>N/A</option></select><input class="qc-remark" placeholder="หมายเหตุ / ค่าที่วัด / สิ่งที่พบ" value="${esc(it.remark||'')}" oninput="qcDraftChecklist[${i}].remark=this.value"></div>`).join('');updateQcSummary()}
function updateQcSummary(){if(typeof qcSummary==='undefined')return;const st=qcChecklistStats({checklist:qcDraftChecklist});qcSummary.innerHTML=`<div><small>ตรวจแล้ว</small><b>${st.done}/${st.total}</b></div><div><small>PASS</small><b>${st.pass}</b></div><div><small>FAIL</small><b>${st.fail}</b></div>`}window.updateQcSummary=updateQcSummary;
function openQcModal(id='',presetMachine=''){if(!requirePermission('qc.edit'))return;const old=qcRecord(id)||{},m0=machineById(old.machineId||presetMachine),cid=old.customerId||m0?.customerId||'';qcEditingId=old.id||'';qcPendingMedia=JSON.parse(JSON.stringify(old.media||[]));qcDraftChecklist=(old.checklist?.length?old.checklist:QC_CHECKLIST_MASTER.map(x=>({id:x.id,labelTh:x.th,labelEn:x.en,result:'Pending',remark:''}))).map(x=>({...x}));openModal(old.id?'แก้ไข QC เครื่อง':'สร้าง QC เครื่อง','เชื่อม Customer / Machine / Service Case และบันทึก Checklist เพื่อใช้ตรวจรับหรือปล่อยเครื่อง',`<form id="qcForm"><div class="qc-form-grid"><div class="field"><label>QC No.</label><input id="qcNoInput" value="${esc(old.qcNo||qcNo())}" readonly></div><div class="field"><label>ประเภท QC *</label><select id="qcType">${['Incoming Inspection','Pre-Delivery QC','Post-Service QC','Installation Acceptance','PM Verification'].map(x=>`<option ${old.type===x?'selected':''}>${x}</option>`).join('')}</select></div><div class="field"><label>วันที่ตรวจ *</label><input id="qcDate" type="date" value="${esc(old.qcDate||new Date().toISOString().slice(0,10))}" required></div><div class="field"><label>ลูกค้า *</label><select id="qcCustomer" onchange="qcCustomerChanged();qcCase.innerHTML=qcCaseOptions(this.value,qcMachine.value,'')"><option value="">เลือกลูกค้า</option>${customers.map(c=>`<option value="${c.id}" ${cid===c.id?'selected':''}>${esc(c.name)}</option>`).join('')}</select></div><div class="field"><label>เครื่องจักร *</label><select id="qcMachine" onchange="qcCase.innerHTML=qcCaseOptions(qcCustomer.value,this.value,'')"></select></div><div class="field"><label>Service Case</label><select id="qcCase"></select></div><div class="field"><label>ผู้ตรวจ QC *</label><input id="qcInspector" value="${esc(old.inspector||currentUser?.name||'')}" required></div><div class="field"><label>ผู้อนุมัติ / Reviewer</label><input id="qcReviewer" value="${esc(old.reviewer||'')}"></div><div class="field"><label>สถานะ / ผล QC</label><select id="qcStatus"><option value="Draft" ${old.status==='Draft'?'selected':''}>Draft / รอตรวจ</option><option value="In Progress" ${old.status==='In Progress'?'selected':''}>In Progress</option><option value="Pass" ${old.status==='Pass'?'selected':''}>Pass / ผ่าน QC</option><option value="Conditional" ${old.status==='Conditional'?'selected':''}>Conditional / ผ่านแบบมีเงื่อนไข</option><option value="Fail" ${old.status==='Fail'?'selected':''}>Fail / ไม่ผ่าน</option></select></div><div class="field full"><label>Checklist QC</label><div id="qcSummary" class="qc-summary-box"></div><div id="qcChecklistBox" class="qc-checklist"></div></div><div class="field full"><label>สิ่งที่พบ / Findings</label><textarea id="qcFindings">${esc(old.findings||'')}</textarea></div><div class="field full"><label>Corrective Action / การแก้ไข</label><textarea id="qcCorrective">${esc(old.correctiveAction||'')}</textarea></div><div class="field full"><label>หมายเหตุ / Release Note</label><textarea id="qcNote">${esc(old.note||'')}</textarea></div></div><div class="button-row"><button type="button" class="soft-btn" onclick="closeModal()">ยกเลิก</button><button type="button" class="soft-btn" onclick="previewQcDraft()">🧾 ดูใบ QC</button><button class="primary-btn action-3d-orange">💾 บันทึก QC</button></div></form>`,true);modalPanel.classList.add('large');setTimeout(()=>{qcCustomerChanged(old.machineId||presetMachine);qcCase.innerHTML=qcCaseOptions(cid,old.machineId||presetMachine,old.caseId||'');renderQcChecklist();qcForm.onsubmit=e=>{e.preventDefault();saveQc()}},20)}
window.openQcModal=openQcModal;
function qcDraftRecord(){const m=machineById(qcMachine?.value),cu=customerById(qcCustomer?.value);return{id:qcEditingId||'DRAFT',qcNo:qcNoInput?.value||'DRAFT',type:qcType?.value||'Pre-Delivery QC',qcDate:qcDate?.value||new Date().toISOString().slice(0,10),customerId:cu?.id||'',machineId:m?.id||'',caseId:qcCase?.value||'',inspector:qcInspector?.value?.trim()||'',reviewer:qcReviewer?.value?.trim()||'',status:qcStatus?.value||'Draft',checklist:JSON.parse(JSON.stringify(qcDraftChecklist)),findings:qcFindings?.value?.trim()||'',correctiveAction:qcCorrective?.value?.trim()||'',note:qcNote?.value?.trim()||'',media:JSON.parse(JSON.stringify(qcPendingMedia)),createdAt:new Date().toISOString(),updatedAt:new Date().toISOString()}}
function saveQc(){const rec=qcDraftRecord();if(!rec.customerId||!rec.machineId){toastMsg('กรุณาเลือกลูกค้าและเครื่องจักร');return}const old=qcRecord(qcEditingId);rec.id=old?.id||uid();rec.createdAt=old?.createdAt||new Date().toISOString();if(old)qcRecords=qcRecords.map(x=>x.id===rec.id?rec:x);else qcRecords.unshift(rec);saveLocal();closeModal();renderAll();toastMsg('บันทึก '+rec.qcNo+' แล้ว')}window.saveQc=saveQc;
function qcDocHTML(q){const m=machineById(q.machineId)||{},cu=customerById(q.customerId)||{},c=cases.find(x=>x.id===q.caseId),st=qcChecklistStats(q),rows=(q.checklist||[]).map((it,i)=>`<tr><td class="center">${i+1}</td><td>${esc(it.labelTh||it.labelEn||it.id)}</td><td class="center">${esc(it.result||'-')}</td><td>${esc(it.remark||'-')}</td></tr>`).join('');return `<div class="qc-a4" id="qcPrintDoc"><div class="qc-a4-head"><div><img src="${IMODE_LOGO}" alt="I-MODE"></div><div><h1>MACHINE QC INSPECTION REPORT</h1><div class="qc-a4-meta"><b>${esc(settings.companyNameTh||settings.companyName||'บริษัท ไอโมด พลัส จำกัด')}</b><br>${esc(settings.companyAddress||'')}<br>Tel. ${esc(settings.companyPhone||'-')} · ${esc(settings.companyEmail||'-')}</div></div></div><div class="qc-a4-info"><div><b>QC No.:</b> ${esc(q.qcNo)}<br><b>QC Type:</b> ${esc(q.type)}<br><b>Date:</b> ${esc(q.qcDate||'-')}<br><b>Service Case:</b> ${esc(c?.ticket||'-')}</div><div><b>Customer:</b> ${esc(cu.name||'-')}<br><b>Machine:</b> ${esc(machinePrimaryName(m)||'-')}<br><b>Model:</b> ${esc(m.model||'-')}<br><b>Serial:</b> ${esc(m.serial||'-')}</div></div><table><thead><tr><th style="width:45px">No.</th><th>Inspection Point</th><th style="width:90px">Result</th><th>Remark</th></tr></thead><tbody>${rows}</tbody></table><div class="qc-a4-note"><b>Findings:</b> ${esc(q.findings||'-')}<br><b>Corrective Action:</b> ${esc(q.correctiveAction||'-')}<br><b>Release Note:</b> ${esc(q.note||'-')}</div><div class="qc-a4-info"><div><b>Final Result:</b> ${esc(q.status||'Draft')}<br><b>Checklist:</b> ${st.done}/${st.total} · PASS ${st.pass} · FAIL ${st.fail}</div><div><b>Inspector:</b> ${esc(q.inspector||'-')}<br><b>Reviewer:</b> ${esc(q.reviewer||'-')}</div></div><div class="qc-a4-sign"><div><b>QC Inspector</b><div>Date ....................</div></div><div><b>Service / Technical</b><div>Date ....................</div></div><div><b>Approver / Customer Acceptance</b><div>Date ....................</div></div></div></div>`}
function openQcPreviewRecord(q,draft=false){openModal(draft?'ตัวอย่างใบ QC':'QC '+q.qcNo,draft?'Preview ก่อนบันทึก':`${q.type} · ${q.status}`,qcDocHTML(q)+`<div class="button-row" style="margin-top:12px"><button class="soft-btn" onclick="closeModal()">ปิด</button><button class="soft-btn" onclick="printQcCurrent()">🖨 พิมพ์ / PDF</button>${!draft?`<button class="primary-btn" onclick="openQcModal('${q.id}')">แก้ไข</button>`:''}</div>`,true);modalPanel.classList.add('large')}
function previewQc(id){const q=qcRecord(id);if(q)openQcPreviewRecord(q,false)}window.previewQc=previewQc;
function previewQcDraft(){openQcPreviewRecord(qcDraftRecord(),true)}window.previewQcDraft=previewQcDraft;
function printQcCurrent(){const doc=document.getElementById('qcPrintDoc');if(!doc)return;const css=document.getElementById('qcV66Style')?.textContent||'';const w=window.open('','_blank','width=1100,height=900');w.document.write(`<!doctype html><html lang="th"><head><meta charset="utf-8"><title>Machine QC</title><style>${css}</style></head><body>${doc.outerHTML}<script>window.onload=()=>window.print()<\/script></body></html>`);w.document.close()}window.printQcCurrent=printQcCurrent;
function exportQcCSV(){if(!qcRecords.length){toastMsg('ยังไม่มีข้อมูล QC');return}const rows=[['QC No','Date','Type','Customer','Machine','Model','Serial','Inspector','Reviewer','Status','Checklist Done','Checklist Total','Findings','Corrective Action','Note'],...qcRecords.map(q=>{const m=machineById(q.machineId),cu=customerById(q.customerId),st=qcChecklistStats(q);return[q.qcNo,q.qcDate,q.type,cu?.name||'',machinePrimaryName(m)||'',m?.model||'',m?.serial||'',q.inspector||'',q.reviewer||'',q.status,st.done,st.total,q.findings||'',q.correctiveAction||'',q.note||'']})];const csv=rows.map(r=>r.map(v=>'"'+String(v??'').replace(/"/g,'""')+'"').join(',')).join('\n');downloadTextFile(`I-MODE_QC_${new Date().toISOString().slice(0,10)}.csv`,'\ufeff'+csv,'text/csv;charset=utf-8')}window.exportQcCSV=exportQcCSV;
function downloadTextFile(name,text,type='application/json'){const blob=new Blob([text],{type}),a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=name;document.body.appendChild(a);a.click();setTimeout(()=>{URL.revokeObjectURL(a.href);a.remove()},300)}
function buildSystemBackup(){return{format:'I-MODE-SERVICE-BACKUP',version:'6.6',exportedAt:new Date().toISOString(),data:{customers,machines,cases,technicians,quotations,warranties,machineDocuments,lineRequests,serviceReports,qcRecords,notifications,settings,currentUser}}}
function exportSystemBackup(){const b=buildSystemBackup();downloadTextFile(`I-MODE_Service_Backup_${new Date().toISOString().replace(/[:.]/g,'-').slice(0,19)}.json`,JSON.stringify(b,null,2),'application/json')}window.exportSystemBackup=exportSystemBackup;
function importSystemBackup(input){const file=input.files?.[0];if(!file)return;const r=new FileReader();r.onload=()=>{try{const b=JSON.parse(r.result),d=b.data||b;if(!Array.isArray(d.customers)||!Array.isArray(d.machines))throw new Error('backup invalid');if(!confirm('นำ Backup นี้เข้าระบบและแทนที่ข้อมูลปัจจุบันหรือไม่?'))return;customers=d.customers||[];machines=d.machines||[];cases=d.cases||[];technicians=d.technicians||technicians;quotations=d.quotations||[];warranties=d.warranties||[];machineDocuments=d.machineDocuments||[];lineRequests=d.lineRequests||[];serviceReports=d.serviceReports||[];qcRecords=d.qcRecords||[];notifications=d.notifications||[];settings=mergeSettings(d.settings||settings);currentUser=d.currentUser||currentUser;saveLocal();closeModal();renderAll();toastMsg('Restore Backup เรียบร้อย')}catch(e){toastMsg('ไฟล์ Backup ไม่ถูกต้อง')}};r.readAsText(file);input.value=''}window.importSystemBackup=importSystemBackup;
function clearTestDataWithBackup(){if(!confirm('แนะนำให้ Export Backup ก่อน คุณต้องการล้างข้อมูลธุรกรรมทดสอบหรือไม่?'))return;cases=[];quotations=[];lineRequests=[];serviceReports=[];qcRecords=[];notifications=[];saveLocal();closeModal();renderAll();toastMsg('ล้างข้อมูลธุรกรรมทดสอบแล้ว (Customer / Machine / Warranty ยังอยู่)')}window.clearTestDataWithBackup=clearTestDataWithBackup;
function openDataBackupModal(){const b=buildSystemBackup(),d=b.data;openModal('Data Backup / Restore','ข้อมูลระบบแยกสำรองเป็น JSON สามารถเปิดแก้ไขด้วย Text Editor แล้ว Import กลับได้',`<div class="backup-grid"><div class="backup-card"><h4>💾 Export Backup</h4><p>รวม Customer, Machine, Service Case, Quotation, Warranty, Documents, Service Report และ QC ไว้ในไฟล์เดียว</p><button class="primary-btn action-3d-orange" onclick="exportSystemBackup()">ดาวน์โหลด Backup JSON</button><div class="backup-data-list"><span>Customers ${d.customers.length}</span><span>Machines ${d.machines.length}</span><span>Cases ${d.cases.length}</span><span>Quotations ${d.quotations.length}</span><span>Warranty ${d.warranties.length}</span><span>QC ${d.qcRecords.length}</span></div></div><div class="backup-card"><h4>↥ Import / Restore</h4><p>เลือกไฟล์ Backup JSON ที่เคย Export หรือไฟล์ Data ที่แก้ไขแล้ว ระบบจะนำข้อมูลกลับมาใช้งาน</p><label class="soft-btn" style="cursor:pointer;display:inline-flex">เลือกไฟล์ Backup<input type="file" accept=".json,application/json" style="display:none" onchange="importSystemBackup(this)"></label><div class="backup-warning">ก่อน Restore ควร Export Backup ของข้อมูลปัจจุบันเก็บไว้อีก 1 ชุด</div></div></div><div class="backup-card" style="margin-top:12px"><h4>🧹 Reset สำหรับ Test</h4><p>ล้างเฉพาะข้อมูลธุรกรรม เช่น Cases / Quotations / LINE Requests / Reports / QC โดยยังเก็บ Customer, Machine และ Warranty</p><button class="soft-btn" onclick="clearTestDataWithBackup()">ล้างข้อมูลธุรกรรมทดสอบ</button></div>`,true);modalPanel.classList.add('large')}window.openDataBackupModal=openDataBackupModal;
(function seedQcDemoV66(){if(qcRecords.length)return;const mk=(id,no,cid,mid,type,status,inspector,results)=>({id,qcNo:no,customerId:cid,machineId:mid,caseId:'',type,status,qcDate:'2026-08-31',inspector,reviewer:'',checklist:QC_CHECKLIST_MASTER.map((x,i)=>({id:x.id,labelTh:x.th,labelEn:x.en,result:i<results.length?results[i]:'Pending',remark:''})),findings:status==='Conditional'?'พบจุดที่ต้องแก้ไขก่อน Release':'',correctiveAction:status==='Conditional'?'แก้ไขและ Re-check ก่อนส่งมอบ':'',note:'TEST DATA สำหรับทดลองโมดูล QC',media:[],isDemo:true,createdAt:new Date().toISOString(),updatedAt:new Date().toISOString()});qcRecords=[mk('QC-DEMO-001','QC-202608-001','CUST-0001','MCH-0001','Post-Service QC','Pass','QC Demo',['Pass','Pass','Pass','Pass','Pass','Pass','Pass','Pass','Pass','Pass','Pass','Pass']),mk('QC-DEMO-002','QC-202608-002','CUST-0002','MCH-0020','Pre-Delivery QC','Conditional','QC Demo',['Pass','Pass','Pass','Pass','Pass','Pass','Pass','Fail','Pass','Pass','Pass','Pending']),mk('QC-DEMO-003','QC-202608-003','CUST-0007','MCH-0035','Incoming Inspection','Draft','QC Demo',['Pass','Pass','Pending','Pending','Pending','Pending','Pending','Pending','Pending','Pending','Pending','Pending'])];saveLocal()})();
